# Internal Package Layout

Internal architecture reference for agents and contributors. This document captures per-package responsibilities, invariants, and cross-package contracts that are not derivable from the code alone (or that would be expensive to re-derive every time). User-facing schema / behavior docs live under [`docs/reference/`](../reference/) — this file is purely about *internal code structure*.

The executable entrypoint lives in `cmd/dwe`; most code is under `internal/`. Tests sit next to code as `*_test.go`; fixtures live in package-local `testdata/`.

## Layout

`internal/` is organized in three layers — see [Dependency Rules](#dependency-rules) for the intended contracts (depguard activation is pending):

```
internal/
  cli/                   cobra command tree, no domain logic
  core/                  domain logic, subclustered
    project/             "what is a DWE project" (config, services, stack)
    execution/           pipeline engine + collaborators
    workflow/            deploy, lifecycle, reset, snapshot, setup
    usercommands/        declarative user-command system
    validate/            static validation framework + subdomains
    docs/                embedded docs subsystem
    ui/                  domain-aware renderers (sink — imported only by cli/)
    notify/              desktop notifications
    bridge/              host bridge for dev containers (daemon, compose overlay, shim assets)
  shared/                leaf infrastructure (docker, git, lock, render, i18n, …)
```

## Dependency Rules

Three layering rules are intended contracts. Activation in `depguard` is deferred until the pre-existing cross-layer imports surfaced during the restructure are resolved (see Task 10 notes in `docs/plans/completed/2026-05-28-internal-restructure-layered.md`). Until then they are enforced by convention and code review only:

- **`shared-no-domain`** — `internal/shared/**` must not import `internal/core/**` or `internal/cli/**`. Shared is leaf infrastructure.
- **`core-no-cli`** — `internal/core/**` must not import `internal/cli/**`. Domain code is ignorant of cobra.
- **`ui-is-sink`** — `internal/core/ui/**` is a sink: no other `internal/core/**` package may import it. Only `internal/cli/**` may pull it in. The `internal/core/ui/tui/` TUI framework follows this rule strictly — it is importable only from `core/ui/*` + `cli/` and no new importers were added through Stage 2. The docs browser has been relocated to `internal/core/ui/docstui/` (Stage 4), keeping `core/docs` free of `core/ui` imports and placing the framework dependency on the `core/ui` side of the boundary. A concrete corollary: `internal/core/docs/` must stay free of `internal/core/ui/` imports, which is the whole reason the interactive browser lives in `internal/core/ui/docstui/` rather than under `core/docs/`. Mechanically checkable with `grep -rn "core/ui" internal/core/docs/ --include='*.go'` — which must stay empty; the `--include` filter is required, since without it the generated `embedded/` docs mirror matches its own prose.

Soft (non-enforced) intra-`core/` ordering: `project ← execution ← workflow`. Anything depending on a named user operation lives in `workflow/`; anything that runs a pipeline lives in `execution/`; anything that defines "the project" lives in `project/`.

## CLI (`internal/cli/`)

The cli layer is a thin cobra composition root plus 20 per-domain subpackages. Each subpackage owns one command subtree and exports a single `NewCmd(groupID string, flags *cmdctx.RootFlags) *cobra.Command` entry point that the root composes via `root.AddCommand(<pkg>.NewCmd(group, flags))`; this replaces the old inline `newXxxCmd` declarations and the `addCmd` helper. Three pattern variants exist: (1) **standard** — single-export `NewCmd(groupID, flags)`; (2) **multi-export Go-domain grouping** — `lifecycle/` exposes `NewRunCmd` / `NewStopCmd` / `NewRestartCmd` / `NewResetCmd` because the four commands share a `preflightRun` test seam and the preflight+lock+journal pattern, even though they are independent top-level cobra commands; (3) **special** — `completion/` exposes `AttachInstallUninstall(parent *cobra.Command, flags *cmdctx.RootFlags)` because its subcommands attach to cobra's auto-generated `completion` command (`root.InitDefaultCompletionCmd()` cannot be replaced). `version.NewCmd(groupID string)` is the lone standard-variant that omits `flags` because it needs none.

**Cross-cutting CLI behaviors anchored on `cmdctx.RootFlags`** (details under each subpackage):

- **JSON output mode (`--output json` + `--pretty`)**: Read-only commands emit naked DTOs on stdout; errors go through `cmdctx.WriteError` as a `{"error":{code,message,hint,details}}` envelope on stderr. Use `cmdctx.WriteData[T](flags, cmd, data, textFn)` to dispatch text vs JSON. Construct typed errors with `cmdctx.Err(code, msg)` / `cmdctx.ErrWrap(code, err)` and chain `.WithHint(...)` / `.WithDetail(key, val)`. JSON mode side-effects (set in `PersistentPreRunE`): `NO_COLOR=1`, `SilenceErrors=true`, `SilenceUsage=true`. `runRoot` (no subcommand) routes to `runRootJSON` when JSON is on — never emits brand header or pending banner in that mode. DTO convention: snake_case fields, RFC3339 timestamps, durations in seconds and sizes in bytes as numbers; wrap top-level arrays in a keyed object (`{"apps":[...]}`); `omitempty` on optional inner fields but never on the wrapper key. Stderr in JSON mode is reserved for the error envelope — gate any `warning:` writes behind `flags.Output != "json"`. **Diagnostic commands (`validate` and subdomains) are the exception** to the envelope rule: they always emit diagnostics-as-data on stdout, even at severity=error; exit code conveys severity (0 ok/info, 1 error or warning+`--strict`), and `validationFailedError` carries `ExitCode()` so `main.go` short-circuits the envelope path. TUI-capable commands (`status`, future `cmdbrowser`) force TUI off when `flags.Output == "json"`. Exit codes via `cmdctx.ExitCodeFor(err)`: 0 success, 1 runtime, 2 usage (e.g. `invalid_output`, `--pretty` without `--output json`). Use `cli.NewRootCmdWithFlags()` (not `NewRootCmd()`) when you need the `*RootFlags` pointer; the no-flags constructor exists for the 14+ legacy test call sites only.
- **Display-string localization**: Every call site that renders user-command descriptions, parameter descriptions, group names, or doc-generator section headers to stdout must use the `i18n.Translator` interface (via `rflags.I18n`). Use typed helpers: `store.CommandDescription(locale, id, fallback)`, `store.ParamDescription(locale, id, paramName, fallback)`, `store.GroupTitle/Description(locale, id, fallback)`, `store.T(locale, uiKey, fallback)`. Never read `def.Description` directly in display code. Completion-path safety: `__complete` paths bypass `PersistentPreRunE`, so wrap via `i18n.TranslatorOrNop(rflags.I18n)`. Storage/hashing sites (journal, deploy steps, phase definitions) stay English — never introduce locale to `internal/core/workflow/deploy/journal/hash.go` or any persistence layer.

- **Container command policy (host bridge)**: when `DWE_INVOKED_FROM=container` (force-set by the bridge daemon — design D7; client-sent values are stripped, so not spoofable from a container), only an **allowlist, default-deny** subset of the CLI is reachable: the `commands` subtree, `status`, `info`, `logs`, `docs`, `prompt`, `version`, `help`, `vars`, bare `dwe`, cobra's `__complete`/`__completeNoDesc`, plus the nested exceptions `bridge status` and `render config` (the other `render` subcommands — env/ide/ai/git — stay host-only). `vars`'s read subcommands (`get`/`list`/`inspect`) are freely reachable; `vars set` is reachable but **deny-by-default RUNTIME-gated** by `bridge.vars_writable` inside `cli/vars/set.go` (the allowlist is prefix-wide and cannot see the var arg). `render config` rejects `--harvest` in a container (host-only state mutation). `validate` and the user-facing `completion` generator are deliberately host-only (the hidden `__complete` machinery stays allowed so a completion script baked into an image degrades silently instead of injecting a policy error into the shell). Everything else — including every NEW top-level command, until explicitly added to `bridgeAllowedTopLevel` in `internal/cli/bridgepolicy.go` — fails with typed `cmdctx.Err("bridge_command_blocked")` (run-on-host hint; stop/restart/reset additionally carry the suicide explanation). The gate runs inside the EXISTING root `PersistentPreRunE` (step 2b — after output-mode validation, before project resolution); `applyBridgeContainerVisibility` at the end of `NewRootCmdWithFlags` hides blocked commands from help and completion (a parent stays visible when any descendant is allowed — the `bridge` → `bridge status` shape). The policy is inherited by nested invocations (`type: dwe` user commands keep the env). Related fallback: bare `dwe commands`/`dwe cmd` (non-tty stdin) and bare `dwe docs` (non-tty stdout) OR truthy `DWE_NONINTERACTIVE` (the shared `cmdctx.NonInteractiveEnv()` `{"1","true"}` set) print their `list` output instead of the TUI browser. **Inside the allowlisted `commands` subtree there is a second, per-command gate**: user commands are container-reachable only when opted in via a `bridge:` block (`model.BridgeDef` — command-level or file `group:` header, field-wise merge, opt-in default), resolved into `CommandDef.BridgeHidden` by `registry.ApplyVisibility` and enforced by `registry.list` (all listing/completion variants — including the inspect escape hatch), the tree walker in `cli/command/list.go`, and `bridgeGuard` in `cli/command/bridgegate.go` (typed `command_not_bridged` on direct invocation AND inspect). The workflow runner and pipeline engine deliberately never consult `BridgeHidden` — a bridged workflow executes non-bridged sub-commands host-side. See § Core — User Commands. User-visible policy table: `docs/reference/concepts/bridge.md` § Command policy inside containers.

**Naming rationale**: `docker/` and `compose/` are kept as **separate packages** rather than collapsed into one `docker/` package exporting `NewDockerCmd`/`NewComposeCmd`; the latter would force the stuttering `docker.NewDockerCmd` (textbook Go anti-pattern of the `http.HTTPClient` shape). The file hosting `NewCmd` inside each subpackage is named after the package (`lifecycle/lifecycle.go`, `snapshot/snapshot.go`, `deploy/deploy.go`, `render/render.go`, …) to avoid overloading `root.go` — `internal/cli/root.go` is THE root. Where a subpackage name collides with an import (`internal/shared/version`, `internal/shared/prompt`, `internal/shared/docker`, `internal/core/workflow/snapshot`, `internal/core/workflow/lifecycle`, `internal/core/validate`, `internal/core/docs`), callers and the colliding import alias as `versioninfo` / `promptpkg` / `dockerpkg` / `snapshotpkg` / `lifecyclepkg` / aliased `cmdValidate` / `cmdDocs` etc.

**Cross-sibling import policy**: siblings communicate via `cli/cmdctx/` and never cross-import each other. Two pre-existing exceptions are documented and out of scope to fix here: `cli/service/` → `cli/deploy.RunHelper` (service-toggle drives deploys after enable/disable; resolved by lifting `RunHelper` into `core/workflow/deploy/` in a follow-up), and `cli/lifecycle/{run,restart}.go` → `cli/info.Run` (the `ShowInfo` lifecycle callback; the import became a sibling import once `lifecycle/` was extracted).

**Section preamble — cross-cutting behaviors anchored in `root.go`**: **Validate schema-bypass pattern** — `isValidateCommand(cmd)` in `PersistentPreRunE` switches from `project.Resolve` (schema-validating) to `project.Locate` (locate only) so schema errors surface as diagnostics rather than aborting before `RunE`; apply this pattern to any future diagnostic-mode command. **Exit-code interface** — errors implementing `ExitCode() int` are translated to custom process exit codes in `cmd/dwe/main.go` via `errors.As`; the fang error handler suppresses the "Error:" line for them. `validationFailedError` in `cli/validate/validate.go` is the canonical example.

- `internal/cli/` — composition root (`root.go`, ~620 LoC): `NewRootCmd`, `initRootCmd`, `PersistentPreRunE`, `runRoot`, `applyStyles`, `resolveLocalization`, `allowedWithoutProject`, `isValidateCommand`. No inline command builders; no `addCmd` helper. Companions: `root_test.go`, `root_resolver_test.go`, `fang_integration_test.go`.
- `internal/cli/cmdctx/` — typed `RootFlags` bundle threaded into every RunE callback (cfg path, language, accent overrides, project-root resolver, etc.) plus flag-helper utilities (e.g. the exported `CompletionConfigPath(flags *RootFlags, cmd *cobra.Command) (configPath, projectRoot string, err error)`, used by every `ValidArgsFunction` callback to safely resolve project state under cobra's hidden `__complete` path, which bypasses `PersistentPreRunE`). This is the single seam through which siblings exchange context. Shared cross-sibling helpers exported here: `WriteData[T]` / `WriteJSON[T]` (text-vs-JSON dispatch; `WriteJSON` is `WriteData` with an empty text renderer for JSON-only payloads), `WriteError` / `Err` / `ErrWrap` (typed-error envelope), `AcquireProjectLocksOrReport` (the project-lock acquire-and-report wrapper — see § snapshot Lock acquisition), `AcquireProjectLocksSilent` (the **no-print** variant with the *identical* error contract — `*lock.ProjectLockHeldError` returned unchanged with ExitCode 2, any other error wrapped `acquiring project locks: %w` — but writing NOTHING; the sanctioned wrapper for a **live alt-screen** call site (an in-TUI form overlay) where stderr diagnostics mid-frame would corrupt the TUI, so the caller surfaces the returned lock-held error itself as a status flash — see § cli/vars in-TUI edit), and `WarnSilentLog` (the shared pipeline-`ErrSilent` "Full output saved to" log-tail hint used by `deploy run` / `reset run` — the one cmdctx helper that imports `core/execution/pipeline`). Calling `CompletionConfigPath` before any config-dependent work in a `ValidArgsFunction` is a MUST, not a convention: on ANY error (from it or from the subsequent `config.LoadConfig`) the callback must return `nil, cobra.ShellCompDirectiveNoFileComp` and print **nothing**, because the `__complete` protocol reads the callback's stdout as the completion list and a diagnostic line would be offered to the user as a candidate. Neither lock wrapper may be re-rolled inline at a call site: the acquire-and-report body is ~9 lines and every hand-copy is a chance to drop the `*lock.ProjectLockHeldError` passthrough that carries `ExitCode() == 2`, degrading a "someone else is deploying" into a generic exit 1. Also exported here: `EmitDefaultNotice(cmd, flags, pipeline, file)` — the single consumer of the `defaulted bool` returned by every `Ensure*Config` constructor (`EnsureDeployConfig` / `EnsureResetConfig` / `EnsureRunConfig` / `EnsureStopConfig`). It writes ONE `render.Writer.Info` line ("Using built-in default <pipeline> pipeline (override with workspace/<file>.yml).") to **stderr** and is a **no-op under `--output json`** so stdout stays the parseable surface; the same decision is unconditionally mirrored to the Debug firehose via `trace.Debugf`, which is why suppressing the user-facing line in JSON mode loses nothing diagnostically. `cli/validate`'s `emitFilterHint` is modelled on it.
- `internal/cli/info/` — `dwe info` renderer. `NewCmd(groupID, flags)` plus `Run(ctx, …)` (exported because `cli/lifecycle/{run,restart}.go` consume it for the `ShowInfo` lifecycle callback).
- `internal/cli/version/` — `dwe version`. `NewCmd(groupID string)` — the lone flagless variant.
- `internal/cli/prompt/` — `dwe prompt` cobra command for `--help` discoverability only. The fast path bypasses cobra entirely via `cmd/dwe/main.go`'s `isPromptInvocation` dispatch into `shared/prompt.Run` (lipgloss would auto-downgrade to no-color when stdout is piped). `NewCmd` `RunE` is unreachable in normal startup. The command is registered on root with `Hidden: true`: it stays in `root.Commands()` (findable programmatically) and `dwe prompt --help` still renders its own help block — it is only dropped from the `dwe --help` listing (cobra also excludes `Hidden` commands from completion suggestions, so `dwe pro<TAB>` does not offer it). All three halves are test-pinned (`TestPromptCmd_UseAndFlags` asserts `Hidden`, `TestPromptCmd_HiddenFromRootHelp` asserts the listing omission, `TestPromptRegisteredAtRoot` asserts it is still registered), so unhiding it is a deliberate contract change, not a cosmetic one.
- `internal/cli/shell/` — `dwe shell` plus the misnamed-from-prior-refactor `exec.go` (was `service_cli.go`) carrying `resolveShellOptions`, `serviceContainerState`, `dockerExecCLI`, `composeRunCLI`, `runInteractive`, `errContainerNotFound`. Watch for the `os/user` import — package name `shell` does not collide. `NewCmd(groupID, flags)`. **Container detection is label-based, not name-guessed:** `serviceContainerState(projectName, service, processEnv, dockerBin)` delegates to the shared `docker.ServiceContainerName` (the one home for label-based container lookup; label-key consts `docker.ComposeProjectLabel` / `ComposeServiceLabel` / `ComposeOneoffLabel` are also reused by `stack.ServiceRunning` and `env` port-conflict). `service` is **svc.Container** — the value compose stamps into `com.docker.compose.service` and the service key dwe uses everywhere (topology, stop, logs); it defaults to the folder key but may be overridden in `service.yml`, so `resolveShellTarget` returns it and the probe/exec/run paths target it (NOT the user-facing service name). `serviceContainerState` runs two passes (running, then `--all`) to distinguish "stopped" from "absent"; each delegates to `docker.ServiceContainerName`, which probes `docker ps … --format '{{.Names}}'` (a single portable field — avoids `.State` and `--format json`, whose shapes differ between docker and podman) and **prefers a long-lived service container over an ephemeral one-off** by first filtering `com.docker.compose.oneoff=False`, falling back to the unfiltered query only when that finds nothing (so a one-off `compose run` container — e.g. a concurrent `dwe shell --mode run` session — is never exec'd into when a real service container exists, while compose backends that omit the one-off label still resolve via the fallback). Empty stdout from a zero-exit `docker ps` unambiguously means "no match" and a real Docker error exits non-zero and is surfaced, so a probe hiccup can never be mistaken for absence (which would start a duplicate). The state probe is the injectable `containerProbeFunc` seam (`func(service) (name, status, err)`); `errContainerNotFound` (no labelled container) drives auto-mode's fall-through to `docker compose run`. **One-shot mode** (`-c, --command "<cmd>"`): parallel dispatch path via `dispatchShell` (called from `RunE`) → branches on `shellCLIFlags.command`; non-empty routes to `runOneShotCommand` → `dockerExecOneShot` / `composeRunOneShot` (no info banner, no compose-run hardcoded `-it`); empty routes to existing interactive `runServicesCLI`. TTY auto-detect via package-level `ttyDetector` seam (defaults to `widgets.IsInteractiveFn`, swapped in tests). **TTY resolution is a tri-state `ttyMode` (auto / on / off), not a bool**: `--tty`/`-t` and `--no-tty` map to on/off via `resolveTTYMode` in `RunE`, which rejects the two together; absent flags leave auto. `oneShotTTYMode` makes auto mean **off** for `-c` (unlike the interactive path, which auto-detects) so a piped `-c` stays byte-clean — at the cost of the child block-buffering, which is exactly what `--tty` exists to override. `dockerExecTTYFlags(mode)` returns `["-i","-t"]` / `["-i"]` and **drops `-i` under a forced TTY with non-terminal stdin** (docker refuses `-i -t` there); `composeRunTTYFlags(mode)` returns `["-i"]` or `["-i","-T"]` (compose run allocates a TTY by default — `-T` disables). Compose cannot honour a forced TTY with non-terminal stdin at all: `composeTTYUnavailable` detects it and `warnComposeTTYUnavailable` degrades to the non-TTY vector with a warning written to **stderr only** — stdout may be piped into a consumer that must not see it. Child exit codes flow through cobra/fang via `*shellCommandExitError{code, underlying}` (extracts from `*exec.ExitError` in shared `wrapExitError`); `cmd/dwe/main.go:75-81` extracts `interface{ ExitCode() int }` and calls `os.Exit`, and `errHandler` at main.go:40-44 suppresses Fang's `Error:` banner for `ExitCode`-bearing errors so the user sees only the child's stdout/stderr. The global root `-c` shorthand was dropped (only `--config` long form remains) to free `-c` for `shell`.
- `internal/cli/logs/` — `dwe logs <service>` for streaming container logs by service name (canonical entry point — use it instead of `docker logs <container>` directly, since it handles container-name resolution via `daemon.ResolveContainerName`). Files: `logs.go` (`NewCmd` + `ResolveLogsTarget` exported helper + `runLogs` orchestrator), `parse.go` (NDJSON line parser for JSON mode). Flags: `--tail N` (default 50, 0 = all), `--since DUR|TS`, `-f/--follow`, plus the global `--output text|json` (NDJSON in JSON mode — one `{"ts","stream","msg"}` object per line). Read-only: no lock, no preflight. `ValidArgsFunction` uses `cmdctx.ServiceNameCompletion(flags)` so completion lists current project services. `NewCmd(groupID, flags)`.
- `internal/cli/status/` — `dwe status` group. `NewCmd(groupID, flags)` composes a default `RunE` rendering health + apps + tools + infra + deploy + topology + git + daemons in order; each section also addressable via subcommand (`status apps` / `tools` / `infra` / `deploy [svc]` / `topology` / `git` / `daemons`, one file per subcommand). Default-only `--no-apps` / `--no-tools` / `--no-infra` / `--no-deploy` / `--no-topology` / `--no-git` / `--no-daemons` flags suppress sections; a typed `section` enum + ordered list is the single source of truth for both the default orchestrator order and the `--no-*` flag set. The group does **not** use `PersistentPreRunE` (would replace the root project-resolution hook); instead `loadStatusContext(flags)` runs from each subcommand's `RunE` to load cfg/journal/tracked/topology on demand. `status deploy <svc>` uses `cobra.MaximumNArgs(1)` and `ValidArgsFunction` routed through `cmdctx.CompletionConfigPath` (`trackedServiceCompletion` lives in `deploy.go` since it is the sole caller). All stdout via `cmd.OutOrStdout()`; warnings via `cmd.ErrOrStderr()`. Test seams: `isTerminalFn`, `runStatusTUIFn`. The `PersistentPreRunE` avoidance generalizes: **cobra never chains the hook** — a child's `PersistentPreRunE` *replaces* the nearest ancestor's rather than running after it — so any grouped subtree that declares one silently loses the root's project resolution, style application, and locale setup. The sanctioned pattern for shared per-subtree setup is a plain loader helper invoked from each subcommand's own `RunE` (`loadStatusContext` is the reference implementation); apply it to every future command group, not just `status`.
- `internal/cli/snapshot/` — `dwe snapshot` tree (`list`, `current`, `inspect`, `create`, `restore`, `rollback`, `remove`, `pack`, `unpack`), one file per subcommand. `NewCmd(groupID, flags)` plus shared helpers in `snapshot.go` and `liveui.go` (the `newSnapshotLiveObserver` factory wiring `liveui.LiveLine` into the workflow runner's `StepObserverFactory`). `internal/core/workflow/snapshot` is imported as `snapshotpkg` to avoid collision with the cli package name.
- `internal/cli/validate/` — `dwe validate`. Single-file subtree. `NewCmd(groupID, flags)` plus internal helpers `loadForValidate`, `errPartialLoad`, `validationFailedError` (the canonical `ExitCode()`-bearing error). `docs generate --scope cli` works without a project, but `validate` requires either `Resolve` or `Locate` depending on `isValidateCommand` routing. **Filter hint (agent-discoverability milestone)**: `shouldEmitFilterHint(rows, errors, quiet, levelRaw) bool` + `emitFilterHint(...)` write ONE stderr info line after a long human-mode table, naming `--level` / `--quiet`. Threshold `filterHintThreshold = 20` rendered rows; suppressed when `--quiet` or `--level` is already set, when `rows == errors` (neither flag would remove anything), and in JSON mode. Same shape and constraints as `cmdctx.EmitDefaultNotice` — stderr only, stdout stays the parseable surface. The predicate is split from the emitter so it is unit-testable without a cobra command.
- `internal/cli/command/` — `dwe commands [id]` (alias `cmd`), the single user-command surface. No positional id → cmdbrowser selector; exact id → direct run; `-i/--inspect <id>` prints the resolved definition (mutually exclusive with `--set` / `--yes`); `--set k=v` (repeatable, `StringArrayVar`) prefills the param form. Files: `command.go` (NewCmd), `runbyid.go` (orchestrator — see below), `list.go` (browser/tree rendering, `selectorTitle`), `inspect.go` (`printInspect`/`printInspectAt`/`inspectStepDescription`), `completion.go` (`registryIDCompletion`), `daemonset.go` (`daemonSetCompletion` for `command --set` flag completion). Orchestrator `runCommandByID(ctx, stdin/stdout/stderr, cfg, reg, projectRoot, id, runOpts)` owns the run pipeline: `reg.Get` → private guard → `prepareParams` (= `resolve.ParamDefaults` raw-string prefill, distinct from `resolve.Params` at `BuildRunContext`, + `resolve.Options` membership validation for `--set`/default values, extracted so the browser closures can reuse the exact rules) → `buildAskFields + ask.Run` (huh form for interactive prompting; skipped when all required params pre-satisfied or non-interactive) → `BuildRunContext` → attach I/O channels → render confirmation title against `rctx.Render` → `widgets.ConfirmRun` with `stringifyParams(rctx.Params)` summary → set `rctx.SkipConfirm` to prevent re-prompt → `usercommands.RunCommand`. **In-TUI param-form short-circuit (commands-param follow-up)** — `runOpts.PrefilledParams map[string]string` (nil on `dwe commands <id>` direct / `--set` / non-interactive / JSON paths — untouched) carries the params harvested by the browser's in-TUI overlay: `prepareParams` still runs UNCONDITIONALLY (its membership validation is the idempotent safety net), but when `PrefilledParams != nil` `showForm` is forced false and `values = PrefilledParams`, so `buildAskFields`/`runAsk` are skipped and the harvested map flows straight through; `BuildRunContext`'s `resolve.Params` is still the final validate. **Confirm stays post-exit** — `def.Confirmation` shows its huh yes/no after teardown (right before the run banner + streamed output), never in the overlay. The browser wiring lives in `list.go`: `makeBrowserSelector` takes the raw `setFlags []string` (parsed LAZILY inside the closures via `parseSetFlags` — eager parse would break the non-interactive `writeCommandsList` fallback which returns before `runCommandByID`) + a `prefilledOut *map[string]string` out-param (set from `Result.Values`, mirroring `skipConfirmOut`/`forceFormOut`); `makeRunFormSpec` builds the `cmdbrowser.RunFormSpec` (nil outside ModeRun) whose `BuildForm` recomputes the same `showForm` predicate then `ask.Build(..., RunOptions{ShowHelp:&false})` (returns `(nil,nil)` for no-form / zero-field empty-options commands → quit-and-run) and `Harvest` maps `ask.Result` → the param map via `mergeAnswers`. Test seams: package-level vars `runAsk` / `confirmRun` / `runUserCommand` / `notifyContext` (`signal.NotifyContext`) — subtests overriding any of these MUST NOT call `t.Parallel()`. The TUI `y` toggle is folded into `runOpts.Yes` at the call site as `yesFlag || skipFromTUI`. `runbyid.go` (not `run.go`) disambiguates from `lifecycle/run.go`.
- `internal/cli/docs/` — `dwe docs` tree. Files: `docs.go` (NewCmd + TUI launch + `docsSelectorTitle`), `generate.go` (the huge `newDocsGenerateCmd` block: `runDocsGenerate`, `validateDocsFlags`, `resolveFormats`, `resolveScopes`, `genCLIDocs`, `genHiddenCLI*`, `walkAllCommands`, `genCLIIndex`, `writeCLIIndexEntries`, `genRegistryDocs`, `genRegistryMarkdown`, `writeCommandMarkdown`, `genCommandsIndex`, `genTopLevelIndex`, `mmdcAvailable`), `cache.go`, `export.go`, `list.go`, `search.go`, `show.go`, `llmstxt.go` (newDocsLlmsTxtCmd + runDocsLlmsTxt for `dwe docs llms-txt`), `llmstxt_collectors.go` (project-data collectors — `collectServiceSummaries`, `collectCommandSummaries`, `collectInfoSummary` — kept in cli layer so `internal/core/docs/llmstxt/` stays pure-function and config-import-free). `internal/core/docs` aliased as `coredocs` in files that need both names. `docs generate --scope cli` works without a project; `--scope all` / `--scope commands` requires a project. Docs subsystem is read-only — never calls `lock.AcquireProjectLocks` and never runs preflight; resolves locale **directly** via `i18n.ResolveLocale(flagLang, cfgLang, sysLang)` (NOT `rflags.Locale` — clamped to YAML store, wrong namespace for markdown). `llmstxt_collectors.go` also carries `collectBuiltinSummaries` / `collectConditionSummaries` (agent-discoverability milestone) — `cli/docs` is the ONLY layer that may import `internal/core/execution/...`; the data reaches the generator through `Opts.Builtins` / `Opts.Conditions`, never through an import in `core/docs`. `search.go` carries the `--literal` flag and renders the append-only 4th TSV column (snippet); `--literal` is a FLAG, not shell quoting.
- `internal/cli/completion/` — install/uninstall subcommands attached to cobra's auto-generated `completion` parent via `AttachInstallUninstall(parent, flags)`. Files: `completion.go` (Attach + shared `completionExitError`, `isSupportedShell`, `supportedShells`, `ErrUnsupportedShell`), `install.go` (`newInstallCompletionCmd` + path/profile/atomic-write helpers + seam vars `resolvePowerShellProfile`/`completionHomeDir`/`completionReadFile`), `uninstall.go` (`newUninstallCompletionCmd` + `runUninstall` + `emitZshFpathHint`). In `root.go` the wiring is `root.InitDefaultCompletionCmd()` → `root.Find(["completion"])` → set `GroupID = groupAdvanced` → `completion.AttachInstallUninstall(completionCmd, flags)`.
- `internal/cli/docker/` — `dwe docker` passthrough (351 LoC of subprocess + arg-policy plumbing). `NewCmd(groupID, flags)`. `internal/shared/docker` aliased as `dockerpkg` inside the file because the package itself is now named `docker`. **Build-base prepull hook** (`build.prepull_bases`, gated on `p.dockerCfg.Build.PrepullBases`): both the `build` and `up` `RunE`s call the same-package `prepullBases(errOut io.Writer, compose *dockerpkg.Compose, services []string, force bool)` helper AFTER `newDockerPipeline` (`.env` must already be regenerated — `compose config` depends on it) and BEFORE `compose.Exec`. `prepullBases` is the one CLI-side place that prints user-facing text for this feature (`fmt.Fprintf(errOut, "warning: ...\n")`, `errOut = cmd.ErrOrStderr()`); it never returns an error — `DeriveBuildBases` failing, an individual `PullImage` failing on an already-present base, etc. all degrade to a warning and execution always proceeds to the normal `compose build`/`up` (trap: enabling the flag must never make a build worse than leaving it off). The one loud warning is a **missing** base that also failed to pull — it names the ref and states the build will likely fail. `resolveBuildInvocation` gained a `prepull bool` param: `force && prepull` emits only `--no-cache` for compose (daemon-side prepull already re-pulled the bases unconditionally); every other `(force, prepull)` combination is byte-identical to pre-flag behavior. `build`'s service filter narrows prepull derivation to the named args; `up` always derives against every service (nil filter) since `up` builds transitive dependencies too.
- `internal/cli/compose/` — `dwe compose` passthrough. `NewCmd(groupID, flags)`. Split from `docker/` to avoid the stuttering `docker.NewComposeCmd` (see Naming rationale).
- `internal/cli/deploy/` — `dwe deploy` tree. `NewCmd(groupID, flags)` exposed; `deploy.go` hosts the entry point (renamed from `root.go` for symmetry). Bare `dwe deploy` (no subcommand) in a TTY opens an interactive menu via huh's `NewSelect` with options Wizard (first, only on fresh projects) / Run (all) / Run service… / Plan (all) / Plan service… / Exit. Wizard runs only on fresh projects (missing `local.yml` or empty) when setup questions are present OR port conflicts exist; orchestration in `internal/core/workflow/setup/`. Non-TTY `dwe deploy` prints help and exits 2. Subcommands `plan`, `run`, `state` are non-interactive CLI. `deploy step` is removed entirely — no alias or shim. `cli/deploy/menu.go` uses package-private `runDeployRunFn`/`runDeployPlanFn` rather than dispatching into `cli/lifecycle/`. `RunHelper` is consumed by `cli/service/` (documented pre-existing cross-import).
- `internal/cli/lifecycle/` — multi-export Go-domain grouping for `run`/`stop`/`restart`/`reset`. Files: `lifecycle.go` (package preamble + `var preflightRun = preflight.Run` test seam — **only `reset.go` uses the seam**; `stop.go` calls `preflight.Run` directly and `run.go`/`restart.go` route through `deploy.RunHelper`), `run.go` / `stop.go` / `restart.go` / `reset.go`. Exports: `NewRunCmd`, `NewStopCmd`, `NewRestartCmd`, `NewResetCmd` (each `(groupID, flags)`). `stop.go` also defines `StopService(ctx, StopServiceDeps, name)` / `stopServiceLocked(ctx, StopServiceDeps, name)` (per-service docker-stop bypass for `dwe stop <name>` — compose overlay is filtered out for disabled services, so direct `docker stop` is required; locked variant for callers that already hold project locks). Per-service reset does NOT use `stopServiceLocked` — it drives stop+rm through the synthetic-pipeline `docker_stop_remove_container` builtin instead. `restart.go` also defines `RestartService(ctx context.Context, baseDir string, cfg *config.DweConfig, name string, out io.Writer) error` (per-service docker-restart bypass for `dwe restart <name>` — no preflight, no project locks; `docker.ErrNoSuchContainer` maps to a user-actionable "run `dwe deploy run` first" error; `var restartContainerFn = docker.RestartContainer` seam for test injection). Lifecycle commands acquire project locks **after** preflight succeeds so `type: command` checks do not run under operation locks. `internal/core/workflow/lifecycle` aliased as `lifecyclepkg` in `run.go`/`restart.go`/`stop.go`. `info.Run` consumed via the sibling import documented above. **`reset --clear-generated`**: clears the `generatedstore` (scoped by `--service` via `ClearService` / all via `ClearAll` + `Save`) **only after the FULL reset succeeds — including the post-pipeline journal cleanup** (`journal.Remove` for full reset / `journal.ReplaceServiceWithPending` per-service), never merely after `RunWithOptions` returns; a pipeline OR journal-mutation failure leaves the store untouched (else a deployed-journal + empty-store mismatch would make the run gate trust a service with no secrets). Full reset has NO command-level confirm to hook (confirmation is a pipeline step), so the interactive "also clear N generated values?" prompt is decided at the command level up front (default No); per-service reuses the existing `resetConfirmFn` seam. Default behavior unchanged — store preserved. The per-service restart bypass is deliberately *lighter* than argument-less `dwe restart` (which runs the full stop lifecycle then the full run lifecycle): it skips compose, lifecycle hooks, preflight and project locks entirely and issues a single `docker restart`, so do not "restore" preflight/locks there. Per-service reset likewise never removes volumes — that stays opt-in per service by adding `docker_remove_project_volumes` to `workspace/services/<name>/reset.yml`.
- `internal/cli/render/` — `dwe render` tree (env / ide / ai / git / **config**). `NewCmd(groupID, flags)`; entry point in `render.go` (renamed from `root.go` for symmetry). `config.go` adds `dwe render config [service]` (optional positional → one service; none → all eligible **app** services via `DeployOrder(cfg, []string{"app"})` — config rendering is app-only, only apps declare `dir`/`render`/`generated`) driving `templates/config.RenderConfigs`; `--harvest` switches to a **harvest-only** pass (`HarvestGenerated`, NO render) for bootstrapping an already-committed value into the store. The default render path is read-only wrt locks (no preflight, no project locks); `--harvest` mutates the shared store, so it acquires project locks first (via `cmdctx.AcquireProjectLocksOrReport`, mirroring the deploy harvest builtin and `reset --clear-generated`) before loading the store. Resolves the store path from baseDir + `generatedstore.DefaultRelPath`; cli is the single stdout writer. Opt-in — no `dwe init` scaffold wiring (matches the user-authored copy mechanism it replaces). **Container reachability**: `render config` is a nested bridge exception (so a container can regenerate config after `vars set`); the other render subcommands (env/ide/ai/git) stay host-only. Because `--harvest` MUTATES host `.dwe/generated.yml` (a write path that bypasses `bridge.vars_writable`), `config.go` rejects `--harvest` with typed `render_harvest_host_only` when `bridgeclient.InContainer()` — only the read-only render is container-reachable.
- `internal/cli/vars/` — `dwe vars` tree (registered under `groupConfiguration` alongside `service`/`render`/`validate`/`scaffold`). `NewCmd(groupID, flags)` plus `get.go` / `list.go` / `inspect.go` / `set.go` / `browser.go`. `get <var>` (effective value — scalar or YAML subtree), `list [namespace]` (flat `vars.*` leaves, namespace-filterable, mirrors `commands list`), `inspect <var>` (per-layer `Default`/`Local`/`Current` via `config.ResolveLayeredPath` + usages via `varsusage.ScanUsages` → `uirender.VarInspectView`), `set <var> [value]` (writes `local.yml` via the comment-preserving node path; `vars.*` path-confined; coerces via `varsusage.CoerceScalar`; acquires project locks via `cmdctx.AcquireProjectLocksOrReport` (symmetry with the `services` toggle that shares the writer/file — no preflight, not a lifecycle mutation); pre-state-bytes rollback; reloads config; no `value` + interactive → huh form via the `runAsk = ask.Run` seam, JSON/non-interactive with no value → `vars_value_required`). **Container-write gate** lives here (runtime, since `bridgeCommandAllowed` is prefix-wide): when invoked from a container (`bridgeclient.InContainer()`), the target must match `config.VarsWritableAllows(cfg.Bridge.VarsWritable, target)` else typed `vars_not_container_writable`; from the host `set` is unrestricted. Bare `dwe vars` → TUI browser (`browser.go`, `cmdbrowser` in `ModeEdit`); `!IsInteractive || cmdctx.NonInteractiveEnv()` or a namespace arg or JSON mode → `vars list`. Completion via `cmdctx.CompletionConfigPath`. **In-TUI edit-and-stay** (TUI Stage 7): the ≥80-col frame path wires `cmdbrowser.Options.Edit` via `newVarsEditSpec(flags, leaves, inspectCache)` — `BuildForm(idx)` opens the shared `set` form (`ask.Build`, `RunOptions.ShowHelp:false`, fields from `buildVarSetFields` carrying the layered `varSetFormDescription` + a `varsusage.CoerceScalar` `Validate` probe — invalid scalars/maps/sequences rejected **in-form**, an improvement over the old post-submit error, though the post-submit coercion stays authoritative), and `Commit(idx, res)` coerces → `writeVarOverrideSilent` (`cmdctx.AcquireProjectLocksSilent` → the shared `writeVarOverrideCore` — capture / apply overlay / atomic write / reload / restore-on-failure, extracted from `writeVarOverride`) → `delete(inspectCache, path)` → rebuilds the one `Item` (fresh value + `layerBadge` + `Inspect` closure via `buildVarBrowserItem`) → `CommitOutcome{Item, Flash}`. A held lock is **returned as the error flash, never printed** (alt-screen is live). `writeVarOverride` keeps the printing `cmdctx.AcquireProjectLocksOrReport` wrapper over the same core for the CLI path. **Behaviour change:** after an in-TUI edit the browser stays open with the row refreshed and a `✓ …` status flash — the old `✓ set …` stdout line is gone (nothing is printed on exit); edit-and-stay applies **only** to the frame path — the <80-col fallback keeps the flat exit→`runVarsSet`→reopen loop. Standalone `vars set <path>` (no value) and the JSON / non-interactive paths are unchanged (plus the new inline field validation on the interactive form). The container check at the `vars set` call site is `bridgeclient.InContainer()`, the *equivalent but separate* detector to `bridgeInvokedFromContainer()` in `internal/cli/bridgepolicy.go` used by the prefix-wide command allowlist — two call sites reading the same `DWE_INVOKED_FROM` contract, so a change to one must be mirrored in the other or the read gate and the write gate disagree about what "in a container" means.
- `internal/cli/service/` — `dwe services` tree. `NewCmd(groupID, flags)`. Bare `dwe services` opens a single multi-select toggle in TTY text mode across every optional service of any type with mandatory items pre-checked/disabled and a `Type` column visible in the table; non-TTY stdin OR `--output json` dispatches to a read-only list renderer (`service/service_list.go`): text mode emits Apps / Tools / Infra sections reusing `stack.RenderApps`/`RenderTools`/`RenderInfra`, JSON mode emits a unified `{"services":[{name,type,container_name,mandatory,enabled,running,status,ports?,hosts?}, …]}` array. All-mandatory short-circuit before opening the form; `services enable <name>` / `services disable <name>` look up the service by name across all types (no `--type` flag); `enable <mandatory>` warns and exits 0; `disable <mandatory>` errors and exits non-zero; completion via `completeDisabledOptional` / `completeEnabledOptional` filters span the union across all service types. **`dwe tools` is dropped** — no alias, no shim; cobra emits the standard "unknown command" error. `service/service_plan.go` contains `buildTogglePlan(cfg, registry, svcDeploys, toggles) (TogglePlan, error)` (pure plan builder, no filesystem access), `executeTogglePlan(ctx, deps ExecuteDeps, plan TogglePlan, opts ExecuteOptions) error` (orchestrates before/apply/after steps via stubbed runtimes `RunDeploy`/`RunRestart`/`RunUserCmd`), `runDeployHelper(ctx, cmd, flags, opts DeployOpts) error` (extracted from `deployRunCmd` for reuse by the toggle executor; `DeployOpts.Services []string` supports nil=full-project, one=single-service, N=multi-service batch). `TogglePlan{BeforeSteps, ApplySteps []ApplyStep, AfterSteps, Notes}`. `ApplyStep{Kind PendingKind, Services []string}`. `mutateAndPlan`/`mutateAndPlanBatch` now write `local.yml` via `local.ServiceTogglesOverlay` → `local.ApplyOverlayToNode` → `local.WriteLocalYAMLNode` (the comment-preserving node path shared with `vars set`/setup), replacing the load-map → mutate-map → `WriteLocalYAML` flow; validation (`ValidateServiceToggle`) and pre-state-bytes rollback are preserved. The pre-existing `cli/service/` → `cli/deploy.RunHelper` cross-import is documented and out of scope to fix here. **Pending-state gate.** A toggle without `--apply` writes `local.yml` and regenerates `.env` (`envfile.Write`) immediately, then records the deferred work as journal pending — but only through the `journalSnapshot.everDeployed` gate (`journalSnapshot` / `loadJournalSnapshot` in `service_toggle.go`). Note the shape: pending is **project-level** `journal.ProjectState.Pending *PendingApply`, with the affected services carried inside `PendingOp.Services []string` — there is no per-service pending field. `everDeployed` is true when ANY service state is non-empty and not `StatusNotDeployed` (so `StatusDeployed` / `StatusFailed` / `StatusInProgress` / `StatusPartial` all count), OR `state.Project.LastRun != nil`, OR `state.Project.Status` is non-empty and not `StatusNotDeployed` (the project-only-deploy case, where `state.Services` is empty). A journal **load error** also yields `everDeployed = true`: silently skipping the pending write on a corrupt file would drop the toggle's intent invisibly, whereas letting `AddPendingOps` run means the file is either repaired or the command hard-fails with the corruption in the message. The gate is therefore closed only when there has been NO deploy attempt at all — pending means "config drifted since deploy" and is meaningless before the first one. With the gate closed the flow both skips the pending write AND suppresses the stack probe, the "stack is not running; hooks and pending state were skipped" banner and the apply prompt, printing instead a single "stack has not been deployed yet; run `dwe deploy` to apply." line after the rendered plan — emitted regardless of plan shape, because even a `RequiresNone` toggle takes effect only at the first deploy. `--apply` is the exception: it still runs the docker probe on a never-deployed stack so a broken docker setup surfaces as an early warning instead of a deeper executor error, and it always executes (the apply step is itself what brings the stack up). After a successful apply, `executeTogglePlan` clears pending with `journal.ClearPendingOps` under a fresh `lock.AcquireProjectLocks`, passing the `[]journal.PendingClear` built by `buildPendingClears(contributors)` — never `ClearPending`, which would erase pending recorded by other sessions.
- `internal/cli/scaffold/` — `dwe init` (the bootstrap command — creates a project rather than acting inside one). `NewCmd(groupID, flags)`. **Package name `scaffold`, command `Use: "init"`** — `init` is a reserved Go function name and an illegal package name, so both the cli and core packages are named `scaffold`. Owns flag parsing, mode selection, the huh form, and result reporting; all disk work is delegated to `internal/core/workflow/scaffold` (this package is the only writer to stdout/stderr). **Mode selection**: interactive iff `widgets.IsInteractiveFn(os.Stdin) && !useDefaults && flags.Output != "json"` (the `-d/--default` flag takes all defaults; `-f/--force` recreates/overwrites). Flags pre-fill the form's field defaults and the **entire form is collected before any disk write**, so a mid-form `Ctrl-C` (surfaced as `widgets.ErrCancelled`) leaves the disk untouched and exits 0. The form is behind a package-level `runFormFn` seam (mirrors `cli/command/runbyid.go`'s `runAsk`) so the abort path and value-mapping are testable without a real TTY; flag→`core.Options` mapping and output shaping stay in plain functions outside the seam. **Existing-project gate**: before any write, if `core.HasProjectConfig(core.ResolveTarget(targetDir))` reports a pre-existing `workspace.yml` in the *target itself* (distinct from the ancestor `NestedWarning` check) and `--force` is not set, the run requires explicit consent — interactive confirmation via the `confirmRecreateFn` seam (returns `widgets.ErrCancelled` on Esc/Ctrl-C → clean exit), or a `scaffold_project_exists` error in non-interactive mode telling the user to pass `--force`. On confirmation it sets `force=true` and recreates everything; declining is a clean no-op. **Field validation**: `validateName`/`validatePrefix`/`validateAccent`/`brandTextValidator` run both as per-field huh validators AND, via `validateInput`, on the final resolved values before scaffolding — so flag-driven (non-interactive) inputs are checked too (accent must be 6-digit `#RRGGBB`; prefix lowercase `[a-z0-9][a-z0-9_-]*`; branding text single-line, no control chars). Name resolution precedence: `--name` → positional `[name]` → cwd basename; a positional `[name]` also sets the target dir to `./<name>/`. JSON output shape: `{target, created, skipped, symlink_fallback, nested_warning}` via `cmdctx.WriteData`; errors via typed `cmdctx.Err`/`ErrWrap`. **No preflight, no project locks** (there is no project yet) — `init` is in `allowedWithoutProject` keyed on the full path `"dwe init"` (not bare `"init"`, which `cmd.CommandPath()` never yields).
- `internal/cli/bridge/` — `dwe bridge` subtree (Advanced group): `start.go` / `stop.go` / `status.go` / `logs.go` plus the hidden `bridge daemon` foreground entry (`daemon.go`) that the lifecycle ensure spawns. `NewCmd(groupID, flags)`; every subcommand is `cobra.NoArgs`. `bridge daemon --project-root <abs>` is in `allowedWithoutProject` (it takes everything from the flag; cwd discovery must not gate a detached spawn) and redirects its own stderr/stdout to `.dwe/bridge/daemon.log` via the spawner. **Every daemon-touching call goes through a same-package seam** (`ensureDaemonFn` / `stopDaemonFn` / `probeDaemonFn` + an `init()` stub for the whole test binary, mirroring `cli/status`) — the production functions spawn/probe via `os.Executable()`, which from a test recursively re-executes the test binary (the documented recursion hazard). `start` refuses with typed `bridge_not_enabled` when no enabled service has bridge enabled (a daemon for zero bridged services is useless); `stop` stays an idempotent no-op and appends the not-enabled explanation to its not-running message. `status` reports liveness via `bridge.ProbeDaemon` (pidfile-flock probe; uptime = pidfile mtime, written exactly once by the daemon's own startup acquire), socket/port from the bridge-dir files (a stopped daemon shows neither), and shim state via `shimassets.Status`; everything through `cmdctx.WriteData` (JSON contract). `logs --tail N` defaults to 50, `0` = all, negative → `invalid_tail` (exit 2, same code as `dwe logs`); missing file → stderr notice in text mode, `{"lines":[]}` in JSON. From containers only `bridge status` is reachable (see Container command policy above). User-visible behavior: `docs/reference/concepts/bridge.md`.
- `internal/cli/test/` — `dwe test` tree (Pipelines group, integration-tests milestone — stages 1b through 3b): `run.go` (`newTestRunCmd`, `runTestRun`) and `list.go`, registered via `NewCmd(groupID, flags)` in `test.go`. `test` requires a project — absent from `allowedWithoutProject`, and absent from `bridgeAllowedTopLevel` (`bridgepolicy.go`), so it is blocked in container context with zero production code (pinned in `bridgepolicy_test.go`). `runTestRun` calls `envtest.ScrubComposeEnv()` as its first statement — before the run-level `signal.NotifyContext`, the `scenarioRunner` seam, or any subprocess — per the spec §3 ordering. `ScrubComposeEnv` unsets every `COMPOSE_*` variable in the process and deliberately leaves `DOCKER_*` untouched (daemon/context selection must still apply), so the disposable copy's compose invocations can never inherit an ambient `COMPOSE_PROJECT_NAME`/`COMPOSE_FILE` from the developer's shell; it is environment hygiene, unrelated to the bridge daemon's dangerous-env strip set (no trust boundary here). The canonical ordering rule is "before the per-scenario flock, any goroutine, UI, or subprocess" — which is why stage 3b's parallel entry point `runTest` scrubs again at its top (after only the `--parallel < 1` guard) before fanning out; the unset is idempotent, so the sequential path's second call in `runTestRun` is harmless. `scenarioRunner` is a one-method interface (`RunScenario(ctx, envtest.RunRequest) (*envtest.ScenarioResult, error)`) satisfied by `envtest.NewRunner()`; the package-level `newRunner` var is the test seam (a real `Runner` would spawn `dwe validate`/`dwe deploy run` subprocesses and recurse into the test binary). No args → every scenario from `envtest.ListScenarios` (sorted); explicit names are validated against that same list up front so an unknown name fails via a typed `cmdctx.Err("unknown_scenario", …)` before any scenario runs — `"unknown_scenario"` was added to `cmdctx.ExitCodeFor`'s usage-class switch (mirroring the existing `logs` package's `"invalid_since"` entry) rather than inventing a parallel exit-code mechanism. A scenario `RunScenario` refuses to even attempt (lock held, kept run, load failure — a bare `error`, no `*ScenarioResult`) is folded into the same result set as a synthetic `StatusError` outcome, so the batch keeps going and still reports every requested scenario. Exit codes are carried by a private `testRunOutcomeError{code}` whose `Error()` renders no text (same pattern as `deploy`'s `deployCancelledError`) — any `error`-status outcome → 2, else any `failed` → 1, else 0; the run's JSON/text payload is written via `cmdctx.WriteData` BEFORE this sentinel is returned, so main.go's `ExitCode()` path suppresses further stderr output and the already-written payload stays the sole stdout content. **JSON mode never leaks live pipeline output**: `flags.Output == "json"` swaps in `silentReporterFactory` (screen writer = `sharedrender.NewWriter(io.Discard)`, termOut = `io.Discard`, but the file log via `pipeline.OpenPipelineLog` still records) as the `envtest.RunRequest.ReporterFactory`, rather than hardwiring the runner to `os.Stdout` (`silentReporterFactory` is renamed from the old `jsonReporterFactory` because stage 3b's parallel text path reuses it too — see the stage-3b bullet below). JSON payload shape: `{scenarios: [{name, status, failed_step, duration_seconds, report_dir}], summary}` (`failed_step`/`report_dir` are `omitempty` — omitted for a passing scenario); text mode renders one line per scenario (status, failed step or prep-error message, duration, and — under `--keep` — the kept compose project name + copy path) followed by the same `buildSummary` line used by both modes (`"N passed, M failed (name: step \"…\"; …)"`). `flags.I18n`/`flags.Locale` thread into `envtest.RunRequest` so a scenario's own `type: command` steps keep the display-string contract; no new `ui.*` i18n keys were needed since `dwe test` introduces no TUI surface (verified against `internal/shared/i18n/translator.go` — that table is scoped to TUI/docs-browser strings, not a general per-command string table, matching every other plain CLI subcommand). `list.go` renders scenario name + verbatim `description:` (spec §8, never localized) via `envtest.LoadScenario` per discovered name; an absent `workspace/tests/` directory renders an empty list, not an error. **Stage 2 — `clean.go`** (`newTestCleanCmd`, `dwe test clean [scenario...]`, `--dry-run`): mirrors `run`'s composition — `envtest.ScrubComposeEnv()` FIRST (clean drives `compose down` via `Teardown`, same ordering rule as `run`), then the package-level `cleanFn = envtest.Clean` seam var (mirrors `newRunner`) builds and runs a `CleanRequest` from args/flags, rendered via `cmdctx.WriteData` — JSON `{dry_run, swept, skipped, failed, orphans}`, text = one summary line (`"swept N, skipped M (live), P failed, K orphan(s)"`, "would sweep" wording under `--dry-run`). Exit code uses the same write-payload-then-return-a-no-text-sentinel pattern as `testRunOutcomeError`: any `Failed` entry → 1 (the payload is still emitted so the caller can see which manifest failed); `Skipped`(live) and `Orphans` are NOT failures (exit 0); a hard `Clean` error (unreadable manifests dir) surfaces as a normal `cmdctx.Err`. `clean` is registered in `test.go` alongside `run`/`list` and inherits the whole `test` subtree's container-block (absent from `bridgeAllowedTopLevel`) with zero extra production code — `bridgepolicy_test.go` pins `dwe test clean` explicitly in addition to the top-level `test` entry. `run.go`'s `--keep` hint (`renderScenarioLine`) now points at `dwe test clean <scenario>` instead of "clean up manually"; `renderScenarioLine` also gained a `report: <dir>` line whenever `o.ReportDir != ""` (a failed, non-kept scenario), so the failure-report path is discoverable in the default text output the same way `report_dir` already was in JSON. Both are best-effort: `Runner.finish` sets `ReportDir` only when `CollectReport` returns without error (i.e. the report directory was created) — a failed scenario whose report dir could not even be created renders no `report:` line and an empty `report_dir`, and the artifacts inside a created report may themselves be partial/blank when a capture fails. **Stage 3b — `--parallel N` orchestration** (`run.go` + `livestatus.go`): the `--parallel` int flag (default 1) fans scenarios out; `N < 1` → typed `cmdctx.Err("invalid_parallel", …)` (exit 2) before anything runs. **Orchestration lives entirely in the CLI** (`envtest` stays UI-free — it only gained the coarse `Progress` seam) — `runTest` computes `effective = min(parallel, len(names))`, and `effective <= 1` takes the **byte-identical** sequential path (`runTestRun`, the pre-stage-3b loop). Only `effective > 1` enters `runTestParallel`: a plain `errgroup.Group` (NOT `errgroup.WithContext` — sibling scenarios must never cancel each other) with `SetLimit(effective)`, one goroutine per scenario in original order writing its outcome into `slots[i]` by index. **Goroutines ALWAYS return nil** — a `RunScenario` error becomes a per-scenario `StatusError` outcome exactly as the sequential path folds it, never a group cancellation; each goroutine's first statement is `if ctx.Err() != nil { return nil }` (a cancelled ctx before dispatch leaves the slot nil, matching the sequential `break`). After `Wait`, nil slots (scenarios never started due to Ctrl+C) are compacted out, so both text and JSON output stay deterministic and in name order regardless of completion order. The parallel path uses `silentReporterFactory` (the same silent factory JSON mode uses — per-scenario deploy/pipeline output lands in the copy's own run log only) and installs the aggregated display + a per-scenario `Progress` closure (`req.Progress = func(p) { display.Phase(i, p) }`) **only in text mode** (`flags.Output != "json"`); JSON mode at N>1 passes a nil display and the same silent factory with suppressed warnings — enforced by construction. **`livestatus.go`** — `runLiveStatus` wraps one `liveui.LiveLine` in block mode: constructor seeds every visible row via `SetBlockRowPending(i, name)` where `visibleRows = max(1, min(len(names), termHeight−3))` (via an injectable `termSize` seam; overflow scenarios beyond `visibleRows` report start/finish via framed `Println` lines instead of a row). `Started`/`Phase`/`Finished`/`Warn`/`Close` map to the block-row methods (running label `name  <phase>…`, final `SetBlockRowFinal` `name  passed` / `name  failed — step "X"`, footer `running k/n scenarios…`); `Warn` routes through `PrintlnDiag` (bytes land on the stderr diag writer, block stays intact). **Non-TTY degradation is explicit**: `Started`/`Finished` branch on `!s.enabled` and emit flat `scenario <name>: started` / `scenario <name>: <status>` `Println` lines for EVERY scenario (not just overflow) — the block-row methods are silent no-ops when the LiveLine is disabled, so relying on them would leave a piped/CI run mute until the final report. `runLiveStatus` holds its own mutex for the full body of every method: in disabled mode `LiveLine.Println`/`PrintlnDiag` write to the shared writer without the LiveLine mutex, so the display mutex serializes those cross-goroutine writes (nesting `s.mu` → `live.mu` is deadlock-free — LiveLine never calls back). `phaseLabels` maps each `envtest.ProgressPhase` to a plain-English label (`preparing…`, `validating…`, …) — the test CLI surface is intentionally non-localized. `fakeRunner`'s call recording is mutex-guarded so the parallel tests (and `make test-race`) can drive it from multiple goroutines. **Output styling (`style.go`)** — the run report, `list`, `clean`, and per-scenario warnings render through the `internal/core/ui/styles` (lipgloss) palette: accent scenario names, severity-colored status words (success/danger/warning), muted secondary text, and ✓/✗/• glyphs matching the aggregated live view. Styling is gated per output surface by `writerIsTTY(w io.Writer)` (true only for a `*os.File` TTY) — the color-off branch of every render helper is **byte-identical** to the historical plain form, so a pipe/redirect/CI run (and every exact-match golden, which writes to a `*bytes.Buffer`) stays ANSI- and glyph-free; only interactive terminals get color. The JSON `payload.Summary` is built with `color=false` so ANSI never leaks into the JSON envelope; the parallel display's `Warn` gates on the block's own `s.enabled` (stdout-TTY) flag. Color-path tests force the profile via `lipgloss.SetColorProfile(termenv.TrueColor)` (mirroring `styles_test.go`) and assert on `stripANSI`ed visible text + glyph presence. **Cost profile (`profile.go`, agent-discoverability milestone)**: `newCostProfiler(baseDir, configPath) *costProfiler` + nil-receiver-safe `(*costProfiler).profile(scn) *testCostProfileJSON` back the `cost_profile` object on each `dwe test list --output json` row. Three invariants: (a) the profiler is built **only** in JSON mode, so the text path keeps `list`'s original no-config-load shape byte-for-byte; (b) EVERY failure path in `newCostProfiler` returns nil (config, `docker.yml`, project/service deploy pipelines) and `profile` is nil-receiver-safe, so an unloadable config degrades to "no profile", never to an error — `list` must keep working while the config is mid-edit; (c) the command registry is loaded best-effort (nil on failure) and only feeds `host_steps`. `scenarioServices` applies the scenario's `env.services` overlay (enable before disable; a `required: true` service stays enabled) onto a shallow config view, so the compose chain is scenario-specific too. **`host_steps` is the gate field and is deliberately wider than `type: shell`** — see `countHostSteps`: shell steps, the `shell` builtin, a `type: command` resolving to a non-container command (recursing through `workflow`s; an UNRESOLVABLE id counts as host — the safe direction for a field that gates an unattended run), a `type: dwe` whose subcommand re-enters project code (`cmd`/`run`/`restart`/`deploy`/`reset`/`test`), and shell `when:`/`check:` including the `check: auto` sentinel; a step counts once however many channels it uses, and a phase-level shell `when:` counts as one. dwe's own subcommands are NOT counted — the built-in default pipeline is all `type: dwe`, so counting them would leave every project permanently gated and the field would report nothing. `isolation_findings` carries only the **non-blocking** kinds (the blocking ones abort the run before deploy anyway). Facts only — no cheap/expensive verdict; that decision rule lives in `skills/dwe/SKILL.md` so it can change without a release. Invariants (a) and (b) together protect `list`'s **full** read-only contract, not just the config load: `dwe test list` takes no locks and touches no Docker, so every fact in `cost_profile` is derived by statically parsing config/compose/pipeline files — never by probing a daemon — and a future field that needs a probe belongs somewhere else. The `effective <= 1` byte-identity is not aspirational either: `TestRunTest_ParallelExceedsScenarioCount_SequentialPath` runs `--parallel 8` over a single scenario against `--parallel 1` and asserts exact stdout AND stderr equality (plus that the sequential path installs no `ReporterFactory`). Parallel per-scenario warnings render as `[<scenario>] warning: <msg>` (`scenarioWarnLine`, `style.go`) before `PrintlnDiag`, so an interleaved warning from N goroutines is always attributable to its scenario.

## Core — Foundation (`internal/core/project/`)

- `internal/core/project/project/` — project discovery: `Locate(flag)` (upward walk from cwd; explicit mode when flag non-empty), `Resolve(flag)` (Locate + validates required project config fields); `ErrNotFound` sentinel; constant `ConfigFilename`.
- `internal/core/project/config/` — `DweConfig` and all YAML loaders (`LoadConfig`, `LoadServices`, `LoadServiceFolder`, `LoadProjectDeployConfig`, `LoadServiceDeployConfig`, `LoadDockerConfig`, `LoadLifecycleConfig`, `LoadInfoConfig`, `LoadStylesConfig`); merged-config types (services, render policy, parallel groups, actions, file gates, `DeployStep.SubStepOverrides` / `SubStepOverride`, `StatusColumn` for custom per-row columns). **Unified services schema.** All service-shaped entities (apps, tools, infra) live under `workspace/services/`, one folder per service. `service.yml` is the per-folder declaration file; folder name = map key (no `name:` field inside the file). Services are discriminated by `type:` (`app` | `tool` | `infra`). `ServiceType` is a named string type with typed constants `ServiceTypeApp` / `ServiceTypeTool` / `ServiceTypeInfra`, predicate methods `IsApp()` / `IsTool()` / `IsInfra()` on both `ServiceType` and `ServiceConfig`, sentinels `ErrServiceTypeMissing` / `ErrServiceTypeUnknown` / `ErrServiceFieldNotAllowed` / `ErrServiceExtendsCrossType` / `ErrServicePortsShape` / `ErrServiceHostsShape` / `ErrServicePortOutOfRange` / `ErrDependsOnTool`, and an unexported `allowedFieldsFor(t)` field allowlist consumed by the loader's strict decode and hand-mirrored by the validators (see the allowlist-bookkeeping paragraph below — nothing cross-checks the two tables) (updated to include `on_enable`, `on_disable`, `notes` for all toggleable service types). Each `ServiceConfig` owns `Ports map[string]ServicePortSpec` and `Hosts map[string]string` (always maps — no scalar shorthand). `ServicePortSpec{Port int, Scheme string}` accepts two YAML shapes via a custom `UnmarshalYAML`: bare-int shorthand (`http: 5173` → `{Port: 5173, Scheme: ""}`) or rich form (`http: {port: 5173, scheme: https}`). The mapping branch enforces its own strictness (unknown fields rejected, `port:` required at service.yml decode, null `port:` rejected) because `yaml.Node.Decode` does NOT inherit a parent decoder's `KnownFields(true)`. Helpers: `Port(name) int` returns `Ports[name].Port` (zero for missing); `PortScheme(name) string` returns the per-port override (empty when not set); `EffectiveScheme(portName, runtimeUseHTTPS) string` walks the precedence chain (per-port `Scheme` → `Info.Scheme` → runtime fallback → `"http"`/`"https"`) and is the canonical way to resolve a URL scheme for a service port; `PortNumbers() map[string]int` flattens to a scheme-less view for downstream consumers that don't carry scheme metadata (status rows, conflict probes). `ServiceInfoBlock.Scheme string` is the service-wide scheme override consulted by `EffectiveScheme`. For reverse-proxy URLs (`auto-urls` `port_via`), the **routed** service's `Info.Scheme` participates — it both wins the URL scheme and pins which proxy listener key (`http` vs `https`) gets looked up, via `resolveProxyTarget` in `internal/core/ui/render/info_auto_urls.go` / `resolveProxyTargetData` in `internal/cli/info/info_data.go`. The **proxy's own** `Info.Scheme` is deliberately ignored (it would otherwise leak the proxy's dashboard scheme onto every routed service); only a per-port override on the proxy's chosen listener plus `runtime.use_https` participate as fallbacks. `Host(name) string` returns the hostname for missing keys. `ServiceConfig` also has `OnEnable *ServiceToggleHooks`, `OnDisable *ServiceToggleHooks`, `Notes *ServiceNotes` fields. `ServiceToggleHooks{Requires ToggleRequires, Before []string, After []string}`; `ToggleRequires` is a string enum with zero-value `RequiresUnspecified`, values `RequiresNone`/`RequiresRestart`/`RequiresDeploy`. The dot-path namespace is `services.<name>.ports.<port-name>` (always a bare int, even when the typed `ServicePortSpec` carries a scheme override — preserves the contract for env-export rules like `from: services.main.ports.http`) and `services.<name>.hosts.<host-name>`. Per-port scheme overrides are surfaced as a sibling dot-path under `services.<name>.port_schemes.<port-name>` (string), emitted by `injectServicesIntoRaw` only for services that actually use the feature. **`LoadConfig` sequencing**: load services via `LoadServices(baseDir)` → `validateServicesOverlay` per layer (rejects any key other than `enabled:` / `ports:` / `hosts:` under `services.<name>` with per-layer source attribution; per-port value is either a bare int in 1..65535 or a rich-form mapping `{port?, scheme?}` — at the overlay layer, `port` is OPTIONAL so a developer can override scheme alone for an inherited port, but at least one of `port`/`scheme` must be present; hosts map → string) → merge 3-layer raw YAML (workspace.yml → defaults.yml → local.yml) → resolve `Enabled` and **deep-merge** per-developer port/host overrides into `cfg.Services[name].Ports` / `.Hosts` by entry name (`applyOverlayPorts` / `applyOverlayHosts` — declared entries the overlay does not mention are preserved; new entries are added) → `injectServicesIntoRaw` (mirrors every resolved field including post-overlay `ports` / `hosts` so `${services.X.ports.Y}` resolves through `Raw`). `LoadServiceFolder(baseDir, name)` reads `workspace/services/<name>/service.yml` with strict decode; `LoadServices(baseDir)` enumerates `workspace/services/*/`, calls `LoadServiceFolder` per folder, applies cross-service `extends:` toposort + inheritance merge, and collects all folder errors via `errors.Join`. Missing `workspace/services/` directory → empty map (no error). **Per-developer port/host overrides are a load-bearing DWE feature** — developers must be able to remap a clashing port or change a `*.local` hostname in `local.yml` without touching the shared service folders, and the resolved values must stay visible in `dwe status`. Do not narrow the overlay allowlist back to `enabled:` only, and do not remove the HOSTS / PORTS columns from `render.ServicesTable`. `RuntimeConfig` is now `{ UseHTTPS, SPX }`. `extends:` resolution uses `slices.Clone` / `maps.Clone` defensive copies so child mutation never corrupts parent. Per-type accessors `(c *DweConfig).AppServices()` / `ToolServices()` / `InfraServices()` (each filters by type); `ComposeFiles()` / `ComposeFilesAll()` emit `base → tools (sorted) → infra (sorted) → apps (sorted) → c.Compose.Extra` — order is part of the public surface, locked. Per-service `compose.extra` overlays from `workspace/local.yml` (`ServiceConfig.LocalComposeExtra`, `yaml:"-"`) are emitted **inside the same `all || svc.Enabled` gate** as `svc.Compose`, immediately after the service's own compose files; project-wide `compose.extra` (`ComposeConfig.Extra`, `yaml:"-"`) is appended **strictly last** to `composeFiles()` output and is unconditional. Both fields are populated by post-decode injection from the local.yml raw map only — never decoded directly from any YAML; injection runs BEFORE `ResolveServiceExtends`, which copies `LocalComposeExtra` parent→child when the child has none. Pre-merge validators `validateOverlayCompose` / `validateLocalCompose` / `validateNonLocalCompose` gate the schema to local.yml only (non-local layers see a hard error pointing at `workspace/local.yml`). Path safety after injection runs four ordered checks via `validateComposeExtraPathSafety` / `validateComposeExtraPath`: (1) reject `filepath.IsAbs(p)` — absolute paths bypass containment since `filepath.Join(baseDir, "/x")` returns `/"/x"`; (2) reject `filepath.Clean(p) != p` — unclean paths such as `link/../evil.yml` erase symlink components before `filepath.Join`, defeating the symlink check in step 4; (3) `pathsafe.ContainedRel(baseDir, abs)` — rejects `..`-escapes; (4) `pathsafe.CheckNoSymlinks(baseDir, abs, "compose overlay")` — rejects symlink components in the resolved path. Existence (`os.Stat`) is step 5 but is skipped for disabled-service overlays (`validateComposeExtraPathSafety` without Stat) because disabled services are excluded from `ComposeFiles()` while structural checks 1–4 still run since `ComposeFilesAll()` includes their paths. Paths are stored as-written (relative); Docker Compose resolves them via `cmd.Dir = BaseDir`. User-facing details: `docs/reference/config/workspace.md` § Compose overlays. **Always** access binaries via `config.DweBin`/`DockerBin`/`ShellBin`/`GitBin` (nil-safe), never `cfg.Binaries.*`. **Deploy config loaders** were split by context: `LoadProjectDeployConfig(path)` (project-wide `workspace/deploy.yml` — `after:` is not a field of `ProjectDeployConfig`, so strict `KnownFields(true)` decode rejects it); `LoadServiceDeployConfig(path)` (per-service deploy — permits `after:`); `LoadServiceDeployConfigs(baseDir, services)` (bulk loader for all services); `ParseDeployConfigForValidation(path)` (parse-only for validators, permits `after:` so validators can inspect and emit per-file diagnostics). `LoadResetConfig` (project-wide) rejects `after:`. New `LoadServiceResetConfig(baseDir, name)` and `LoadServiceResetConfigs(baseDir)` for per-service `reset.yml` (also reject `after:`). Additional loaders: `LoadSnapshotConfig(baseDir)` (strict-decode, `KnownFields(true)`; returns `(nil, nil)` cleanly when `workspace/snapshot.yml` is absent — caller decides whether absence is fatal per subcommand); `LoadValidateConfig(baseDir)` (strict-decode; single parse point — callers thread the result into `validate.Context` rather than re-parsing); `UIConfig` (the `ui:` block, decoded inline into `DweConfig` via `LoadConfig`; pointer fields distinguish absent from explicit zero; read via the nil-safe accessors `UICommandsDefaultDepth` / `UICommandsAutoCollapseEmpty` / `UICommandsShowTypeBadges`). **Strict root + `vars:` sandbox.** The merged 3-layer root is strict: `LoadConfig` enforces a single source-of-truth allowlist `allowedRootKeys` (declared in `internal/core/project/config/workspace.go`; enforced per layer by `validateLayerRoots` in `layers.go`) (`{schema_version, project, runtime, state, exports, compose, ui, docs, services, vars, update, bridge, stop}`) — any other top-level key is a **hard load-time error** with a `vars:`-hint message naming the offending layer file. The check iterates **per layer** (so the error attributes the source file) and runs **after** the dedicated `binaries:`/`tools:` legacy-key rejections (which keep their own migration messages) and **before** the `__configPath` / `injectServicesIntoRaw` injections. Arbitrary free-form project values have exactly one legal home: the `Vars map[string]any` (`yaml:"vars"`) block — strict at the root (it's an allowed key) but **unvalidated and arbitrarily nestable inside**; references resolve through `Raw` dot-paths unchanged (`${vars.db.password}`, `from: vars.db.user`). `schema_version` is tolerated-but-uninterpreted forward-compat metadata. **Formalized top-level `update:` block.** `Update *UpdateConfig{ Mode string }` (`yaml:"update"`) is the project self-update policy — 3-layer merged (scalar `mode` last-layer-wins), so the author sets policy in `workspace.yml` and a dev overrides in `local.yml`. Pointer so absent (nil → off) is distinct from present-empty (→ on). `UpdateConfig.EffectiveMode()`: nil → `"off"`; empty mode → `"on"`; else the value. `LoadConfig` value-validates `mode ∈ {on, off}` at load time (the allowlist only checks key *names*), mirrored as a `dwe validate` diagnostic. This **replaced** the old `lifecycle.yml` `run.update` flag (removed — enabling update no longer blanks the lifecycle `run` phases); the run consumer reads `cfg.Update.EffectiveMode()`. **Top-level `bridge:` block (`BridgeConfig{VarsWritable []string}`, `yaml:"bridge"`).** `"bridge"` is in `allowedRootKeys`; 3-layer merged; the deny-by-default container-write allowlist behind `dwe vars set`. Nil-safe accessor `BridgeVarsWritable(cfg) []string` (nil/empty → no container writes). The matcher `VarsWritableAllows(patterns, target) bool` uses **dot-boundary** semantics (NOT `strings.HasPrefix`): an exact pattern matches only the identical path; a `vars.x.*` wildcard matches only when `target` begins with `base + "."` — so `vars.db.*` allows `vars.db.host` but DENIES `vars.db`, `vars.dbx.host`, `vars.database.host`. **Distinct scope** from per-service `services.<name>.bridge:` in `service.yml` (`ServiceBridgeConfig`, enablement) — this top-level block is project-wide container-write POLICY. **Shared layer helper (`layers.go`)**: `LoadLayers(workspacePath) ([]Layer, error)` + `ResolveLayeredPath(workspacePath, path) (LayeredValue, error)` factor the 3-layer load/validation out of `LoadConfig` so `dwe vars inspect`'s per-layer (`default`/`local`/`current` + origin; `LayeredValue.Default`/`Local`/`Current`) resolution and `LoadConfig` share one source of truth for path selection, optional-layer handling, and strict-root/legacy attribution — they cannot drift. **`check: auto` sentinel (`action.go` + `validateStepShape` in `workspace.go`)**: `*Action` is used ONLY for `DeployStep.Check`, so its `UnmarshalYAML` accepts one scalar — exactly `auto` (`Auto` / `AUTO` / `"auto "` keep the pre-existing "action must be a mapping" message; a null `check:` never reaches `UnmarshalYAML` at all — yaml.v3 resolves the null tag first and leaves the pointer nil) — decoding it into `Action{Type: config.AutoCheckType}`. Consumers MUST ask through `config.IsAutoCheck(*Action)`, never string-compare `Type`; `IsAutoCheck` additionally requires an EMPTY payload, so the mapping form `{type: auto, cmd: …}` is an unknown action type rather than a sentinel (a cmd written there is rejected, not silently dropped by the rewrite). The sentinel exists from load time onward specifically so `step.Check != nil` holds for `pipeline.StepForcesRun`, `journal.deployStepToMap` and `dwe reset step` — a resolve-time-only rewrite would silently stop an auto-check step from forcing re-runs. `Action.Validate()` exempts it (its shape rules need the enclosing step); `validateStepShape` owns the three load-time rejections, each with its own reasoned message: `auto` without `when:` (nothing to invert), with `when: {type: builtin}` (the predicate and check-builtin registries are disjoint), and with `when: {type: template}` (a false template `when:` already dropped the step, so the inverse would always fail). Note the hash consequence: `journal.StepHash` does not hash `Check`, but `deployStepToMap` feeds `Service/ProjectConfigHash`, so migrating a step from an explicit inverse to `auto` shifts that hash and costs a one-time re-run of the service's steps. **Nil-safe binary accessors.** The set is `config.DweBin` / `DockerBin` / `ShellBin` / `GitBin` / `MmdcBin` (the last defaults to `"mmdc"` and is read by `internal/cli/docs/` when constructing the mermaid `MmdcRenderer`); the one deliberate non-accessor is `internal/core/execution/condition`'s hardcoded `sh -c` for step `when:` predicates, chosen for POSIX portability rather than the project's overridable `ShellBin` (`internal/core/execution/builtin/shell.go` carries a comment citing that convention, so it is not free to "fix"). **Loader strictness is a per-surface contract, and a new loader must match the surface it joins.** Strict (`yaml.NewDecoder` + `KnownFields(true)`, unknown field ⇒ hard load error): the pipeline loaders here (`LoadLifecycleConfig`, and every deploy/reset entry point via `loadProjectDeployConfigDecode` / `loadServiceDeployConfigDecode` / `loadDeployConfigDecode`), `LoadServiceFolder`, `LoadSnapshotConfig`, `LoadValidateConfig`, the user-command file loader (`usercommands/model`), the template-pack `manifest.LoadManifest`, `envtest.LoadScenario`, `workflow/setup`'s loader, and `shared/i18n`'s. Lenient (`yaml.Unmarshal`, unknown keys silently ignored): `LoadInfoConfig`, `LoadStylesConfig`, `LoadDockerConfig`, the `local.yml` toggle reader (`project/local`), and the topology reader (`ui/render/topology.go`). Mixing the two is a silent-failure generator in both directions — a lenient decode on a strict surface swallows the typo the strict decode exists to catch, and a strict decode on a lenient surface hard-fails a file authors treat as free-form. **`io.EOF` carve-out — exactly four strict *pipeline* loaders.** `LoadLifecycleConfig`, `loadProjectDeployConfigDecode`, `loadServiceDeployConfigDecode` and `loadDeployConfigDecode` (the last three back `LoadProjectDeployConfig` / `LoadServiceDeployConfig` / `LoadResetConfig` / `LoadServiceResetConfig` / `ParseDeployConfigForValidation`, so the tolerance is edited in three unexported places, not eight public ones) treat an `io.EOF` from an all-comment or empty document as an ABSENT file rather than a parse error, leaving the built-in default pipeline in force unchanged. It exists for `dwe init`'s inert scaffold overrides (§ Core — Workflow, `scaffold/` § Inert overrides) — mirror it in any new strict pipeline loader, or a freshly-scaffolded project fails to load its own commented-out `deploy.yml`. The carve-out is deliberately NOT universal: `manifest.LoadManifest` maps `io.EOF` to `manifest is empty` and `envtest.LoadScenario` to `scenario file is empty or invalid`, because neither has a built-in default to fall back to (`shared/i18n`'s `parseBundle` also tolerates `io.EOF`, but on its own reasoning — an empty translation bundle is a valid bundle — not as part of this carve-out). `LoadInfoConfig` reaches the same outcome by another route — `yaml.Unmarshal` never returns `io.EOF`, so it runs a separate `map[string]any` probe decode and treats zero top-level keys as absent (deliberately not `len(cfg.Sections) == 0`, which would also override a deliberate `sections: []` or a `footer:`-only file). **Config-load wrappers.** `LoadConfigOrWrap(workspacePath) (*DweConfig, error)` is the single home of the canonical `loading config: %w` prefix (it replaced ~29 identical hand-rolled load-and-wrap blocks, which is the only reason the prefix reads the same in every command); it lives here rather than in `cli/cmdctx/` precisely so `core/workflow/` can reach it without violating `core-no-cli`. Never re-wrap `LoadConfig` by hand. The typed `cmdctx.ErrWrap("project_invalid_config", err)` envelope (see § CLI cross-cutting behaviors, JSON output mode) is a **separate, non-overlapping** path used by JSON-capable read-only commands — do not fold it into `LoadConfigOrWrap`, or every config failure in `dwe status --output json` / `dwe logs` / `dwe vars` loses its machine-readable `{"error":{code}}` envelope. The docker sibling is `LoadDockerConfigOrEmpty(baseDir, cfg) (*DockerConfig, error)`: `os.ErrNotExist` → `(&DockerConfig{}, nil)` because a project with no `workspace/docker.yml` is legal, every other failure → `loading docker config: %w`. Use it wherever a missing `docker.yml` must not be fatal, instead of re-testing `errors.Is(err, os.ErrNotExist)` at the call site. **The `service.yml` field allowlist is hand-mirrored, not shared.** `allowedFieldsFor` stays unexported per project Go conventions, so `internal/core/validate/config/workspace.go` keeps a duplicate table `servicesAllowedFields`. Adding ANY new top-level `service.yml` field — a new toggle, a `bridge:` sub-field, `generated:`, anything — therefore requires updating **BOTH**: the decode is `KnownFields(true)`, so a field missing from the loader table is a hard load error (`ErrServiceFieldNotAllowed`) rather than an ignored key, and a field missing from the validator table makes `dwe validate` report a valid file as invalid. The loader side is guarded by the `services_loader` tests, the validator side by the `servicesValidator` tests; nothing cross-checks the two tables. The two local-compose fields are the deliberate inverse: neither `ComposeConfig.Extra` nor `ServiceConfig.LocalComposeExtra` may EVER be added to either table — that single edit silently undoes the local.yml-only gate above, because the strict `service.yml` decode would then accept `compose.extra` as a structural, git-tracked service field. `render.config` needs neither entry, because `render` is already allowlisted and its `config:` sub-block is struct-enforced by `ServiceRenderConfig.Config *RenderConfigSection`. **Service-folder file set (symmetry).** A service folder holds at most three files and `service.yml` is the only REQUIRED one — an absent `service.yml` fails `LoadServiceFolder` with `loading service %q definition: …` (and `dwe validate`'s `config.services-folder` validator raises a `SeverityError` for the folder), so an empty folder under `workspace/services/` is never silently ignored. `deploy.yml` and `reset.yml` are OPTIONAL and **type-agnostic**: `LoadServiceDeployConfigs` / `LoadServiceResetConfigs` iterate every service regardless of `type:` and `continue` past `os.ErrNotExist`, so a `tool` or `infra` service may declare a pipeline exactly like an `app`. Adding a fourth per-service file type also means extending `knownServiceFiles` in `internal/core/validate/config/services_folder.go` (see § Core — Validation). **Adding a new formalized top-level config field means adding its key to `allowedRootKeys`** — the strict root is a hard error, so until the key is listed every fixture and every real project declaring the field fails to load. The `binaries:`/`tools:` legacy rejections are themselves iterated **per layer** and name the offending layer file, and that per-layer placement is load-bearing rather than symmetric-for-neatness: `deepMerge` drops nil values, so a layer carrying ONLY a `binaries:`/`tools:` key never reaches the merged map and `validateLayerRoots` (`layers.go`) is the only pass that can see it — without it such a layer would load silently. Within one layer the legacy checks run FIRST so their migration messages win over the generic "unknown top-level key" message. `schema_version` needs no special-case branch anywhere: it is a plain `allowedRootKeys` member that nothing reads, and adding an exception path for it would be the wrong fix. **One documented exception to `tpl`'s leave-it-literal known-head whitelist**: `docker.yml`'s `project_name` is resolved by `resolveVarTemplate` (`docker.go`, reached from `LoadDockerConfig` and `ResolveComposeProjectName`), an older lightweight resolver with NO namespace whitelist — any dot-path into `Raw` resolves via `ResolvePath` — that **errors** on an unresolved path (`unresolved path %q in template %q`, plus `unclosed ${` and a 10-iteration circular-reference guard) instead of leaving the reference literal the way `CompileVarSyntax` does. That inversion is deliberate: a broken compose project name must fail loudly rather than reach `docker compose -p ${...}`. User-visible schema details: `docs/reference/config/` (and `docs/reference/config/vars.md` for the `vars`/`bridge.vars_writable` surface).
- `internal/core/project/config/compose_scan.go` (integration-tests stage 3a) — `ScanComposeIsolation(cfg *DweConfig, projectRoot string) []IsolationFinding`, a leaf function (no `diag`/`validate` import) that parses `cfg.ComposeFiles()` with a narrow YAML struct (`services.<name>.{container_name, ports}`, top-level `volumes`/`networks` `{external, name}`) looking for constructs that bypass Docker-Compose project-name scoping. Each `IsolationFinding{Kind, Resource, HostPort, Blocking, Message, File}` carries an **intrinsic** `Blocking bool` — `true` for `KindContainerName` / `KindRawHostPort` (real collisions with the working environment), `false` for `KindNamedVolume` / `KindNamedNetwork` / `KindExternalVolume` / `KindExternalNetwork` (shared-resource hazards) — severity policy is entirely **caller-owned** (the `envtest` runner treats `Blocking` as fatal unless `--skip-isolation-check`; `internal/core/validate/tests/` maps everything to `SeverityWarning`), keeping the scanner itself policy-light. **every construct is collapsed across the `-f` chain, not reported per file** — a base declaration a later overlay corrects would otherwise be flagged for a resource the merged stack never creates. Two collapse shapes, both owned by `ScanComposeIsolation`: (a) the **scalar** fields (`container_name`, a volume/network's `name:` / `external:`) are last-declaration-wins, tracked per `declKey{kind, resource}` — a file that declares one non-effectively (`external: false`, a `!reset`) CLEARS the key instead of adding a finding, an absent key leaves the earlier winner standing; (b) **`ports:`** is a sequence, which compose merges by APPENDING, so every file's literal host port really is published and stands as its own finding — until a file replaces the whole list with `!reset` (key dropped, its own entries never reach the merged stack either) or `!override` (this file's entries replace every earlier one), which cancels the earlier files' port findings for that service. That difference is load-bearing in both directions: collapsing ports last-wins would drop a genuinely published port, and reporting them per file blocks `dwe test run` over a port an `!override []` overlay — the exact fix an author would write — already removed. Every field of the narrow shape is therefore decoded as a `yaml.Node`, never its natural Go type: yaml.v3 leaves an unknown tag unresolved, so `container_name: !reset null` decoded into a `string` reads as a service literally named `"null"`, while `ports: !reset null` into a `[]yaml.Node` is a `TypeError`. `mergeTagOf` is the single classifier (`mergeNone` / `mergeReset` / `mergeOverride`, alias-resolved first); `classifyContainerName` maps its node to `absent` (missing key, or a plain explicit null — `!reset` is the only documented clear) / `cleared` (`!reset`) / `set` (anything else, including `!override`, whose value is an ordinary name). A `*yaml.TypeError` from the file-level decode is deliberately TOLERATED (everything yaml.v3 could decode is kept) — dropping a whole file over one field this narrow shape cannot read would silently blind the scanner to that file's blocking findings; any other unmarshal error still skips the file. Callers must NOT re-derive this collapse — `internal/core/validate/config/container_name.go` consumes the scanner's findings directly. Port detection handles both short (`"8080:80"`, `"127.0.0.1:8080:80"`, host ranges `"8080-8090:80-90"`) and long (`{target, published}`) compose syntax, strips a `/tcp`|`/udp` suffix first, flags a token matching `^\d+(-\d+)?$` (single literal or literal range — range stores the low end in `HostPort`, full range in `Message`), and ignores `${...}`-interpolated tokens and container-port-only entries; IPv6 bracketed hosts (`[::1]:8080:80`) are explicitly out of scope (not flagged). An unreadable/malformed compose file is skipped silently — the scanner is advisory; real parse errors surface through the copy's `dwe validate` subprocess / `docker compose` itself. Two callers: `internal/core/workflow/envtest/` (runner prepare-phase gate, before the `dwe validate` subprocess spawns) and `internal/core/validate/tests/` (all findings as warnings). **`ScanComposeCost(cfg, projectRoot) ComposeCostFacts`** (agent-discoverability milestone) is the sibling scanner feeding `dwe test list`'s `cost_profile`: `BuildServices` / `ExternalImages` / `MaxStartPeriod`. It shares `parseComposeFiles` with `ScanComposeIsolation` — **do not add a third compose parser**. Per-service facts merge across the `-f` chain the way compose resolves it (later `image:` wins, `build:` anywhere marks the service as building), a building service is EXCLUDED from `ExternalImages` (its `image:` is a local tag, not something to pull), and `start_period` is **max, not sum** (`up --wait` waits in parallel). Every merged fact must also CLEAR, not just accumulate: a later file's `healthcheck.disable` drops an earlier `start_period`, and a `!reset` on `image:` / `build:` / `healthcheck:` drops that fact outright (`mergeTagOf`, shared with `ScanComposeIsolation`) — a sticky `build:` would report a build the merged stack never runs AND suppress the image it actually pulls. `!override` is an ordinary declaration here: these are all scalar/mapping fields, which already merge last-one-wins.
- `internal/core/project/config/docker.go` (compose identity) — **compose project name: one resolver, lowercased.** `docker compose -p` and every compose-BYPASS lookup must agree on ONE string, so the name has exactly **two** resolution entry points and no third. `ResolveComposeProjectName(baseDir, cfg)` reads `project_name` from disk through the narrow `readDockerProjectName`, which deep-merges `workspace/docker.yml` with `workspace/docker.local.yml` (local wins, same two-file precedence as the prompt's mirror) — never a full `DockerConfig` round-trip, so the lightweight per-service paths (`dwe restart|stop|logs <svc>`, `reset --service`) stay immune to an unrelated `args:`/`volumes:` schema error elsewhere in the file; `os.ErrNotExist` or an empty value falls back to `cfg.Project.FullName()`, an empty `baseDir` skips the read entirely, and a template-resolution error on `project_name` itself IS propagated (a broken `-p` must fail loudly). `ComposeProjectName(dockerCfg, cfg)` is its in-memory sibling for callers already holding a resolved `DockerConfig` (pipeline builtins via `spec.ExecContext.DockerConfig`, status collectors, completion paths): both arguments nil-safe, no template expansion. Both route through `normalizeComposeProjectName` (`strings.ToLower`) — Compose requires `[a-z0-9][a-z0-9_-]*` and rejects uppercase, while `project.name` / `project.prefix` / `docker.yml project_name` are free-form user text, making this the one Compose constraint dwe cannot construct around. **Never re-derive the `dockerCfg.ProjectName ?: cfg.Project.FullName()` precedence inline.** That is exactly how `docker.buildCompose` (the `-p` value itself), `dwe docker project-name`, the reset `docker_stop_remove_container` builtin, `spec.RunContext.Compose`, and the `ports_free` preflight each grew their own copy, and how the `-p` value diverged from the lowercased name that per-service stop/restart/logs, status probes, the bridge overlay, and the `config.container_name` validator all build `<project>-<container>` from — compose then scopes its own containers, networks, and volumes under a different project than every label lookup that goes looking for them. `buildCompose` also ALWAYS passes `-p`: without the `FullName()` fallback an absent/empty `docker.yml` left `ProjectName` empty, `BuildArgs` omitted `-p`, and compose v2 silently scoped by directory basename. The single deliberate exception is `internal/shared/prompt.readComposeProjectName`, a standalone hot-path reader that must not load the full config and therefore repeats the lowercasing by hand. Lowercasing is a **behavior change for existing projects with uppercase names** — compose treats their pre-existing containers and volumes as belonging to a different project. `ComposeProjectNameCandidates(dockerCfg, cfg)` is the migration bridge for compose-BYPASS label lookups (daemon builtins, `docker_stop_remove_container`, stop/reap/status/completion): canonical normalized name first, then the PRE-normalization spelling of `dockerCfg.ProjectName`, then the legacy `cfg.Project.FullName()` scope, empties and duplicates dropped — a container created before normalization still carries the original-cased `com.docker.compose.project` label, so probing only the lowercased name reports "nothing to stop" while the container keeps running.
- `internal/core/project/user/` (package `user`, imported as `userpkg`) — user-level config loader (`Load(projectRoot)`): defaults → `~/.config/dwe/config` (identical path on every OS — no platform-native location, no XDG fallback) → `<projectRoot>/.dwe/config` → env (`DWE_NOTIFY_*` and `DWE_LANGUAGE`). Flat `key = value` parser (full-line `#` only, inline-`#` rejected, dotted keys rejected, unknown keys warn for forward-compat). `Config.NotifyEnabledFor(kind string) bool` is the master+per-op gate keyed on `"deploy"` / `"run"` / `"command"` (string-keyed so `notify` doesn't import `user` cyclically). `Config.Language string` field holds the resolved locale (empty = unset, resolved at startup). **Alias contract**: all callers import as `userpkg "github.com/semsemyonoff/dwe/internal/core/project/user"` because bare `user` collides with `os/user`. Dropping the alias where unambiguous is a follow-up. User-visible details: `docs/reference/config/notifications.md` and `docs/reference/config/i18n.md`.
- `internal/core/project/local/` (package `local`, imported as `localpkg`) — local YAML toggle helpers: `LoadLocalYAML`, `WriteLocalYAML`, `SetLocalEntryEnabled`, `ValidateServiceToggle`, `ServiceTogglesOverlay`, plus the comment-preserving node round-trip in `local_node.go`. **Single `local.yml` write path**: `LoadLocalYAMLNode(path) (*yaml.Node, error)` (missing file → fresh empty mapping doc) → `ApplyOverlayToNode(doc, overlay map[string]any) error` (recursive patch: scalar → replace the matched value node's `Value` and set `Tag`/`Style` **from the coerced new value, NOT the old node** — preserving only Head/Line/FootComments, so a coerced `true`/`42` over a previously quoted string emits a BARE scalar that reloads typed; nested map → recurse, create child mapping if absent; absent key → append; never delete) → `WriteLocalYAMLNode(path, doc)` (shared atomic temp+rename + `MkdirAll(0o755)` + `Chmod(0o600)`). The map-based `WriteLocalYAML` is now a **thin wrapper** over this path (map → overlay onto a fresh doc); the old `MergeIntoLocal`/`deepMerge`/`validateMergeable`/`deepCopyMapInternal` were removed. ALL three writers route through it — `vars set` (`cli/vars/`), `services enable/disable` (`cli/service/service_toggle.go`, via `ServiceTogglesOverlay`), and the setup wizard (`workflow/setup/wizard.go`) — so `local.yml` comments/formatting survive every mutation. The node-aware **mergeable guard** rejects descending a map overlay through an existing scalar / sequence / alias / explicit-null / merge-key (`<<`) node by default (avoids silently discarding developer data), with one documented exception ported from the old `setup/merge.go`: a legacy bare-int port leaf upgrades to a `{port: N}` map. Multi-doc files operate on the first document (error otherwise); flow-style and comment-only/empty docs handled explicitly. **Alias contract**: callers import as `localpkg "github.com/semsemyonoff/dwe/internal/core/project/local"` because `local` is a common local-variable name (confirmed collision in `cli/service/service_toggle.go`). The style-derivation rule is a correctness control, not cosmetics: blindly preserving the OLD node's `DoubleQuoted` style would break the `set` coercion contract outright — the value would reload as a string no matter what `varsusage.CoerceScalar` decided, so `vars set x true` would silently keep writing a quoted `"true"` forever.
- `internal/core/project/varsusage/` — pure (stdout-free) helpers behind `dwe vars`. `resolve.go`: `EnumerateVars(cfg) []string` (sorted dot-paths of leaves under merged `cfg.Vars`, nested maps = namespaces), `ResolveVar(cfg, path) (any, bool)` (effective value via `config.ResolvePath(cfg.Raw, path)`; subtree for namespace paths), `CoerceScalar(raw) (any, error)` (the PINNED `vars set` grammar — parse via `yaml.Node`, REQUIRE a `ScalarNode` (reject `{...}`/`[...]`); legacy octal `0755` / leading-zero `01` / `1.2.3` / bare timestamps → string; an EMPTY argument (`raw == ""`, which is what the shell leaves after stripping `set x ""`) → YAML **null**, while only the 2-char literal `""` — written `set x '""'` so the shell cannot strip it — → the empty string, and bare `null`/`~` → YAML null; `yes`/`no`/`on`/`off` → string under interface decode). `scan.go`: the **field-aware** static usage scanner — `ScanUsages(projectRoot, queryPath) (ScanResult, error)` returning sorted/de-duped `Usage{File, Line, Kind, Text}`. It scans ONLY the fields the runtime renders, keyed by file type (NOT a flat file grep — that yields both false positives and false negatives): `${vars.x}` matched with the renderer's OWN exported pattern `tpl.VarPattern` for `${...}`-rendered fields (`templatedKeys` scalars / `templatedMapKeys` mappings / `templatedSeqKeys` sequences — the last covering `argv:` and `compose_args:`, whose elements each render through `tpl.RenderCommand` in `runio.RenderArgvWithArgs` / `service.buildRenderedComposeArgs`) plus **config** render templates under `workspace/templates/config/**` (`scanConfigTemplates`; arbitrary text → per-line raw scan for BOTH `${vars.x}` and `{{ resolve .Raw "vars.x" }}`). The YAML walk skips each service's `src/` hub (`workspace/services/<name>/src`) — that is the application's own git checkout, which dwe only ever writes RENDERED output into, so walking it costs a full `node_modules`/`vendor` traversal and can only produce hits in files dwe does not own; that matters because `config.template_refs` runs this scan on every `dwe validate`, not just on an explicit `dwe vars inspect`. The template-pack walk skips ONLY the pack `manifest.yml`, matched by **basename** — NOT by `.yml`/`.yaml` extension. A config pack can declare a YAML-bodied render output (`render: [{from: config.yaml, to: ...}]`), and `RenderConfigs` feeds that body through `tpl.RenderCommand` like any other template, so a `host: ${vars.db.host}` inside it IS a real usage; an extension-based skip silently misses those references (a regression caught in review). Only the `config` template-pack kind is scanned — it is the one rendered through the `${...}` substrate (`RenderConfigs`); the sibling ide/ai/git packs use the raw `{{ }}`/`packcommon.TemplateData` substrate (no `${...}` resolution) so a `${vars.x}` there is inert and is NOT scanned. Templated fields are ALSO scanned for the Go-template `{{ resolve .Raw "vars.x" }}` form (bare `vars.x` token inside a `{{ }}` block) — info `text`/`value`/`title` and custom-column `value` render through `tpl.Render`, where a `${...}` literal is inert, so the `{{ }}` form is the real reference there. Structural `from:`/`default_from:` + typed `when.expr` are walked via a `yaml.Node` walk. Matches by exact path OR namespace prefix (dot-boundary). **Documented caveat**: Go-template `.Vars.x` field access (capital-V sprig field form) and dynamically-built paths are NOT tracked. **`templatedKeys` completeness is an invariant, not a convenience list**: it must name EVERY scalar field the pipeline resolver renders — currently `cmd` / `text` / `value` / `title` / `project_name` / `confirm` / `when`, plus `timeout`, `command` (`files_gate.command`) and `argv_append_from`. Omitting one is silent in both directions: `pipeline.renderStepFields` substitutes `step.Timeout` and `files_gate.command` alongside `cmd`, so a `${vars.typo}` there renders to `""` — a step that quietly becomes unbounded, or a gate that quietly falls back to probing the step's own `cmd` — and because `config.template_refs` reports only what this scanner enumerates, an un-enumerated field is invisible to `dwe validate` as well as to `dwe vars inspect`.
- `internal/core/project/services/` — per-service-folder loading helpers and shared types layered over `config/`. Imports `config/` directly.
- `internal/core/project/stack/` — Docker Compose topology + health, plus status section collectors / renderers: `AggregateHealth`, `HealthFromStatusInput(in StatusInput) Health` (extracted topology-vs-rows selection — topo with runtime statuses wins over rows fallback; consumed by `cli/status/` for the opportunistic `promptcache.Write` call so JSON and TUI paths get accurate state too, not just the plain branch), `HealthState(h Health) string` (pure mapping `HealthRunning→"running"` / `HealthPartial→"partial"` / `HealthStopped→"stopped"`; default `"stopped"`; the three literals are the on-disk schema of `.dwe/prompt-cache.yml` — duplicated rather than importing `shared/promptcache` so `core/` stays independent of `shared/promptcache`), `FetchComposeTopology`, `ParseTopologyFromFiles`; `RenderHealth` / `HealthIndicator` / `RenderApps` / `RenderTools` / `RenderInfra` / `RenderDeployStatus` / `RenderTopology` are pure string-returning section renderers; `HealthIndicator(in StatusInput) string` returns just the indicator glyph + state (e.g. `● running`); `RenderHealth` is `"DWE: " + HealthIndicator(in)` composed by the `status` group — one per service `type:` (apps / tools / infra are three distinct sections, each filtered by `IsApp()` / `IsTool()` / `IsInfra()`). **Collect/render split** (responsive-table Task 9): each section's probe half and render half are separate functions — `CollectApps`/`CollectTools`/`CollectInfra(in) (ServiceSection, []error)` never call `IsRunning` and never format a table; `RenderAppsRows`/`RenderToolsRows`/`RenderInfraRows(sec ServiceSection, width int) string` never call `IsRunning` and are pure formatting at the given width (the collect/render boundary is spy-tested directly: `IsRunning` must be observed on the collect side only). `RenderApps`/`RenderTools`/`RenderInfra(in) (string, []error)` are unchanged convenience wrappers kept for the pre-existing non-TUI callers: they `Collect*`, then **branch** — `in.Width > 0` → `render.ServicesTableAt(..., in.Width)`, otherwise the sink-probing `render.ServicesTable(...)`. **The branch is load-bearing, not redundant**: `Render*Rows(sec, 0)` means *unbounded*, NOT "probe stdout", so collapsing the zero-width path into `Render*Rows` would silently opt `dwe status` / `dwe services` out of responsive tables. Same shape for `DeployStatus(in)` (`render.DeployStatusAt` vs `render.DeployStatus`) and for `RenderDaemons(rows)`, which routes through the sink-probing `render.DaemonTable` while `RenderDaemonsAt(rows, width)` is the explicit-width sibling (0 = unbounded). `StatusInput.Width int` (`0` = resolve from the sink) is the override channel for those wrappers, but has **no production caller today** — `cli/status` leaves it `0`, and the status TUI does not use it at all: the TUI goes through the collect/render split (`CollectApps` + `RenderAppsRows(sec, width)`), which takes the width as a parameter. The same split pattern extends to `CollectDeployStatus(in) []render.DeployStatusRow` + `RenderDeployStatusRows(rows, width) string`, to `RenderDaemonsAt(rows, width)`, and to `render.GitWorkspaceAt(rows, width)` (`render.GitWorkspace(rows)` is its sink-probing sibling) — `CollectGitWorkspace`/`CollectDaemons` were already probe-only, so only their render side gained a width parameter. `RenderTopology` has no width parameter and needed no split — it renders a tree diagram, not a table, with no Docker probe of its own. `RenderApps` / `RenderTools` / `RenderInfra` return `(string, []error)` so the command layer aggregates one stderr warning per section; `BuildCustomColumns(cfg, t ServiceType)` + `RenderCustomCells(defs, data)` power the per-service `status:` block (hermetic `tpl.Render` only — never `RenderCommand`); `CollectGitWorkspace(ctx, cfg, projectRoot)` runs `<config.GitBin(cfg)> status -b --porcelain=v2` per service under `errgroup.WithContext` + `SetLimit(8)` with `exec.CommandContext` for cancellation; probes `<svc.Dir>/src` (mirrors `internal/core/execution/templates/git`, which writes hooks to `<svc.Dir>/src/.git/hooks/`) — services whose `<dir>/src` is missing or not a directory are silently omitted; pre-checks `<dir>/src/.git` to avoid walking to a parent repo (blank cells when absent, nil row.Err); dedups services that extend a parent and share its `dir` by keeping the extends-chain root (shallowest depth, alphabetic tiebreak); reports per-row errors on `GitWorkspaceRow.Err` (goroutines always return nil so siblings aren't cancelled on first error); `CollectDaemons(ctx, cfg, dockerCfg)` shells out `docker ps --format=json` filtered on `dwe.project` + `dwe.daemon.id` labels, parses NDJSON per line, returns `([]statusview.DaemonRow, []error)`; `RenderDaemons(rows)` returns `(string, []error)` (hidden when empty — same treatment as topology). **Service running-state is label-based**: `ServiceRunning(projectName, composeService, dockerBin, processEnv) bool` delegates to `docker.ServiceContainerName(..., runningOnly=true)` and treats a non-empty name as running. It matches on compose labels rather than a guessed `<project>-<container>` name (the old `docker inspect <project>-<container>` guess produced false "stopped" readings), excludes one-off `compose run` containers (so a transient `dwe shell --mode run` container alone does not flip status to running), and stays correct under any `container_name` override and compose's default `<project>-<service>-<index>` index suffix. `processEnv` carries `docker.yml` `process_env` (DOCKER_HOST / DOCKER_CONTEXT) so status probes the same daemon as lifecycle commands and `dwe shell` — `cli/status/` and `cli/service/` build it via `docker.MergeEnv(dockerCfg.ProcessEnv)`. The `ContainerCheckFn` seam's argument is the **compose service name = `svc.Container`** (the `com.docker.compose.service` value; defaults to the folder key but may be overridden), NOT the folder map key — both callers close over the project name + processEnv and call `ServiceRunning`; `collectRowsByType` passes `svc.Container`. **Signature contract**: nothing in this package (nor in `core/ui/render/`) takes an `io.Writer` for section output — collectors and renderers return strings plus per-row errors, and `internal/cli/` is the **single** writer to stdout/stderr. Threading a writer down would move sink policy (`--output json` suppression, stdout-vs-stderr routing, `cmd.OutOrStdout()` test capture) out of the CLI layer and make the byte-compared goldens impossible. The one grandfathered exception here is `RenderServiceDeployDetail(w, …)`, kept for the `dwe status deploy <svc>` detail view; do not add a second. Passing a width on does not stop at the table body either: `wrapSection(title, body, width)` (`status.go`) hands the *same* budget to `render.SectionTitleAt`, because the plain `render.SectionTitle` always sizes its `── Title ──…` bar from the ambient terminal — a panel-width table under a terminal-width title is precisely the overflow the responsive-table work exists to prevent, so a new section renderer must thread its width into `wrapSection` as well as into the table call. `RenderTopology` is the deliberate width-`0` exception: it calls `wrapSection(..., 0)` on purpose, since its body is a `render.Topology` dependency tree rather than a `tableView`, so no panel budget is honored by the body and there is none for the title to match.

## Core — Execution (`internal/core/execution/`)

- `internal/core/execution/pipeline/` — pipeline executor and `PlainReporter`: `Reporter` interface (concurrent-safe; implementations must tolerate sub-step events from N goroutines); `SuspendForExec` / `ResumeAfterExec` wrap every sequential step body so the child writes directly to the host terminal. Action dispatch: `ExecAction` (pure dispatch — no `when:`, no reporter, no check). `RunWithOptions` owns `when:` eval, reporter calls, `check:` orchestration, and `continue_on_error`. Plan-time helpers: `ResolvePhaseSteps`, `PrintPlanTable`, `FormatCondition` / `FormatAction` / `FormatFilesGate`. **Resolve-time rendering (`render.go`)**: `RenderStep(cfg, step)` / `RenderWhen(cfg, when)` are the exported entry points (`dwe reset step` bypasses `ResolvePhaseSteps` entirely and must call them before any raw-field read); internally `renderStepFields` renders `cmd`, the string leaves of `with` (recursively), `check`, `files_gate`, and `timeout` **into a deep copy** — `With`/`Check`/`FilesGate` are reference types shared with the loaded config, so rendering in place would make `journal.ProjectConfigHash` depend on deploy scope and double-render a second resolve. The gate is `hasKnownVarRef`, **not** a bare `tpl.VarPattern` match: a string enters `tpl.RenderCommand` only when it carries a `${...}` that passes `tpl.IsVarNamespaceRef` (known head AND a dot-path tail, `${args}` excepted — the single membership rule; never re-index `tpl.KnownVarHeads` or re-derive the tail test locally), so a bare Go-template idiom, a shell-style `${VAR}`, or a head-only `${host}` shell variable never reaches the engine. The `with:` widening for an inheriting `files_gate` skips a `type: builtin` host step (`gateInheritsWith`) — a builtin's parameter map must never be dragged into this template space, even when the gate borrows it. A field mixing a known head WITH a literal `{{ }}` **does** reach it and fails — an authoring constraint documented in `docs/reference/config/deploy/index.md`, NOT a bug to be fixed by weakening the gate. The narrow-vs-wide switch is the `wide bool` threaded through `renderWith` / `renderValue` / `renderWithLeaf`; a `type: builtin` `with:` map is never widened, because builtins render it in a *different* template space — `message`'s `text` is a documented Go template over `DweConfig` (`{{ .Project.Name }}`) and the `shell` predicate takes `docker inspect -f "{{.State.Status}}"` format strings, so widening there hard-fails the resolve of every project using the documented form. The known-head gate is necessary but not sufficient: `renderContextFor` populates only `Raw` and `Host`, so `renderIfKnown`/`renderWithLeaf` call `tpl.ValidateRawScope` FIRST — on every non-empty string, gate or no gate — to reject `${param.*}` / `${context.*}` / `${files.*}` / `${generated.*}` / `${args}`, whose lenient resolvers would otherwise substitute `""` for a namespace that has no pipeline source at all (`${snapshot.*}` already failed here via `validateSnapshotScope`).  `UnresolvedTemplateRefs(s)` (`step.go`) is the inverse scan used to annotate plan output; `ResolvedStep.DisplayPhaseWhen()` is what plan renderers must read for the phase header (the rendered `PhaseWhen`, falling back to the raw `Phase.When` only for a template condition) — reading `rs.Phase.When` directly reintroduces the "plan lies about what will run" divergence. Plan-time sentinels: `ErrNestedParallel`, `ErrUnnamedSubStep`, `ErrInteractiveInParallel`, `ErrEmptyParallelSteps`, `ErrDuplicateStepName`, `ErrSubStepOverridesInvalid`. State: `Recorder` / `FileRecorder` (mutex-protected). Log files: `OpenPipelineLog` / `OpenSubStepLog`. **Executor step ordering**: `phase-when → step-when → files_gate probe → (journal-skip only when FilesGate == nil) → execute` (gated steps always re-probe). **Always-run helper (`forcesrun.go`)**: `StepForcesRun(rs ResolvedStep) bool` is the single "always re-run" lever — true for `check:` steps and predicate-body builtin steps (`step.Type == "builtin" && builtin.KindOf(step.Cmd) == KindPredicate` — a predicate used as a step body is an assertion and must re-assert every deploy), recursing **one level** into `rs.Parallel.Steps` (deeper nesting is schema-rejected). Predicate-body detection MUST gate on `Type == "builtin"` — a `type: shell` step whose command text is `shell` must not force execution. `files_gate` and `when:` are deliberately excluded (files_gate keeps its own journal-bypass at the call sites; `when:` stays a pure conditional). Consumed by both deploy skip sites (`internal/cli/deploy/deploy.go`: the `hasAlwaysRunSteps` early up-to-date gate and the `makeSkipDecider` per-step decider that feeds `journal.Decide`'s `hasCheck → Run` lever). User-facing parallel / `files_gate:` semantics: `docs/reference/config/deploy/`. `Run` and `ExecStep` are deprecated thin wrappers. **Per-step timeout (integration-tests stage 3a)**: a general, opt-in `ResolvedStep.Timeout time.Duration` (parsed from `config.DeployStep.Timeout` string at resolve time in `resolveLeafStep`/`resolveParallelStep`; `< 0` is a resolve error, `0`/absent leaves the step unbounded — byte-identical to today) is enforced in `executeStepBody` by wrapping ONLY the step **body** `ExecAction` call in `context.WithTimeout(ctx, rs.Timeout)` — the `check:` action deliberately stays on the shared, unwrapped `ctx` (a fast skip-predicate, not the work being bounded). A non-nil body error is relabelled `step %q timed out after %s` only when `ctx.Err() == nil && errors.Is(stepCtx.Err(), context.DeadlineExceeded)`, so an outer Ctrl+C cancellation is never mislabelled a timeout. The contract is honestly scoped to **ctx-honoring bodies**: `shell`/`dwe` subprocesses terminate via the existing `bindCancelTerm` SIGTERM+`WaitDelay` path, ctx-aware builtins (`http_check`, `docker_wait_healthy`, `tcp_reachable`, …) return on deadline, but a body blocked on interactive input (e.g. a `confirm` builtin awaiting stdin) is NOT force-interrupted — the deadline fires but the goroutine stays blocked until input arrives (accepted, documented limitation; making every interactive prompt ctx-aware is out of scope). Parallel substeps need no extra plumbing — each substep is its own `ResolvedStep` and flows through the same `executeStepBody` path via `runParallelSubStep`. No group-level timeout exists — only per-leaf. **`check: auto` (`autocheck.go`)**: `ResolveAutoCheck(*condition.Condition) (*config.Action, error)` + `InvertShellCommand(cmd)` are the SINGLE derivation of a `check: auto` step's real check — `resolveLeafStep` and `dwe reset step` (`internal/cli/lifecycle/reset.go`, which bypasses `ResolvePhaseSteps`) both call them, never a copy. **Ordering inside `resolveLeafStep` is load-bearing**: whole-step render → `resolveStepWhen` → sentinel rewrite → `builtin.Validate` of the check. Rewriting before the render would invert an unrendered `when.Cmd` while the runtime evaluates the rendered one; rewriting after the validate would send the literal `auto` into the builtin registry or skip validating the derived check. The rewrite assigns a **fresh pointer** (`step.Check = derived`) onto the local copy — mutating through the shared pointer would make a second resolve produce `! ( ! ( … ) )` (silently restoring the original logic) and shift `Service/ProjectConfigHash` depending on deploy scope. The derived form is `{type: builtin, cmd: shell, with: {cmd, timeout: "0"}}`: the builtin gets a hard `sh -c` and `ectx.ProjectRoot` as cwd (matching `condition.EvalCmd`), whereas a `type: shell` action would use the project's overridable `ShellBin`; `timeout: "0"` is unbounded (`builtin.Shell.Run` treats 0 as no deadline — NOT an already-expired `WithTimeout(ctx, 0)`), matching the `when:` it inverts. `InvertShellCommand` wraps across newlines (`! (\n<cmd>\n)`) — an inline `! ( … )` turns a trailing `# comment` / `&` / unterminated heredoc into a syntax error instead of an inversion. `ResolvedStep.AutoCheck` records that the rewrite happened and `ResolvedStep.DisplayCheck()` is what every plan renderer must call (human table, `--output json`, `--format shell` comment, reset plan) so plan output reports `auto (inverse of when)` rather than the derived machinery. Load-time shape rules (sentinel decode, the three rejections) live in `internal/core/project/config/`. A runtime `when:` renders at three scopes — leaf step (`resolveStepWhen`), phase (`ResolvePhaseSteps`), and parallel-group parent — through `renderWhen`; a parallel group's own body is NOT re-rendered, so `rs.Step.Parallel.Steps[i]` stays raw while `rs.Parallel.Steps[i].Step` is the rendered copy, a known divergence that is harmless only because `Parallel` is never hashed. Rendering at **exec** time instead would desync the journal hash from the command actually run, leave `dwe deploy plan` printing stale `${...}` literals, and skip `with:` entirely — which is why a `RenderCommand` error here fails the step outright rather than letting the literal through to reproduce as a worse-worded `Bad substitution` at run time. Rendering happens ONCE and is **not idempotent**, so every downstream consumer of a resolved `with:` must enter through `usercommands.BuildPreRenderedRunContext`, never `BuildRunContext`; both pipeline sites (`execCommandAction` and the `evalFilesGate` probe in `executor.go`) do. A test that hand-builds a `ResolvedStep` with an unrendered `With` / `FilesGate.With` is exercising a state production cannot produce — build fixtures through `ResolvePhaseSteps`. The gate lives ONLY in this resolve helper and must never move into `tpl.RenderCommand`: user commands legitimately and documentedly mix `${...}` with raw `{{ }}` in one field, so a gate there would break them. The **wide** gate for `with:` leaves admits a known head **OR** a literal `{{`, and applies at exactly the two sites where `usercommands.BuildRunContext` used to render the map at exec time and no longer does: a `type: command` step/action, and a `files_gate` probe — its own `with:` (always wide, regardless of the host step's type), or the host step's map when the gate declares none (`len(files_gate.with) == 0`); a gate carrying its own `with:` never reads the host map, which therefore keeps its native narrow gate. Two concrete points behind the `ValidateRawScope` pre-scan: without it `cmd: "git checkout ${param.branch}"` renders down to `git checkout` plus a trailing space — a plausible, silently wrong command rather than a visible blank — and the erasure is invisible to both downstream detectors, since `UnresolvedTemplateRefs` skips known heads by construction and `config.template_refs` resolves root keys only, so no post-hoc report can catch it. `UnresolvedTemplateRefs` scans the **rendered** command and reports head-only tokens (`${host}`) alongside unknown heads; it feeds the human plan table (`print.go`) and the `unresolved` field of `--output json`, but never `--format shell`, whose output must stay executable. Because every known-head reference has by then either been substituted or already failed the resolve, a surviving match is a genuine leftover — which is what licenses annotating it at all. On `DisplayCheck()`: the call it replaces is `FormatAction(rs.Step.Check)` — the neighbouring plan helper, and the natural habit — which on an auto-check step prints the derived `shell` builtin's inverted command instead of `auto (inverse of when)`, so the plan advertises machinery the author never wrote.
- `internal/core/execution/condition/` — deploy step `when:` evaluator. Two APIs coexist: legacy string-based (`Classify`, `IsRuntime(string)`, `EvalRuntime`, `Kind*` constants) for workflow string `when:`, and typed (`Condition` struct, `EvalRuntimeTyped`) for pipeline `when:`. Template conditions evaluate at plan time in `pipeline/resolve`, not here. Uses hardcoded `sh -c` (POSIX-portable, intentionally not `ShellBin`). **Known wart**: `type: builtin` in `when:` resolves the *predicate* registry here; `type: builtin` in a step body or `check:` resolves the *engine builtin* registry in `internal/core/execution/builtin/` — same keyword, two registries, disambiguated by YAML position. **`generated-missing <svc> <field>` predicate**: gates a service's generate step (true when the field is absent from `.dwe/generated.yml` or the store is missing). ⚠️ `EvalBuiltin` does `SplitN(predicate," ",2)`, so `parts[1]` is the WHOLE remaining string — the case re-splits it on whitespace into `<svc>`/`<field>` via the exported `ParseGeneratedMissing(args)` (the single two-arg parser, reused by the `config` validator's cross-check), NOT the single-path `path`/`rel` variable. User-facing semantics: `docs/reference/config/conditions.md`. **`Predicates() []PredicateEntry`** (agent-discoverability milestone) is the enumeration surface for `dwe docs llms-txt`, **disjoint** from `builtin.Inventory()` despite the shared word: a verb listed here is rejected in a `check:`/step body and vice versa. `TestPredicates_MatchEvalBuiltinSwitch` guards BOTH directions — forward by evaluating each listed verb, reverse by parsing `EvalBuiltin`'s `switch verb` case literals out of `condition.go` with `go/ast`. A count pinned to `len(Predicates())` would be tautological (a new switch case alone leaves it green), so keep the AST walk. Author-facing consequence of that two-direction guard: adding a `when:` verb means updating **both** `Predicates()` and `EvalBuiltin`'s `switch` — either half alone fails the drift test.
- `internal/core/execution/filesgate/` — deploy step file-existence gate (parser-types): `FilesGate`, `State` (`readable` / `missing`), `RequireSpec`, `StepRef` (minimal step-shaped value so validators avoid importing `config.DeployStep`). Subpackage `spec/` does plan-time validation (`Validate`, `ResolveRequireIDs` against command `files:` block). Root package stays import-light. User-facing semantics: `docs/reference/config/deploy/conditions.md` § `files_gate:`.
- `internal/core/execution/builtin/` — engine builtins usable as step bodies or `check:` actions. **Package layout**: a leaf `spec/` subpackage (cycle-breaker — `Builtin` interface, `Kind`, `CallerContext`, `ExecContext`, `Entry` struct, exported helpers `GetStringParam`/`GetBoolParam`/`GetStringSlice`/`GetStringMap`/`GetMapAny`/`GetDurationParam`/`SortedKeys`, plus shared env-file parsers `ParseEnvEntries`/`ParseEnvKeys`/`EnvLineKey` used by both `services/` and `env/`) + six domain subpackages each exposing `func Builtins() map[string]spec.Entry`: `containers/` (8 entries — `docker_daemon_start` / `docker_daemon_logs` / `docker_daemon_stop` / `daemons_reap` / `containers_running` / `docker_stop_remove_container` / `docker_wait_healthy` / `docker_remove_project_volumes`), `services/` (6 — legacy copy mechanism `service_configs_copy` / `service_configs_check` (deprecation-warned) + `service_dirs_ensure`, plus the render mechanism `service_configs_render` / `service_configs_render_check` / `service_generated_harvest`), `fs/` (2 — `file_exists` / `remove_paths`), `env/` (2 — `env_keys_present` / `executable_in_path`), `interaction/` (2 — `confirm` / `message`), `source/` (1 — `source_clone`). Root `builtin/` holds the public API (`Get` / `Run` / `Validate` / `Describe` / `IsInteractive` / `KindOf`), 4 cross-cutting predicates (`shell`, `tcp_reachable`, `http_check`, `config_keys_present`) that have no natural domain home, and the registry assembly: `buildRegistry()` merges the 6 subpackage `Builtins()` maps with the 3 root entries and panics on duplicate names (the panic is paired with `TestNoDuplicateRegistryNames` for compile-time-equivalent coverage). `config_keys_present` (`KindPredicate`, `with: {keys: [<dot-path>...]}`) asserts each dot-path resolves to a non-empty value in the merged `cfg.Raw` — the config-aware counterpart of `env_keys_present` (reads in-memory merged config, not an on-disk `.env`); addressing matches the setup wizard's `writes:` dot-paths, so it pairs naturally with the `post-setup` check stage. `http_check` (`KindPredicate`, `with: {url, status?=200, contains?, retries?=0, interval?=1s, timeout?=5s}`, modeled on `tcp_reachable.go`) asserts an HTTP GET returns the expected status (and optional body substring); the retry loop runs `retries+1` attempts, waiting `interval` (ctx-cancellable) between and applying `timeout` per attempt via `context.WithTimeout`. **Predicate-as-body / kindAllowed relaxation (integration-tests stage 1a)**: `kindAllowed` now permits `KindPredicate` in the step-body context (`CtxUserYAML`), not just `check:`/validate — a predicate used as a step body is an **assertion** (`false` fails the step with the predicate's message; `true` succeeds). Predicates already return `error` on `false`, so the executor/resolve needed no change — only the kind gate opened. Because `CtxUserYAML` is shared, user-command `type: builtin` definitions accept predicate builtins too (intentional — commands and pipelines share the registry). `KindInternal` stays engine-only. The exported classifier `KindOf(name) (Kind, bool)` (mirrors `IsInteractive`) is what `pipeline.StepForcesRun` uses to detect predicate bodies — `Get(cmd, CtxPredicate)` cannot distinguish predicates from actions since both are valid there. The validate domain mirror is test-only: `dwe validate` routes body-kind checks through the same `kindAllowed`; the only `validate/checks/loader.go` `builtin.Validate` calls are `CtxPredicate` (unaffected). `http_check` is also in that loader's hardcoded `workspace/validate.yml` checks allowlist (now seven builtins). Root re-exports `spec.Builtin`/`spec.Kind`/`spec.CallerContext`/`spec.ExecContext` and the const groups via type/const aliases so external callers (`pipeline/`, `validate/checks/`, `workflow/lifecycle/`) continue using `builtin.Builtin` / `builtin.ExecContext` / `builtin.CtxUserYAML` unchanged. Behavior contracts (unchanged by the refactor): `Builtin` interface threads `ctx` for cancellation; `spec.ExecContext.DockerConfig` (loaded once per top-level invocation, normalised `os.ErrNotExist` → `&DockerConfig{}`) is the single source of docker policy — builtins never call `config.LoadDockerConfig` themselves; `IsInteractive(name)` is the centralised predicate (`confirm`, `docker_daemon_logs`) consumed by both pipeline plan-time (`pipeline/resolve.go` `checkInteractive`) and workflow runtime (`runners/builtin/builtin.go` parallel guard); `docker_stop_remove_container` backs the per-service reset synthetic `container` phase (stop+rm one named container, idempotent on missing, skips rm if stop fails); `containers_running` is the fast "is running" check (no polling, no timeout, no healthcheck — the right partner for `check:` on `docker up` steps); `daemons_reap` is auto-injected into `dwe stop` via `lifecycle.EnsureStopConfig` as the synthetic `_auto_reap_daemons` phase; the `containers/` daemon trio (`docker_daemon_start` / `docker_daemon_logs` / `docker_daemon_stop`) backs registry expansion of `type: daemon` user commands (see `internal/core/usercommands/`). **Config-render trio** (all `KindAction`, thin wrappers over `internal/core/execution/templates/config/`): `service_configs_render` (`with: {service, mode?}`, default `replace`) → `RenderConfigs`; `service_configs_render_check` exists to be paired as the render step's `check:` so its PRESENCE forces re-run every deploy via the `hasCheck → Run` lever in `journal/decision.go` (mirrors `service_configs_copy`+`service_configs_check`); `service_generated_harvest` (`with: {service}`) → `HarvestGenerated`. The copy pair (`service_configs_copy` / `service_configs_check`) is deprecated — `ConfigsCopy.Run` emits a single `ectx.Output.Warning` notice (NOT also from `ConfigsCheck.Run`, which would double-warn as the copy step's check). **`source/source_clone`** (`KindAction`, `with: {repo, dir, branch?}`): clones a git repo into a project-relative dir with the idempotency gate built in (destination carries a `.git` entry — matched by `Lstat` of ANY type, since a worktree/submodule `.git` is a file — → skip+success, branch-blind on purpose; absent/empty → clone; non-empty non-git → error naming the path). The destination goes through `pathsafe.ContainedRel` + `CheckNoSymlinks` BEFORE touching the filesystem and `EnsureRealUnder` on the symlink-resolved parent AFTER `MkdirAll` (which follows symlinks) — both halves are required. Git binary via the nil-safe `config.GitBin`, never `cfg.Binaries.*`. Non-interactive posture is load-bearing (an unattended deploy must not hang on a credential prompt): `GIT_TERMINAL_PROMPT=0` plus **empty** `GIT_ASKPASS`/`SSH_ASKPASS` (git only runs an askpass helper when the value is non-empty, so emptying defeats an inherited GUI helper and falls through to the terminal prompt that `GIT_TERMINAL_PROMPT=0` turns into an immediate error — do NOT "fix" this to a dummy program), `GIT_SSH_COMMAND=ssh -o BatchMode=yes` only when unset **or empty** (empty is meaningful for the askpass pair but not here — git would take an empty ssh command literally instead of falling back to its default, so nothing can be expressed by clearing it), and `cmd.Stdin = nil`. It is an ACTION, so `kindAllowed` makes it reachable from `check:` (see the general relaxation above) — deliberately not "fixed", since narrowing `kindAllowed` would affect every action builtin; it is blocked from `workspace/validate.yml` by that loader's hardcoded seven-name allowlist. **`Inventory() []InventoryEntry`** (agent-discoverability milestone) is the enumeration surface for `dwe docs llms-txt`. The human summary lives on **`spec.Entry.Summary`**, NOT as a method on the `spec.Builtin` interface — so a new builtin needs its summary added in the owning subpackage's `Builtins()` map, and a test (not the compiler) asserts every entry has a non-empty one. The package doc-comment deliberately no longer lists the builtins (it drifted) — it points at `Inventory()`. Disjoint from `condition.Predicates()`. The render check-pairing exists because the render step's real inputs are invisible to the journal hash — a template-pack edit and a cleared `generatedstore` both leave `ProjectConfigHash`/`ServiceConfigHash` unchanged — so without the paired `check:` a `dwe deploy run` would report `already up-to-date` over stale rendered configs; the pairing is what makes template edits and store clears always apply. Because the `source_clone` gate is built in, it **replaces** the `when:`/`check:` pair a workspace would otherwise wrap around a hand-rolled `git clone` step; re-adding one is redundant at best, and a `when:` that disagrees with the `.git` probe is wrong — the builtin, not the caller, owns the "already materialised" decision. Both `Inventory()` and `InventoryEntry` live in `builtin.go`. The disjointness from `condition.Predicates()` is a **documentation** rule as well as a runtime one: never write (in prose, in a doc-comment, or in generated output) that `check:` accepts `when:` predicates or that a step builtin is usable in `when:` — the shared `builtin` spelling invites exactly that sentence, and `llmstxt`'s `writeBuiltinsSection` exists to say the opposite.
- `internal/core/execution/templates/` — template pack helpers shared between render commands and validators (`manifest/`, `packroot/`, `ide/`, `ai/`, `git/`). Manifest schema, `<pack>.local/` overrides, and collision policies (IDE / git: deepest extends wins; AI: shallowest) are documented in `docs/reference/render/index.md`. Each kind exposes `SelectServices`, `ResolveTemplatePack` (implicit-missing returns `found=false, err=nil`; explicit-missing or invalid-name errors), `LoadManifest`, `ValidateManifest`, `DryRunRender`, `ExtendsRoot`. Manifest is **mandatory** — packs without `manifest.yml` return `ErrManifestMissing`. **`ResolveTemplatePack` implicit chain** (when `render.<kind>.template` is unset): service name → walk `extends:` chain ancestor-by-ancestor → `default`. First existing pack wins. Invalid pack names anywhere in the chain are skipped silently. **`DryRunRender`** parses + executes every render template in-memory against `TemplateData` without writing — used by the `templates` validator domain to surface missing-key / parse / execution errors at `dwe validate` time. **`AIRenderEnabledExplicit`** defaults to `s.IsApp()` (matches IDE/Git): only app services render AI docs by default; tools/infra opt in explicitly via `render.ai.enabled: true`. **`TemplateData.Service` vs `Resolved`**: `Service` is the canonical config identity (extends-chain root, used to key raw-config lookups); `Resolved` is the rendering identity (collision winner); `ServiceCfg` is the merged block of `Resolved` (so e.g. `.ServiceCfg.Container` reflects the extender's overlay).
- `internal/core/execution/templates/config/` — the **config** render kind: materializes service config files from template packs + a durable value store, replacing the legacy copy mechanism. **Deliberately does NOT use `packcommon`/`TemplateData`/raw-`{{ }}`** like ide/ai/git — config templates use the `${...}` shorthand substrate via `tpl.CompileVarSyntax`+`RenderContext` (user/devbox ergonomics, `${APP_*}`/`${DB_*}` parity). Two core functions: `RenderConfigs(projectRoot, cfg, serviceName, store)` resolves the pack (convention service-name → `extends` chain → `default`, plus `render.config.template` pin and `<pack>.local/` override via `packroot.Resolve`), loads + validates the manifest (`manifest.File`, `render: from→to`), builds the per-service `RenderContext` (merged config + `Generated = store.Service(serviceName)`), and renders each entry into `<svc.Dir>/<to>` (mode `replace`; pathsafe destination guard — authors write `to: src/...`, the `src/` join is convention not hardcoded). `RenderConfigs` is single-service; callers that process the whole project (the `dwe render config` no-arg path and `renderConfigsForRun`) iterate `config.DeployOrder(cfg, []string{"app"})` (deterministic golden output — never `range cfg.Services`). **Config rendering is app-only** — only app services may declare `dir` / `render` / `generated` (see `allowedFieldsFor`), so the auto-iteration loops pass just `"app"`; sweeping tool/infra would resolve the implicit `default` pack for a dir-less service and error. `HarvestGenerated(projectRoot, cfg, serviceName, store)` iterates the service's `generated:` fields, reads `<svc.Dir>/<file>` (pathsafe), applies `pattern` (regex, **capture group 1**), `SetIfAbsent` into the store, then `Save` — errors (missing file / no match / no capture group) surface, never silently skipped (**harvest, not mint**: the engine is hermetic, so the service mints the secret and DWE only reads a string back). **A field the store already holds is skipped before the read** (`store.Has` → record `Wrote:false` with the stored value, `continue`), so those strict errors apply only to a not-yet-stored field — precisely when a bad read could pollute the store. That skip is load-bearing rather than an optimisation: the documented pipeline gates only the mint step, and `dwe reset run` (without `--clear-generated`) keeps the store while wiping the service hub, so on the next deploy the minted file is gone while its value is still authoritative; an unconditional re-read failed the whole deploy over a value it already had. Both `HarvestResult` consumers (the builtin and `dwe render config --harvest`) read only `Field`/`Wrote`, which is why reporting the stored value for a skipped field is free. `CheckRendered(projectRoot, cfg, serviceName)` centralizes pack/manifest resolution + target-existence for the `service_configs_render_check` builtin. Consumed by the three `services/` builtins, the `dwe render config` CLI command, and `renderConfigsForRun`. The mint side is always the service's own tooling (`php artisan key:generate`, `bin/magento setup:install`, …) — DWE never runs a secret generator itself. There is a fourth strict error alongside missing file / no match / no capture group: an **empty capture** — a pattern that matches a line but whose group 1 is empty is rejected (`pattern %q matched but captured an empty value`), since a half-minted secret must not enter the store. The unconditional-re-read regression was found by a control run doing a plain `dwe reset run` → `dwe deploy run`, not hypothesised. Do NOT "fix" a harvest failure by bolting a second `generated-missing` gate onto the harvest step: the skip belongs inside `HarvestGenerated` so every project inherits it, whereas a gate pushes the workaround into every project's `deploy.yml`. Allowlist bookkeeping for this feature: `generated:` is a top-level `service.yml` key, so it needs entries in BOTH `config.allowedFieldsFor` and `validate/config.servicesAllowedFields` (see § Core — Foundation); `render.config` needs neither, because `render` is already allowlisted and its `config:` sub-block is struct-enforced.
- `internal/core/execution/templates/packcommon/` — shared leaf for the ai/ide/git template-pack infrastructure they have in common (the per-kind *validators* and collision resolvers deliberately stay separate — AI is shallowest-wins, IDE/git deepest-wins). Owns the extends-walk trio `ImplicitPackCandidates` / `ExtendsDepth` / `ExtendsRoot` plus the single `const maxDepth = 32` (previously duplicated 9×); the shared `TemplateData` struct with `AppServices` / `ToolServices` / `InfraServices` accessors; and `DryRunRender(kind, …)` / `executeTemplateInMemory(kind, …)` (the kind literal is the only per-pack difference). `ai`, `ide`, and `git` keep backward-compatible `type TemplateData = packcommon.TemplateData` aliases and `var` aliases for the walk funcs so external callers in `cli/render` and `validate/templates` compile unchanged. Cycle-free: `config` does not import `templates`.
- `internal/core/execution/preflight/` — assembler for the preflight hook invoked from lifecycle commands. `Run(ctx, cfg, cmdRegistry, baseDir, stage, skip, errOut) error`: when `skip == true` prints `"preflight skipped (--skip-preflight)"` to `errOut` and returns nil — NO validate.yml load, NO validator assembly, NO execution (the flag is a true bypass because `type: command` checks invoke arbitrary user scripts). Otherwise performs the single `LoadValidateConfig` parse, populates `validate.Context` with cfg + load result + warnings + load error, assembles `env.All(cfg)` + `checks.AllForStages(...)` + the single `config.validate` validator (so a malformed validate.yml surfaces inline as part of preflight, not silently), runs sequentially, renders diagnostics via `render.FormatDiagnostics` + `render.DiagnosticsTable` to `errOut` (always stderr at the command boundary), returns `*preflight.Error` (with `ExitCode() int = 1`) when any error-severity diagnostic surfaces. **Ordering contract**: every caller MUST invoke preflight BEFORE any side effect on Docker, git, or the filesystem — including `lock.Acquire` (deploy creates `.dwe/deploy/deploy.lock`), git probe/pull (run), and container stops (stop). cfg load and `loadCommandRegistry` are hoisted above preflight; registry-load failure is tolerated (nil registry yields unknown-command diagnostics). **Deploy two-gate / `post-setup`**: the check stage set comes from `stagesForPreflight(stage)` — for `"deploy"` it returns `{deploy, post-setup}`, every other stage maps to itself, and `""` → nil (match all). This is the ONLY place `post-setup` checks run: the interactive deploy menu's early pre-wizard gate (`cli/deploy/menu.go::runPreWizardPreflight`) queries only `"deploy"`, so `post-setup` checks are skipped there and execute at this final preflight after the setup wizard has populated `local.yml` (or, on `dwe deploy run`, immediately before the pipeline). User-visible schema: `docs/reference/config/validate.md` § Stages.

## Core — Workflow (`internal/core/workflow/`)

- `internal/core/workflow/deploy/` — deploy plan resolution: `ResolvePlan`, `ResolveServicePlan`, `ResolveServicesPlan`, `ResolveServicesPlanSubset(cfg, reg, deploys, names)` (subset deploy: validates every name has deploy.yml, topo-sorts by `after:`, strips duplicate ImplicitEnvStep, prepends one env step), `FindStep` (updated to read `workspace/services/<name>/deploy.yml`), `TrackedServices`, `LoadTrackedServices` (canonical tracked-service set from the resolved plan; nil registry tolerated — gate validation skipped). `TopoSortByAfter(deploys, services)` performs deploy-time ordering by the `after:` field (distinct from runtime `depends_on:` sort); returns names in dependency-first order with alphabetical tie-break; sentinel errors `ErrDeployCycle`, `ErrDeploySelfReference`, `ErrDeployUnknownAfterRef`. **Pipeline defaults**: `DefaultDeployConfig()` returns the built-in deploy pipeline (services → docker up --wait → post-deploy info+success); `EnsureDeployConfig(loaded *config.ProjectDeployConfig) (*config.ProjectDeployConfig, bool)` returns user config (false) or default (true). The CLI deploy entrypoint overwrites `cfg.Deploy` with the Ensure result so downstream resolvers always see a non-nil config. Subpackage `journal/` owns the state-file types and I/O (`ProjectState`, `ServiceState`, `PhaseState`, `StepState`, `Status`, `Load` / `Save` / `Remove` / `RemoveService`, hashing: `ActionHash` / `StepHash` / `ServiceConfigHash` / `ProjectConfigHash`, `Decide` for skip logic, `Recompute` for aggregates). `ProjectState` carries `Pending *PendingApply` (yaml omitempty). `PendingApply{Operations []PendingOp, ConfigHash string, CreatedAt time.Time}`; `PendingOp{Kind PendingKind, Services []string}`. `PendingKind` string enum: `PendingKindUnspecified` (zero), `PendingRestart`, `PendingDeploy`. Helper methods: `(*PendingApply).Find(kind)`, `(*PendingOp).ServiceNames()` (defensive copy). Package-level atomic helpers: `AddPendingOp`, `AddPendingOps` (atomic batch), `ClearPending`, `ClearPendingForKind`, `ClearPendingForServices`, `ClearPendingOps` (atomic batch clear), `ReplaceServiceWithPending` (atomic combine of RemoveService + AddPendingOp). State file: `.dwe/deploy/state.yml` (gitignored). The `defaulted bool` returned by every `Ensure*Config` has exactly one consumer across all four pipelines — `cmdctx.EmitDefaultNotice` (stderr-only, no-op in JSON mode; see § `internal/cli/cmdctx/`); `lifecycle` additionally routes it through the `OnDefaultUsed` callback so it can stay cobra-free. Pick between the clear helpers by ownership, not convenience: a caller that recorded specific ops must clear with `ClearPendingOps(statePath, clears)` passing exactly the `[]PendingClear` its own contributors produced — `ClearPending` wipes the whole `PendingApply` block and would therefore erase pending recorded by an unrelated concurrent session. On the write side, `AddPendingOps` on a corrupt state file either rewrites it cleanly or hard-fails; callers deliberately rely on that (rather than pre-validating) so corruption surfaces instead of silently dropping a pending intent. **`vars:` participates in both config hashes.** `ProjectConfigHash` mixes in `cfg.Vars` and `ServiceConfigHash` takes the same block as its third input, because resolve-time rendering substitutes `${vars.*}` into a step's `cmd`/`with`/`check`: omitting it from either would let `already up-to-date` skip a step whose rendered command actually changed. Both are needed, not one: `internal/cli/deploy/deploy.go`'s `makeSkipDecider` compares a service-scoped step against `ServiceConfigHash` and a project-scope step against `ProjectConfigHash` — and a `--service` deploy still carries project-scope steps, because `ResolveServicePlan` prepends the same `ImplicitEnvStep` (`Service: ""`) that the full plan does, so both hashes are live on every scope. Adding `vars` shifted every existing project's hash once on upgrade; the resulting one-time re-run is safe because steps are idempotent/gated. User-facing journal semantics: `docs/reference/config/state/index.md`.
- `internal/core/workflow/envtest/` — integration-test scenario schema + loader (stage 1a) plus the runner/isolation implementation (stages 1b–3b: reports, per-step timeouts, compose-isolation gate, parallel scenarios with the `Progress` seam and process-wide port leases; CLI lives in `internal/cli/test/` below). `Scenario{Description, Env ScenarioEnv{Services ScenarioServices{Enable, Disable []string}, Vars map[string]any}, Timeout string, Steps []config.DeployStep}` — `Steps` reuses `config.DeployStep` verbatim (the full field set: `files_gate`, `parallel`, `continue_on_error`, …; no test-only step allowlist). `Timeout` stays a raw string (parsed in 1b). `LoadScenario(path)` does a **strict** `KnownFields(true)` decode then calls `config.ValidateDeploySteps` for step-shape validation. **Loader strictness divergence**: unlike the strict pipeline loaders, an empty / all-comment file is an **error** ("scenario file is empty or invalid") — `io.EOF` is NOT tolerated (a scenario has no meaningful default; spec §8 carve-out). Registry-dependent validation (command existence, `sub_step_overrides`) stays with `ResolvePhaseSteps`/the 1b runner. `ListScenarios(baseDir)` enumerates `workspace/tests/*.yml` (`.yaml` accepted; absent dir → empty list, no error); the scenario name = basename without extension and must already match `^[a-z0-9][a-z0-9_-]*$` — reject otherwise (`ValidateScenarioName`), **no** case-folding or sanitising (the name feeds the compose project name in 1b). Only `AutoPortSentinel = "auto"` ships for var values — a raw-string sentinel meaning "free host port" (allocation logic is 1b; no typed field). **`${...}` rendering is `ResolvePhaseSteps`' job, and there is no scenario-local pre-pass.** `envtest` deliberately owns NO renderer: `resolveLeafStep` renders `cmd:`/`with:`/`check:`/`files_gate`/`timeout` (and `when.cmd` at every scope) into a copy at the top of the function, before `builtin.Validate`/`spec.Validate` read the step, so the order contract that once justified a loader-side pre-pass is already satisfied — and a scenario's context is the same Raw + Host one, so `${param.*}` / `${context.*}` / `${files.*}` / `${generated.*}` / `${args}` are hard errors there via `tpl.ValidateRawScope` exactly as they were here. **Adding a pre-pass back would render twice, and rendering is not idempotent**: a var whose value is itself a `${...}` reference expands twice, and a var holding a literal `{{` is re-parsed as a Go template and hard-fails the resolve — in both cases the scenario would exercise a command the deploy pipeline never runs. The step-shape validator is reached via the new `config.ValidateDeploySteps(steps []DeployStep, context string) error` — a thin exported wrapper over the unexported `validateStepShape`/`validatePhaseSteps` in `project/config/workspace.go`, since full step-shape validation was otherwise unreachable outside `project/config`. **Stage 1b runner** — `Runner{execDwe, allocatePorts, newTeardownDeps, clock}` (`runner.go`), every external effect a seam so tests never touch real Docker/dwe; `NewRunner()` wires production implementations. `RunScenario(ctx, RunRequest) (*ScenarioResult, error)` runs spec §6's flow: `lock.Acquire(LockPath(baseDir, scenario))` (never `lock.AcquireProjectLocks` — the copy's own `.dwe/` locks are the deploy subprocess's job) — the flock path is `.dwe/tests/locks/<scenario>.lock` (`LockPath`) → `LoadScenario` → resolve timeout (flag > scenario `timeout:` > `defaultScenarioTimeout=30m`) → **kept-run guard** (`existingManifestPaths` finds any `<scenario>-<runid>.yml` under `ManifestsDir`; a hit returns `*KeptRunError` with nothing touched — a `--keep` environment or half-dead run owns the copy) → `CopyTree` → seed `local.yml` load + `writeCopyLocalYAML` (host-port remap + `AllocatePorts` + `BuildLocalOverlay` + `ApplyHostPortOverrides` + `WriteGeneratedLocalYAML`) → `WriteDockerIdentity` → `WriteManifest` (before any Docker interaction) — so a half-dead run is always sweepable from the manifest alone; symmetrically, every prep failure BEFORE the manifest exists (`prepFail` — seed `local.yml` load, `writeCopyLocalYAML`, `WriteDockerIdentity`, and `WriteManifest` itself) best-effort `os.RemoveAll`s the copy and warns, because manifest-driven `Teardown` has nothing to find yet and stage-2 `dwe test clean` would otherwise never see the leftover; from `WriteManifest` onward the inverse holds and cleanup is manifest-driven only, never a raw `os.RemoveAll` → subprocess `dwe validate` then `dwe deploy run --silent` (one retry via `retryDeployWithFreshPorts` when the copy allocated any port and deploy fails — trigger is a deploy failure that BOTH has allocated ports (`hasAllocatedPorts` — a remapped host port for an enabled service, OR an `env.vars: auto` port) AND looks like a host-port bind conflict (`isPortBindConflict` string-matches the combined deploy tail + error; a TOCTOU loss can surface as either the preflight text or a later compose bind error, so both are matched — a non-port failure with allocated ports does NOT retry, pinned by `TestRunScenario_NonPortConflictFailure_NoRetry`); the retry re-allocates EVERY port via `writeCopyLocalYAML`) → `runSteps` in-process → deferred `Teardown` (fresh `context.Background()`, skipped entirely under `Keep`). **Error-vs-result split**: a bare `(nil, error)` return means nothing was created to report against (lock held, kept run, scenario/timeout load failure) — from `CopyTree` onward every failure is instead captured in a non-nil `*ScenarioResult` (`StatusError` for copy/config/manifest/validate failures, `StatusFailed` for deploy/step/timeout failures) with a nil error, so a multi-scenario CLI run keeps going and still reports every scenario. A timeout firing during any phase forces `StatusFailed` regardless of which phase was in flight. `runSteps` loads the copy's own post-deploy `config.LoadConfig` + `usercommands.LoadRegistryFromConfigPath`, calls `reg.ApplyVisibility(copyCfg, copyRoot)` BEFORE `ResolvePhaseSteps` (mirrors deploy — `executor.go`'s Hidden-target skip reads `CommandDef.Hidden`, zero-value false until `ApplyVisibility` runs), then a synthetic `config.DeployPhase{Name:"tests"}` → `ResolvePhaseSteps(copyCfg, reg, phase, "")` → `pipeline.RunWithOptions` with `SkipConfirm:true`, `SkipDecider:nil` (every step runs), a `failStepCapture` reporter wrapper recording the first failing step's resolved address for `ScenarioResult.FailedStep`. `ReporterFactory func(workDir, name string) (rep pipeline.Reporter, logWriter, subprocOut io.Writer, cleanup func(), err error)` is CLI-injected via `RunRequest` (nil → `defaultReporterFactory` = `OpenPipelineLog`+`NewPlainReporter`, always `os.Stdout`-backed) so `internal/cli/test/` can substitute an `io.Discard`-screen variant in JSON mode without touching the runner. **Subprocess color** — the `dwe validate`/`dwe deploy run` subprocess writes to a pipe (`subprocOut`), so lipgloss would downgrade its output to no-color even when the runner streams it to a real terminal; `RunRequest.ForceColor` adds `CLICOLOR_FORCE=1` to the subprocess env (`TERM`/`COLORTERM` inherited, so the profile matches the parent's own rendering) and the CLI sets it ONLY in interactive, sequential text mode on a TTY — never JSON or `--parallel` (>1), where nothing is streamed to a terminal. The run log stays plain regardless: `defaultReporterFactory` tees `subprocOut` to `termOut` raw but through `stripANSI(logFile)` on the log side (a stateful streaming ANSI stripper in `ansi.go` that holds back an escape sequence split across two writes), so the run log — and, on failure, the report's `pipeline.log` — never gains color codes. The `dwe validate`/`dwe deploy run` subprocess spawn is the injectable `execDweFunc` seam (`execDweProcess`, resolving `os.Executable()` — the same pattern as the bridge daemon's `SpawnFunc`); tests MUST stub it, since letting it resolve to the real binary would recursively re-exec the test binary. **Copy** (`copy.go`) — `CopyTree(srcRoot, dstRoot, gitBin, warn)`: `os.RemoveAll(dstRoot)` first (the copy path is fixed per scenario; kept/live runs are protected upstream by the kept-run guard, not by skipping the wipe), then `git -C srcRoot ls-files -co --exclude-standard -z` selects files (worktree-absent entries — uncommitted deletions — are skipped; `.dwe/`, `.env`, `.git/` always excluded top-level; symlinks recreated as symlinks, permissions preserved); a non-git tree or git failure falls back to a full `filepath.WalkDir` copy with the same exclusions plus one `warn` call (gitignored artifacts included). **Generated `local.yml`** (`localyaml.go`) — `BuildLocalOverlay(seed, scn, projectName, ports, warn)` composes low→high: seed = original `local.yml` (`compose.extra` + every `services.<name>.compose.extra` stripped, warned) → scenario `env.vars` dot-paths under `vars:` (`AutoPortSentinel` values replaced from the `ports` map) + `env.services.enable/disable` → `services.<name>.enabled` → identity `project.prefix = projectName` + `update.mode = "off"` (explicit — an empty/absent mode means ON per `EffectiveMode`). `ports.go`'s `AllocatePorts(n)` opens `n` `net.Listen("tcp", ":0")` listeners and only closes them after harvesting every port, guaranteeing intra-batch uniqueness (TOCTOU across the allocate→write→deploy gap is accepted per spec §9 — the one retry covers it). **Automatic host-port isolation** (`hostports.go`): `enabledHostPortKeys(cfg, scn)` enumerates every host port declared under `services.<name>.ports` by a service that will be ENABLED in the copy (original merged `Enabled` overridden by the scenario's `env.services.enable/disable`; ports outside 1..65535 skipped, mirroring `collectDeclaredPorts`), `buildHostPortOverrides` pairs each with a freshly allocated port (scheme preserved), and `ApplyHostPortOverrides` merges them into the overlay AFTER `BuildLocalOverlay` as `services.<name>.ports.<x>` (bare int, or `{port, scheme}` when the original carried a scheme) — coexisting with any `services.<name>.enabled` toggle. This is what makes a test run alongside the working environment with NO scenario port config: `ports_free` preflight reads `services.<name>.ports` literally, and a project sourcing its compose host bindings from the same field (directly or via an `exports.env` `from: services.<name>.ports.<x>`) binds from it too, so preflight and the actual bind move together. Host-port isolation is host-wide, not merely project-wide: `AllocatePorts` listens on `:0`, so the run also dodges ports currently held by other projects, not only by the developer's own working environment. `writeCopyLocalYAML` (`runner.go`) drives host-port + `auto`-var allocation as a SINGLE `AllocatePorts` batch (so a host port and a var port never collide) and is re-invoked verbatim by the deploy retry. Deliberately independent of `env.vars: auto`, which only rewrites vars-routed ports and cannot touch a `services.<name>.ports` int (the strict-int preflight reads it literally — `${vars.x}` in a service `ports:` is rejected at load). **Docker identity** (`dockeridentity.go`) — `WriteDockerIdentity(copyRoot, projectName)`: copy has `workspace/docker.yml` → write `workspace/docker.local.yml` with only `project_name:` (overwrites any copied one, last-word layer); copy has none → write `workspace/docker.yml` with `project_name:` PLUS an explicit `[]` for every `DockerArgs` key (semantics-neutral — reproduces `LoadDockerConfigOrEmpty`'s missing-file zero-value exactly, pinned by a `LoadDockerConfig`-equivalence test; a no-`args:` file would wrongly gain the up/logs/run/down defaults) and deletes any stray copied `docker.local.yml`. **Manifest** (`manifest.go`) — `Manifest{Scenario, RunID, ComposeProject, CopyPath, BridgeDir, ReportDir, CreatedAt}`; `WriteManifest`/`LoadManifest`/`DeleteManifest` (atomic write, mkdir -p) at `.dwe/tests/manifests/<scenario>-<runID>.yml` — the field set is chosen to cover stage-2 `dwe test clean` (`ReportDir` is populated but unused until stage 2). **Teardown** (`teardown.go`) — `Teardown(ctx, m, TeardownDeps, warn)` runs, each step best-effort (errors joined, later steps still run): `ComposeDown` (built from `docker.NewCompose(...).BuildInternalArgs("down", "--remove-orphans")` — **`BuildInternalArgs`, never `BuildArgs`**: bypassing the copy's `docker.yml` `args.down` policy is the mechanism that enforces "never `-v`", since a project setting `args.down: ["-v"]` would otherwise turn teardown into `compose down -v` and delete shared named volumes ahead of the prefix-scoped `RemoveVolumes` step, so teardown is always exactly `compose down --remove-orphans` — run via `exec.CommandContext` + `Dir=copy` + `BuildEnv()` — deliberately NOT `Compose.Exec`, which has no context param and hard-wires `os.Stdout`/`Stderr`/`Stdin`; never appends `-v`; a copy that no longer loads reports `skipped=true` via `warn` and every later step still runs) → `ReapContainers` (`docker ps -aq --filter label=com.docker.compose.project=<m.ComposeProject>`, `-a` required since exited/one-off containers still hold volume refs, then `docker.RemoveContainer` each — exact manifest-recorded identity, never a name-pattern guess) → `RemoveVolumes` (delegates to the Task-5-extracted `containers.RemoveVolumesByProjectPrefix`, prefix `<ComposeProject>_`, shared unprefixed volumes survive) → `StopBridge` (`bridge.StopDaemon(m.BridgeDir)`, stale/missing pidfile is a clean no-op) → `RemoveCopy` (`os.RemoveAll`) → `DeleteManifest`. `TeardownDeps` is one seam per step (`NewTeardownDeps(manifestPath, log)` wires the real subprocess/docker/bridge calls; tests construct a recording `TeardownDeps` directly). Callers MUST pass a fresh context — never the scenario's expired deadline, since teardown must still run after a timeout or Ctrl+C (the runner uses its own `teardownTimeout=5m` background context). **Stage 2 — failure reports** (`report.go`): `CollectReport(ctx, m, ReportDeps, warn) (reportDir string, err error)` writes three artifacts into `m.ReportDir` — `os.RemoveAll` then `os.MkdirAll` first (per-scenario overwrite: the latest failure is what you debug), then `pipeline.log` (pure-fs copy of `copyRoot/.dwe/logs/test.log`, missing source = warn+skip, never an error), `compose-ps.txt` (`deps.PS`), `container-logs.txt` (`deps.Logs`, combined, `==== <id> ====` headers) — every step best-effort, a partial `(output, err)` from `PS`/`Logs` still gets written plus a warn. `ReportDeps{PS, Logs func(ctx, *Manifest) (string, error)}` mirrors `TeardownDeps`; `NewReportDeps()` wires `reportPSReal`/`reportLogsReal`, which key the same compose-vs-identity graceful degradation off `loadCopyConfig(m.CopyPath)` as `Teardown`: success → `docker.NewCompose(...).BuildInternalArgs("ps", "--all")` / `BuildInternalArgs("logs", "--no-color", "--tail", "200")` — **`BuildInternalArgs`, never `BuildArgs`**, because `BuildArgs` injects global flags (`--ansi always`/`--progress tty`) AND per-command policy defaults (`dockerCfg.Args.Logs`), so a project's `docker.yml` setting `args.logs: ["-f"]` would make report collection follow logs forever; `ps` MUST carry `--all` since plain `compose ps` is running-only and would drop exactly the crashed service a failure report exists to surface. Config-load failure falls back to identity-label capture (`docker ps -a --filter label=com.docker.compose.project=<proj>` for `PS`; per-container-id `docker logs --tail 200 <id>` for `Logs`, reusing `listContainersFn`). Both real impls route through one generic seam `captureCmdFn(ctx, bin string, args, env []string, dir string) ([]byte, error)` (default `captureCmdReal` = `exec.CommandContext(...).CombinedOutput()`) — **combined** stdout+stderr, not `Output()`: `docker logs` replicates a non-TTY container's stderr to its own stderr, so an stdout-only capture would drop exactly the crash/panic output the report exists to surface, and combining also folds a failing capture command's own diagnostic into the artifact. `reportLogTailLines = 200` const. **Runner wiring** (`runner.go`): `Runner.collectReport func(ctx, *Manifest, warn func(string)) (string, error)`, defaulted by `NewRunner` to `CollectReport(ctx, m, NewReportDeps(), warn)`; `finish` calls it once — **after** the status is finalised (incl. the deadline→`StatusFailed` override) but **before** `teardown()` (containers must still be alive, the pipeline log must still be present) — only `if r.collectReport != nil && !req.Keep && status != StatusPassed`, under a **fresh** `context.WithTimeout(context.Background(), reportTimeout)` (never the scenario's expired deadline, same reasoning as teardown's own fresh context). Success sets `result.ReportDir`; failure only `warn`s — collection is best-effort and never changes the scenario outcome. The `r.collectReport != nil` guard exists purely for `runner_test.go`'s inline `&Runner{...}` literals (no shared constructor) — production `NewRunner` always sets the seam. **`Clean`** (`clean.go`): `ListManifests(baseDir)` generalises the 1b `existingManifestPaths` pattern across every scenario (all `<scenario>-<runid>.yml` under `ManifestsDir`, sorted; absent dir → empty, no error); `projectBaseName(cfg)` was extracted out of `ComposeProjectName` so the orphan-scan prefix cannot drift from the real compose-name derivation. `Clean(ctx, CleanRequest{BaseDir, Scenarios, DryRun, Warn}) (*CleanResult, error)`: enumerates manifests (unreadable dir → hard error; absent → empty result), builds `known` from the **full** unfiltered manifest set (a filtered-out or skipped-live manifest still "explains" its docker project — never counted as orphan), then filters by `Scenarios` for the sweep. Per manifest: `lock.Acquire(LockPath(baseDir, m.Scenario))`, classified via `errors.AsType[*lock.HeldError](err)` (never a bare type assertion, so future error-wrapping can't misclassify) → `Skipped{reason:"live"}`; any other lock error → `Skipped{reason:"lock error"}` + warn (never destroy on uncertainty); free + `DryRun` → `Swept` (would-sweep) without touching anything; free + real run → `cleanTeardownFn` (default: `Teardown(ctx, m, NewTeardownDeps(manifestPath, nil), warn)`) under a fresh `teardownTimeout` context — **nil error ⇒ `Swept`, non-nil error ⇒ `Failed{…, Error}` + warn, NEVER `Swept`** (`Teardown` is best-effort and joins errors after attempting every step, so a partial failure could still have deleted the manifest; reporting it swept would be false success and strand the leftover for the next `dwe test run` to trip over). The flock is released either way. **Orphan scan is report-only and best-effort**: `config.LoadConfigOrWrap` failure on the original project → warn + skip *only* the scan (the sweep itself needs nothing from the original config, since `Teardown` resolves everything from the manifest + copy); `listComposeProjectsFn(ctx, dockerBin)` (default `docker ps -a --format '{{.Label "com.docker.compose.project"}}'`, deduped) error → warn + empty orphans, never aborts `Clean`. Orphans = distinct scanned projects whose name has the `normalize(projectBaseName(origCfg) + "-t-")` prefix (fed through the exact same normaliser `ComposeProjectName` uses) minus `known`; each becomes `Orphans{ComposeProject, Note:"no manifest — remove manually"}` and is **never torn down** — the label scan may only report, matching by name pattern is explicitly not a destroy trigger anywhere in this package. **Stage 3b — coarse `Progress` seam + process-wide port lease set.** `RunRequest.Progress func(phase ProgressPhase)` is a **UI-free** coarse-progress callback (nil → no-op, nil-guarded at the top of `RunScenario` exactly like `Warn`) — envtest fires the typed `ProgressPhase` enum (`PhasePreparing`, `PhaseValidating`, `PhaseDeploying`, `PhaseDeployRetry`, `PhaseRunningSteps`, `PhaseCollectingReport`, `PhaseTearingDown`) at the natural start of each phase, and the CLI (not envtest) maps phase → display label, keeping the layering intact (envtest imports no `ui`). Firing points: `PhasePreparing` after the kept-run guard + config load, before `CopyTree`; `PhaseValidating`/`PhaseDeploying` before their subprocess; `PhaseDeployRetry` only on the one `retryDeployWithFreshPorts`; `PhaseRunningSteps` before `runSteps`; `PhaseCollectingReport` only inside the `collectReport != nil && !Keep && status != StatusPassed` branch; `PhaseTearingDown` only when teardown actually runs (not under `--keep`). **Port lease set** (`ports.go`): a package-level `leaseMu sync.Mutex` + `leasedPorts map[int]struct{}` records every port `AllocatePorts` ever returns in this process; a harvested port already in the set is skipped (that listener closed, another opened) and every returned port is registered, so two concurrent `AllocatePorts` batches in one process can never collide — closing the intra-process TOCTOU that parallel scenarios would otherwise hit. Leases are **never released**: `dwe test` is short-lived and allocates at most dozens of ports; the failed deploy attempt's ports stay leased so the retry automatically gets fresh ones. A bounded per-batch attempt count (`allocateAttemptsPerBatch = 100 + 20*n`) guards a pathological loop and returns `envtest: allocating free port: exhausted N attempts (M ports leased)` on exhaustion. Cross-process races between separate `dwe test run` invocations stay covered by the copy's `ports_free` preflight + the one deploy retry (spec §9). An unexported `resetLeases()` test helper clears the global under the mutex so sibling tests isolate lease state via `t.Cleanup(resetLeases)`. The same `BuildArgs` policy hazard covers the `ps` capture, not only `logs`: `BuildArgs` would apply `dockerCfg.Args.Ps` (`args.ps`) and reshape a machine-readable table, which is why BOTH captures take `BuildInternalArgs`. **Manifest identity guard (`clean.go`, stage 2)** — `sweepManifest` calls `validateManifestIdentity(baseDir, manifestPath, m)` BEFORE the flock and before any destructive step; a mismatch warns `refusing untrusted manifest` and records `Skipped{reason:"invalid manifest"}` — never a teardown. It re-derives every path a sweep touches from `(baseDir, m.Scenario, m.RunID)` and rejects anything that does not match: `copy_path == RunDir(baseDir, scenario)`, `bridge_dir == bridge.DefaultBridgeDir(copy_path)`, the on-disk filename `== "<scenario>-<runID>.yml"` (so one scenario's manifest cannot declare another's identity), and `copy_path` lexically within `.dwe/tests/runs/` AND symlink-free via `pathsafe.CheckNoSymlinks` — the lexical test cannot see a symlinked component, and a planted symlink at `runs/` or any ancestor would make `os.RemoveAll(copy_path)` resolve through it and delete host state outside the test root. The threat is concrete rather than theoretical: manifests live under the project's `.dwe/tests/manifests/`, which DWE mounts into containers, so a lower-trust container can plant one with an arbitrary `copy_path`/`bridge_dir` that a host developer's `dwe test clean` would then obey (the command is itself container-blocked — this guards the host-run case, and plain corruption). `validateComposeProject` pins the destructive Docker identity the same way, since `compose_project` drives container reap-by-label and volume removal-by-prefix: it must ALWAYS carry the normalised `-t-<scenario>-<runID>` test infix the runner unconditionally produces (enforced with `strings.HasSuffix`) — a floor the real dev environment's `<base>` never satisfies, so teardown can never be aimed at it even if the copy's own identity were tampered with — and, when the disposable copy still loads, must EXACTLY equal `copyComposeIdentity(copyPath)` (`config.ComposeProjectName` over the copy's own `docker.yml`/`docker.local.yml`, i.e. the value the copy's own `docker compose down` resolves). That identity is deliberately read from the COPY and NEVER re-derived from the current root config: a run kept (`--keep`) or left by a failure before a `project.name`/`project.prefix` rename still carries the exact identity teardown uses, so re-deriving would reject a legitimate manifest as `invalid manifest` and strand its copy plus its Docker resources — contradicting `dwe test clean`'s manifest+copy recovery contract. A gone/unloadable copy degrades to the infix floor alone. **Isolation gate (stage 3a, `scanComposeIsolationGate` in `runner.go`)** — after the reporter factory is wired but BEFORE the `dwe validate` subprocess spawns, the runner loads the COPY's config and runs `config.ScanComposeIsolation`, warning EVERY finding; one or more `Blocking` findings with `RunRequest.SkipIsolationCheck` false ends the scenario via `finish(StatusFailed, "")` — teardown still runs and NEITHER the validate nor the deploy subprocess is ever spawned — after a warning naming each blocking message plus the `--skip-isolation-check` escape. A copy whose config fails to load skips the scan entirely (the `dwe validate` subprocess surfaces the real config error instead).
- `internal/core/workflow/lifecycle/` — lifecycle pipeline execution: `RunPhases`, `RunRun` (git update + phases + show-info), `RunRestart` (stop + run), `RunStop`; `RunContext.ShowInfo` is injected to avoid importing cobra renderers; `RunContext.SkipNotify` is set by `RunRestart` on its inner `RunRun` call so a restart fires at most one desktop notification (restart is not a user-invoked run — spec says zero notifications). **Pipeline defaults**: `DefaultRunConfig()` / `DefaultStopConfig()` return freshly-allocated built-in pipelines (callers may mutate safely). `EnsureRunConfig(loaded *LifecycleConfig) (*LifecycleRunConfig, bool)` and `EnsureStopConfig(cfg *LifecycleConfig) (*LifecycleStopConfig, bool)` each return the user config (false) or the default (true) — the bool signals whether the CLI should emit the info line. `EnsureStopConfig` always prepends `autoReapPhase()` regardless of defaulting; the auto-reap phase is the sole exception to full-replacement composition. `RunContext.OnDefaultUsed func(DefaultedPipeline)` and `StopContext.OnDefaultUsed func(DefaultedPipeline)` let CLI adapters emit the info line without importing cobra renderers. `DefaultedPipeline` is a typed string enum (`DefaultedRun`, `DefaultedStop`). `RunRestart` propagates `ctx.OnDefaultUsed` into `stopCtx` so restart fires the callback for both legs. Deploy gate is unconditional — no bypass flag (compose errors immediately on undeployed services). **Run-preamble config render**: `renderConfigsForRun` auto-renders service configs (replaying stored `${generated.*}` values) and is called **AFTER the deploy gate AND after the post-pull cfg reload, but BEFORE `RunPhases`** — NOT at the early `.env` render; `run` never runs generate/harvest. Ordering IS the safety argument: a `reset --clear-generated` + `dwe run` fails the gate before render is reached, so secrets are never blanked. `missingGeneratedKeys` makes run-render non-destructive: if any declared `generated:` key is absent from the store, that service's render is SKIPPED with a `dwe deploy run` hint (deploy render stays lenient — first deploy mints; this guard is run-only). `config.SharesExtendsParentHub` skips (silently) an `extends` child whose hub dir is inherited from its parent: such a child is a config-render **alias** (same resolved pack + hub → byte-identical output) whose `${generated.*}` values are minted under the PARENT's store key, because it has no own `deploy.yml`/harvest (e.g. a debug sidecar that bind-mounts the app's source). The parent renders the shared hub; rendering the alias again is redundant and would falsely trip `missingGeneratedKeys` (its keys are absent under the child's own name). This mirrors deploy, which never schedules a render for such a child. A child with its OWN `dir` is a genuine render target and is NOT skipped. The helper is the **exported** `config.SharesExtendsParentHub(svc, services)` in `internal/core/project/config/workspace.go` (there is no unexported `sharesExtendsParentHub` in `lifecycle/` — `renderConfigsForRun` only calls it); its predicate is `svc.Extends != "" && svc.Dir != "" && svc.Dir == parent.Dir`, so "a child with its OWN `dir`" means a dir DIFFERENT from the parent's — re-declaring the same string is still an alias. The other consumer is `dwe render config` (`internal/cli/render/config.go`, both the render loop and the `--harvest` loop), where the skip is silent only in the no-arg sweep: an explicitly named alias gets an Info line naming the parent to render/harvest instead. On that path the consequence is worse than redundancy — there is no `missingGeneratedKeys` guard, so a lenient render would **blank the shared secret the parent just wrote**, and `--harvest` would mint a spurious store entry under the child's own name.
- `internal/core/workflow/reset/` — reset plan resolution (`ResolvePlan`, `FindStep`); mirrors deploy. `DefaultResetConfig()` returns the built-in reset pipeline (confirm → docker down → remove volumes + remove services/); `EnsureResetConfig(loaded *config.ProjectDeployConfig) (*config.ProjectDeployConfig, bool)` returns user config (false) or default (true).
- `internal/core/workflow/scaffold/` — pure domain behind `dwe init`. `Scaffold(Options) (Result, error)`: resolves the target dir, renders the embedded template plan, writes each file atomically (temp-file + `rename`), merges `.gitignore`, and links `CLAUDE.md` → `AGENTS.md`. **No cobra, no `io.Writer`** (honors "core returns data, cli writes"); the cli layer is `internal/cli/scaffold/`. `Options{TargetDir, Name, Prefix, Service, Branding{Title,Tagline,Accent}, Force}`; `Result{Target, Created, Skipped []string, SymlinkFallback, NestedWarning bool}`. **Idempotent at the file level** — fill-gaps, skip pre-existing files unless `Force`; a `Force` run reports everything `Created` (except the always-idempotent `.gitignore` append-merge). Two exported helpers let the cli layer pre-resolve the same path Scaffold writes to: `ResolveTarget(targetDir) (absPath, error)` (empty → cwd, then `Abs`) and `HasProjectConfig(absTarget) (bool, error)` (stats `workspace.yml` in the target *itself* — used for the cli existing-project consent gate, distinct from the ancestor-walking `detectNestedProject`/`NestedWarning`). Load-bearing details: **Starter artefacts (agent-discoverability milestone)**: `serviceScopedOutputs` lists the rendered paths that belong to the chosen service but do NOT live under `workspace/services/<name>/` — the ai render pack and `workspace/tests/smoke.yml`. `applyServicePlan` drops the service folder **plus** every path in that list when `Service == ""`, pinned against the rendered plan so a template rename cannot silently make the rule a no-op. Those two are also the ONLY inert-mirror files that must ship ACTIVE (a strict manifest needs one entry; the scenario loader errors on an empty document); the per-service `deploy.yml` skeleton in particular MUST stay commented — **not** because it fails to resolve (`TestEmbeddedTemplates_ServiceDeployPipelineResolves` uncomments it, loads it through the strict service-deploy loader and runs `ResolvePhaseSteps` green for every phase, which is what proves the skeleton teaches a shape that actually runs, `source_clone` params and `check: auto` derivation included), but because an **active** skeleton makes `dwe deploy run` try to clone its placeholder repo URL (`git@example.com:acme/<service>.git`) on every fresh project. The ai pack's embedded source is `AGENTS.md.tmpl.tmpl` because `mapEmbedPath` strips one `.tmpl`. Any template edit requires regenerating `testdata/golden_default.txt`. The skeleton's `bootstrap` step is `type: dwe` + `docker run …`, never a hand-rolled `docker compose …` in a `type: shell` step: `skills/dwe/SKILL.md` names that exact line (`npm ci` pasted into `deploy.yml`) as the anti-pattern, and a bare `docker compose` runs on the HOST and bypasses dwe's `-p`/`-f` chain — it resolves a *different* compose project and misses the service's overlays. Uncommenting the skeleton also moves the project's `host_steps` (`dwe test list --output json`, see § `internal/cli/test/`) from 0 to 1: its shell `when:` is real host execution, and the `check: auto` it demonstrates is only valid alongside a shell `when:` — so a project adopting the skeleton has to weigh that against the unattended-`dwe test run` gate; as shipped, commented, a fresh scaffold reports `host_steps: 0`. Symmetrically, the files carrying real content — `workspace.yml`, `defaults.yml`, `service.yml` — are active by design, and every other mirror ships inert.
  - **Template storage** (`templates.go`): `//go:embed templates` mirrors the output tree. Walker rule — files ending `.tmpl` render through `text/template` with **custom `[[ ]]` delimiters** (`Delims("[[", "]]")`), then the suffix is stripped; every other file is copied **verbatim**. The `[[ ]]` choice is deliberate: the inert `info.yml`/`docker.yml` references contain literal `{{ .Project.Name }}` examples that must survive copy unrendered. Dotfiles are stored with a `dot-` path-segment prefix (`dot-editorconfig` → `.editorconfig`, `dot-dwe/config` → `.dwe/config`) because `go:embed` silently skips `.`/`_`-prefixed names — **not** `all:` (which would also pull in editor junk). `renderPlanFS(fs.FS, opts)` is FS-injectable for tests.
  - **Inert overrides** — `deploy.yml`/`lifecycle.yml`/`info.yml`/`docker.yml` and the per-service `workspace/services/<name>/deploy.yml` ship **fully commented**. DWE pipeline composition is full-replacement (an active `deploy.yml` replaces the whole built-in pipeline, and a half-edit silently drops phases), so these stay inert and the built-in default stays active until the user uncomments. There is no divergence test — the only drift guard is the source pointer in each file's header (`Default*Config` constructor / `docs/reference/config/<x>.md`). A fully-commented YAML doc decodes to `io.EOF`, which the strict `KnownFields(true)` pipeline loaders in `project/config/workspace.go` now tolerate (empty doc == absent file → built-in default applies); see the Task 9 note in the plan.
  - **`styles.yml` shape** — branding renders the **nested** schema (`header.lines: [Title]`, `header.tagline: Tagline`, `colors.accent: Accent`); there is no top-level `title:` key, and `LoadStylesConfig` is lenient `yaml.Unmarshal` so a flat key would silently vanish. Empty branding falls back to commented placeholders via `[[- if ]]` guards.
  - **`.gitignore` is not a template** — the DWE block is the Go const `dweGitignoreBlock` in `gitignore.go` (single source of truth), applied via a separate `applyGitignore` append-merge path (create when absent; append only missing lines under a `# dwe` marker when present; idempotent). A template would make `renderPlan` double-emit `.gitignore`.
  - **`CLAUDE.md` symlink** — `linkClaudeMd(dir) (fallback bool, err error)` tries `os.Symlink("AGENTS.md", dir/CLAUDE.md)`; on error or pre-existing target it writes a verbatim copy and returns `fallback=true` (surfaced as `Result.SymlinkFallback`). The symlink call is a func var for testability.
  - **Starter service** — `applyServicePlan` rewrites the embedded `workspace/services/app/` segment to the chosen `Service` name, or drops the folder **plus every path in `serviceScopedOutputs`** (the ai render pack, `workspace/tests/smoke.yml`) when `Service == ""`. `service.yml` ships class-1 defaults active — `type`, `container`, `dir`, `dir_internal`, `work_dir_internal`, `icon`, `info.title` (a fully-commented `service.yml` is invalid under strict decode) — with the rest commented.
  - **Nested-project detection** — `detectNestedProject` walks ancestors (not the target itself) for a `workspace.yml`; a hit sets `Result.NestedWarning` (the cli decides how to present it) and never blocks.
- `internal/core/workflow/snapshot/` — snapshot subsystem (synthetic-command builder for workflow execution, create/restore/remove/list/exec operations, workspace-files capture, services diff, observer). Organised as a three-tier strict layering: `meta/` (leaf: descriptor types + path coordinates + `ScanArtifacts` + `WriteFileAtomic`; no internal subpkg imports) → `archive/` (tar I/O: `Pack`, `Unpack`, `VerifyExtractedArtifacts`, `ReadManifestFromTar`; imports `meta/` only) → root (business workflows: imports both). The cycle-break that pinned `scan.go` into `meta/` is intentional — `archive.VerifyExtractedArtifacts` re-scans extracted artifacts via `meta.ScanArtifacts`, and if `scan.go` lived at the root the import would go archive→root while root already imports archive. Same reason `WriteFileAtomic` is exported in `meta/atomic.go` (was private `writeFileAtomic`) — both `workspace_files.go` and `restore.go` at the root call it. Helper files split out from the create/restore flows: `workspace_files.go` (`captureDWEFiles` / `restoreDWEFiles` with `preserve_keys`-aware strip + merge over `*yaml.Node`, plus the 1 MiB `localYMLMaxBytes` cap on untrusted archive content; returns/accepts `meta.DWEFiles`) and `services_diff.go` (`DiffServices` + exported `FormatServicesDiff` shared by `snapshot inspect`, the restore prompt, and the `snapshot.<name>.services_diff` validator). User-visible schema: `docs/reference/config/snapshot.md`. Load-bearing invariants:
  - **Atomic IO contract** — `meta/manifest.go` / `meta/current.go` write a temp file **in the same directory** as the final file (cross-filesystem `rename` is not atomic), `chmod 0o644`, then `os.Rename`. Mirrors the `journal/state.go` pattern. The shared helper is `meta.WriteFileAtomic` (`meta/atomic.go`). The pre-restore backup at `.dwe/snapshots/.pre-restore-backup/` follows the same contract and overwrites the previous backup on each restore.
  - **No `schema_version` field** in `Manifest` — per project policy (CLAUDE.md). The manifest has no version gate, no migration path; rename / restructure freely as needed.
  - **Streaming sha256, int64 size, no symlinks** — `meta/scan.go` walks the snapshot directory excluding `manifest.yml` and `workspace/`, computes per-file sha256 via `io.Copy(hasher, f)` (never `io.ReadAll`) so multi-GB dumps don't OOM; rejects any entry with `os.ModeSymlink` set with a clear error — workflows are not allowed to produce symlinks in the snapshot dir.
  - **Render-context propagation point** — snapshot vars (`${snapshot.name|path|description|variant|created_at}`) are threaded from `runtime.RunContext.Render.Snapshot` + `.SnapshotScope` into every sub-step `RenderContext` in `runners/workflow/step.go` (`renderCtx.Snapshot` / `renderCtx.SnapshotScope` assignment block) and at the two leaf-context constructions in `build_context.go` (the `BuildRunContext` and `BuildSnapshotRunContext` constructors). Without this propagation, snapshot vars in `with:` values of leaf user commands resolve empty. Scope visibility is gated by the `tpl.SnapshotScope` enum (`SnapshotScopeNone` → compile error; `SnapshotScopeCreate` → all keys except `created_at`; `SnapshotScopeRestoreOrRemove` → all keys). The scope check runs in `tpl.RenderCommand` via `validateSnapshotScope` **before** `CompileVarSyntax` so `CompileVarSyntax` stays pure-syntactic with no error path.
  - **Workflow executor reuse** — `exec.go` builds a synthetic `*model.CommandDef{Type: "workflow", Steps: workflow.Steps}` and dispatches via `usercommands.RunCommand`. Snapshot workflows are user-command workflows executed at runtime from a different source file; there is **no** parallel pipeline executor, no shadow scheduler. `BuildSnapshotRunContext` mirrors `BuildRunContext` but injects `Snapshot` + `SnapshotScope` into `Render`.
  - **Live-view observer wiring** — `ExecParams.StepObserverFactory func([]model.WorkflowStep) StepObserverCloser` is invoked **after** `SelectWorkflow` and **after** the pre-workflow confirmation prompt, immediately before dispatching to the runner; this defers any TUI footer until all `huh` prompts are done. `StepObserverCloser` is a local interface embedding `runtime.WorkflowStepObserver` plus `Close()` — the snapshot package owns the `defer obs.Close()`; the CLI never holds the observer past factory construction. The CLI implementation lives in `internal/cli/snapshot/liveui.go` (`newSnapshotLiveObserver`); it returns `nil` for non-TTY or `--no-live` so the runner's nil-observer path produces plain output. A depth-counted pause/resume helper on the observer bridges `widgets.SetHuhHooks` and `StepIOSuspender` against the same `liveui.LiveLine` so nested suspends from in-step `ConfirmCommand` prompts compose with the step-level suspend window — `live.Pause` / `live.Resume` each fire exactly once per command step.
  - **Archive-safety contract** (`archive/unpack.go`, with the rejection types + size caps in `archive/archive.go`):
    1. Path validation runs pre-checks on the raw `tar.Header.Name` (`filepath.IsAbs`, empty / `.` / `..` rejection, `filepath.IsLocal` Go 1.20+), then joins against the staging root, then `pathsafe.ContainedRel` as belt-and-braces. **Never** rely on `filepath.Join` + `ContainedRel` alone — `Join` normalises `..` away before `ContainedRel` ever sees the original header.
    2. Type allowlist: `tar.TypeReg` and `tar.TypeDir`. Go 1.11+ normalises `TypeRegA` ('\x00') to `TypeReg` in `tar.Reader.Next()`, so the allowlist does not need a separate case for it. `TypeSymlink` / `TypeLink` / `TypeChar` / `TypeBlock` / `TypeFifo` / `TypeXGlobalHeader` (and any other typeflag) are rejected with an error naming the entry.
    3. Resource caps: package-level constants `maxUnpackBytes = 50 << 30` (50 GiB) and `maxUnpackFiles = 100_000`; enforced via `io.LimitReader` wrapping the decompressor. Constants, not config — promote to config later if needed.
    4. No follow on existing files: `O_CREATE|O_WRONLY|O_EXCL` refuses to overwrite anything inside the staging tree.
    5. Atomic install with backup rollback: extract into `./snapshots/.unpack-<random>/` (suffix from `mkdirRandom` via `crypto/rand` — blocks TOCTOU symlink pre-creation). If the final `<name>/` already exists, rename it aside as `.backup-<random>/`, rename staging into place, then remove the backup; on second-rename failure restore the backup so the final directory is never left half-installed. Any error during extraction triggers `os.RemoveAll` of the staging dir.
    6. Manifest-driven artifact verification (`VerifyExtractedArtifacts`) runs **after** extraction and **before** the rename-into-place step. Each `manifest.yml` artifact path is validated against the staging root with `filepath.IsLocal` + `pathsafe.ContainedRel` **before** `os.Open` — a maliciously crafted manifest cannot make the verifier read sibling-snapshot or system files; path-safety failure is a hard error, not a warn. Per-artifact sha256 is recomputed via `io.Copy(sha256.New(), f)` (never `io.ReadAll`). Three diagnostic groups (`Missing` / `HashMismatch` / `Extra`) are written to `cmd.ErrOrStderr()` (never `os.Stderr` directly); `Missing` and `HashMismatch` share a single grouped `continue? [y/N]` prompt — declined → cleanup + `UnpackVerifyDeclinedError`. `--no-verify` bypasses verification but always prints the bypass warning to stderr for CI visibility. The CLI summary line is driven by the typed `UnpackResult.Verification` (`VerificationSkipped` / `Clean` / `Warned`) — stderr is never parsed to reconstruct it. Pack writes only `<name>.tar.gz` — no `.sha256` sidecar.
  - **Lock acquisition** — every mutating subcommand (`create`, `restore`, `rollback`, `remove`, `pack`, `unpack`) wraps its `RunE` body with the CLI lock helper `cmdctx.AcquireProjectLocksOrReport(baseDir, w)` + deferred release — **never** `lock.AcquireProjectLocks` directly. The helper wraps `lock.AcquireProjectLocks`, prints `*lock.ProjectLockHeldError` via the writer (exit code 2 preserved, error returned unchanged), and wraps any other error as `acquiring project locks: %w`. **`pack` is included**: without the lock, packing a snapshot mid-`remove` produces a corrupt or truncated archive.
  - **Synthetic command ID** — internally constructed `CommandDef` uses `ID: "<internal>.snapshot.<scope>"`; suppressed from notifications via `SkipNotify: true` at sub-step level (`runners/workflow/step.go`, in `runCommandStep`'s `subCtx` literal). Snapshot subcommands emit their own notifications at the command boundary using `notify.OpCommand` with operation labels `"snapshot:create"` / `"snapshot:restore"` / `"snapshot:rollback"`.
- `internal/core/workflow/setup/` — interactive setup wizard for fresh projects (`workspace/setup.yml` schema + executor). **Model**: `SetupConfig{Questions []Question}`, `Question{ID, Type, Title, Description string; Required bool; Writes string; Options []Option; Validate *ValidateSpec}`, `Option{Value, Label string}`, `ValidateSpec{Preset, Regex string}`. **Loader**: `LoadSetupYAML(path)` (strict-decode, missing file → `(nil, nil)`, parse errors wrapped); semantics-permissive (unknown `Type` / duplicate IDs / invalid `writes:` caught by validators, not loader). **Merge logic**: `BuildOverlay(questions, answers) map[string]any` (dot-path → nested map), `BuildPortOverlay(overrides) map[string]any` (port key → `services.<name>.ports.<port_name>`). The wizard now applies these overlays via the comment-preserving `local.ApplyOverlayToNode` + `local.WriteLocalYAMLNode` path (sequentially, last wins) — the old map-based `MergeIntoLocal` was removed; the node-aware mergeable guard in `local/local_node.go` reproduces `validateMergeable`'s map-over-scalar rejection AND the single legacy bare-int-port-leaf → `{port:N}` exception. **Validation**: `ValidateAndCoerce(q, raw) (any, error)` — processes `input`-type answers through preset validators (`port` → `int`; `hostname`, `path`, `non-empty` → `string`) or regex checks, returning typed values; other question types bypass coercion (huh already produces typed output). **Wizard executor**: `Run(ctx, WizardDeps)` orchestrates port-conflict prompts + setup questions (answers → overlay → merge → atomic write); huh-bound asker functions (`AskQuestions`, `AskPortOverrides`) are dependency-injected for testability. **Huh bindings**: `NewHuhAsker(out)` returns the production asker functions; `coerceInputAnswers`, `coercePortOverrides` are pure helpers extracted for unit testability. Sentinel error `ErrWizardCanceled` indicates user abort (returns without writing). Belt-and-suspenders type/semantic enforcement at wizard runtime: type assertions per question type, required-answer checks (zero-value rejection), re-validation of `input` presets against answer map values, `BuildPortOverlay` range check (1..65535).

## Core — User Commands (`internal/core/usercommands/`)

- `internal/core/usercommands/` — declarative command system facade (re-exports subpackages):
  - `model/` — pure types: `CommandDef`, `CommandFile`, `ParamDef` (carries `Widget` enum and `Options` field), `ContextDef`, `FileSpec`, `WorkflowStep` (with optional `Name` for pipeline-side `sub_step_overrides` targeting; `StepName()` returns `Name` when set else `Command`) + `WorkflowParallel`, enums. `ParamWidget` enum: `WidgetInput` / `WidgetSelect` / `WidgetMultiselect` / `WidgetConfirm`; zero value inferred from `Type` and presence of `Options`. `ParamOptions` (union: static `[]OptionItem` or dynamic `From` dot-path; unmarshals from YAML scalars `${...}` or sequences, rejecting mixed types); `OptionItem{Value, Label, Description}`. `ParamDef.EffectiveWidget()` helper returns the defaulted widget per the type-driven inference. `CommandType` payload field is `Cmd` (legacy `Run` / `type: command` are strict-decode errors). **`type: daemon`**: declarative sugar for long-running named containers — one block declares `service` / `argv` / `params` plus a `daemon:` sub-block (`container_template`, `on_already_running` ∈ `{error, noop}`, `auto_remove`, `stop_timeout`); the source command is consumed by the registry expander and **dropped** from `byID`, replaced by four virtual `CommandDef`s `<base>.start` / `.logs` / `.stop` / `.restart`. `.start/.logs/.stop` are `type: builtin` targeting the daemon-runtime builtins; `.restart` is `type: workflow` composing `.stop` + `.start`. Each synthetic carries `DerivedFromDaemon = <base>` plus `SourceDaemon *DaemonSpec` (yaml:"-", expansion-time metadata for inspect — never validated, never loaded from YAML). Sentinel errors `ErrDaemonServiceRequired` / `ErrDaemonServiceNotLiteral` / `ErrDaemonContainerTemplateRequired` / `ErrDaemonStopTimeoutInvalid` are joined via `errors.Join` by model validation and matched per-field by `validate/commands/daemon.go` to suppress double-emission. Container naming + standard labels (`dwe.project` / `dwe.daemon.id` / `dwe.daemon.params`) live in `internal/shared/daemon/` (`ResolveContainerName`, `StandardLabels`, `FilterArgsByLabels`); the regex `^[a-zA-Z0-9_][a-zA-Z0-9_.-]*$` is the authoritative runtime gate on argv → `docker --name`. **`service:` is literal-only in v1** — no `${...}` / `{{...}}` substitution allowed (label stability).
  - `loader/` — YAML discovery and parsing: `DiscoverCommandFiles`, `LoadCommandFile` (parse + validate; runtime callers), `ParseCommandFile` (parse only; validators only — never use at runtime, it skips semantic validation), `ComputeGroup`, `ComputeCommandID`.
  - `registry/` — `Registry`, `LoadRegistry`, `GroupNode`, cross-ref validation; diagnostic API (`BuildRegistryFromParsed`, `ValidationIssue`, `Diagnostics`); `WalkWorkflowSteps` recursive walker (path: `step[i]` at root, `step[i].parallel.steps[j]` inside groups). **Bridge visibility (container command surface)**: `ApplyVisibility` additionally runs `applyBridgeVisibility` — outside a bridged invocation it resets every `CommandDef.BridgeHidden` to false (idempotent re-apply); inside one (`bridgeclient.InContainer()`) it DFS-collects the effective `model.BridgeDef` per group node (field-wise `MergeBridge` down the tree — deeper file `group:` blocks override shallower ones), merges each command's own block over its owning group's effective value, and sets `BridgeHidden = !AllowedFromChain(callerExtendsChain(cfg, bridgeclient.CallingService()))` — the caller chain is the calling service plus its service-level `extends:` ancestors (children inherit the parent's command rights; nil cfg or unknown caller degrades to exact-name matching, and a visited set caps malformed cycles). Opt-in default: no block anywhere → hidden in containers. `registry.list` filters `BridgeHidden` UNCONDITIONALLY (unlike `Hidden`, even `ListAllIncludingHidden` excludes it — the inspect escape hatch is a host-side debug aid), while `Get` still resolves it (workflow steps must execute non-bridged sub-commands; the CLI-side `bridgeGuard` rejects direct invocation instead). Daemon expansion copies `src.Bridge` onto every synthetic; `addCommandFile` merges `group.bridge` last-wins-per-field like Title/Description/Hide. `model.BridgeDef` carries exactly two fields: `enabled` (a `*bool`, so "absent → inherit" stays distinguishable from an explicit `false` — that is what makes the group/command field-wise merge meaningful) and an optional `services` list of workspace service keys (empty/absent → reachable from every bridge-enabled service). **Single-block → multi-command sugar** is a registry-level pattern, currently instantiated once by `type: daemon` and intended as the shape any future declarative multi-command type follows: `LoadRegistry` runs the expander at **load** time, the source `CommandDef` is consumed (never inserted into `byID`, so `Get("<base>")` is not-found by design), a synthetic group node for the base ID is `ensureGroup`'d — inheriting the source's `LocalName`/`Description` as `GroupMeta` and its `Hide` expression, so `ApplyVisibility` evaluates the expression once for the group instead of N times and no phantom group header leaks into `dwe commands list --all` — and each synthetic carries a `DerivedFrom*` back-reference plus a `Source*Spec` pointer (`yaml:"-"`) for inspect; a synthetic ID colliding with an explicit command is a hard error naming both. **Expansion is config-blind**: `LoadRegistry(baseDir)` has no `*config.DweConfig`, so nothing config-derived (project full name, dot-path resolutions, compose files, docker binary) may be baked into a synthetic's `with:` — it is resolved at builtin runtime from `ectx.Config` — and anything param-derived is emitted as a `${param.<name>}` template literal that `renderBuiltinWith` resolves at dispatch. Packing resolved `ParamDef` structs into `with:` instead would serialise defaults / patterns / descriptions into the daemon label — wrong shape and lossy on read-back.
  - `resolve/` — `Params`, `ParamDefaults` (raw-string prefill for the parameter form: merges `provided ∪ DefaultFrom ∪ Default`; empty-as-missing parity with `Params`; no coercion, no required-check, no pattern check — those stay in `Params` at run time), `Options` (resolves dynamic `${...}` dot-paths in `ParamDef.Options` to a flat `[]OptionItem` list, normalizing scalar lists, maps, and `[]{value, label}` objects), `Context`, `BuildEnv`.
  - `runtime/` — runners and execution. **Package layout**: a leaf `spec/` subpackage (cycle-breaker — `Runner` interface, `RunContext`, `FileProbeResult`, `StepStatus`/`StepResult`/`WorkflowStepObserver`/`StepIOSuspender`, `ErrConfirmInsideParallel` + `ErrWorkflowNestedParallel` sentinels) + `internal/runio/` (shared I/O helpers — `StdoutOf` / `StderrOf` / `StdinOrOS` / `RenderArgvAppendFrom` / `AppendArgvFrom` / `ParallelChildIO` / `WireChildIO` (the `defer runio.WireChildIO(rc, c)()` child-IO wiring shared by all runner subpackages — PTY in parallel, full PTY on a bridged-interactive run via `bridgedTTYChildIO`, ctx-default stdout/stderr/stdin otherwise) / `BuildRenderedEnv` / `IsNonInteractive` / `BindCancel` / `ColorForceEnv`; placed under `internal/` so only runtime-subpackages can import). **Child color/TTY contract**: `ColorForceEnv(rc)` returns the CLICOLOR_FORCE/FORCE_COLOR/COLORTERM trio when the child writes into a capture (parallel LineTee) OR when dwe itself runs color-forced on a pipe (`ColorForced()` = CLICOLOR_FORCE truthy + NO_COLOR absent — the host-bridge shape; `stdoutIsTerminal` is the test seam); on a real terminal it returns nil (children inherit the tty). `bridgedTTYChildIO` gives a SEQUENTIAL child a full PTY (stdin+stdout+stderr) only when `ColorForced() && !stdoutIsTerminal() && DWE_BRIDGE_STDIN_TTY=1` — the stdin condition is load-bearing: `docker compose exec` refuses a TTY session over a non-terminal stdin, and a PTY wired onto piped stdin data would never deliver EOF to the child. Cleanup must NEVER wait unconditionally on the stdin pump: it reaps the pump via `os.File` read deadlines only when the fd actually supports them — in the daemon-forked shape fd 0 is a plain blocking pipe (`exec.Cmd` StdinPipe, not pollable, `SetReadDeadline` fails), and waiting there deadlocked every bridged command the moment its child exited (frozen output, dead Ctrl+C); on deadline-incapable stdin the cleanup closes the master and abandons the pump goroutine instead (accepted cost: a keystroke arriving in that window feeds the dying pump, not the next step). + five per-runner subpackages under `runners/`: `host/` (`host.Runner`, `host.DWERunner`), `service/` (`service.ExecRunner`, `service.RunRunner`), `script/` (`script.Runner`), `builtin/` (`builtin.Runner` — imports the engine builtin package as alias `engbuiltin` to avoid same-package collision), `workflow/` (`workflow.Runner` plus step / parallel / log / helpers / observer-fire files; PTY allocation for parallel child IO lives in `internal/runio.ParallelChildIO`, not in `workflow/`). Root `runtime/` holds the `NewRunner` factory (switch on `Cmd.Type` dispatching to `&host.Runner{}` / `&host.DWERunner{}` / `&service.ExecRunner{}` / `&service.RunRunner{}` / `&script.Runner{}` / `&workflow.Runner{}` / `&builtin.Runner{}`), `BuildRunContext` / `BuildSnapshotRunContext`, `ComputeFilePathsProbe` + `ComputeFilePaths` + `PrepareFileEffects` (split between `files_probe.go` and `files_resolve.go`), `ConfirmCommand`, `RunCommand`, notify helpers, and a comprehensive set of type aliases that re-export every spec contract type (`type Runner = spec.Runner`, `type RunContext = spec.RunContext`, etc.) plus every concrete runner type (`type HostRunner = host.Runner`, `type WorkflowRunner = workflow.Runner`, etc.) so external callers and the `internal/core/usercommands/usercommands.go` re-export chain compile unchanged. `runners/workflow/` aliases the filesgate `spec` package as `fgspec` to avoid collision with the runtime `spec`. **Cycle-break for `workflow → RunCommand`**: the workflow runner needs `runtime.RunCommand`, `runtime.BuildRunContext`, and `runtime.ComputeFilePathsProbe`, but importing the root package from a subpackage would form a cycle (root imports `runners/workflow` for the factory). Resolution: `runners/workflow/workflow.go` declares three function-variable seams (`RunCommandFn`, `BuildRunContextFn`, `ComputeFilePathsProbeFn`) wired by root `runtime`'s `init()` — set once at process start, called from workflow internals. A workflow-package external test file (`wire_test.go`, `package workflow_test`) blank-imports root runtime so the workflow test binary picks up the same wiring without forming a build cycle. Script env contract: `DWE_BIN`, `DWE_FILES_JSON`, `DWE_NONINTERACTIVE`. **Shell env contract** (`hostContractEnv` in `runners/host/host.go`): `DWE_BIN` + `COMPOSE_PROJECT_NAME` + `COMPOSE_FILE` (colon-joined, absolutised against `ProjectRoot`) — appended after `env:` so contract wins on duplicate keys. **Service-runner template fields**: `resolveServiceFields` renders `service`, `workdir`, `workdir_from` (and their `runner:` overrides) through `tpl.RenderCommand` before resolution — same contract as `argv`/`cmd`/`compose_args`. This unblocks `service: app-${param.service}` + `workdir_from: services.${param.service}.work_dir_internal` generic commands. `BuildRunContext` + `ComputeFilePathsProbe` are reused by the executor's files_gate evaluation. **`BuildPreRenderedRunContext`** is the pipeline-only sibling of `BuildRunContext`: identical except that `with:` values are stringified but NOT run through `tpl.RenderCommand`. The pipeline renders every step's `cmd`/`with`/`check`/`files_gate` leaves once at resolve time (`pipeline.renderStepFields`), and that render is **not idempotent** — a second pass would parse a value that resolved to text containing a literal `{{` as a Go template and fail the step, and would expand a value that resolved to another `${vars.*}` reference twice. Both pipeline consumers of a resolved `with:` (`execCommandAction`, the `evalFilesGate` probe) take this entry point; every non-pipeline caller (`dwe cmd`, workflow sub-steps via `BuildRunContextFn`, `validate/checks`) keeps `BuildRunContext` because their `with:` is still raw. **`RunContext.UnderParallel`** is set on every sub-step of a parallel group (pipeline or workflow); guards reject nested parallel (`ErrWorkflowNestedParallel`) and transitive interactive confirmations (`ErrConfirmInsideParallel`). **`RunContext.WorkflowSubStepOverrides`** carries pipeline-side per-sub-step `files_gate` directives forwarded from `DeployStep.SubStepOverrides` via `ActionContext.SubStepOverrides`; the workflow runner applies the gate before each matching sub-step in both sequential and parallel paths via `evalSubStepOverrideGate`. Overrides are intentionally not propagated to inner workflows. **`WorkflowStepObserver`** (2 methods: `OnStepStart` / `OnStepEnd(idx, step, StepResult{Status, Duration, Err, SkipReason})`) is an optional opt-in event sink for top-level sequential workflow steps; set via `RunContext.StepObserver` (nil → current plain behavior unchanged). Parallel groups are reported as a single step at the observer surface — the inner per-sub-step rendering keeps its existing `liveui.LiveLine` block path. Every early-`continue` branch (`when:` false, `files_gate` skip, `continue_on_error` failure) emits `OnStepEnd` before continuing so live rows can finalize. Companion optional capability **`StepIOSuspender`** (`SuspendForExec` / `ResumeAfterExec`, detected via comma-ok type assertion) is invoked iteration-scoped around every sequential command step so a surrounding live footer can pause for the child's stdout without stacking across iterations. The workflow's per-block `liveui.NewLiveLine` does NOT call `widgets.SetHuhHooks` — interactive prompts are forbidden inside parallel, and overwriting hooks set by a surrounding `PlainReporter` would corrupt its pause/resume dance. Parallel block rendering: the per-sub-step `lineTee` callback refreshes the live row on **every** frame (final and non-final) so `\r`-progress bars are visible, but only commits newline-terminated frames to the per-sub-step buffer and log; the post-Wait emit suppresses per-sub-step status lines in TTY mode (already shown in the frozen block rows) and prints a single `writeParallelSummary` footer (`✓ [<elapsed>] parallel: <workflow-id>`), while non-TTY mode keeps the per-sub-step status lines plus a plain-text footer. Failure-output dumps print in BOTH modes. **`argv_append_from` (`internal/runio/args.go`)**: `AppendArgvFrom(ctx, rc, argv)` runs the command's host shell expression via `config.ShellBin` with `cmd.Dir = rc.ProjectRoot` (NOT `cmd.Workdir` — for a `service_exec` command that names a path inside the container; the project root is the rule `condition.EvalCmd` already follows), captures stdout only (stderr streams to the user, stdin stays unwired), and appends ONE argv element per output line **byte-for-byte** — never re-parsed as shell, trailing newline ignored, blank lines dropped (a stray `""` silently changes what a tool does). Appended items come AFTER the declared `argv` with `${args}` already spliced in place. Rendering goes through the exported `RenderArgvAppendFrom` wrapping the UNEXPORTED `withoutArgs(rc)` — keep `withoutArgs` unexported: the point is one safe entry point both `runners/service` and `runners/host` share, not a second primitive each can wire up its own way (`${args}` is rewritten to `"$@"` and passed as positional parameters everywhere else, so leaving `Args` visible in this one field would make it interpolate caller bytes into program text; a literal `${args}` is additionally rejected at load time). Empty output returns **`spec.ErrArgvAppendEmpty`**, whose full skip contract is written on the sentinel and translated in exactly one place — `runtime.RunCommand`: stderr note + nil error (exit 0), `messages.success` suppressed, desktop notification suppressed (the declined-confirmation precedent), file effects rolled back as on the error path. A pipeline `type: command` step therefore **Finishes** rather than Skipping and journals as success — such a step needs a `files_gate`/`check:`. Load-time placement rules (argv-only, `type: daemon` rejected) live in `model/types.go` `validateArgvAppendFrom` + the per-type `allowedFieldsFor` allowlist. The `BuildPreRenderedRunContext` hazard is that `BuildRunContext`'s per-value `tpl.RenderCommand` loop is **ungated** — it renders every `with:` value unconditionally, unlike the pipeline's `hasKnownVarRef`-gated pass — so the literal-`{{` failure is not hypothetical: a var holding a `docker inspect -f "{{.State.Status}}"` format string resolves to text the second pass then parses as a Go template. The `${args}` invisibility in `argv_append_from` is a **consistency rule, not a security boundary** *for that field*, and the plan that introduced it says so outright (`docs/plans/completed/20260804-pipeline-primitives.md` constraint 3a): dwe runs on the developer's own machine and a workspace author who can write `argv_append_from` can already run anything via `type: shell`, so there is no boundary left to defend at that level. Its output becomes argv elements, never program text, and the value is never sanitized or quoted on this basis; the rule exists so no single field becomes the one that interpolates caller arguments into program text while every other field does not. The **shared** `withoutArgs` helper is a different matter and must stay unexported: in `RenderShellCommand` it is what stops the raw Go-template form `{{ index .Args 0 }}` from bypassing the `"$@"` positional transport, which `args.go`'s own comment calls "the whole security property" and records as verified exploitable before the change (this is also why `RenderArgvAppendFrom` is th and `RenderArgvAppendFrom` is the only entry point; it is belt-and-braces to the load-time literal-`${args}` rejection, since it also covers the raw Go-template form `{{ index .Args 0 }}` that a textual check cannot see). The two placement rules whose reasons are non-obvious: `cmd:` is rejected because appending to a shell string would splice the computed values back into program text — exactly the surface the `${args}` → `"$@"` transport exists to avoid — while `type: daemon` is rejected *even though it accepts argv*, because the expansion packs that argv into the synthetic `.start` command, so the documented "empty output → skip" semantics would read as "silently fail to start the daemon". `validateArgvAppendFrom` additionally requires a non-empty `argv:` that declares the program: an `argv:` of nothing but `${args}` splices to an EMPTY vector when the command runs without pass-through arguments, which would make the first computed line `argv[0]` and execute it relative to the project root. The sentinel contract also leaves `--output json` unaffected — command execution streams the child's output and has no JSON envelope to carry a skip state.

## Core — Validation (`internal/core/validate/`)

- `internal/core/validate/` — static project validation framework: `Validator` interface, `GroupValidator` interface (a `Validator` that owns child validators sharing its `Domain()`; `Children() []Validator` + `RunGroup(ctx, children) []Diagnostic` — `Registry.Run` expands children during scope matching so `[<domain>]` and `[<domain>, <child-id>]` both work, then delegates execution to `RunGroup` so the group can choose its own scheduling. Used by `validate/linters/` and `validate/i18n/` for parallel fan-out or grouped validation; the group's `Run` is unused), `Diagnostic` (severity / domain / target / file / line / message / hint), `Context` (carries `CommandRegistry any` — nil-tolerant: validators self-skip gate checks; also carries `ValidateCfg *config.ValidateConfig` + `ValidateCfgWarnings []Diagnostic` + `ValidateCfgLoadErr error` populated by the caller's single `LoadValidateConfig` parse so validators never re-parse the file), `Registry` (scope-dispatch), `Summary`, `ExitCode` (severity-gated). `Severity` and `Diagnostic` are type aliases for the leaf `internal/core/validate/diag` package (avoids an import cycle with `internal/core/project/config`, which references `diag.Severity` from `CheckEntry`). Subpackages: `config/` (per-file YAML validators + `deploy_files_gate` aggregator + `parallel_groups` walker matching the pipeline sentinels via `errors.Is`; `config.validate` validator reads `ValidateCfgLoadErr` / `ValidateCfgWarnings` from `Context` and is the SOLE emitter of validate.yml load-error diagnostics — `os.ErrNotExist` is silent, every other load error becomes one error diagnostic; the `generated` validator (`generated.go`) checks each `generated.<field>` — `pattern` required and compiles as a regex with **≥1 capture group** else `SeverityError`, `file` is a pathsafe contained-relative path, field name a valid `${generated.<name>}` identifier — plus `render.config.template` pin resolution (warn if a pinned pack does not resolve) and a cross-check that `generated-missing <svc> <field>` predicates reference a declared `generated:` field via the shared `condition.ParseGeneratedMissing` parser; the `deprecations` validator (`deprecations.go`) emits `SeverityWarning` with `File:Line` (via a yaml-node walk) for `configs:`/`mountpoint` in `service.yml` and `service_configs_copy`/`service_configs_check` in `deploy.yml`/`reset.yml` — warnings do not change exit code outside `--strict`), `templates/` (per-kind manifest + override validators; explicit `enabled: false` opt-out is silent — only policy and actionable skips emit info), `commands/` (uses `loader.ParseCommandFile` for categorised diagnostics), `env/` and `checks/` (see below), `i18n/` (see below). **Domain assembly convention**: every domain exposes an `All(...)`-shaped constructor returning `[]Validator` (`config.All()`, `env.All(cfg)`, `checks.AllForStage(s)`, `commands`/`templates` loops, `setup.All(...)`, `linters.All(...)`, `i18n.All(...)`, `snapshot.All(...)`, `bridge.All()`, `tests.All()`), and `internal/cli/validate/validate.go::buildRegistry` is the single registration site — adding a domain is one `All()` plus one `Register` loop, never a change to `Registry`. **Validators are per-file and mutually independent by design**: `config/` alone registers one validator per config file plus the cross-file aggregators, `Registry.Run` invokes each matching `Run` with no shared error path or early return, and `dwe validate` deliberately proceeds on a failed merged-config load (`loadForValidate` returns the `errPartialLoad` sentinel with a nil `*DweConfig`; domain validators nil-guard `ctx.Cfg`) — so one unparseable file yields its own diagnostic instead of blinding the run to every other file's. **Preflight participation is narrow and opt-in**: `preflight.Run` assembles `env.All` + `checks.AllForStages` + the single `config.validate` validator and nothing else; every other domain (`config/`'s remaining validators, `commands/`, `templates/`, `setup/`, `linters/`, `i18n/`, `snapshot/`, `bridge/`, `tests/`) is `dwe validate`-only, because preflight answers "can we run?" and a content mistake in an unrelated domain must never block a lifecycle command. Registering a new domain in `preflight.Run` is therefore a deliberate decision, not the default. The `config/` subpackage also owns `services_folder.go` — validator ID `services-folder`, the per-service-folder shape check that pins the folder symmetry: a folder without `service.yml` is a `SeverityError` (`service folder %q has no service.yml`, hint `every service folder must contain a service.yml file`) and suppresses the folder-level `SeverityOK` diagnostic, while any regular file outside the `knownServiceFiles` allowlist (`service.yml`, `deploy.yml`, `reset.yml`) is a `SeverityWarning` naming the expected set; subdirectories and a missing `workspace/services/` directory are ignored. **Adding a new per-service file type therefore requires adding it to `knownServiceFiles`** — otherwise every project that adopts the new file gets an `unknown file in service folder` warning from `dwe validate`. And `template_refs.go` owns the `config.template_refs` check, the `dwe validate` complement to `tpl`'s known-head whitelist: a `${head.path}` whose head IS a known root key but whose remaining path does not resolve in `cfg.Raw` is a `SeverityWarning` — almost always a `vars:` typo, which otherwise renders to a literal `${vars.opechatka}` with no error until the step runs. It enumerates references with `varsusage.EnumerateAllUsages(ctx.ProjectRoot)`, drops non-references via `tpl.IsVarNamespaceRef` (a head-only `${state}` / `${vars}` is a shell variable, not a reference), and gates on `config.IsAllowedRootKey` — which deliberately excludes `param` / `context` / `files` / `host` / `snapshot` / `args` (never in `Raw`) and `generated` (lenient-by-contract and empty on first deploy), so the validator does not become its own noise source. The gate is the root-key ALLOWLIST, not a "head present in `cfg.Raw`" probe: the strict root makes `Raw`'s keys a subset of `allowedRootKeys`, so a presence probe would go silent on the case that matters most — a project with no `vars:` block at all, where every `${vars.*}` reference is unresolvable.
- `internal/core/validate/env/` — seven hardcoded environment probes: `env.docker_bin`, `env.docker_daemon`, `env.docker_compose` (accepts any plugin version — the `docker compose` subcommand resolving at all is itself proof of the plugin; legacy v1 was a separate `docker-compose` binary), `env.git_bin`, `env.shell_bin`, `env.project_perms` (verifies `.dwe/` is writable via create-temp-file), `env.ports_free` (collects host ports from `cfg.Services[*].Ports` for enabled services; queries `docker ps --format=json` once and classifies each declared port: containers labelled `com.docker.compose.project=<our project>` are tolerated, foreign-compose / unlabelled docker containers trigger a conflict naming the foreign container, ports outside Docker fall back to `net.Listen` to detect non-Docker processes). Probes are config-blind beyond the nil-safe binary accessors `config.DockerBin` / `config.GitBin` / `config.ShellBin` (a nil cfg yields the defaults). Docker daemon / compose / ports probes use a 5s timeout and self-suppress when the docker binary is missing (avoid double-reporting alongside `env.docker_bin`). `env.ports_free` also self-skips on `Context.Stage == "stop"` (port conflicts irrelevant when stopping); load failures for `docker.yml` (no compose project name available) fall through to a docker-blind run that still catches conflicts but cannot distinguish "ours" from foreign. `All(cfg)` returns the seven probes in fixed order. Hints follow the project's concise format; long remediation text uses `\n` splits. **Test seams**: `dockerPSOutFn` (canned `docker ps` NDJSON) and `portListenFn` (simulated busy/free port) live as package-level vars in `ports.go` for swap-in-tests.
- `internal/core/validate/checks/` — declarative project checks from `workspace/validate.yml`. `AllForStage(cfg, loadErr, baseDir, cmdRegistry, stage, services, skipServicesGate)` and its multi-stage form `AllForStages(... stages ...)` produce one synthetic `Validator` per `CheckEntry` (Domain=`checks`, ID=entry.ID). Both signatures are nil-tolerant — a nil `*config.ValidateConfig` yields an empty slice; a nil `*registry.Registry` yields unknown-command diagnostics for any `type: command` entry. Both return `[]Validator` only — never an `error`. Load-time problems (unknown builtin, unknown user command, `with:` validation failure against `builtin.Validate`, target user command type outside the `{shell, script}` whitelist) become a pre-baked failing validator that emits a single cached `Diagnostic` from `Run`. Runtime `type: command` dispatch goes through `runtime.BuildRunContext(cfg, cmdRegistry, def, entry.With, projectRoot)` (the `with:` block IS the user-command params payload) and is locked down: `SkipConfirm=true`, `NonInteractive=true`, `SkipNotify=true`, `Stdout=io.Discard`, `Stderr=&bytes.Buffer{}` (captured for inclusion in the failure diagnostic), `Stdin=nil`. `MatchStage(entry, stage)` and `MatchAnyStage(entry, stages)` are the stage-filter primitives (empty stage / empty slice = match all). `AllForStage` delegates to the multi-stage `AllForStages`, which `preflight.Run` calls via `stagesForPreflight(stage)` — passing `{deploy, post-setup}` for the deploy final preflight so `post-setup` checks (skipped at the interactive pre-wizard gate, which queries only `deploy`) run there. Stage is metadata on `CheckEntry`, not on `Validator` — `validate.Registry.Run` only filters via `MatchScope` (domain / domain+id), so stage filtering happens at *assembly time* one level up. `MatchServices(entry, cfg.Services)` is the second assembly-time filter: a check with `services: [a, b]` only assembles when at least one of `a` / `b` is enabled in the merged config (OR semantics; empty/missing list = run unconditionally). `AllForStage` takes a `skipServicesGate bool` to bypass this filter when the caller is `dwe validate checks <id>` (explicit-by-ID escape hatch — currently used by `internal/cli/validate/` when `scope = ["checks", "<id>"]`). Unknown service names are NOT detected here — the loader has no view of `cfg.Services`. Detection lives in the `config.validate` validator (`internal/core/validate/config/validate_yml.go::validateUnknownServiceRefs`) so typos surface as load-time error diagnostics in the single `dwe validate` pass. User-visible schema: `docs/reference/config/validate.md`.
- `internal/core/validate/setup/` — setup wizard validators from `workspace/setup.yml`. `All(setupCfg, setupErr, setupPath)` (function-arg form, no `validate.Context` extension to avoid import cycles) produces one `setup.parse` validator + per-question validators. Assembly mirrors the snapshot-domain pattern: the SOLE emitter of `setup.parse` load-error diagnostics, called once by both `runValidate` and the menu RunE. Validators include `setup.type_known` / `setup.id_required` / `setup.writes_required` / `setup.id_unique` / `setup.writes_unique` / `setup.writes_syntax` / `setup.writes_scope` (three enforcement layers: forbidden top-level namespaces, service-overlay leaf shapes, non-service paths) / `setup.options_valid` / `setup.validate_exclusive` / `setup.validate_only_on_input` / `setup.validate_preset_known` / `setup.validate_regex_compiles` / `setup.type_writes_consistent` / `setup.required_consistent`. Reuses `config.ValidIdentifierKey` for identifier validation so rules can't drift. Reuses `config.overlayAllowedKeys` for service-overlay scope. User-visible schema: `docs/reference/config/setup.md`.
- `internal/core/validate/linters/` — external linter domain (`linters.*`). Built-in adapters (`shellcheck`, `hadolint`) plus a `type: generic` exec-and-wrap adapter; `Adapter` interface (`DefaultBin` / `DefaultPaths` / `DefaultExtensions` / `DefaultFilenames` / `ReservedFlags` / `BuildArgs` / `ParseOutput`); `walk.collectFiles(baseDir, paths, exts, filenames, pathsAreDefaults)` (recursive `filepath.WalkDir` rooted under `baseDir`; symlinks and `.git/` always skipped; `pathsafe.ContainedRel` containment with a short-circuit for `paths: ["."]` since `ContainedRel` rejects `.` by design; missing **default** paths are silently dropped, missing **user-configured** paths surface a Warning); `runtime.linterValidator` (per-linter `validate.Validator` — autodetect via `exec.LookPath`, default-bin-missing silent / explicit-bin-missing Warning, `exec.CommandContext` with `context.WithTimeout(parent, DefaultLinterTimeout)` where parent falls back to `context.Background()` when `ctx.Ctx` is nil, output bounded by `MaxLinterOutputBytes` via `boundedWriter`; two diagnostic buckets — `operationalDiags` never clamped, `findings` clamped from above by `entry.Severity` if set — so `severity: info` cannot silence a timeout Error or panic); `lintersGroup` implementing `GroupValidator` (`sync.WaitGroup` + buffered-channel semaphore — **NOT** `errgroup.WithContext` because one linter's failure must not cancel siblings; per-goroutine `recover()` converts panics to Error; concurrency capped at `MaxLinterConcurrency = runtime.NumCPU()`; pre-allocated `results[i]` slot per child — no shared slice, no mutex); `All(validateCfg, validateLoadErr, baseDir, userCfg)` (corrupt-config short-circuit returns zero validators; `validateCfg == nil` falls through to per-adapter autodetect; reserved-flag rejection via `validateUserFlags(adapter, entry.Flags)` produces a synthetic error validator instead of registering the linter, so a bad config short-circuits before any subprocess runs). Package-level vars `DefaultLinterTimeout` / `MaxLinterOutputBytes` / `MaxLinterConcurrency` are exported for test override (test helpers swap + `t.Cleanup`-restore). Built-in adapters lock output format via `ReservedFlags` (parser-load-bearing) — reservation match policy covers exact, `--long=value`, `["--long", "value"]`, and **short-attached `-fgcc`** forms. Real-binary smoke tests for shellcheck/hadolint are gated by `exec.LookPath` + `testing.Short()`. User-visible schema: `docs/reference/config/validate.md`.
- `internal/core/validate/i18n/` — translation-file domain (`i18n.*`). Validates project-layer translation files (`workspace/i18n/*.yml`) via three validators: `parse_error` (strict-decode failures and directory-level read errors), `orphan` (warnings for `commands.<id>` / `groups.<id>` entries whose ID is not in the user-command registry; per-locale disambiguation via `ID = pf.Locale+"/"+id`), `unknown_ui_key` (warnings for `ui.*` keys not in the whitelist maintained by `internal/shared/i18n.KnownUIKeys`). `All(projectFiles []i18n.ProjectFile, reg *registry.Registry) []validate.Validator` aggregates the three; per-file read/parse errors surface via the `parse_error` validator (ID `_load_error`), and the CLI call site discards the loader error. Loader: `i18n.LoadProjectBundles(projectRoot)` returns per-file parse metadata for the validator; silent on absent directory, non-fatal on per-file errors. Does NOT run in preflight (preflight answers "can we run?", not "is the content clean?"); only in `dwe validate`. User-visible schema: `docs/reference/config/i18n.md`.
- `internal/core/validate/snapshot/` — static validators for `workspace/snapshot.yml` and on-disk snapshot directories. `All(cfg, snapCfg, snapCfgErr, baseDir, reg, verifyChecksums) []validate.Validator` is the single assembly seam, registered from `internal/cli/validate/validate.go` (`buildRegistry`) alongside `env.All` and `checks.AllForStage`. Validators emit `snapshot.*` diagnostics: `config_loadable` (silent on `os.ErrNotExist`), `create_defined` / `restore_defined` (info), `variant_pairing` / `rollback_target_exists` (warn), `<name>.manifest_valid` / `<name>.artifacts_exist` (error), `<name>.checksums` (warn, runs only with `--verify`), `<name>.last_create_failed` (info), and `template_scope` (error — catches `${snapshot.created_at}` in `create:` blocks earlier than the runtime compile check). `dwe validate snapshot [<name>] [--verify]` is the dedicated subcommand (mirrors `validate env` / `validate checks`); `<name>` scoping filters via `MatchScope` to a single snapshot.
- `internal/core/validate/bridge/` — host-bridge domain (`bridge.*`): a single `servicesValidator` (ID `services`) over every service's `bridge:` block. Errors: unknown `on_unreachable` value (closed enum `fail`|`warn` — an unknown value is inert at runtime, behaving as `fail`, which is exactly how a typo silently flips the shim's exit-code policy, so it fires **regardless of the enabled toggle**) and relative `shim_path` (container path → `path.IsAbs`, NOT `filepath.IsAbs` — a Windows host must not reject `/usr/local/bin/dwe`). Warnings (only when bridge is effectively enabled): no container target (unreachable on loaded configs — `container:` defaults to the folder name — kept for literal-built configs, mirroring the `BuildOverlaySpec` skip), and the reachable hazard: no `dir`/`dir_internal` workspace mapping → the overlay omits `DWE_HOST_WORKSPACE`/`DWE_CONTAINER_WORKSPACE`, shim cwd translation is unavailable, and the daemon containment check rejects every in-container invocation (type-aware hint: apps declare the pair; tool/infra cannot declare `dir` under strict decode → opt out or mount at an identical path). Registered in `internal/cli/validate/validate.go` via `valbridge.All()` alongside the other domain loops, with a `dwe validate bridge` scoped subcommand. Participates in `dwe validate` ONLY — never preflight (preflight consumes only `env.All`, `checks.AllForStages` and the single cherry-picked `config.validate` validator; a bridge config mistake must not block unrelated lifecycle).
- `internal/core/validate/tests/` (integration-tests stage 3a) — `workspace/tests/` scenario domain (`tests.*`). `All()` → a single `scenariosValidator` (`Domain()=="tests"`, `ID()=="scenarios"`), guarded on `ctx.Cfg != nil` throughout (mirrors the nil-cfg guard other domains use for the `errPartialLoad` path); resolves `workspace/tests/` and iterates `*.yml` in filename order. Per file: loads via `envtest.LoadScenario` (a load error is one `SeverityError` diagnostic and — for free — covers **name normalisation**, since `LoadScenario` already runs `ValidateScenarioName`), parses `Timeout` via `time.ParseDuration`, and checks every `env.services` enable/disable entry against `ctx.Cfg.Services`. Steps are validated as **one whole phase, exactly like runtime**: all steps' `with:`/`cmd:` are rendered, wrapped in a single synthetic `config.DeployPhase{Name: "tests"}`, and resolved via **one** `pipeline.ResolvePhaseSteps` call — matching `envtest`'s own `runSteps`, so it catches whole-phase invariants (notably `checkUniqueStepNames`) that a per-step validation pass would miss. The render context is `ctx.Cfg.Raw` overlaid with the scenario's own `env.vars`, with any `env.vars` entry whose value is the literal `auto` substituted by a fixed placeholder host port — `auto` is the only magic value in the schema (ports only), so a `${vars.db.port}` param renders to a valid int and passes strict-typed builtin validation, while a genuinely bad **concrete** param elsewhere still fails as `SeverityError` — deliberately NOT a blanket `${...}`-in-raw downgrade heuristic (that would mask concrete errors alongside templated ones). Documented residual limitation: a var populated only post-deploy (e.g. `${generated.*}`) is absent at validate time and can yield a spurious diagnostic unless given a project-level default. `type: command` step refs resolve via `reg, ok := ctx.CommandRegistry.(*registry.Registry)` (re-implemented locally, 3 lines — `registryFrom` is unexported in `internal/core/validate/config`, not shared); nil/absent registry skips the command-ref check entirely. Emits `config.ScanComposeIsolation(ctx.Cfg, ctx.ProjectRoot)` findings once per file-set (outside the per-file loop), always as `SeverityWarning` — the runner, not this validator, is the tier that treats a `Blocking` finding as fatal. **Never registered in `preflight.Run`** — validate-only by design, so a scenario file typo never blocks an unrelated `dwe deploy run`. `dwe validate tests` is the dedicated CLI scope (registered in `buildRegistry` alongside the other domains).

## Core — Docs (`internal/core/docs/`)

- `internal/core/docs/` — embedded documentation browser: `embed.go` exposes `BuiltinFS fs.FS` via `//go:embed embedded` (synced tree committed to `internal/core/docs/embedded/`, mirrored from `docs/reference`, `docs/internals`, `docs/i18n` plus the repo-root `README.md` copied flat to `embedded/README.md` at build time); `source.go` defines `DocRoot{Name, FS, ProjectPath}` and `Sources(projectRoot)` returning built-in + optional project roots; `tree.go` walks the trees to build a file-node hierarchy; `topic.go` implements topic parsing (`ParseTopic` splits `path#anchor`) and resolution (exact match → fuzzy substring match across roots); `lang.go` implements per-file language fallback with content-hash staleness detection; `content_hashes_gen.go` is a committed generated file (regenerated by `make build`) containing `var ContentHashes map[string]string` — the manifest of `sha256(file bytes)[:12]` per en-language file for translation staleness checking. The README is keyed by the flat `"README.md"` relpath (not docs-prefixed); translation walkers and resolvers must accept this top-level relpath alongside `reference/...` paths. Every markdown-*content* call site resolves `locale` through `i18n.ResolveLocale(flagLang, cfgLang, sysLang)` **directly** rather than using `rflags.Locale` (clamped to the YAML store, wrong namespace for markdown); `dwe docs generate` is the deliberate counter-example — it renders YAML-store command strings, so `rflags.Locale` is the right source there. Docs subsystem is **read-only** — never calls `lock.AcquireProjectLocks` and never runs preflight; works without `workspace.yml` (project DocRoot simply omitted). The `//go:generate` directive in `embed.go` uses the relative path `../../../scripts/sync-embedded-docs.sh` (three segments up due to the deeper `internal/core/docs/` location). **Anchor derivation is single-sourced through `parseHeadingSlugLabel` (`headings.go`)** — it returns `(level, slug, label)` where the **slug comes from the RAW heading text and the label from the markdown-stripped one**, and every producer (`ParseHeadingSlugs` → `docs show --anchors`/`--toc`; `ParseDoc` → `Heading.Slug`, consumed by `docstui`'s link jumps; `search.go`'s section attribution → the `#anchor` in every `docs search` row) plus the consumer (`SliceByAnchor` → `topic#anchor`) goes through it. The asymmetry is the whole point: `stripInlineMarkdown` ends in `stripEmphasis`, and slugging its output dropped the `_` in every builtin name and snake_case key, so three of the four surfaces advertised `servicedirsensure` while the resolver answered only to `service_dirs_ensure` — the tool handed out anchors it then rejected. Never call `Slugify(stripInlineMarkdown(x))`, and never recompute a slug from `Heading.Text` (it is the lossy display label; read `Heading.Slug`). `stripEmphasis` itself follows CommonMark's intra-word rule for `_` (a run flanked by alphanumerics on both sides is literal), so labels read `env_file`, not `envfile`; asterisks are still dropped unconditionally. Guarded by `TestAdvertisedAnchorsResolve`, which asserts over the **real embedded tree** that every advertised anchor resolves — the defect was invisible to per-side unit tests because the two sides were never compared. **`docs show` long-document hint**: when a whole document ≥ `longDocMinLines` (120) with ≥ `longDocMinSections` (4) is emitted to a **non-TTY** stdout and no anchor was requested, `emitLongDocHint` (`internal/cli/docs/show.go`) writes one **stderr** line naming `--toc` and the `topic#anchor` form. stderr is load-bearing — the case it addresses is `docs show <topic> | head`, which truncates stdout only. It exists because documenting the flags was measurably insufficient: an observed agent session read the `llms-txt` briefing line telling it to use `--toc`/`--anchors` "instead of piping through `head`/`sed`" and then piped 17 of 17 `docs show` calls through `head`/`sed`. **Translation freshness gate**: every file under `docs/i18n/<lang>/` MUST begin with `> Translated from: <relPath> @ <hash>` where `<relPath>` is the en-source path relative to `docs/` (or bare `README.md`) and `<hash>` is the first 12 hex chars of `sha256(en-source)` recorded in `ContentHashes`; `lang_test.go::TestRussianTranslationsAreFresh` walks every `i18n/ru/**.md` in `BuiltinFS` and fails CI on missing headers, unknown relpaths, or stale hashes. Adding a new language requires a sibling `TestXxxTranslationsAreFresh` (current test is hard-coded to `i18n/ru`). User-visible schema: `docs/reference/docs/index.md` and `docs/reference/config/i18n.md` (long-form section). **`search.go`** (agent-discoverability milestone) — `Search(roots, query, locale, SearchOptions) []SearchHit`. Tokenized: `strings.Fields` (never `Split(q, " ")` — an empty token would zero every result), AND across tokens, ranked by **min** per-token count (sum lets a section repeating the commonest word outrank the one about the pair). Two tiers: section-level hits, plus per-document hits for a doc whose tokens all appear but never inside one section (the tier is per document — a global "nothing matched anywhere" tier would never fire). Both tiers are sorted together by `count desc → tier asc → path asc → section asc`: the tier is a **tie-break below the count**, so a doc-level hit matching four times outranks a section hit matching once (ordering the tiers outright buried the strongest answer below every weak one, invisible at a narrowed `--limit`). The tier lives on an unexported `rankedHit` for the duration of the sort and deliberately never reaches `SearchHit`, which is the `--output json` payload. Matching is case-insensitive **substring** by design (`uid` matches `guide`; `depends_on:` depends on it) — do not "fix" it to word boundaries. `SearchOptions.Literal` treats the whole query as one needle and, like the tokenized path, trims first so an all-whitespace query finds nothing. `SearchHit.Snippet` is the densest source line, whitespace-collapsed and capped at `snippetMaxLen` bytes **including** the ellipsis on a rune-then-word boundary, so the append-only 4th TSV column can never give a row a fifth field. The measurement behind the tie-break ordering: with the tiers ordered outright, a doc-level hit matching four times landed below twelve section hits — nine of them matching once — on the built-in tree for the query `UID GID env`. The tier is deliberately kept off `SearchHit` so it never reaches the JSON payload. `SearchOptions.Literal` is a flag rather than a quoting convention because `dwe docs search` takes exactly one argument and the shell strips quotes, so `"a b"` and `a b` arrive identical; matching stays case-insensitive **substring** by design (`uid` matches `guide`, and `depends_on:` depends on it) — do not "fix" it to word boundaries. Snippet sanitization is **two** passes and both are load-bearing: non-printable runes are DROPPED first — the snippet is the only channel through which document content reaches stdout and a doc tree can be untrusted (`--source project` inside a cloned repo), so an embedded ESC/BEL/OSC sequence would otherwise clear the screen, recolor it, or set the window title — and then `strings.Fields`+join collapses every whitespace run (tabs and newlines included, since markdown tables carry both) to a single space, which is what keeps the snippet from breaking a tab-separated row. The comparator carries a fifth key, `Source asc`, purely to make the order total. On the long-document hint: `--anchors` / `--toc` are a third silence gate — they short-circuit `runDocsShow` and return **before** `emitLongDocHint` is reached, so asking for structure never also triggers the hint; do not move the call earlier. The thresholds are deliberately high for the same reason the gates exist: the note must read as a property of *this* document being large, not as a banner on every read.
- `internal/core/docs/llmstxt/` — pure-function generator for the agent-targeted `llms.txt` index. `Generate(Opts) (string, error)` takes `ProjectRoot`, `IncludeIntern`, `DocTopics` ([]docs.TopicEntry), and exported summary types (`CommandSummary`, `ServiceSummary`, `InfoSummary`) populated by collectors in `internal/cli/docs/llmstxt_collectors.go`. No disk I/O, no config import — keeps the package golden-testable. Empty `DocTopics` (fresh checkout without `make build`) renders an empty Documentation section silently. CLI surface: `dwe docs llms-txt` (read-only, no lock, no preflight). `Opts` also carries `Builtins []BuiltinSummary`, `Conditions []ConditionSummary` and `ReservedEnvNames []string` (agent-discoverability milestone) feeding four static briefing sections; the layering rule is what forces them through `Opts` rather than an import. The **project-agnostic** (`--no-project`) output is capped at 12 KB by `TestDocsLlmsTxtCommand_SizeBudget` — the cap is on `--no-project` only, since a project-aware cap would test someone else's workspace size. That number is duplicated in the `Long` text, both locales of `docs/reference/docs/commands.md` and `AGENTS.md.tmpl`; change all four together. `--output` on this command is a **file path, not a format**. The four briefing sections exist because an agent otherwise reverse-engineers this knowledge from other repositories; the whole milestone's goal is that it finds what already exists in dwe instead — which is also why the sections are identical inside and outside a project (they describe dwe, not the project).
- `internal/core/docs/render/` — glamour rendering: `Render(content, opts) (RenderResult, error)` where `RenderResult{Output []byte, Diagrams []DiagramRef}`. **Text-level mermaid preprocess** before glamour invocation: `mermaid_preprocess.go` scans fenced mermaid blocks BEFORE rendering and emits placeholder text (`<📊 [...]>`), capturing source into the `Diagrams` side-channel. Reason: glamour exposes no `goldmark.Extender` hook, so a parallel AST parser would double-parse; text-level preprocess is the canonical design. Placeholder variants depend on renderer status (disabled → `[diagrams disabled]`; unavailable → `[mmdc not installed — Y to copy]`; otherwise → `[view schema]`). Raw markdown path (non-TTY / `--raw`) skips `Render` entirely and emits verbatim bytes. Tests use golden fixtures and preprocess edge cases (nested fences, unclosed blocks at EOF).
- `internal/core/docs/mermaid/` — mermaid rendering chain: `Renderer` interface with `Render(ctx, src, theme, width)` where width is a cache-key component (callers vary per render). `Chain(...Renderer)` composes renderers in order; `Disabled{}` short-circuits to `ErrRenderingDisabled`. `FileCache{ cacheDir, capBytes, versionFunc }` wraps a renderer with XDG cache (fallback to tempdir), LRU eviction by mtime (atime unreliable), atomic write-temp+rename, and singleflight dedup of concurrent same-key misses. `MmdcRenderer{ bin, versionFunc }` invokes mmdc via `exec.CommandContext` with 10s timeout and theme/width args; per-platform process-group kill on Unix via `Setpgid` + `Kill(-pgid, SIGKILL)`, default kill on Windows; `ErrMmdcNotAvailable` on `exec.ErrNotFound`. `term.go` capability detection: `CanInline()` → false in tmux (passthrough unreliable), true on kitty-capable via rasterm, false elsewhere (best-effort hint); `OpenSystem(path)` → `open` (darwin) / `xdg-open` (linux) / `cmd /c start ""` (windows — empty title arg mandatory for space safety). `CacheDir()` → XDG / UserCacheDir / TempDir. Defaults: `auto` mode tries mmdc silently; `mmdc` mode requires it (error placeholder on missing); `off` mode never invokes (disabled placeholder). Tests use fake-mmdc.sh fixture and injection seams for call-count assertions.
- `internal/core/ui/docstui/` — interactive documentation browser. **As of TUI Stage 4 this is a `tui.Plugin` (`plugin.go`, `var _ tui.Plugin = (*browser)(nil)`) on the shared `tui.Frame` — NOT a standalone `tea.Model`** (old `renderTwoPanel`/`View`/title-bar/help-footer/`keys.go` deleted; frame owns geometry, borders, focus, status line, mouse, alt-screen, teardown). Public entrypoint: `docstui.Run(ctx context.Context, opts Options) error` (replaces the old caller-owned `tea.NewProgram` + `NewModel`). `Options` fields: `Roots`, `Renderer`, `ProjectRoot`, `MermaidTheme`, `Title`, `Locale`, `Translator`, `MmdcMissingNotice` (mmdc-install guidance surfaced in the diagram render-error overlay when `E` is pressed on a "rendering disabled" diagram — replaces the old global banner that `loadTopic` prepended to every topic; `openErrorOverlay`/`currentDiagramError` route the prefetch-captured "render failed" text and this notice through the same overlay). `StatusContext()` returns the middle zone: current path + 📊 N/M diagram progress + `[lang]`. `docstui.Run` maps `tui.ErrTooNarrow` to a clean user-facing error (`terminal too small for the docs browser`) rather than degrading to a flat UI — unlike cmdbrowser's huh-selector `runFallback` and statustui's plain-text `renderDefaultStatus`, docs has no non-TUI rendering path worth reusing; `ErrNotTTY`, `ErrCancelled`, and wrapped panics pass through unchanged (`TestRunMapsErrTooNarrow`).
  - **Plugin shape**: two static panels `Panels() = {tree:Weight 1, viewport:Weight 5}`. `ViewPanel(id, inner Region)` renders each panel body into the frame-computed INNER region (no panel border drawn by the plugin). Panel consts `panelTree`/`panelViewport`.
  - **Actions** (`actions.go`): `RegisterStandard(reg, NavUp, NavDown, PageUp, PageDown, Top, Bottom, Select, Filter, Reload)` — reload binds `ctrl+r` only (pre-migration `r` alias dropped). Plugin-local actions: `diagram.prev` (`[`), `diagram.next` (`]`), `diagram.open` (`o`), `diagram.copy` (`y`) in a **Diagrams** help section; `locale.cycle` (`L`), `locale.english` (`e`) in a **Locales** section; `tree.collapse` (`h`/`←`), `tree.expand` (`l`/`→`) — `nav.left`/`nav.right` stdlib NOT registered (these plugin-local IDs own the keys instead). **Intentional Stage 4 change**: `h`/`l` previously toggled expansion on both keys; now directional — `h`/`←` collapse (step to parent if already collapsed), `l`/`→` expand (step into first child if already expanded) — locked in `actions_test.go`. `tab`/`shift+tab`/`?`/`q`/`esc`/`ctrl+c` stay framework built-ins.
  - **Inline filter** (`/`, `ActionFilter`) = plugin-internal capture mode: `CapturingInput()` returns true while filtering; raw keys edit the query (printable/backspace/enter/esc), registry bypassed; query row "/ query (N)" renders in the tree panel; `enter` commits (expand ancestors, keep selection), `esc` cancels (restore snapshot), `FocusRequestMsg` returns focus to tree — mirrors cmdbrowser filter.
  - **Async structure + plugin-owned context**: plugin holds a `context.Context`+`cancel` derived from the `ctx` passed into `docstui.Run`. `Init() tea.Cmd` batches watcher subscription + prefetch-progress subscription. **First `loadTopic` fires from `Update(tea.WindowSizeMsg)` (only command-capable hook; `Resize` is void), guarded by `firstLoadDone`**, at the computed viewport-panel inner width (cached body region + `{1,5}` weights — `layoutPanels` formula replicated in the plugin). `WindowSizeMsg` updates `ContentWidth` for subsequent loads without re-rendering (no reload storm, no `YOffset` reset — matches old `WindowSizeMsg` handler behavior). `FileChangedMsg` and `ProgressMsg` flow via the Frame's non-key forwarding to `plugin.Update`; generation filtering + per-topic cancel preserved exactly. **`ProgressMsg` (one per rendered background diagram) used to re-inline the WHOLE document via `SetContent` on every tick — that per-completion storm hitched any in-progress scroll on diagram-heavy docs (background rendering "touching" the foreground render). It now only updates progress/StatusBar synchronously and coalesces the placeholder refresh onto a trailing tick** (`scheduleDiagramRefresh`/`applyDiagramRefresh`/`diagramRefreshMsg`/`diagramRefreshDelay=150ms`): the first ProgressMsg of a run arms the tick, later ones ride it (`diagramRefreshTickInFlight`), and when it fires it re-inlines once — BUT **re-arms instead (deferring) while a wheel burst is still in flight (`wheel.tickInFlight`)**, so background diagram rendering never rewrites the viewport mid-scroll. `Close() error` → `cancel()` + `Watcher.Close()` + `Prefetch.Close()` (idempotent/nil-safe). Do **not** add a construction-time or `Init`-time topic load: the only command-capable hook that knows the real panel width is `Update(tea.WindowSizeMsg)` (`Resize` is void and returns no `tea.Cmd`, which is why the first load is `firstLoadDone`-guarded there), so a load started earlier renders at a wrong — or zero — width.
  - **Mouse**: `Update` handles `tui.PanelClickMsg` (tree click → `focusRow(Y)`; viewport click on the scrollbar column → proportional jump, see below) and `tui.FocusChangedMsg` (tracks active panel `b.active` for nav/scroll routing); wheel arrives as `tui.WheelMsg{Panel,Delta}` via `Update` (pointer-routed by hit-zone, dispatched directly by the frame — not through `HandleAction`/`nav.*`). **Both wheel paths keep per-notch work O(1) and defer the EXPENSIVE follow-up to a single debounced flush** (revdiff pattern, `wheelState`/`wheelDebounceMsg`/`wheelLoadDelay=60ms` in `plugin.go`) — without this a buffered wheel flood ran the heavy work once per notch, stalling the bubbletea event loop so the backlog drained (and interrupts were reached) only long after the user stopped scrolling. **`WheelMsg.Delta` is the coalesced notch count** (the framework's WithFilter batches a wheel flood — see § `internal/core/ui/tui/`): the viewport scrolls `ScrollBy(Delta*wheelViewportStep)`, the tree moves `abs(Delta)` rows. **Tree panel**: moves the cursor + `EnsureFocusVisible` immediately and arms a deferred topic load (`loadPending` → `selectCursor` → glamour render at settle). **Viewport panel**: `ScrollBy` (only the offset moves — O(1)) stays immediate, but `syncActiveDiagram` (which re-inlines the WHOLE document via `SetContent` on a diagram-boundary crossing) is deferred (`syncPending`, flushed once at settle) — that per-notch `SetContent` was an event-loop stall on diagram-heavy docs. Coalescing: every notch bumps `wheel.gen` + sets its pending flag; only the first notch of a burst schedules a `tea.Tick` (`tickInFlight` gate via `armWheelTick`), later notches ride it; the tick re-arms while `gen` keeps advancing (mid-burst) and `flushWheel` runs the armed work once `gen` matches (sync first, then load). **Interrupt**: `HandleAction` (any bound key) and `handlePanelClick` call `cancelWheelLoad()` (drops `loadPending` only) so the new command's own load takes over immediately. `wheelViewportStep=2` (deliberately small — finer control, screen keeps pace). **Scrollbar click-to-scroll**: `scrollViewportToClick(x,y)` maps a click on the last inner column (`viewportInner.Width-1`, where `applyInnerScrollbar` draws the bar) to a proportional `ScrollToLine` (`y*maxOffset/(h-1)`, clamped); off-column clicks and short (no-scrollbar) documents are ignored. **Keyboard scroll is the primary path** (the wheel is a bonus): on top of `j/k`+arrows, `PgUp/PgDn`, `g/G`+`Home/End`, the docs browser adds vim **half-page `ctrl+d`/`ctrl+u`** (`actionHalfPageDown`/`actionHalfPageUp` → `navHalfPage`: viewport `ScrollBy(±VisibleHeight/2)`, tree jumps `treeInner.Height/2` rows).
  - **Mermaid theme**: constructor calls `resolveMermaidTheme(opts.MermaidTheme)` and stores the resolved theme (NOT `opts.MermaidTheme` raw) — probes `lipgloss.HasDarkBackground` for `auto`; golden tests must force a concrete resolved theme via the `mermaidThemeForTest` seam.
  - **Tree surface (Frame-facing)**: **As of TUI Stage 4 (Plan 2) `TreeWidget` is a thin wrapper over `*tree.Engine[*TreeNode]`** (`internal/core/ui/tui/tree`) — the prior "mirror cmdbrowser exactly" duplicated `topIdx`/`ensureFocusVisible`/`focusRow`/`clipToViewport` core is now engine-owned. `MoveUp/MoveDown/MoveStart/MoveEnd/Collapse/Expand/Toggle/Cursor/SetCursor/VisibleNodes` delegate to the engine. The **`Expanded bool` field on `TreeNode` was DELETED**; every read routes through `e.IsExpanded(node)` and every write through `e.SetExpanded(node, b)`/`e.Toggle()`. Adapter: `Children → node.Children`, `Key` is **kind-prefixed** (`group\x00<root>` / `node\x00<root>\x00<path>` / `heading\x00<root>\x00<path>\x00<idx>`), `Expandable → true for any non-heading directory PLUS files carrying heading rows` (Codex #4 — NOT "dir-with-children"; empty/index-only dirs stay Enter/Toggle-able), false for heading rows. **Expansion + cursor now survive a locale `Rebuild`** — the engine's `expanded` map is keyed by stable `Key`, so `Rebuild` rebuilds the node graph then calls `e.SetRoots(newRoots)` (the manual `findByPath` transfer is gone) with a **two-tier cursor fallback** (`SetCursorByKey(prevHeadingKey)` → on false `SetCursorByKey(prevParentFileKey)` → on false `ParkCursorIfHidden()`; a vanished heading falls back to its parent file, NOT first-visible). This is the **intended Stage-4 bugfix** (docs previously lost expansion on locale change). Multi-root group nodes are seeded default-expanded in `NewTreeWidget` only (after `SetRoots`, before the first `RebuildVisible(nil)`), so user collapse/expand persists across `Rebuild`. The filter (`tree_filter.go`/`ApplyFilter`) drives the engine's `keep` predicate — `e.RebuildVisible(nil)` when nil/inactive/**empty-query** (Codex #3: `Matches("")` is true, so a naive keep would emit the whole tree), `e.RebuildVisible(matchPredicate)` only for a non-empty active query — each followed by `e.ParkCursorIfHidden()`. `TreeFilter`/`/`-UX/ancestor-expand-on-commit are untouched.
  - **`index.md` folding** (TUI-only, in `tree_widget.go::addNodeAsChild`, NOT in `docs.BuildTree`): when a directory contains an `index.md`, the file is folded into the directory node — the directory borrows the index file's H1 as its tree label (`nodeLabel`), selecting the directory renders the index file's content (`contentNodeFor` → `loadTopic`), and the `index.md` itself is dropped as a separate row. A directory without an `index.md` keeps the prior behavior (blank viewport on select). The fold lives in the TUI layer so non-TUI consumers (`docs export`, `docs list`) still see `index.md` as an ordinary file.
  - **Diagram placeholders** (`diagram_inline.go`): each mermaid block renders as an inline text line substituted *after* glamour; the currently selected diagram (the one `o` opens) is styled in the accent color + bold with a `▸` marker, the rest muted, so the active one is obvious without reading the counter. **Cursor-driven diagram focus**: `diagramLineIndices` records each placeholder's rendered line per load; `syncActiveDiagram` (called after every cursor move / scroll and after the initial load / heading jump in `applyTopicLoaded`) makes `activeDiagramForCursor` the active one — the last diagram at or above the reading cursor, but preferring the topmost diagram *visible in the window* when that one has scrolled off above (so `o`/`y`/`E` act on what's on screen after a mouse-wheel scroll, which pins the cursor to the window's top edge via `pinCursorToWindow`). This is distinct from `jumpToDiagram` — the `[`/`]` manual switch, which moves the cursor onto the chosen diagram's line and scrolls it into view; `syncActiveDiagram` never scrolls. The `E` error-log hint is advertised only on the *active* failed diagram (it opens the overlay for `DiagramState.Current`, so a hint on a non-current diagram would be a no-op).
  - **Scrollbar** (`plugin.go::applyInnerScrollbar`): the **last inner content column** is carved into a full-height scrollbar — a muted shaded track (`░`) with a solid accent thumb (`█`) at the current position; rows at or over the inner width are `ansi.Truncate`d to make room. It operates on **border-free inner content** — the Frame owns the border, so nothing overdraws a border rune (that technique survives only in `tui/overlay_scrollbar.go::OverlayScrollbar`, which does own its box). Returns the content unchanged when the whole document fits, or when the inner width is under 2. Thumb size/position derive from `Viewport.TotalLines()` / `YOffset()` and the height the Frame passes in — **not** `VisibleHeight()`.
  - **Render-error overlay + selection mode** (`errorlog.go`, `plugin.go::updateErrorOverlay`) — the `E` overlay is a `CapturesInput` viewport modal (rounded box + `tui.OverlayScrollbar`) that takes raw keys: `c` copies the whole raw error text to the clipboard (`tea.SetClipboard`) plus a status flash, `s` toggles **selection mode**, every other key scrolls the viewport, and `esc` never reaches here (the Frame closes it). Selection mode republishes the overlay with `ReleaseMouse: true` **and** `FullScreen: true` and resizes the block to the whole terminal via `applyErrorOverlayDims` (`b.TermWidth × b.TermHeight−1`, the `−1` reserving the hint row); `errorState.selectionOverlay` then renders a border-free opaque block plus that hint row, so every on-screen cell is error text or blank padding and a released-mouse drag-select can only grab the error — no border columns or status line bleed in (the terminal owns native selection and cannot be clipped to a sub-rectangle, so the fix is to remove everything else from the screen). While selection mode is on the framework gets no mouse, so click-outside-close and overlay wheel-scroll are off by design. The hint is `MaxWidth`-bounded to the block so a narrow terminal cannot push the overlay past the edge and get truncated by `fullScreenOverlayView`'s clamp. Leaving selection mode clears both flags and `applyErrorOverlayDims` shrinks back to the content-fit box. The `c` confirmation and the diagram-copy `y` share one flash channel — `StatusBarWidget.SetFlash`/`ClearFlash` driven by `setStatusFlash`, **generation-gated** via `statusFlashClearMsg{gen}` against `b.flashGen` so a stale clear tick from an earlier flash never wipes a newer one (this is the pattern cmdbrowser later copied).
  - `watcher.go` uses `fsnotify` for hot reload of project-docs only (embed is static); goroutine exits cleanly on context cancellation. `prefetch.go` spawns a bounded worker pool (2 workers) with priority queue (current file first) to render diagrams asynchronously; individual render failures logged but not cancelling; producer-consumer channel pattern with `ok` check after receive (mandatory to avoid spin on closed channel). Language fallback banners prepended when translation missing/stale; per-file resolution via `ResolveContent` with `stale` flag (hash mismatch). Tests cover tree state, action dispatch, filter transitions, diagram tracking, watcher+prefetch goroutine cleanup via `goleak.VerifyNone(t)`; golden frame tests at width buckets 60/79/80/99/100 via `tui.RenderFrame`/`tui.BuildHelp`.
- `internal/core/docs/export/` — tree export to disk: `Tree(dst, roots, opts) error` where `Opts{Lang, IncludeProject, IncludeInternals, Force}`. Refuses non-empty target without `--force`. Per-file fallback: missing translation appends en with a banner `> **Note:** This file is not translated to <lang>. Original English version below.` Mermaid blocks preserved as-is (IDE-friendly). Creates `<dst>/<root.Name>/<relPath>` hierarchy with deep markdown structure intact.

## Core — UI (`internal/core/ui/`)

Domain-aware renderers. Intended to be a sink — only `internal/cli/**` should import these. (The `ui-is-sink` rule is one of three layering contracts whose `depguard` activation is deferred; see [Dependency Rules](#dependency-rules) above.)

The root `internal/core/ui/` directory holds **zero `.go` files** — all output, styling, and prompt code lives in seven sibling subpackages. The historical flat package was split into three domain-aligned subpkgs (`styles/`, `widgets/`, `render/`) plus four pre-existing siblings (`ask/`, `cmdbrowser/`, `statusview/`, `statustui/`). One-way dep: `render, widgets → styles`. `styles/` is the only subpkg with a third-party `charm.land/huh/v2` import (used to construct `HuhTheme`); that does not make it a non-leaf for internal layering purposes.

- `internal/core/ui/styles/` — palette + icon + huh-theming. **3 files**: `styles.go`, `icon.go`, `icon_replacements.go`. Owns:
  - **Palette resolution**: `ApplyStyles(cfg *config.StylesConfig)` resolves light/dark hex pairs once at apply time via `lipgloss.HasDarkBackground()`; user overrides apply to both modes. Empty `text:` resolves through `lipgloss.NoColor{}` (terminal default) rather than `lipgloss.Color("")`. String-typed accessors `ColorAccent`, `ColorSuccess`, `ColorWarning`, `ColorDanger`, `ColorMuted`, `ColorBorder`, `ColorText` (**7 semantic tokens**) are the only sanctioned bridge between v1 lipgloss here and v2 lipgloss in `cmdbrowser/` (and Fang's `ColorScheme`).
  - **Style accessors**: lowercase `lipgloss.Style` vars (`styleAccent`, `styleMuted`, `styleSuccess`, `styleWarning`, `styleDanger`, `styleBorder`, `styleText`) remain unexported to preserve the `ApplyStyles`-mutation invariant. Cross-pkg readers go through 1-line accessor functions: `AccentStyle()`, `MutedStyle()`, `SuccessStyle()`, `WarningStyle()`, `DangerStyle()`, `BorderStyle()`, `TextStyle()`. Render-pkg files call `styles.AccentStyle().Render(text)`. String-render helpers `StyleKey`, `StyleMuted`, `StyleWarning`, `StyleInfo`, `StyleGroup`, `StyleSectionTitle`, `StyleSubheader`, `StyleFailed`, `StyleService*`, `StyleOption*`, `RenderEnabled`, `RenderPartial`, `RenderStopped` are the recommended path for external callers; also exports `DefSep` (used by `render/info.go` + `render/summary.go`) and `ServiceTypeStyle(serviceType) lipgloss.Style` (consumed by topology renderer).
  - **huh theming**: `HuhTheme`, `ApplyFormGlyphs`, `BuildPaletteApplier`, `Theme()` — moved here from the old `huh.go` because they construct themes from palette colors (semantically styling, not widget runtime).
  - **Icons**: `SafeIcon`, `IconPrefix`, `IsAmbiguousWidthIcon` (in `icon.go`); `SuggestSafeIcons` plus internal `stripTrailingVS16` (in `icon_replacements.go` — tightly coupled, must stay together).
  - **Logomark helpers**: `LogoMark()` / `LogoMarkPlain()` were intentionally moved to `render/logo.go`, NOT `styles/`, because they are output composites (icon + accent style wrapping), not palette primitives.
  - **`TermWidthOrZero(f *os.File) int`** (`styles.go`) is the width probe behind `render/`'s responsive-table budget (see `render/`'s Responsive tables entry): it returns 0 — not a fallback width — when `f` is not a terminal or its size is unknown. This is the opposite contract from the pre-existing `TermWidth()`, which has no `*os.File` parameter (always probes `os.Stdout`) and falls back to 80 columns on the same non-TTY/unknown-size condition; `TermWidth`'s 80-column fallback exists for callers that need *some* width unconditionally, while `TermWidthOrZero`'s 0 exists so a piped/non-TTY renderer can distinguish "no terminal, don't shrink" from "an 80-column terminal, maybe shrink" — conflating the two would silently push every piped run (and the entire golden test suite) into narrow-mode wrapping. Two test seams, `termWidthIsTerminalFn` / `termWidthGetSizeFn`, let tests simulate TTY/non-TTY and specific sizes without a real terminal.
- `internal/core/ui/widgets/` — interactive huh-based forms. **6 files**: `confirm.go`, `multiselect.go`, `selector.go`, `huh.go`, `interactive.go`, `filter_quit.go`. Imports `styles/` for theme + glyph application; no other internal subpkg deps.
  - `RunSelector`, `RunMultiSelect`, `RunConfirm`, `ConfirmRun` (caller pre-renders title; renders a sorted `key = value` summary above the prompt). `SelectorItem`, `MultiSelectItem`, `MultiSelectResult` types.
  - `ErrCancelled` (used by 20+ call sites — defined in `selector.go`).
  - **`RunHuhForm(ctx, form) error`** (TUI Stage 6, `huh.go`) is the single canonical executor for every huh form in dwe: wraps `RunWithPromptHooks`, calls `form.RunWithContext(ctx)`, translates `huh.ErrUserAborted` → `ErrCancelled`. `defaultRunConfirmForm` / `defaultRunConfirmRunForm` / `defaultRunSelectForm` / `defaultRunMultiSelectForm` (the default `run*FormFn` seams) and `internal/core/ui/ask`'s `Form.Run` all route through it; the per-wrapper hook-snapshot blocks that used to live in `RunConfirm`/`RunSelector`/`RunMultiSelect` were removed in the same change (**hooks-fire-once invariant**: each wrapper keeps only a defensive `ErrUserAborted → ErrCancelled` mapping so seam-swapped tests returning raw huh errors keep their contract — the hook wrap itself must not double-fire).
  - **`SetHuhHooks` / `ClearHuhHooks`** — package-level hooks fired around every `widgets.Run*` call (and, transitively, every `RunHuhForm` call) so a `LiveLine` footer can pause/resume cleanly. Any new prompt-like UI must go through `widgets.Run*`/`RunHuhForm`, never bypass. **`RunWithPromptHooks(fn)`** — production helper that snapshots the hooks, runs `before`, defers `after`, and invokes `fn`; used by full-screen UI (cmdbrowser, statustui) that cannot go through `Run*`, and internally by `RunHuhForm`. `SnapshotHuhHooks` remains test-only.
  - `IsInteractiveTTY` (and similar env helpers in `interactive.go`).
  - Intra-pkg lowercase symbols stay lowercase (`snapshotHuhHooks`, `multiSelectMinHeight`).
- `internal/core/ui/render/` — DTO → string section renderers. **19 implementation files** + 20 test files: `info.go`, `info_auto_hosts.go`, `info_auto_urls.go`, `summary.go`, `deploy_info.go`, `pending.go`, `topology.go`, `gitworkspace.go`, `diagnostics_table.go`, `table.go`, `brand_header.go`, `logo.go`, `selector_header.go`, `vars.go`, plus the responsive-table engine — `table_wrap.go`, `table_fit.go`, `table_view.go`, `table_record.go`, `table_budget.go` (test count is +1 over impl count for `test_helpers_test.go`, which pins the `TestMain` non-TTY seam and has no matching impl file) — and a `testdata/` golden tree (one `.golden` file per table renderer, byte-compared, `UPDATE_GOLDEN=1` to regenerate). Imports `styles/` (and `statusview/` for view-model DTOs). **Exported names dropped the `Render` prefix per revive** — e.g. `RenderInfo` → `Info`, `RenderBrandHeader` → `BrandHeader`, `RenderSummary` → `Summary`, `RenderServicesTable` → `ServicesTable`, `RenderPendingBanner` → `PendingBanner`, `RenderTopology` → `Topology`, `RenderDiagnosticsTable` → `DiagnosticsTable`, `RenderDeployStatus` → `DeployStatus`, `RenderDeployInfo` → `DeployInfo`, `RenderGitWorkspace` → `GitWorkspace`, `RenderDaemonTable` → `DaemonTable`, `RenderSectionTitle` → `SectionTitle`, `RenderSubheader` → `Subheader`, `RenderDefinition` → `Definition`, `RenderDefinitionAt` → `DefinitionAt`. The pre-existing `BrandHeader` *type* was renamed to `Brand` to free the function name. **Signature contract**: renderers return strings — never take an `io.Writer` — so that `internal/cli/` stays the single writer to stdout/stderr and every table stays golden-file comparable; the lone sanctioned exception is the thin `PrintSelectorHeader(w, …)` helper below. The same rule binds `internal/core/project/stack/`'s collectors and section renderers (whose own grandfathered exception is `RenderServiceDeployDetail`). Every sink-probing table entry point ships an explicit-width sibling (`ServicesTableAt` / `DaemonTableAt` / `DeployStatusAt` / `GitWorkspaceAt` / `SectionTitleAt`) for TUI panels that must bypass the probe — ship both halves when adding a table.
  - **Responsive tables** (`table_wrap.go`, `table_fit.go`, `table_view.go`, `table_record.go`, `table_budget.go`) — every table renderer (`Table`, `DaemonTable`, `DeployStatus`, `GitWorkspace`, `ServicesTable`, `DiagnosticsTable`/`DiagnosticsByDomain`) is built on a shared `tableView{Headers, Rows, Cols []columnSpec, Style, Padding, BorderRow, Zebra, Center}` and degrades across three stages at `Render(budget int)`: fits → render as today; does not fit → shrink flexible columns and wrap once at the final width (`fitRows`); does not fit even at column floors → `fitRows` returns `ok=false` and `Render` falls back to `renderRecords`, a labeled-record layout (no ellipsis truncation anywhere — an unbreakable long token, e.g. a URL, floors its column instead of being cut). The budget is derived from the renderer's **sink**, not a hardcoded stream: `stdoutBudget()`/`stderrBudget()` (`table_budget.go`) call `styles.TermWidthOrZero(f *os.File)` on `os.Stdout` for every renderer except `DiagnosticsTable`, which uses `os.Stderr` (its three call sites — preflight, deploy menu ×2 — all write diagnostics to stderr). **Its sibling `DiagnosticsByDomain` is NOT one of them**: same rows, different sink — its only call site (`dwe validate`, `cli/validate/validate.go`) writes to `cmd.OutOrStdout()`, so it probes stdout. Getting this backwards is the concrete failure mode the rule exists to prevent: probing stderr there would shrink `dwe validate > report.txt` to the terminal width and leave `dwe validate 2>/dev/null` unbounded on a narrow terminal. **A non-TTY sink means budget 0, and budget 0 means unbounded, byte-identical legacy output** — `fitRows` with `budget == 0` still applies `Max` column caps and still wraps at them, it just never shrinks a flex column below its natural width; this is why every Task-1 golden stays byte-identical across all of Tasks 2–13. The goldens hold regardless of how the suite is launched because `TestMain` (`test_helpers_test.go`) pins the package-level `termWidthFn` seam to the non-TTY default (0) for the whole run — narrow-mode tests opt in explicitly via `withTermWidth(t, w)`. Explicit width wins over the sink probe: every sink-probing entry point has an explicit-width sibling — `ServicesTableAt`, `DaemonTableAt`, `DeployStatusAt`, `GitWorkspaceAt` (all `width int`, 0 = unbounded) — used by TUI panels that already know their own width. A new table should ship both halves. **A `Max` cap never overrides an unbreakable token**: `naturalWidths` clamps cell content to `Max`, but `fitRows` then raises each natural width back to the column's `columnFloors` value, because the wrap helpers refuse to split a URL or a wide rune and Lipgloss lays the column out at the token's width regardless. Skipping that step makes `distributeDeficit` see negative headroom and *widen* the column, so the table overflows the budget while still reporting `ok=true`. Cells must be plain text — the wrap helpers are not ANSI-aware, so semantic color is applied by `tableView.Style` *after* wrapping, never baked into a cell before it. The table/record mode decision is made once per `Render` call (per renderer, per call site); `DiagnosticsByDomain` instead calls `tableView.fit(budget)` per domain (it never calls `Render` at all) to force one shared mode across every domain it emits (see below) — reusing each domain's already-fitted rows — so `dwe validate` never mixes table and record layout mid-output — this is a deliberate exception to "each renderer decides independently" and the only place `dwe status`'s cross-table mode mismatch (Services vs. Deploy vs. Git each deciding on their own row/column shape) is accepted rather than unified. A new table therefore inherits shrink/wrap/record degradation for free simply by being built as a `tableView` — that is the extension point, not a per-renderer layout pass. Two clarifications on the sentences above. First, budget 0 skips the **record fallback** as well as the shrink pass: with `budget == 0` `fitRows` runs neither deficit distribution nor the floor check, so `ok` is never `false` and `Render` can never reach `renderRecords` — "unbounded" means unbounded in both directions, and wrapping at `Max` column caps still happens. Because `TestMain` (`test_helpers_test.go`) pins the package-level `termWidthFn` seam to 0 for the whole suite (narrow-mode tests opt in via `withTermWidth(t, w)`), the goldens do not depend on `go test` happening to pipe stdout — so a golden mismatch is almost never a budget bug: check that seam before touching budget logic. Second, `DiagnosticsByDomain` is the one renderer that never calls `tableView.Render` at all — it calls `fit(budget)` for every domain up front and then drives the shared `renderTable` / `renderRecords` helpers directly with that single verdict, because a table sitting immediately above a record block reads as a bug. The `DiagnosticsTable`/`DiagnosticsByDomain` split generalizes: audit a new renderer's actual call sites to pick `stdoutBudget()` vs `stderrBudget()`, never copy the neighbouring renderer's budget func — backwards, it shrinks `dwe validate > report.txt` to the terminal width and leaves `dwe validate 2>/dev/null` unbounded on a narrow terminal. Equally, a `width` argument of 0 means unbounded, never "probe the sink": a wrapper that wants the probe must call the sink-probing entry point (`render.DaemonTable`), not `…At(rows, 0)`. On the floor-raise: skipping it is not a cosmetic overflow but the terminal hard-wrap the whole feature exists to prevent — a table that reports `ok=true` and then wraps in the terminal is strictly worse than the record fallback it declined. And record mode is the one place over-long output is legitimate: it has no lower width bound and never truncates, so an unbreakable token wider than the budget overflows by design there.
  - `Summary`, `Info`, `PendingBanner(p *journal.PendingApply) string` (renders one warning line per pending op; empty string when `p == nil`; used by `dwe status` and its subcommands).
  - `ServicesTable(rows, extraCols, withDirCol bool)` is the single table renderer used by all three type-keyed status sections — apps pass `withDirCol=true`, tool/infra pass `false`; built-in columns NAME [+DIR] CONTAINER **HOSTS PORTS** STATE RUNNING. `Hosts` and `Ports` from `ServiceTableRow` are always-visible columns formatted by `formatHostsCell` / `formatPortsCell` (single entry → bare value, multi-entry → sorted `name=value` pairs, empty → `—`); dynamic per-row `Extras map[string]string` filled in by `stack.RenderCustomCells`; missing key → `—`.
  - `Topology`, `DiagnosticsTable` (paired with `FormatDiagnostics` and `FormatSummary`), `DeployStatus`, `GitWorkspace`, `DaemonTable(rows)`. **Two diagnostics layouts**: `DiagnosticsTable(rows)` renders the 6-column table **with** a DOMAIN column (used by preflight + the deploy menu, where rows span only env+checks); `DiagnosticsByDomain(rows)` renders **one titled table per domain** with the DOMAIN column dropped (used by `dwe validate`). Domains order by `domainDisplayOrder` (unknowns appended alphabetically); each title is colored by the worst severity in its group. Both share the column-agnostic `diagnosticsTable(rows, showDomain)` core. The FILE column wraps on `/` via `wrapPath` so long relative paths don't widen the table — linter findings are relativized to the project root by `internal/core/validate/linters` (`relToBase` in `runtime.go`), not here.
  - **Logomark helpers**: `LogoMark()` (accent-colored `{▪}` for v1 contexts that own no outer color envelope) and `LogoMarkPlain()` (unstyled `{▪}` for v2 contexts and outer-colored envelopes that would otherwise nest escapes). **Logomark exclusion list** (do NOT prefix): notification titles, tab-completion descriptions, pipeline phase headers, template/PR/commit footers, generated doc artifact bodies.
  - **Selector / TUI title helpers** (`selector_header.go`): `SelectorTitle(projectName, base)` composes `DWE · <project> · <base>` (middot-joined, project omitted when empty); `BrandedSelectorTitle(projectName, base)` prefixes the plain logomark → `{▪} DWE · <project> · <base>`. **`BrandedTitleForConfig(cfg *config.DweConfig, base string) string`** is the **single shared source** the three full-screen TUIs use for their status-line brand (`cli/status` via `statustui`, `cli/docs` via `docstui`, `cli/command` via `cmdbrowser`): it takes the **config** and reads `cfg.Project.Name` (nil-safe), so the *compose* project name (`dwe_tbm`) can never leak into a title where the *display* name (`tbm`) belongs — the bug that made status drift from docs/commands. `PrintSelectorHeader(w, projectName, base)` writes the styled subheader for non-TUI selectors.
  - **vars renderers** (`vars.go`, consumed by `cli/vars/`): `VarValue(value) (string, error)` (scalar or YAML subtree for `get`), `VarsList(items, namespace) string` (flat leaves — path + effective value + layer badge, namespace-filterable), `VarInspectView(in, width) string` (per-layer `Default`/`Local`/`Current` block + origin file + grouped usages with `file:line` + line text + the "dynamic paths not tracked" caveat). All return strings (no `io.Writer`) per the section-renderer contract; names follow the no-`Render`-prefix convention.
  - **info.yml auto-blocks (render-time expansion)**: `type: auto-urls` and `type: auto-hosts` items in `workspace/info.yml` expand at render time (when `dwe info` is executed), not at YAML load time. The `InfoItem` struct carries a `Source*Spec` pointer (yaml:"-") populated by `InfoItem.UnmarshalYAML` at load time, dispatching on the `type:` field to unmarshal into the concrete `AutoURLsSpec` or `AutoHostsSpec` (standard Go `type alias InfoItem` shadow technique avoids infinite recursion). `LoadInfoConfig` is config-blind — it never computes topology or iterates services. At render time, `renderInfoItem` dispatches to `renderAutoURLs` and `renderAutoHosts` (pure string-returning functions in `info_auto_urls.go` / `info_auto_hosts.go`, both intra-pkg lowercase since same package) which call `config.DeployOrder(cfg, types)` directly to iterate services in deterministic deploy order (never `range cfg.Services`, whose iteration is randomized). `autoDetectPortVia(cfg)` helper searches for a single enabled `type: infra` service with `ports.http == 80` or `ports.https == 443` — zero or >1 candidates return nil; the `port_via:` field overrides auto-detection when explicitly set. When `workspace/info.yml` is absent, `LoadInfoConfig` returns `DefaultInfoConfig()` (synthesized with `auto-urls` and `auto-hosts` sections whose `Source*Spec` pointers are populated directly in Go code, not via UnmarshalYAML). Service display fields `icon`, `info.title`, `info.primary_host` (formerly `host_key`), `info.primary_port` (formerly `port_key`), and `info.paths` are read-only display fields consumed only by info renderers (and status/cmdbrowser for `icon`). The randomized-map-order rule is enforceable rather than stylistic because every one of these renderers is byte-compared against a `testdata/*.golden` file: iterating `cfg.Services` directly makes those tests flaky, not merely non-deterministic in production output. User-visible schema and examples: `docs/reference/config/info.md`.
- `internal/core/ui/ask/` — shared huh-based form-runner for generic `[]Field` shapes, and (as of TUI Stage 6) the single declarative-keymap construction layer for every huh form in dwe. **Purpose**: unified form asker used by the setup wizard's question loop, user-command param forms, the deploy menu's two interactive selects, and the setup wizard's port-override/service-toggle prompts; previous two parallel implementations merged into one, and Stage 6 folded in the three remaining raw-`huh.NewForm` sites. `FieldKind` enum: `FieldInput` / `FieldSelect` / `FieldMultiselect` / `FieldConfirm` (zero `FieldUnknown` is a sentinel error rejected at `Build`). `Field` struct (Key, Title, Description, Kind, Required, Default/Defaults for pre-fill, Options, per-field Validate callback, `Height int` for select/multiselect viewport height, `Filterable *bool` — **multiselect only**, huh/v2 `Select` has no `Filterable` method). `Option` struct (Value, Label, Description). `Result` struct carrying a map of resolved values typed by kind (string for scalar, `[]string` for multiselect, bool for confirm); typed accessors `String(key)` / `Strings(key)` / `Bool(key)` prevent untyped access.
  - **Build/Run split** (Stage 6, the seam Stage 7's in-TUI form overlay needs): `Build(title, fields, opts) (*Form, error)` constructs a `huh.Form` (one group dispatching each field to `huh.NewInput` / `NewSelect` / `NewMultiSelect` / `NewConfirm`, `styles.Theme()` applied, keymap assembled per `QuitSpec`/`SubmitHelp`) without running it; `(*Form).Run(ctx) (Result, error)` executes via `widgets.RunHuhForm` (hooks + `RunWithContext` + abort translation) and harvests the result; `(*Form).Huh() *huh.Form` exposes the underlying form for Stage 7 to embed as a capturing-overlay child model (drives `Update`/`View` directly, never calls `Run`); `(*Form).Result() Result` harvests bound values independent of how the form was driven. Package-level `Run(ctx, title, fields, opts)` is exactly `Build` + `Form.Run` (unchanged call signature from pre-Stage-6). An empty `fields` slice short-circuits at `Build` (`Form.empty`) without invoking huh.
  - **Cancel contract**: `Run`/`Form.Run` return `widgets.ErrCancelled` on user abort (Stage 6 changed this from raw `huh.ErrUserAborted` — every caller checks `errors.Is(err, widgets.ErrCancelled)` now).
  - **Declarative keymap overrides** (Stage 6, `RunOptions`): `Quit *QuitSpec{Keys []string, Help string}` (nil = huh defaults; empty `Keys` treated as nil) sets the form-level Quit binding and, per-field-kind, hijacks a help slot so the quit hint renders in huh's field help line (huh hides the form-level Quit binding from field help): select/multiselect → `Filter` slot (rebinds the Filter *activation* key to quit, which as a side effect makes `/`-filtering unreachable — intended for the sites that use it); input → `AcceptSuggestion` slot plus a fake single-blank `SuggestionsFunc` (huh only exposes that binding when suggestions are enabled). For a `Filterable: false` multiselect the hijacked Filter slot never renders at all (huh's `WithKeyMap` disables and hides Filter binds when not filterable) — quit still works via the form-level binding, only the help hint is absent; this is a known, documented limitation, not a bug. `SubmitHelp string` cosmetically relabels the submit help verb (e.g. "select"). `ShowHelp *bool` (nil = leave huh's default). No `filterAwareQuit` wiring in `ask` — the Filter-slot hijack and live filtering are mutually incompatible on the same field, and no `ask` consumer needs both; add it only when one does. A site that needs a non-default quit binding declares a `QuitSpec`; hand-rolling a raw `huh.NewForm` + keymap at the call site is not an available escape hatch (that carve-out is what Stage 6 closed — see **Migrated call sites** below). `docs/internals/tui-keymap.md` § 7 ("Forms guidance") is the cross-surface companion to this bullet: it records that registry wiring was evaluated and **declined** (a huh form runs standalone via `ask.Run`/`(*ask.Form).Run` → `widgets.RunHuhForm`, not as a `tui.Plugin` driven by `Frame.Update`, so there is no registry dispatch point to wire through), and that consequently only the **quit** binding and its help-line presentation are unified across sites — form-internal navigation stays on huh's own key routing.
  - **Migrated call sites** (Stage 6 closed both carve-outs the ask package previously documented): `internal/cli/deploy/menu.go`'s `selectMenuItemInteractive`/`selectDeployServiceInteractive` and `internal/core/workflow/setup/huh.go`'s `askPortOverrides`/`askServiceToggles` are now plain `ask.Run`/`ask.Build` calls with `QuitSpec` overrides — no more raw `huh.NewForm` outside `internal/core/ui/ask` and `internal/core/ui/widgets` in production code (`setup/help_runtime_test.go` keeps a raw form as a huh-behaviour canary; test-only, exempt). `internal/core/ui/widgets/selector.go`, `confirm.go`, `multiselect.go` remain separate pre-existing primitives (command tree / cmdbrowser callers), unaffected by this stage — the rule is still "use `ask` when `[]Field` suffices", not "no raw huh anywhere". `internal/core/ui/tui/formoverlay_test.go` likewise builds raw `huh.Form`s as Stage 7 overlay fixtures — also test-only, also exempt; the "grep for `huh.NewForm` should come back empty" invariant is scoped to production code, not to the whole tree.
- `internal/core/ui/cmdbrowser/` — two-panel interactive command browser used by `dwe commands` when no exact ID is given (no-ID with `--inspect` is a hard error, not a browser launch). **As of TUI Stage 3 this is a `tui.Plugin` (`plugin.go`, `var _ tui.Plugin = (*browser)(nil)`), the framework's first real consumer — NOT a standalone `tea.Model`** (`model.go` / `keymap.go` deleted; geometry, borders, focus, status line, alt-screen, mouse mode, and teardown now belong to `tui.Frame`/`tui.Run`). Public surface is **frozen and unchanged**: `Run(title, items, opts) (Result, error)`, `Item` (carries precomputed `Inspect` string — package is decoupled from `usercommands.CommandDef`), `Options` (incl. `Edit *EditSpec` and `RunForm *RunFormSpec`), `DefaultOptions()`, `Result { Idx, Action, SkipConfirm, ForceParamForm, Values }` (`Values` = params harvested from the in-TUI RunForm overlay, nil on every non-browser and no-form-needed path), `Action` / `Mode` enums (each with `*Unknown` at iota 0). Builds its tree internally (`treeModel`/`treeNode`) by splitting `Item.ID` on `.` — no dependency on `usercommands.GroupNode`. **As of TUI Stage 4 (Plan 2) `treeModel` is a thin wrapper over `*tree.Engine[*treeNode]`** (`internal/core/ui/tui/tree`): nav/scroll/expansion/directional-collapse-expand are engine-owned (the local `moveUp/moveDown/onLeft/onRight/toggleFocused/ensureFocusVisible/focusRow/rebuildVisible/clipToViewport/indexOfFocused` copies were deleted). `treeNode` stays as payload (counts/leaves); `build`/`ensureGroup`/`recomputeCounts`/`setIncludePrivate`/`itemsForFocus`/`nearestVisibleAncestor` stay. The adapter is `Children → n.children`, `Key → n.id`, `Expandable → len(n.children) > 0`. Cursor mapping is golden-critical: `focusedNode()` → `e.Cursor()` (nil ⇒ root), `focusedID` reads → `e.Cursor().id` or `""`, all `focusedID = …` writes → `e.SetCursorByKey(id)` (incl. `""`/root + `nearestVisibleAncestor`/`nearestRestoredAncestor`); `nodesByID`/`root` are kept only for `breadcrumb()` "(root)" and `itemsForFocus()`. The filter (`filter.go`) keeps its dim+counts logic but routes expansion storage through `e.ExpandedSnapshot()`/`e.SetExpandedByKey()`/`e.RestoreExpanded()`; the `exitFilter`/`commitFilter` `if !focusVisible() { … nearestVisibleAncestor }` sequence relies on `RebuildVisible` not re-parking the cursor. Palette is wired through `cmdbrowser/palette.go`, which constructs `charm.land/lipgloss/v2` styles from `styles.Color*()` strings and applies them to `list` / `viewport`. `ActionEdit` and `ModeEdit` are declared in `run.go`, appended at the END of their iota blocks — that is what makes them additive: every pre-existing `Action`/`Mode` constant keeps its numeric value.
  - **Plugin shape**: two static panels `Panels() = {tree:Weight 2, list:Weight 7}` (≈ the old `leftWidth ≈ 2·w/9` split). `ViewPanel(id, inner Region)` renders each panel body into the frame-computed INNER region (no panel border drawn by the plugin). **Framework padding delta**: `Frame.renderBody` adds `Padding(0,1)` inside the border, so plugin inner width = **outer − 4** (vs the old cmdbrowser inner = frame − 2); every width threshold was recomputed against framework inner widths — `treeCountsMinWidth=18` (tree counts, `tree_render.go`) and `listBadgesMinWidth=74` (list badges/param counts, `plugin.go`), the inner widths at terminal 99–100. The `−4` delta is a **framework-wide** rule, not a cmdbrowser detail: any new plugin must recompute its width buckets against the frame's INNER width (see the Geometry bullet under `internal/core/ui/tui/`), because sizing against terminal/outer width overshoots by exactly the 2 border + 2 padding columns.
  - **Actions are per-mode** (`actions.go`, `browser.Actions(reg)` / `HandleAction`): always `nav.*`/`select`/`filter` (`/`)/`inspect` (`i`); in `ModeRun` ALSO `cmd.skip-confirm` (`y`, toggles `skipConfirm`) and `cmd.force-form` (`e`, sets `ForceParamForm` + selects) — registered in NEITHER `ModeEdit`/`ModeInspect`, so they vanish from the `?`-modal help automatically. Tab/?/q/esc stay framework built-ins. List-panel `nav.left`/`nav.right` are **no-ops** (panel switching is Tab/click-only; the legacy left-arrow "return to tree" affordance is dropped). Each `Binding.Section` (`Navigation`/`Panels`/`Actions`/`General`) groups the help modal.
  - **Inline filter** = plugin-internal capture mode (Decision 3, `filter.go` + `*browser` filter methods in `plugin.go`): `CapturingInput()` returns true iff `b.filter != nil`; `Frame` then forwards raw keys (printables extend the query, action-letters are TYPED not dispatched), the query line renders in the tree panel, the tree filters live (`applyAutoCollapse` + match counts); `esc` restores the snapshot, `enter` commits + focuses the nearest visible ancestor. No dimming overlay.
  - **Inspect** = `CapturesInput` overlay (Decision 4, `inspect.go` + `*browser` inspect methods): `inspect` action builds the viewport via `Item.Inspect(width)`, `PendingOverlay()` returns `Overlay{Content, Width, Height, CapturesInput:true}` once per open (`inspectPending` blocks double-push); navigation scrolls the viewport, `enter` selects (`Result{Idx, Action}` + `tea.Quit`), `esc` closes (Frame-side via `routeWhileCapturing`) — the Frame then forwards a `tui.OverlayClosedMsg` which `Update` handles by clearing `b.inspect`/`inspectPending`, so a later unmatched raw key (forwarded raw to `Update` in normal mode) cannot re-mark it pending and resurrect the closed modal. A proportional scrollbar is overdrawn onto the box's right border when the body overflows the viewport (`inspectState.applyScrollbar`, mirroring the docs-browser bar). Filter (no-overlay branch) and inspect (modal-open branch) are mutually exclusive. The bar is the shared framework helper `tui.OverlayScrollbar(box, yOffset, totalLines)` (glyphs `tui.OverlayScrollbarThumbGlyph`/`OverlayScrollbarTrackGlyph`) — docstui's error overlay calls the same function, so this is one implementation, not two mirrored copies; do not re-roll a per-package variant.
  - **Edit** = in-TUI form overlay (TUI Stage 7, `edit.go` + `Options.Edit *EditSpec`, mirrors `inspectState`): opt-in per Options — `Edit == nil` keeps ModeEdit's exit-and-return commit-and-quit **byte-identical** (goldens unchanged). With `Edit != nil`, `onSelect` (Enter / double-click) in ModeEdit runs `openEdit(idx)` → `EditSpec.BuildForm(idx) (*ask.Form, error)` → `tui.NewFormOverlay(form.Huh(), b.body, {Hint: "enter save · esc cancel"})`, stores `editState{fo, form, idx}`, sets `editPending`, returns `fo.Init()`; a BuildForm error (or nil form) shows an error flash and opens nothing. This is cmdbrowser's `ask` coupling (`edit.go` plus the `EditSpec`/`CommitOutcome` types in `run.go`, whose `BuildForm`/`Commit` signatures reference `*ask.Form`/`ask.Result`) — deliberately accepted so the plugin harvests results via `ask.Result` (the framework `FormOverlay` stays `ask`-free, taking `*huh.Form`). `updateEdit` forwards EVERY message (typed keys AND async blink/next-group ticks) to `fo.Update`, re-marks `editPending`, then polls `fo.State()` — **completion is async** (the Enter Update leaves `StateNormal`; a follow-up `nextGroupMsg` sets `StateCompleted`, so State is polled after every forwarded msg): `StateCompleted` → `commitEdit` runs `EditSpec.Commit(idx, form.Result()) (CommitOutcome, error)` synchronously, replaces `items[idx]` in place + `refreshList()` on success (a var edit never adds/removes leaves — tree shape + cursor untouched), sets the flash, and returns `tea.Batch(requestCloseOverlay(token), flashClearTick)`; a Commit error flashes `✗ …` and still closes; `StateAborted` (ctrl+c never reaches the form — defensive) = cancel. `OverlayClosedMsg` (esc / click-outside) clears `edit`/`editPending` so a later raw key cannot resurrect the closed form. `requestCloseOverlay(token)` returns a `tui.CloseOverlayMsg{Token}` cmd (plugin-initiated pop, no `OverlayClosedMsg` echo; the token — minted per open in `editState.token` and carried on the overlay via `FormOverlayOptions.CloseToken` — makes the Frame ignore a stale close after the overlay was already dismissed). Golden full-frames with the overlay open live in `testdata/frame_editform_{80,99,100}.golden`.
  - **RunForm** = in-TUI param-form overlay (commands-param follow-up, `runform.go` + `Options.RunForm *RunFormSpec`, mirrors `edit.go`) — the **ModeRun** sibling of Edit. Opt-in per Options: `RunForm == nil` keeps ModeRun's exit-then-form behaviour **byte-identical** (existing goldens unchanged; every caller that does not supply one — `DefaultOptions`, tests, the vars browser — is untouched). The terminal action differs fundamentally from Edit: EditSpec is **write-and-stay** (Commit persists `local.yml`, browser stays open, row refreshes); RunFormSpec is **harvest-and-quit** (collect the param values, close the overlay, quit the browser — the command executes AFTER alt-screen teardown because it streams docker/pipeline output to the plain terminal). The overlay-driving plumbing (pending/token handling via `runFormPending`/`editTokenSeq`, `FormOverlay` forwarding, `setStatusFlash`/`flashError`, `requestCloseOverlay`) is shared with the edit machine; only the terminal step differs. `RunFormSpec{BuildForm func(idx int, force bool) (*ask.Form, error); Harvest func(idx int, res ask.Result) map[string]string}` (in `run.go`) — `force` is true when the user pressed the force-form key (`e`), false for plain Enter, so the CLI can auto-skip the form when all required params are satisfied. Three entry points route through `openRunForm(idx, force)` when `RunForm != nil`: `onSelect` (Enter on a list item, `force=false`), `onForceForm` (`e`, `force=true`), and `updateInspect`'s Enter (`force=false`, from inside the inspect overlay — `ActionInspect` is registered unconditionally so inspect is reachable in ModeRun; the inspect state is retired only when a form actually opened or immediate-run was chosen, a BuildForm error keeps inspect valid). `openRunForm` calls `RunForm.BuildForm`: a BuildForm error → `setStatusFlash(flashError(...))`, opens nothing; a **nil form OR an empty `ask.Form` whose `Huh()` is nil** (`ask.Build` returns `&Form{empty:true}` with a nil huh form for zero fields — hosting it would be an inert cancel-only overlay that never completes, trapping the user) → commits `Result{Action: ActionRun, ForceParamForm: force}` with NO `Values` and `tea.Quit`, so the orchestrator recomputes the identical prefilled from the same `--set` and runs exactly as the exit-and-run path (byte-identical); else wraps in `tui.NewFormOverlay(form.Huh(), b.body, FormOverlayOptions{Hint: "enter run · esc cancel", CloseToken: token, MaxHeight: b.body.Height})` (MaxHeight bounds multi-field forms to scroll inside huh), stores `runFormState{fo, form, idx, force, token}`, sets `runFormPending`, returns `fo.Init()`. `updateRunForm` mirrors `updateEdit` — `OverlayClosedMsg` (esc / click-outside) clears run-form state so a later raw key cannot resurrect the form; else forward to `fo.Update`, re-mark pending, poll `fo.State()`: `StateCompleted` → `finishRunForm` harvests `Result.Values = RunForm.Harvest(idx, form.Result())`, sets `Result{Idx, Action: ActionRun, SkipConfirm, ForceParamForm, Values}`, returns `tea.Batch(requestCloseOverlay(token), tea.Quit)`; `StateAborted` (ctrl+c never reaches the form — defensive) = cancel. `Update` routing AND `PendingOverlay` give `runForm != nil` **priority over `inspect`** (mirrors the `edit != nil` precedence) so an open run-form and a reachable inspect overlay never cross-route. Golden full-frames with the param-form overlay open (single- AND multi-field, to exercise the height bound) live at 80/99/100 × 24.
  - **Status flash** (TUI Stage 7, docstui pattern, `edit.go`): `setStatusFlash(text)` sets `b.flash`, bumps `b.flashGen`, and returns a `statusFlashDuration=2s` tick emitting `statusFlashClearMsg{gen}`; `StatusContext()` returns the flash text while set (overriding the breadcrumb), and a stale-gen clear tick is ignored so a newer flash is never wiped. `✓ …` (success, built by the caller's Commit closure so it carries the value) / `✗ …` (error, `flashError`), single-line.
  - **Mouse** (Decision 7): `Update` handles `tui.PanelClickMsg` (single click moves tree cursor / list selection, no run/toggle), `tui.FocusChangedMsg` (tracks the active panel `b.active` for nav/scroll routing); wheel arrives as `tui.WheelMsg{Panel,Delta}` via `Update` (pointer-routed by hit-zone, dispatched directly by the frame — not through `HandleAction`/`nav.*`); double-click → `select` (toggle group / run list item).
  - **Status line** middle = breadcrumb of the focused tree group + `[--yes ON]` when `skipConfirm` (Decision 6, `StatusContext()` called every render). The breadcrumb noun (`command`/`commands` vs `var`/`vars`, `itemNoun`) stays **hardcoded English** — not translated this pilot. **Additive `ActionEdit`/`ModeEdit`** power the `dwe vars` browser: in `ModeEdit` Enter returns `ActionEdit` and the noun is "var"; the ModeRun-only `e`/`ForceParamForm` binding is byte-identical and untouched (regression-pinned). `cli/vars/browser.go` maps vars → `Item` (`ID = vars.<path>`, `Description = effective value`, `Type = layer-source badge`, `Inspect` closure). There is no key-hint footer to adapt: post-Stage-3 the `dwe commands` chrome is exactly the framework `?`-modal help plus this status line, so a mode-specific binding is advertised by its registration (or absence) in `Actions(reg)`, never by a rendered footer row.
  - **Run rewiring** (`run.go`): `applyDefaults` → empty-items / non-TTY guard → read size → `width < minBrowserWidth(80)` or `height < minBrowserHeight(15)` → `runFallback` (huh selector; the old in-TUI 60–79 single-panel layout is GONE — Variant A) → else `out, err := runTUI(newBrowser(...), tui.RunOptions{Brand, Project, Mouse:true, Translator, Locale})`, `res, _ := out.(Result)`. `runTUI` is a package-local seam (`var runTUI = tui.Run`, swapped in tests). The second `widgets.RunWithPromptHooks` wrap is **dropped** — `tui.Run` owns it now; `tui.Run`'s `widgets.ErrCancelled` maps straight through, and the `ActionUnknown` → `ErrCancelled` guard via `res.Action` stays. **Translator/locale** thread in via new `Options.Translator`/`Options.Locale` (nil-safe → `i18n.NopTranslator`/`"en"`), populated by `cli/command/list.go` + `cli/vars/browser.go`, mapped onto `tui.RunOptions`. Test seams `isTerminalFn` / `terminalSizeFn` / `runSelectorFn` / `runTUI` are package-local function vars. Golden full-frame renders live in `testdata/frame_{run,edit}_{80,99,100}.golden` + `help_{run,edit}.golden` via the exported `tui.RenderFrame`/`tui.BuildHelp` harness.
- `internal/core/ui/statusview/` — view-model types passed from the command layer to renderers (ui must never load YAML or compute hashes): `DeployStatusView`, `DeployStatusRow`, `DeploySummary`, `ConfigDelta` enum (`ok` / `changed` / `missing`), `GitWorkspaceRow`, `DaemonRow`.
- `internal/core/ui/statustui/` — interactive read-only status TUI. **As of TUI Stage 5b this is a `tui.Plugin` (`plugin.go`, `var _ tui.Plugin = (*plugin)(nil)`) on the shared `tui.Frame` — NOT a standalone `tea.Model`** (the legacy `*model`'s `View()`/`Update(tea.Msg)(tea.Model,tea.Cmd)`, `renderTitleBar`, the status-bar chrome, and `keys.go` were deleted in the cutover; the Frame now owns geometry, borders, the status line, `?` help, mouse, alt-screen, and teardown). `Deps` mirrors the fields from `statusContext` that the TUI needs (Cfg, State, Tracked, SvcDeploys, DockerCfg, Topo, TopoStatus, IsRunning, ProjectRoot) plus **`Translator i18n.Translator` and `Locale string`** (Stage 5b addition, threaded into `tui.RunOptions`; nil Translator falls back to `i18n.NopTranslator{}`, empty Locale to `tui.Run`'s default) — a decoupled bundle to avoid an import cycle. `Run(ctx, Deps) error` owns a child context (`runCtx, cancel`), builds a `*model` sized `(0,0)` (the Frame owns geometry; real dimensions arrive via `Plugin.Resize`/`ViewPanel`) and wraps it in `newPlugin(m, cancel)`, then calls the package seam `var runStatusTUI = tui.Run` with `tui.RunOptions{Brand: render.BrandedTitleForConfig(d.Cfg, "Status"), Mouse: true, Translator, Locale}` (`Project` left empty — the whole branded title is the `Brand`); `Close()` (called by `tui.Run` on every exit path) invokes `cancel`, replacing the old `defer cancel()`. Returns `nil` on clean quit, `tui.ErrTooNarrow` unchanged (caller falls back to plain text), and passes through `tui.ErrNotTTY`/panic errors otherwise — `mapRunError` and the old `isTerminalFn`/`terminalSizeFn` seams are gone (`tui.Run` owns the TTY + size gates).
  - **Single panel, tabs as body content** — `Panels()` returns one panel (`panelMain`, Weight 1); tabs are stacked body content (tab strip + shared viewport), not side-by-side Frame panels. `ViewPanel` renders a centered spinner while `m.loading`, else `renderBody`: sizes the viewport to `inner` minus `panelChromeRows=2` (tab-strip + divider rows) via `viewport.SetWidth`/`SetHeight`, then joins tab strip + a muted divider line + `viewport.View()`. Five tabs unchanged: Services (apps+tools+infra), Deploy, Topology, Git, Daemons.
  - **`buildTabs` returns a data snapshot, not rendered strings** — `buildTabs(ctx, d) (tabSnapshot, string)` (`load.go`) runs every Docker-/git-backed probe (`stack.CollectApps`/`CollectTools`/`CollectInfra`, `CollectDeployStatus`, `RenderTopology` — no per-service width, so it stays pre-rendered here — `collectGitWorkspaceFn`, `collectDaemonsFn`) exactly once and packs the results into a `tabSnapshot` struct; it does **not** call any table renderer. `tabsLoadedMsg` carries the `tabSnapshot` (not `anchors` — those are now width-dependent and recomputed at render time). Composing a tab's body — including the per-tab title/warning-prefix/empty-placeholder text that `buildTabs` used to produce — happens in `renderTab(snap tabSnapshot, index, width int) (body string, anchors []int)`, a pure function with no Docker/git probe, called from `renderBody` (`plugin.go`) at the panel's real inner width via `stack.RenderAppsRows`/`RenderToolsRows`/`RenderInfraRows`/`RenderDeployStatusRows` and `render.GitWorkspaceAt`/`stack.RenderDaemonsAt` (the width-parameterized Task 9 render halves — see the `render/` and `stack` entries). This is why the same snapshot can be re-rendered at a new width (a resize) without re-probing Docker or git. `renderActiveTab` (`plugin.go`) memoises the `renderTab` result on the key tuple `(m.loadGen, m.active, width)` in `m.renderCache*`, so repeated `View()` calls with none of the three changed reuse the previous render — a tab switch changes `active` and a terminal resize changes `width`. **The key tuple alone does NOT cover a reload**: `ActionReload` bumps `loadGen` when the reload *starts*, and the spinner keeps ticking during the load, so at least one frame renders the pre-reload snapshot and caches it under the already-bumped gen; `Update`'s `tabsLoadedMsg` branch therefore clears `renderCacheValid` explicitly when the new snapshot actually lands. Without that the reloaded body never reaches the screen until a tab switch or a resize.
  - **Reload/`YOffset` preserved verbatim** — `Plugin.Update`'s `tabsLoadedMsg` handling (stale-`gen` drop, **snapshot** assign + `renderCacheValid = false`, `loadedAt`/`healthIndicator`, `YOffset` restore when `reloadGen==gen && reloadActive==active` else `GotoTop`) and `spinner.TickMsg` are byte-identical to the pre-migration `model.Update`, per the migration plan's single most important invariant. The plugin's OWN `WindowSizeMsg` case is a no-op (`return nil`) — unlike the deleted legacy handler, sizing is owned entirely by `Resize`/`ViewPanel`, never recomputed from raw terminal dimensions. Unmatched messages delegate to `viewport.Update` for scroll.
  - **Actions** (`actions.go`) — plugin-local **Tabs** section: `tab.prev` (`shift+tab`/`left`/`h`), `tab.next` (`tab`/`right`/`l`), `tab.1`..`tab.5` (jump); a **Navigation** section: `section.next` (`]`), `section.prev` (`[`); plus stdlib `tui.ActionReload` (`ctrl+r`, registered first). **`tab`/`shift+tab` are rebound to switch tabs, NOT the framework focus built-ins**: because this is a single-panel surface the Frame calls `Registry.DisableFocusNav()` before the plugin's `Actions` hook (see `internal/core/ui/tui`), stripping the dead `focus.next`/`focus.prev` built-ins (they also vanish from the `?`-modal). Within-tab table jumping lives on `]`/`[`. `HandleAction` operates on the held `m *model`: prev/next wrap via `m.setActiveTab((active±1+n)%n)` where `n` is the fixed `len(tabTitles)` (5, never zero — the count is static, so there is no modulo-by-zero to guard); the `!m.loaded` guard instead blocks navigation before the first `tabsLoadedMsg` has delivered any data; jumps call `m.setActiveTab(k)` directly; `section.next`/`section.prev` call `m.jumpSection(±1)`, which scrolls `viewport` to the next/previous sub-table anchor of the active tab (clamped at the ends, no wrap; no-op for a tab with `<2` anchors). Anchors are per-tab 0-based line offsets held in `model.sectionAnchors` (aligned with `tabTitles`), computed by `renderTab` (via `joinSectionsWithAnchors`, specifically `renderServicesTab`) at render time — width-dependent since a shrink can push a sub-table into record mode and change its line count — only **Services** stacks multiple sub-tables (Apps/Tools/Infra) today; the rest are single-section (nil). Reload re-runs the existing `loadGen++`/`reloadActive`/`reloadYOffset`/`reloadGen`/`reloading` sequence + `buildTabsCmd`.
  - **Branding** (`run.go`) — the status-line left zone is composed exactly like the other TUIs, through the **single shared helper** `render.BrandedTitleForConfig(d.Cfg, "Status")` (`{▪} DWE · <project> · Status`) passed whole as `RunOptions.Brand`, with `Project` left empty. The helper takes the **config** (not a name string), so it always uses the bare display name `cfg.Project.Name` — the earlier bug fed `sc.ProjectName` (the *compose* project name `dwe_tbm`) into the brand while docs/commands used `tbm`; routing all three TUIs through `BrandedTitleForConfig` makes that drift impossible. `statustui.Deps` therefore has **no `ProjectName` field** — the brand derives from `Deps.Cfg`.
  - **Mouse** (`mouse.go`) — `handlePanelClick` acts only on `Y==0` (tab-strip row); `tabAt(x)` maps a panel-local X column to a tab index via `tabHitZones()`, which mirrors `renderTabStrip`'s layout exactly (1-col leading pad, `▌ ▐` decorations on the active tab, 3-space inter-tab gaps) so click hit-zones match what is drawn — clicks past the last tab or on the leading pad return `(-1, false)` (no-op); `Y>0` clicks (viewport body) are no-ops (status rows are not selectable). `handleWheel` scrolls `viewport` by `Delta * wheelViewportStep(2)` lines via `ScrollUp`/`ScrollDown`; a wheel turn never changes focus.
  - **StatusContext** returns the middle status-line segment (health indicator + "loaded X ago", or a loading/reloading state) — the old `renderStatusBar` `leftParts` minus the help text, which the Frame now supplies itself on the right. Recomputed every render.
  - The dispatcher in `internal/cli/status/status.go` uses `shouldUseTUI(noTUI, noFlags)` which short-circuits to plain when `--no-tui`, any `--no-<section>`, `TERM=dumb`, or non-TTY; on `errors.Is(err, tui.ErrTooNarrow)` from `runStatusTUIFn` it now also falls back to `renderDefaultStatus` (plain text), mirroring cmdbrowser's narrow-terminal fallback. Tests mutating package-level seams MUST NOT call `t.Parallel()`. `ViewPanel` must remain a pure render function — mutation belongs in `Update` handlers. That fallback is deliberately a **third** narrow-terminal shape rather than a copy of cmdbrowser's: statustui already owned a complete non-TUI text rendering path (`renderDefaultStatus`), so falling back to it costs nothing, whereas cmdbrowser must synthesise a huh selector and docstui — having no text path — can only error.
- `internal/core/ui/tui/` — **shared full-screen TUI framework (registry/keymap/overlay-input surface locked in Stage 1; mouse layer wired in Stage 2; first real consumer + two `Plugin`/Frame revisions landed in Stage 3).** The reusable chrome/layout/input/overlay layer the three full-screen TUIs (cmdbrowser, docs browser, statustui) migrated onto across Stages 3–5b. **First production importer (Stage 3): `internal/core/ui/cmdbrowser/`** (the command/vars browser pilot) — see its entry above; docs browser migrated in Stage 4 (`internal/core/ui/docstui/` — see its entry above); statustui migrated in Stage 5b (`internal/core/ui/statustui/` — see its entry above), completing all three consumers. It depends on `core/ui/styles`, `core/ui/widgets` (for the `RunWithPromptHooks`/`ErrCancelled` exit wrapper around the full-screen program) + the charm.land v2 stack (`bubbletea/v2`, `bubbles/v2`, `lipgloss/v2` — **never** v1 `github.com/charmbracelet/lipgloss`; the v1 that shows in `go list -deps` is transitive via `core/ui/styles`, which exposes raw color-string accessors). **Locked surface** (Stage 1): `Binding`/`Action`/`Registry`, stdlib shared actions (`RegisterStandard`), `Aliases` semantics (dispatch + hidden from help), `CapturesInput` overlay contract, and the i18n namespace (`tui.help.section.<id>`, `tui.help.action.<id>`, `tui.help.title`) — **now in live use as of Stage 3**: real YAML keys ship in the built-in en bundle (`internal/shared/i18n/translations/en.yml`, allowlisted in `KnownUIKeys`) and the help modal renders localized section/action strings. **Mouse layer** (Stage 2, all in `frame.go` / `hittest.go` / `registry.go` / `actions.go`): (1) **CellMotion enable gate** — `View` sets `tea.MouseModeCellMotion` when `f.opts.mouse && mouseCapable()` (`TERM != "dumb"`), else `tea.MouseModeNone`; per-program opt-in via `RunOptions.Mouse`; (2) **`Registry.MatchMouse(event string) (Action, bool)`** — scans registered bindings for `Binding.Mouse == event`; after the Stage 4 wheel overhaul `standardBindings` populate `Mouse` only for `ActionSelect` (`"double-click"`); `ActionNavUp`/`ActionNavDown` no longer carry a `Mouse` field (`"wheel-up"`/`"wheel-down"` are no longer valid registrable events); `"click"` is intentionally never registered (frame-owned); (3) **`classifyHit` Region-math hit-test** — pure `classifyHit(geo Geometry, panels []panelRect, helpHint Region, ov *Overlay, x, y int) (hitZone, PanelID)` over `Geometry` + panel outer regions + help-hint region + active overlay's centred bounds, returning a `hitZone` (`zoneNone`/`zonePanel`/`zoneHelpHint`/`zoneModal`/`zoneOutsideModal`); uses Region math, NOT `lipgloss.Compositor.Hit` (the compositor cannot classify panels or the help-hint zone; Region math is sufficient and already needed); (4) **Pointer-routed `WheelMsg`** (Stage 4 routing; the Stage-4b coalescer described under Frame below re-introduced the accumulator, so this paragraph's *routing* claims hold but its "synchronous, one message per raw notch" claim does not) — `handleMouse` is now only the fallback dispatch path for a `tea.MouseWheelMsg` that the input filter let through. Routing: when the top overlay `CapturesInput` the raw `tea.MouseWheelMsg` is forwarded to `plugin.Update` and `refreshCapturingOverlay` swaps in the re-rendered snapshot (mirrors the captured-key path); a non-capturing overlay or an active inline filter (`plugin.CapturingInput()`) swallows the wheel; otherwise `classifyHit` classifies the pointer position — a `zonePanel` hit forwards `WheelMsg{Panel: id, Delta: ±1}` (`Delta` −1 = up, +1 = down) to `plugin.Update` **without touching focus**; `zoneHelpHint`/`zoneNone`/horizontal wheel are swallowed. The plugin owns the per-panel scroll amount (`WheelMsg.Delta` is the signed notch count ±1; viewport panels multiply by their own step; tree/list panels move one cursor row per notch). `double-click` remains the only event dispatched via `MatchMouse`; (5) **Double-click via injected `frameClock`** — `frameClock interface{ now() time.Time }` with production `realClock{time.Now}`; `Frame.clock` defaulted in `newFrame`; `lastClick struct{ id PanelID; x, y int; t time.Time }` scopes the double-click to the same panel + same cell within `doubleClickWindow=400ms`; `!lastClick.t.IsZero()` sentinel (zero `time.Time` is never a valid prior click); cleared in full after the Select fires (triple-click → exactly one Select). **Stage 3 kept `doubleClickWindow=400ms`** as the chosen value (on-device feel sign-off is a Post-Completion item); (6) **Click routing** — `handleMouse` classifies via `classifyHit` first, then acts by zone: `zoneHelpHint` → `ActionHelp`; `zonePanel` → `focus.Set` + double-click test; `zoneNone`/status → swallow; overlay open → `zoneModal` (inside) swallowed (body never acts behind it) while `zoneOutsideModal` **dismisses the overlay** via `dismissTopOverlay` (click-away-to-close, mirroring `esc`; a capturing overlay is also notified via `OverlayClosedMsg`); `tea.MouseReleaseMsg`/`MouseMotionMsg` ignored; `panelLocal(outer Region, x, y int) (lx, ly int)` helper (pure, unit-tested) translates absolute click to panel-inner-local coordinates. **Superseded by the Stage-4b wheel coalescer (see Frame below):** item (4)'s "immediate / synchronous / one message per raw notch" description no longer holds — `tea.WithFilter(wheelFilter)` drops raw notches and `flushWheelAccum` emits ONE `WheelMsg` per panel per trailing tick, so `WheelMsg.Delta` is the NET coalesced notch count (±N), never guaranteed ±1, and the `wheelAccum`/`wheelTickArmed`/`wheelArmMsg`/`wheelFlushMsg` machinery is back (in `filterWheel`, not `handleMouse`). Only the routing claims of item (4) — pointer-by-hit-zone, focus untouched, plugin owns the step, `MatchMouse` uninvolved — survive unchanged.
  - **Stage 3 revisions (two, sanctioned by spec § 7 "one revision back")** — (1) **Unified input-capture**: the new `Plugin.CapturingInput() bool` method (see Plugin below) lets a plugin take raw input *without* an overlay — `Frame.handleKey` adds a capture branch BEFORE the registry match (`f.overlay.Empty() && f.plugin.CapturingInput()` → forward the key to `plugin.Update`, only `ctrl+c` → `tea.Quit`), so the cmdbrowser inline filter takes printables live with no dimming overlay; for `CapturesInput` overlays the modal-open branch now routes via `routeWhileCapturing` (swallow→`plugin.Update`, hardQuit→`tea.Quit`, close→`overlay.Pop()`), the Stage-1 helper finally wired in. (2) **Translator/locale wiring**: `RunOptions` gained `Translator i18n.Translator` + `Locale string`, mapped by `Run` to new `withTranslator`/`withLocale` frameOptions (nil/zero → `NopTranslator`/fixed locale), so the `?`-modal help renders localized strings instead of Stage 0's hard-coded `NopTranslator`. Also new in Stage 3: exported framework messages **`PanelClickMsg{Panel PanelID; X, Y int}`** (forwarded to `plugin.Update` on a single content-area panel click — border clicks set focus but emit NO message, avoiding negative local coords) and **`FocusChangedMsg{Panel PanelID}`** (forwarded on EVERY focus change — Tab/Shift+Tab built-ins AND click — since `handleBuiltin` focus branches never otherwise reach the plugin; mandatory so the plugin can route nav to the right widget). **Cross-package test harness**: `testsupport.go` exports `RenderFrame(p Plugin, opts RunOptions, w, h int) (string, error)` + `BuildHelp(p Plugin, tr i18n.Translator, locale string, w, h int) (Overlay, error)` for consumers (e.g. cmdbrowser goldens) that cannot reach the package-private `*_test.go` helpers. **Stage 4** migrated the docs browser (`docstui` — see its entry above); **Stage 5b** migrated statustui (see its entry above), completing all three `Plugin` consumers. The `Plugin` interface is **frozen** post-`CapturingInput` (the one deliberate Stage-3 contract addition). See `docs/internals/tui-keymap.md` for the full action taxonomy, default bindings, mouse vocabulary, and i18n namespace.
  - **Frame** (`frame.go`) — the bubbletea `tea.Model`, constructed via unexported `newFrame(Plugin, ...frameOption) (*Frame, error)`. Holds the plugin, registry, focus manager, overlay stack, geometry, and brand/project strings. `newFrame` **validates before launch** and errors on a duplicate action/key (`plugin.Actions(registry)`) **or** invalid panel layout (`Panels()` empty / any non-positive `Weight`) — these never fail at `View`. `Update`: `tea.WindowSizeMsg` → recompute geometry + `plugin.Resize(body)`; **modal-input policy** — while an overlay is open, key msgs are framework-handled only (help-close/quit) and plugin action keys are **swallowed** (no acting behind the modal); with no overlay, keys → `Registry.Match` → built-in (help/focus/quit) handled by the framework else `plugin.HandleAction`, and if unhandled the raw msg forwards to `plugin.Update`. **All non-key msgs always forward to `plugin.Update`** (async preservation, even with help open); `PendingOverlay()` is drained after both a `HandleAction` and a `plugin.Update`; the plugin's returned command is forwarded directly (the framework has no command of its own to batch this stage). **Stage 3 input-capture branches** in `handleKey`: (a) **no-overlay capture** — when `f.overlay.Empty() && f.plugin.CapturingInput()`, the registry is bypassed and the raw key forwards to `plugin.Update` (only `ctrl+c` → `tea.Quit`); placed BEFORE the registry-match block so a capturing plugin (cmdbrowser inline filter) takes printables/action-letters raw; (b) **capturing overlay** — when `Top().CapturesInput`, route via the now-wired `routeWhileCapturing(msg) captureDecision` (small enum: `captureSwallowToPlugin`→`plugin.Update`, `captureHardQuit`→`tea.Quit`, `captureClose`→`overlay.Pop()`+reset `lastClick`+forward `OverlayClosedMsg{}` to `plugin.Update` so the plugin clears the state that produced the overlay — see Plugin); non-capturing overlays keep the esc/?/q-only policy. `View() tea.View` owns the terminal envelope: sets `AltScreen = true` (a `tea.View` field in v2, **not** a program option) and sets `MouseMode` to `tea.MouseModeCellMotion` when `f.opts.mouse && f.mouseCapable()` (CellMotion = click + wheel, no motion spam), else `tea.MouseModeNone`. Plugins return panel **strings** only — never `tea.View`. The status line (brand+project · plugin `StatusContext()` · `? help`, width-aware truncation) is composed **outside** `Composite` so it is structurally never dimmed. **Wheel-coalescing input filter** (`tea.WithFilter(wheelFilter)` installed by `Run` via `buildProgramOptions`; `wheelCoalesceInterval=time.Second/60`): the load-bearing flood-control. bubbletea v2 reads input FIFO from an UNBUFFERED `p.msgs` channel and calls `model.View()` **once per message** (`tea.go` eventLoop), so admitting every raw wheel notch as its own message means a terminal/trackpad flood (macOS momentum scroll emits notches for ~1-2s after the fingers lift) drains FIFO before any later key/click is even seen — the "freeze after stopping" + "can't interrupt", **independent of diagrams**. A WithFilter runs in the event loop BEFORE Update/View: `Frame.filterWheel` accumulates vertical panel wheels into `wheelAccum[PanelID]` and **drops the raw notch (`return nil` → NO Update, NO View)** — a **trailing debounce**: the first notch of an idle burst returns a `wheelArmMsg` so `Update` schedules ONE `wheelFlushMsg` tick (`wheelTickArmed`-gated) `wheelCoalesceInterval` out; when it fires `flushWheelAccum` drains the **net** accumulated delta into one `WheelMsg{Panel, Delta}` per panel. The backlog drains at filter speed (µs each) and an interrupting key/click is reached immediately. **Trailing, not leading**: a leading-edge throttle left a stale partial delta that a later opposite-direction notch combined with and scrolled the WRONG way (codex review) — the net-delta flush keeps direction correct and never drops the tail. Any `tea.KeyPressMsg`/`tea.MouseClickMsg` first calls `clearWheelAccum()` (drop pending scroll so the interrupt wins) then passes through. Non-coalescable wheels — horizontal (`wheelButtonDelta==0`), outside a panel (`classifyHit != zonePanel`), or while a NON-capturing overlay is open / the inline filter is capturing — pass through unchanged (a capturing overlay's vertical wheel IS coalesced, under the `OverlayWheelPanel` sentinel) so `handleMouse` still forwards the raw `tea.MouseWheelMsg` to a capturing overlay's embedded viewport. **`WheelMsg.Delta` is now the coalesced notch count (±N)**: the plugin applies the magnitude (`ScrollBy(Delta*step)`; tree moves `abs(Delta)` rows). This **replaced an earlier per-`View()` wheel-burst render throttle** — that throttle made `View()` cheap but could not stop the FIFO input backlog (codex's diagnosis), so `View()` is once again plain (rebuilds every call); the filter caps wheel-driven Updates to ≈60/s, which is fine. `handleMouse`'s own `tea.MouseWheelMsg` panel-dispatch branch remains as a fallback (e.g. tests calling `Update` directly bypass the program-level filter). **Stage 3 click/focus delivery**: `handleClick` on a single content-area `zonePanel` click always `focus.Set(id)`, then forwards a `PanelClickMsg{Panel, X, Y}` (panel-local coords via `panelLocal`) to `plugin.Update` — but ONLY when the click is inside the inner content region (border clicks set focus, emit no message — no negative coords); `handleBuiltin` focus branches AND click `focus.Set` both emit `FocusChangedMsg{Panel}` to `plugin.Update` (the plugin cannot learn Tab-driven focus otherwise). Double-click continues via `MatchMouse("double-click")` → `HandleAction(ActionSelect)` AFTER the single-click cursor move. **Capturing overlays coalesce too.** The "while an overlay is open … pass through unchanged" clause above is accurate only for a **NON-capturing** overlay (the help modal, which swallows the wheel): when `Top().CapturesInput`, `filterWheel` accumulates the vertical notch under the synthetic sentinel `OverlayWheelPanel` (`const OverlayWheelPanel PanelID = "\x00overlay-wheel"`, `plugin.go`) and the flush delivers `WheelMsg{Panel: OverlayWheelPanel, Delta}` to the plugin, which scrolls its embedded viewport by `Delta` notches — the same freeze fix as body panels, since a momentum flood backing up the FIFO froze the modal exactly like it froze the body. Only **horizontal** (`Delta==0`) overlay wheels still reach `handleMouse` as a raw `tea.MouseWheelMsg`. Consumers do not hand-roll the scroll: `docstui.scrollErrorOverlay` and `cmdbrowser.scrollInspect` are both thin wrappers over `tui.ScrollOverlayViewport(*viewport.Model, delta)` + `tui.OverlayWheelStep`. On flush the two bucket kinds are mutually exclusive (the filter routes to one or the other), so the top overlay's kind selects the follow-up: a capturing top scrolled itself in place → `refreshCapturingOverlay` (`ReplaceTop`, never `Push`); otherwise a panel wheel may have opened a new plugin overlay → `drainOverlay`.
  - **Geometry** (`geometry.go`) — single source of truth for **outer-vs-inner sizing**, border ownership, and the overlay coordinate space, computed once per resize. `Geometry{Term, Outer, Inner, Status, Overlay Region}`; `Region{X, Y, Width, Height}`. **Border ownership rule: the FRAME draws every border; plugins receive INNER dimensions only** (content area inside border + padding) and must never draw the frame border. `Outer` = terminal minus the bottom status line; `Inner` = `Outer` minus border (`borderSize=1`) + padding (`hPadding=1`, `vPadding=0`) per side; `Overlay` = the inner body region, so modals **never** cover the status line. `tooNarrow(w, h)` (below `minWidth=40` / `minHeight=10`) drives the launch fallback. `layoutPanels(body, weights) []Region` splits the body horizontally by weight with a **deterministic remainder policy** — widths sum **exactly** to the body width (last panel absorbs the remainder, mirroring cmdbrowser's `right = total − left`; never naive `w*weight/sum`, which leaks a column at odd widths like 79/99). It is pure with **no error path**: positive-non-empty weights are a documented programmer-error precondition enforced by `newFrame`'s validation, keeping `View`'s hot path branch-free. All chrome constants are provisional. **Consequence for plugin authors** — because `renderBody` adds `Padding(0,1)` inside the border, the region a plugin receives is `outer − 4` columns, not `outer − 2`; every width-bucket threshold inside a plugin must be recomputed against this INNER width (cmdbrowser's `treeCountsMinWidth=18` / `listBadgesMinWidth=74` are the worked example — see its entry above).
  - **Registry** (`registry.go`) — locked (Stage 1) `action → binding + description + section` keymap. `Action` (string id), `Binding{Keys, Desc, Section, Aliases []string, Rebindable bool, Mouse string}` where: **`Aliases`** dispatch (wired into `Register`/`Match` via the same key map, pre-commit collision guard covers aliases too — an alias colliding with any existing key/alias or the binding's own canonical keys is a hard error, no partial mutation) but are **hidden from help** (`buildHelpOverlay` renders `Keys` only — muscle-memory compatibility without cluttering the modal); **`Rebindable`** is metadata-only (no config loader built; future keymap config is sketched in `docs/internals/tui-keymap.md`); **`Mouse`** vocabulary after Stage 4 wheel overhaul: only `"double-click"` is a registrable value (`validMouseEvent` accepts it alone); `"wheel-up"`/`"wheel-down"` are no longer registrable (`validMouseEvent` rejects them — wheel events are dispatched as `WheelMsg`, not through `MatchMouse`); `"click"` is intentionally never registered (frame-owned, so `validMouseEvent` rejects it too); resolved via `Registry.MatchMouse` (wired in Stage 2). `NewRegistry()`, `Register(Action, Binding) error` (pre-commit validation guards duplicate action, duplicate key, and alias collision — no partial mutation), `Match(key) (Action, bool)`, `Binding(Action)`, `Sections() []Section` (ordered, for help generation). **Built-in actions** (auto-registered by `NewRegistry`): `ActionHelp` (`?`), `ActionQuit` (`q`/`ctrl+c` — `esc` is intentionally NOT a quit key/alias: it only ever closes the visible overlay (frame modal-input policy, consulted before the registry) and is a no-op in normal mode, so `esc` never exits the TUI), `ActionFocusNext` (`tab`), `ActionFocusPrev` (`shift+tab`). **`DisableFocusNav()`** removes the two focus built-ins (and their `Navigation` section, via the private `unregister`): `newFrame` and `BuildHelp` call it automatically when the plugin declares **≤1 panel** — focus can never move there, so `tab`/`shift+tab` would be dead keys advertised in help. It runs BEFORE the plugin's `Actions` hook so the freed keys can be rebound (statustui claims them for its within-tab table jump); direct-registry tests must mirror this ordering. **Stdlib shared actions** (opt-in, plugin-handled): `RegisterStandard(reg *Registry, actions ...Action) error` in `actions.go` lets plugins register framework-supplied default bindings for `ActionNavUp`/`ActionNavDown`/`ActionNavLeft`/`ActionNavRight`/`ActionSelect`/`ActionFilter`/`ActionInspect`/`ActionReload`/`ActionTop`/`ActionBottom`/`ActionPageUp`/`ActionPageDown`; `ActionTop`/`ActionBottom` carry dual binds (`["g","home"]`/`["G","end"]`) unifying cmdbrowser-vs-docs divergence with no muscle-memory loss. Stdlib actions are NOT auto-registered; the unexported `standardBinding(Action) (Binding, bool)` accessor is promoted to `StandardBinding` only if a Stage 3 pilot needs the fetch-then-customize path.
  - **Plugin** (`plugin.go`) — the contract, **frozen as of Stage 3** after the one deliberate addition below, proven by a test stub (`stub_test.go`). Methods: lifecycle `Init() tea.Cmd` / `Close() error`; `Resize(body Region)`; `Update(tea.Msg) tea.Cmd`; **per-panel** `ViewPanel(id PanelID, inner Region) string` (replaces a single `View() string` so frame-owned multi-panel layout is expressible — a single-region plugin just declares one panel); `Panels() []Panel` where `Panel{ID PanelID, Title string, Weight int}` (horizontal split weight); `StatusContext() string`; `Actions(*Registry) error` (registration hook, surfaces the duplicate error so `newFrame`/`Run` fail **before** launch) + `HandleAction(Action) (tea.Cmd, bool)` (bool reports whether the plugin handled it; built-in help/focus/quit never reach the plugin); `PendingOverlay() (Overlay, bool)`; `Result() any` (typed plugin result, returned unchanged by `Run` — no wrapper type). **`CapturingInput() bool` (Stage 3 addition)** — reports whether the plugin is currently taking raw input WITHOUT an overlay (the cmdbrowser inline filter); while true, `Frame` suspends registry dispatch and forwards keys to `Update`, reserving only `ctrl+c`. This is the one revision the Stage-3 pilot fed back before the interface froze; every concrete plugin and the test stubs implement it (default `false`). Shared value types `PanelID`, `Panel`, `Overlay{Content string, Width, Height int, CapturesInput, ReleaseMouse, FullScreen bool, CloseToken int}` are defined **here** (Go has no forward declarations); `CapturesInput bool` on `Overlay` signals that raw input (incl. printables) routes to the plugin, the registry is bypassed, and only `ctrl+c` (hard-quit) + `esc` (close overlay) remain live — `?` does not open help while a capturing overlay is `Top()`. The exported framework messages `PanelClickMsg{Panel PanelID; X, Y int}`, `FocusChangedMsg{Panel PanelID}`, `OverlayClosedMsg{}`, and **`WheelMsg{Panel PanelID; Delta int}`** (forwarded to `Update` — see Frame) also live here. `WheelMsg` is pointer-routed by hit-zone, NOT by focus, and the plugin decides the per-panel scroll amount; post-Stage-4b it is emitted by the coalescer's trailing flush rather than per raw notch, so `Delta` is the NET notch count (±N), not ±1; it is never dispatched through `MatchMouse`. `OverlayClosedMsg` fires when a `CapturesInput` overlay is dismissed via esc (the `captureClose` path) **or** by a click outside the modal — both funnel through the single emitter `dismissTopOverlay` — those overlays are always plugin-pushed, so it never false-fires on the framework-owned help modal; it is the ONLY notification a plugin gets that its overlay was popped (the cmdbrowser clears `b.inspect`/`inspectPending` on it, preventing a later unmatched raw key from resurrecting the closed inspect modal). The overlay manager adds methods over this type, never a second definition. **Two completions to the message contract.** (a) These four exported messages are the **only** channel by which mouse, focus, overlay-close, and wheel activity reach a plugin — a plugin never receives raw `tea.MouseClickMsg`/focus events for them, and adding `WheelMsg` did **not** extend the frozen interface (it is a `tea.Msg` value routed through the existing `Update`, not a new method). (b) Post-Stage-4b, `WheelMsg` is emitted by the coalescer's trailing flush rather than per raw notch, and `Delta` is the NET coalesced count (±N) — the `±1` above is the pre-coalescer contract; a plugin that ignores the magnitude under-scrolls every burst (viewport panels apply `ScrollBy(Delta*step)`, tree panels move `abs(Delta)` rows). The plugin side of the routing rule is an obligation, not just a description: a plugin **must not** change focus in response to a `WheelMsg` — pointer-routed scroll must never steal keyboard focus.
  - **Overlay manager** (`overlay.go`) — `overlayStack` over the `Overlay` type with `Push`/`Pop`/`Top`/`Empty` enforcing **mutual exclusivity** (one visible modal). `Composite(base, ov, body Region) string` centres the overlay over the body via lipgloss v2's `Compositor` (base layer + centred overlay layer — adopted over hand-rolled ANSI cell math per the decision recorded in `overlay.go`). **Dimming applies to the body region only** (re-rendered through a muted style before the overlay layer is placed); `Composite` receives only the body string, so it **structurally cannot touch the status row**. The click-outside-modal policy (a click outside dismisses the overlay, mirroring `esc`; a click inside is swallowed) is a documented constant (`overlayClickOutsideDismisses`) + a single hit-test seam — no bespoke zone classifier.
  - **`Overlay.ReleaseMouse` + `Overlay.FullScreen`** (native-selection support, `plugin.go` + `frame.go`) — two orthogonal opt-in flags a top overlay may set. **`ReleaseMouse`** forces `Frame.View` to emit `tea.MouseModeNone` regardless of `opts.mouse`, handing the mouse back to the terminal so the user can natively drag-select and copy the overlay's text; it reverts automatically when the overlay pops. The tradeoff is deliberate and must not be "fixed": with the mouse released the framework receives no click/wheel at all, so click-outside-to-close and overlay wheel-scroll stop working for as long as it is set (keyboard scroll and `esc` still work). It composes with `CapturesInput` — keys still route to the plugin, only mouse reporting flips — and a plugin toggles it by republishing the overlay (`ReplaceTop`) with the flag flipped. **`FullScreen`** makes `View` short-circuit to `fullScreenOverlayView`, rendering the overlay as the ENTIRE terminal (no body panels, no borders, no status line, content padded/clamped to `geo.Term`) instead of compositing it centred over the inner body. It exists because **the terminal owns native selection and cannot be clipped to a sub-rectangle**: with `ReleaseMouse` alone a drag-select would sweep in the frame's border columns and status row, so the only way to make the selection contain exactly the overlay is to leave nothing else on screen. The overlay MUST size its own `Content` to the full terminal (the Frame's pad/clamp is a safety net, not a layout engine). The two flags are orthogonal, but docstui's render-error overlay — the sole consumer — sets both in selection mode.
  - **`OverlayScrollbar` / `ScrollOverlayViewport`** (`overlay_scrollbar.go`) — the shared chrome for capturing viewport overlays, so cmdbrowser inspect and docstui's error modal are one implementation rather than two mirrored copies: `OverlayScrollbar(box, yOffset, totalLines) string` overdraws a proportional thumb/track onto the box's right rounded border (glyphs `OverlayScrollbarThumbGlyph` `█` / `OverlayScrollbarTrackGlyph` `░`), and `ScrollOverlayViewport(vp *viewport.Model, delta int)` scrolls by `delta` **coalesced** notches at `OverlayWheelStep=3` lines each (no-op at `delta==0`). Do not re-roll a per-package copy of either.
  - **Capturing-aware `drainOverlay`** (TUI Stage 7, `frame.go`) — the load-bearing async-overlay fix: `drainOverlay` now checks the top overlay before applying a plugin-pending overlay — when `Top().CapturesInput` it `ReplaceTop`s in place (never `Push`), so an async republish (a huh cursor-blink tick, a resize while a form overlay is open) refreshes the snapshot without stacking a duplicate layer that esc would then pop one at a time; otherwise (no overlay / non-capturing top) it `Push`es as before. The check lives in the **function body**, not the ~11 call sites — the only ones reachable while a capturing overlay is Top() are the async branches (default, `WindowSizeMsg`, `FocusRequestMsg`), the rest run with no overlay open so the fold is behaviour-preserving. `refreshCapturingOverlay` stays a distinct entry point for the explicit key/wheel refresh paths (identical behaviour under a capturing top; it must NOT reset the double-click record).
  - **`CloseOverlayMsg{Token}`** (TUI Stage 7, `plugin.go` + `frame.go` `Update` case) — the plugin→framework close primitive (mirror of `FocusRequestMsg`): a plugin returns it as the msg of a `tea.Cmd` and the Frame pops the top overlay + resets the double-click record but does **NOT** emit `OverlayClosedMsg` (plugin-initiated close — the plugin already cleared its state; the `OverlayClosedMsg` echo is only for esc / click-outside dismissals it did not initiate). Needed so a form overlay can close itself on successful submit (Enter); an empty stack is a harmless no-op. **Stale-close guard:** the msg carries the `Overlay.CloseToken` it targets (the plugin stamps a unique non-zero token on the overlay via `FormOverlayOptions.CloseToken` and echoes it here). Because the close travels as a deferred `tea.Cmd`, the user can dismiss the overlay (esc / click-outside) and open a different one (help, inspect) before it is processed; the Frame therefore pops **only** when the current top's `CloseToken` equals `Token`, so a stale request is ignored instead of popping the newer modal. The match is a plain `top.CloseToken == m.Token` with no special case for zero, so an **untargeted** `CloseOverlayMsg{}` (token 0) still pops any top that also carries 0 — which is every framework-owned overlay and every overlay pushed without a token. That is the pre-token behaviour preserved for single-overlay plugins, NOT a no-op and NOT protected against a stale close. cmdbrowser mints tokens from `editTokenSeq` (pre-incremented, first token 1) and passes them through `requestCloseOverlay(token)`. Every framework-owned overlay (the `?`-help modal, and any overlay pushed without a token) leaves `CloseToken` 0, so an untargeted close pops one of those and nothing else — safe only while a plugin can have at most one overlay open. **Any plugin that can open more than one overlay in a session MUST mint a distinct non-zero token per overlay** and thread the same value through BOTH `FormOverlayOptions.CloseToken` and the `CloseOverlayMsg` it later returns; leaving the token 0 reintroduces exactly the stale-close race the guard exists to close.
  - **`FormOverlay`** (TUI Stage 7, `formoverlay.go`) — the reusable embeddable huh-form host (the milestone's second form-host mode, "in-TUI overlay, edit-and-stay"); the framework stays `ask`-free by taking a `*huh.Form` (`NewFormOverlay(form, body Region, FormOverlayOptions{MaxWidth, MaxHeight, Hint, CloseToken})`). Sizing is **host-owned**: `applyWidth` sets the form width to `min(body.Width − formOverlayHChrome(4), MaxWidth (0 ⇒ formOverlayMaxWidth=72))` floored at `formOverlayMinWidth=10` via `form.WithWidth`, re-applied by `Resize`. **`MaxHeight` optional bounded height (commands-param follow-up)** — `MaxHeight == 0` keeps the content-driven behaviour (design decision 1 — a taller-than-body form is truncated lossily by `clampOverlay`; this is the vars single-field path, byte-identical). `MaxHeight > 0` is a content **CAP, not a fixed height** — huh's `Group.WithHeight` sets an EXACT viewport height and the bubbles viewport PADS short content, so a naive `form.WithHeight(body.Height)` would balloon a one-field form into a tall blank modal; the wrapper instead captures the uncapped `naturalHeight = lipgloss.Height(form.View())` ONCE at construction (after `WithWidth`, BEFORE any `WithHeight`; `Form.View` works pre-`Init`) and `applyHeight` clamps from it against `budget = MaxHeight − formOverlayVBorder(2) − hintRows(1 iff Hint != "")`: `budget < natural` → `WithHeight(budget)` (huh scrolls its group viewport to the focused field, `clamped=true`); `budget ≥ natural` AND previously `clamped` → `WithHeight(natural)` (un-clamp — huh's `WithHeight` is one-way, `WithHeight(≤0)` a no-op not a reset, so the restore must be explicit); `budget ≥ natural` never-clamped → **leave the form untouched** (renders content-driven, byte-identical to `MaxHeight == 0`; calling `WithHeight(natural)` here pads a row via huh's group footer). The capped view is NEVER re-measured — every clamp derives from the stored natural height, so a shrink-then-grow `Resize` (via `applySize` = `applyWidth` + `applyHeight`) un-clamps correctly. cmdbrowser's run-form passes `MaxHeight: b.body.Height` (multi-field command forms scroll); vars `openEdit` passes 0. `Init`/`Update` forward to the form (re-asserting `huh.Model` → `*huh.Form`), `Update` **swallows `tea.WindowSizeMsg`** (huh would otherwise auto-size its group height from terminal dims while its own height is 0) and is a no-op post-completion. **Embedding contract** (verified against huh/v2 v2.0.3): `SubmitCmd`/`CancelCmd` are nil when embedded, so completion is detected ONLY by polling `State() huh.FormState` after every forwarded Update; completion is **asynchronous** (the Enter Update leaves `StateNormal` + returns a follow-up cmd → `nextGroupMsg` → a later Update sets `StateCompleted`, so the host must return huh's cmds to bubbletea and re-poll — tests pump the returned cmds before asserting); the bubbles/v2 virtual cursor renders inline in `View()` (no `tea.View.Cursor` plumbing), and its blink arrives as async msgs (why the capturing-aware `drainOverlay` above is load-bearing). `Overlay()` renders the form (plus an optional muted footer hint row) in a rounded-border `Padding(0,1)` box (mirroring inspect), measuring Width/Height from the rendered box, and returns `Overlay{CapturesInput: true}`. A nil form yields an inert wrapper (State=`StateNormal`, empty Overlay) so an upstream BuildForm error never panics the Frame. The follow-up cmd that produces `nextGroupMsg` is huh's own `NextField`. **Consumers build the embedded form with `ask.RunOptions{ShowHelp:false}`** (`cli/vars/browser.go`, `cli/command/list.go`) — note that the flag is set by the CONSUMER at `ask.Build` time, not by `FormOverlay`: huh's built-in help line advertises `ctrl+c = quit the form`, which is wrong under the Frame (there `ctrl+c` hard-quits the whole TUI and never reaches the form at all, so `StateAborted` is defensive only), so it is suppressed and the `Hint` footer row becomes the **single authoritative key hint** for the overlay (the rendered rows are `enter save · esc cancel` for edit and `enter run · esc cancel` for the run form; hardcoded English, per Stage 6's "form chrome i18n is out of scope" decision). Consequently a consumer whose form can exceed the body region must opt into `MaxHeight` **deliberately**: with `MaxHeight == 0` there is no scrolling at all, only lossy `clampOverlay` truncation.
  - **Focus manager** (`focus.go`) + **palette** (`palette.go`) — `focusManager` tracks the active `PanelID` across the plugin's declared panels: `Next()`/`Prev()` (wrap; no-op on zero/one panel), `Set(PanelID) bool` (false on unknown id), `Active() PanelID`, `BorderFor(PanelID) lipgloss.Style` (focused style only for the active panel). `palette.go` is the single styling bridge — `focusedBorder()`/`unfocusedBorder()` build v2 `lipgloss.Style` from `styles.ColorBorder()`/`styles.ColorAccent()` accessors (no v1 styles).
  - **Help** (`help.go`) — `buildHelpOverlay(reg *Registry, tr i18n.Translator, locale string, width, height int) Overlay` renders the registry's sections/bindings into the `?`-modal body (section label · keys · description). The modal is clamped on **both axes** (`MaxWidth`/`MaxHeight`) to the body region passed in — load-bearing because `Composite` does not clip, so an unclamped modal taller than a small-but-permitted terminal (`tooNarrow` only floors height at `minHeight`) would grow the composited frame past the screen and corrupt the alt-screen. `locale` is required because `Translator.T(locale, uiKey, fallback)` takes it; Stage 0 passed `i18n.TranslatorOrNop(nil)` + a fixed locale and deferred real wiring — **Stage 3 wires a real translator/locale** through `RunOptions` → `withTranslator`/`withLocale` so the modal localizes. The framework i18n key namespace (`tui.help.section.<id>`, `tui.help.action.<id>`, `tui.help.title`) is **now in live use as of Stage 3**: real keys ship in the built-in en bundle (`internal/shared/i18n/translations/en.yml`, stored without the `ui.` prefix) and are allowlisted in `KnownUIKeys` (`internal/shared/i18n/known_keys.go`) — kept in exact sync with en.yml (`TestBuiltinCoverage`). en-only in the built-in bundle; ru comes from a workspace overlay or an injected `Store` (the i18n test injects a ru bundle). `select` is deliberately NOT keyed — its label is mode-dependent ("Select"/"Edit") and a single key would flatten the vars-browser relabel. Aliases are intentionally excluded from the rendered help body (`buildHelpOverlay` renders `Binding.Keys` only).
  - **Run** (`run.go`) — the **sole exported entry point**. `RunOptions{Brand, Project string, Mouse bool, Translator i18n.Translator, Locale string (Stage 3), + unexported test seams input/output/isTTY/size}`; `Run(p Plugin, opts RunOptions) (any, error)` returns `Plugin.Result()` **unchanged**. `Translator`/`Locale` map to `withTranslator`/`withLocale` frameOptions (nil/zero → `NopTranslator`/fixed locale); `newGeometry` is unaffected. Builds the frame via `newFrame` and **returns its error before any program start** (duplicate action/key surfaces here, not at runtime). `tea.WithInput`/`tea.WithOutput` are appended **only when each seam is non-nil** — a zero-value `RunOptions` must fall through to default stdin/stdout, because in bubbletea/v2 `WithInput(nil)` *disables* input. **Capability gate before launch**: non-TTY → typed `ErrNotTTY` (no program start, no plugin `Init`); `tooNarrow` → `ErrTooNarrow` fallback sentinel (caller drops to a plain selector; the fallback UI is a later stage). **Teardown**: a named-return surfaces the `Close()` error **only when the program returned no error**, else `_ = plugin.Close()` (errcheck-safe; documented close-error precedence). The actual run routes through the package-private `runProgram` seam (default `(*tea.Program).Run`) so the zero-value-input, mouse-flag, and close-error tests are deterministic without a real event loop. Alt-screen and mouse are **not** program options in v2 — they live on `Frame.View`, fed via `frameOptions` mapped from `RunOptions`.
- `internal/core/ui/tui/tree/` — **generic tree behavioral engine (TUI Stage 4, Plan 2)**, a pure-generic leaf depending on nobody in the project (`maps`/`slices`/`strings` only), consumed by `cmdbrowser` + `docstui`. It replaces the prior "mirror cmdbrowser exactly" duplication (the byte-identical `topIdx`/`ensureFocusVisible`/`focusRow`/`clipToViewport` core + structurally-identical visible-set rebuild and directional collapse/expand) with a **compiler-guaranteed** shared engine. `Engine[N comparable]` owns the *behavior* — visible-row set, cursor, vertical scroll (`topIdx`), a `parent` map, expansion state, directional collapse/expand, navigation, and scroll/clip math; each consumer keeps its own node graph, row rendering, payload, and filter UX. **Rendering is deliberately NOT in the engine (Decision 7, the load-bearing boundary)**: the engine returns `VisibleNodes()` + `Cursor()` + `Clip(full, height)`; consumers render each row themselves because glyphs, counts, truncation, and styles genuinely differ. The engine has **zero filter/query/count concepts** — `RebuildVisible(keep func(N) bool)` takes an optional per-node `keep` predicate (Decision 6: `nil` → all expanded nodes; non-nil → include a node if `keep(node)` OR any descendant is kept, the ancestor-inclusion mechanic) and knows nothing about *why* a node is kept; docs drives its reduce-set filter through `keep`, cmdbrowser passes `nil` and dims/counts externally.
  - **Adapter contract** — a 3-method interface (not closures, for a discoverable/testable contract): `Children(N) []N` (ordered children), `Key(N) string` (**stable, unique id surviving a `SetRoots` rebuild** — expansion + cursor identity are keyed by it), `Expandable(N) bool` (false for leaves / heading rows). cmdbrowser → `N = *treeNode`, `Key = n.id` (dot-path); docs → `N = *TreeNode`, `Key` is **kind-prefixed** to dodge collisions (group → `"group\x00"+root`, file/dir → `"node\x00"+root+"\x00"+path`, heading → `"heading\x00"+root+"\x00"+path+"\x00"+idx`, idx = heading sibling position so duplicate/locale-translated headings stay unique-and-stable).
  - **Map-keying discipline (Decision 8)** — `expanded map[string]bool` is keyed by **`Key` (string)** so it survives a node-graph rebuild; `parent map[N]N` and `byKey map[string]N` are rebuilt per generation in `SetRoots` (pointers valid within one generation, `byKey` gives O(1) cursor re-resolution); `cursor` is stored as **`N`** and re-resolved by `Key` on rebuild. The zero value of `N` (`nil` for a pointer) is the **root/none sentinel** (maps to cmdbrowser's `focusedID == ""`); every nav/collapse/expand/`indexOf`/`EnsureFocusVisible` path is guarded against a nil cursor (behaves as "no focus", no panic).
  - **Expansion keyed by stable `Key` is a bugfix (Decision 3)** — `SetRoots(roots)` re-resolves the cursor by its prior `Key` and, because `expanded` is `Key`-keyed, **preserves expansion across a node-graph rebuild automatically**; if the prior cursor's `Key` is gone it sets the cursor to zero and does **NOT** auto-park (the consumer decides the fallback). This fixes docs losing expansion on locale change.
  - **Cursor re-park is a separate call, never folded into `RebuildVisible` (Decision 4, golden-critical)** — `RebuildVisible` does NOT touch the cursor. `ParkCursorIfHidden()` (docs-only; moves the cursor to `visible[0]` when it falls out of a non-empty visible set) is invoked by docs after every rebuild; cmdbrowser **never** calls it (it tolerates an off-screen cursor and resolves it later via `nearestVisibleAncestor`). Folding park into `RebuildVisible` would flip a cmdbrowser golden. `SetCursorByKey(key) bool` resolves `""`/unknown → zero-value cursor (returns false), known key → that node (returns true).
  - **Bulk/key expansion accessors** — `ExpandedSnapshot()` (copy), `RestoreExpanded(snapshot)`, `SetExpandedByKey(key, b)` exist specifically to back cmdbrowser's filter machinery (`newFilterState`/`applyAutoCollapse`/`restoreExpansion` now route their storage through the engine while the filter logic stays in cmdbrowser); node-based `IsExpanded(n)` / `SetExpanded(n, b)` (docs ancestor-expand). `Clip(full, height)` / `EnsureFocusVisible(height)` / `FocusRow(row)` (click-past-last = no-op) are ported verbatim from the old cmdbrowser logic. Tested against a **fake node type** (`tree_test.go`) so the engine is validated with zero consumer coupling.

## Core — Notify (`internal/core/notify/`)

- `internal/core/notify/` — desktop notifications. `New(cfg) *Notifier` (concrete struct, nil-safe `Notify`) dispatches via an unexported `backend` interface to `noopBackend` or `nativeBackend`. Factory short-circuits to noop on `cfg == nil`, master switch off, empty channels, CI / `DWE_NONINTERACTIVE` / non-TTY (test seam: `isInteractiveForNotify`). `nativeBackend` wraps `osNotify` (test seam, signature `func(title, body string, icon any) error` — `any` mirrors beeep.Notify, lets the embedded `[]byte` icon flow through without temp-file conversion) in a goroutine + non-blocking semaphore + 2s timeout — slot release lives in the inner goroutine so a hung OS notifier can't leak more than one goroutine per process. **Platform split**: `native_other.go` (build `!darwin`) binds `osNotify = beeep.Notify` and sets `beeep.AppName = "DWE"` in `init()`; `native_darwin.go` (build `darwin`) binds `osNotify = darwinNotify`, which dumps the embedded PNG to a temp file and execs `terminal-notifier -title T -message M -group DWE -appIcon <png> -contentImage <png>`, falling back to `osascript` (no icon) on missing/failed `terminal-notifier`. Why not beeep on darwin: modern macOS silently ignores `-appIcon` for terminal-notifier, so beeep's bare `-appIcon <icns>` shows terminal-notifier's bundle icon; we add `-contentImage` so the DWE logo at least renders as a thumbnail in the notification body. `assets/icon.png` is embedded via `//go:embed` (`notificationIcon []byte`) and passed to every notify call. Title format: `✓/✗ DWE · <project>: <op> succeeded/failed` (project segment dropped when empty); body carries duration plus optional truncated error. `Op` (`OpDeploy` / `OpRun` / `OpCommand`) and `Outcome` (`OutcomeSuccess` / `OutcomeFailure`) enums with `*Unknown` at iota 0. `OutcomeFromErr(err)` is shared by all hookpoints. Errors always logged at debug, never returned. Consumers each declare a local `notifier` interface (one method) for testability — they never depend on a public interface.
- **Notifier hookpoints**: `internal/cli/deploy/deploy.go` (deployRunCmd), `internal/core/workflow/lifecycle/RunRun`, and `internal/core/usercommands/runtime/RunCommand`. All three follow the same shape: `start := time.Now()`, `var projectName string`, `userpkg.Load` (parser error → `slog.Warn` + nil cfg, never blocks), `newNotifier(cfg)` (consumer-local seam), defer firing `Notify(context.Background(), ...)` from a named-return `err`. The defer is installed **before** the main config load so a malformed `workspace.yml` still triggers a failure notification. **`SkipNotify` invariants**: `lifecycle.RunContext.SkipNotify` is set by `RunRestart` so the inner `RunRun` does not fire (restart is not a notify event). `runtime.RunContext.SkipNotify` is set transitively at **every** internal invocation site (`runners/workflow/step.go` sequential + `runners/workflow/parallel.go` parallel paths, `pipeline/executor.go` action dispatch) — only the top-level `runCommandByID` orchestrator leaves it at zero-value false. The runtime gate is `rc.Cmd.Notify && !rc.SkipNotify`; the cheap-path check installs the defer before any I/O so pre-run failures still notify. Validator emits an info diagnostic for the static-detectable direct-parallel-sub-step case (`internal/core/validate/commands/notify.go`); the runtime guard handles transitive cases the validator cannot see. `notify: true` on `type: daemon` is a validator error.

## Core — Bridge (`internal/core/bridge/`)

The host-side half of the dev-container bridge (the in-container half is `internal/shared/bridgeclient` + `cmd/dwe-shim`). User-visible behavior: `docs/reference/concepts/bridge.md`. Design references (D1–D12) live in the implementation plan; the load-bearing invariants are restated here.

- `internal/core/bridge/` — daemon + lifecycle + overlay generation. **The daemon is a stateless forwarder** (design D5): one connection = one command, fork `dwe <argv>` per connection, no in-memory cache, no project model — never add business logic here. Files: `daemon.go` (Config + env contract consts `EnvInvokedFrom` / `InvokedFromContainer` / `EnvNonInteractive` / `BindOverrideEnv`; `DefaultBridgeDir(projectRoot)` = `.dwe/bridge`), `listeners.go` (unix `host.sock` 0660 in dir 0700 + TCP `127.0.0.1:0` always; best-effort extra bind on the docker gateway IP via injectable resolver — bind failure on Mac is silent and expected; `port` written AFTER bind; `token` 0600 written once and kept across restarts — stable project identity; `DWE_BRIDGE_BIND` override is comma- or whitespace-separated; **never `0.0.0.0`**), `session.go` (per-accept: auth — constant-time token compare on TCP / peercred on unix — then HELLO validation: protocol version, realpath cwd containment inside `--project-root`, `tty:true` → `tty_unsupported`; 10 s HELLO deadline so half-open connections cannot pile up; SIGNAL allowlist = SIGINT/SIGTERM only; conn close → SIGTERM to the process group, 5 s grace, SIGKILL; `daemon_shutting_down` ERROR for accepts during Close), `exec.go` + `exec_unix.go` / `exec_windows.go` (the injectable `LaunchSpec`/`Process`/`LaunchFunc` seam; production `launchOS` resolves `os.Executable()` **lazily** so an injected launcher never touches it — calling it from a test re-executes the test binary, the documented recursion hazard; Setpgid + group signaling; signal deaths map to 128+sig; launch failure → STDERR + EXIT 127, protocol ERROR frames stay reserved for the D4 code set). **Env override contract (design D7)**: the daemon re-filters the client-sent env through `bridgeclient.StripEnv` (single strip-set source — `DWE_BRIDGE_DIR` / `DWE_BRIDGE_PROJECT` / `DWE_BRIDGE_UNREACHABLE`, `DWE_HOST_WORKSPACE`, `DWE_CONTAINER_WORKSPACE`, any `DWE_PROJECT_ROOT*`), then **also drops execution-hijacking variables** (`subprocessEnv` — `dangerousEnvNames` = `PATH`/`BASH_ENV`/`ENV`/`SHELLOPTS`/`BASHOPTS`/`IFS`, `dangerousEnvPrefixes` = the dynamic-loader families `LD_*` + `DYLD_*`), **replaces the host-identity set with the daemon's own values** (`hostIdentityEnvNames` = `HOME`/`USER`/`LOGNAME`/`TMPDIR`/`SSH_AUTH_SOCK`, `hostIdentityEnvPrefixes` = `DOCKER_*`/`COMPOSE_*`/`XDG_*`; `daemonEnviron` is the injectable seam — a container `HOME` silently sends the host docker CLI to `unix:///var/run/docker.sock`, absent on Docker Desktop / OrbStack macs, because contexts resolve via `$HOME/.docker/config.json`), and **force-sets** `PATH` to the daemon's own host PATH plus `DWE_INVOKED_FROM=container` + `DWE_NONINTERACTIVE=1`. The two `DWE_*` are host-controlled only; client-sent values are dropped, which is what makes the CLI container policy unspoofable. The PATH/loader strip is the security counterpart: the forked host `dwe` resolves `docker`/`git`/`sh` by bare name, so a container-controlled `PATH`/`LD_PRELOAD` would otherwise be host code execution — the command allowlist limits WHICH commands run, this hardening keeps allowlisted ones off attacker binaries. `DWE_INVOKED_FROM` must NOT affect `--output json` shape. `subprocessEnv` and both sets (`dangerousEnvNames`/`dangerousEnvPrefixes`, `hostIdentityEnvNames`/`hostIdentityEnvPrefixes`) live in `session.go` alongside the accept path, and `daemonEnviron` is the test seam. Before exempting any variable a bridged command needs forwarded, confirm it cannot influence process spawning — the strip set is the only thing between a container-writable env and the host `dwe`'s bare-name resolution of `docker`/`git`/`sh`, so an exemption granted for convenience reopens exactly the trust-boundary hole the set exists to close.
- Process lifecycle: `ensure.go` (`Ensure`/`Cycle`/`StopDaemon`/`ProbeDaemon` over a **bridge-private flock** on `.dwe/bridge/daemon.pid` via `shared/lock` — NEVER `lock.AcquireProjectLocks`, this is deliberately not a project lock; Ensure releases the probe flock before spawning — the daemon re-acquires at startup and a spawn-race loser exits cleanly; pidfile mtime is the uptime source, written exactly once by the daemon's own startup acquire), `spawn_unix.go`/`spawn_windows.go` (detached spawn: Setsid + released handle, child stdout/stderr → `daemon.log` so panics land there), `autostop.go` (`RunAutoStop` with injectable `Subscribe`/`CountRunning`: docker events filtered on `com.docker.compose.project=<resolved name>` — the label every other dwe probe matches, via `config.ResolveComposeProjectName`; 60 s `docker ps` poll fallback; 10 s startup grace; zero containers → graceful shutdown removes `host.sock`+`port`, **keeps `token`**; count errors are logged and retried, never fatal; deliberately NO connection-idle timeout — a dead daemon cannot be revived from inside a container).
- `composegen.go` + `prepare.go` — **overlay regeneration model (design D8)**: `.dwe/compose.bridge.yml` is dwe-owned machine state (same model as `.env` and rendered configs), regenerated atomically (same-dir temp + rename, write-if-changed) by the prepare hook on EVERY compose-up command and **DELETED when no enabled service has bridge enabled** — the overlay step ALWAYS runs even when bridge is fully off, so toggles can never leave a stale fragment in the `-f` chain. `RenderOverlay(OverlaySpec)` stays a pure renderer (golden tests); `BuildOverlaySpec` iterates via `config.DeployOrder` (never `range cfg.Services`). Two deliberate deviations from the original design sketch: mount sources are ABSOLUTE host paths (relative compose sources resolve against the FIRST `-f` file's dir, which shifts with `compose.base`), and `DWE_HOST_WORKSPACE`/`DWE_CONTAINER_WORKSPACE` are the per-service hub mapping (`<root>/<svc.dir>` ↔ `svc.dir_internal`) — both omitted when the pair is missing, and the daemon containment check then rejects untranslated cwds (the `validate/bridge` warning). Shim arch per service via `DockerArchResolver` (container inspect `{{.Image}}` → image inspect `{{.Architecture}}`, injectable `CommandOutput` seam; fallback = host arch with a warning — except the expected `No such container` case (first deploy / after reset), which falls back SILENTLY with only a `trace.Decision` `-v` line, since the host arch is correct for native images and re-resolves at the next start — never hardcoded amd64). **Container names for the arch probe are derived with `config.ResolveComposeProjectName(baseDir, cfg)`** — docker.yml's `project_name` when set, NOT `Project.FullName()` (the standing compose-bypass rule); `spec.Project` / `DWE_BRIDGE_PROJECT` stays `FullName()` — it is diagnostic identity, not a container prefix. The overlay also injects `DWE_BRIDGE_SERVICE: <OverlayService.Key>` per service — the workspace services map key (NOT `.Name`, the compose container name), because per-command `bridge.services` lists match workspace keys. Chain position: `composeFiles()` inserts `config.BridgeOverlayRelPath` after app overlays, BEFORE local.yml overlays (local keeps the last word); existence is stat'd at CALL time, not load time — the prepare hook regenerates/deletes the file after `LoadConfig` and before compose args are built. `Prepare(PrepareOptions)` = overlay regenerate-or-delete (always) + shim materialize + daemon Ensure-or-Cycle (`CycleDaemon: true` for `deploy run` and whole-stack restart — cycle REPLACES ensure, never runs in addition); call sites: `lifecycle.RunRun` (after the deploy gate AND post-pull reload, before phases) and `deploy.RunHelper` (after locks). `AnyBridgeEnabled(cfg)` gates daemon-touching steps and the best-effort ensure in top-level `dwe status`. **The bridge is strictly opt-in**: `BridgeEnabledExplicit` (in `project/config/`) defaults to OFF for every service type — it deliberately does NOT share the render toggles' app-default-on shape (`renderEnabledExplicit`), because the bridge mounts a host-controlled binary into the container and opens a container→host command channel.
- `internal/core/bridge/shimassets/` — embedded shim binaries + materialization. `//go:embed all:bin` with a COMMITTED `bin/.gitkeep` placeholder: the shims themselves are gitignored build artifacts (`make shims` cross-compiles `cmd/dwe-shim` for linux amd64/arm64; a prerequisite of `build` and all `test*` targets), and `//go:embed` against an empty dir is a hard compile error for the whole module. `Materialize(baseDir)` writes `.dwe/bridge/shim-linux-*` 0755 atomically (same-dir temp + rename — a half-written shim must never be observable at the bind-mount source of a running container), write-if-changed with chmod-only mode repair; `Status(baseDir)` reports current/stale/missing per shim; `FileName(arch)` is the single shim-name source (composegen references names, not bytes). The package is a leaf — no import of parent `core/bridge`.

## Shared — Leaf Infrastructure (`internal/shared/`)

- `internal/shared/version/` — `Version` / `Commit` / `Date` / `BuiltBy` vars + `Info()`; injected via `-ldflags -X`. Makefile LDFLAGS target `github.com/semsemyonoff/dwe/internal/shared/version.<Field>`.
- `internal/shared/prompt/` — shell-prompt segment renderer for starship / similar integrations. Public entry point: `Run(stdout io.Writer, args []string) int`; internal testable form: `runFromDir(stdout, args, cwd string, useColor bool) int` (explicit cwd so tests use `t.TempDir()` without touching process state). **Lipgloss is deliberately not imported**: lipgloss auto-downgrades to no-color when stdout is piped, which it always is when starship invokes `dwe prompt`. Local `findRoot(start)` stat-walks up to `workspace.yml` — intentionally duplicates `project.Locate` logic to avoid `filepath.EvalSymlinks` cost and the cwd-injection issue on the `Locate` path. Four stub structs (`workspaceStub` / `stylesStub` / `stateStub` / `promptCacheStub`) decode only the fields the prompt needs via lenient `yaml.Unmarshal`. Color is emitted as TrueColor ANSI (`\x1b[38;2;R;G;Bm…\x1b[39m`) using `strings.Builder` + `strconv.AppendUint` — never `fmt.Sprintf` to keep the render path allocation-free. `NO_COLOR` env: suppressed when the variable exists at all (`os.LookupEnv`, value irrelevant). Any error (yaml parse, IO, missing project) → silent exit 1. **Output shape**: `{▪} <project> [<service>] <deploy-icon> <stack-icon>` — each tail segment is independently optional; only the `{▪} <project>` prefix is a stability guarantee. **Service tag**: `detectService(cwd, root)` enumerates `workspace/services/*/`, reads each `service.yml`'s `dir:` and `extends:` fields via lenient `yaml.Unmarshal` into `serviceDirStub`, then a two-pass loop: first builds a `stubs` map, then per-service walks the `extends:` chain via `resolveDir` (cycle-safe via a per-walk `seen` map, no fixed depth cap) until a non-empty `dir:` is found. The resolved path is `filepath.Clean(filepath.Join(rootClean, dir))` for relative dirs, or `filepath.Clean(dir)` when `filepath.IsAbs(dir)`. Match: `cwdClean == resolved` or `strings.HasPrefix(cwdClean, resolved+sep)`. Resolved paths equal to root (`dir: .`) or escaping root (`dir: ..`, absolute paths outside root) are rejected via a `HasPrefix(resolved, rootPrefix)` containment check — otherwise they would tag every cwd in (or above) the project. The deepest match wins by `len(resolved)`. Per-file size cap of 64 KB on the hot path: `os.Stat` first, skip if oversized (defense against generated/anchor-bomb YAML on a per-keystroke render path). `cwd` is `filepath.Clean`-ed at the top to tolerate trailing separators or `./` segments from `$PWD`-honoring shells. Sanitized via `sanitizeName` at render time. Does NOT resolve symlinks (mirrors `findRoot`). Cost: one `os.ReadDir` plus N stat+read+parse per prompt invocation (sub-millisecond on typical projects). **Stack icon** (`●` running / `◐` partial / `○` stopped, colored from `palette.success` / `warning` / `muted` — `muted` default `#6B7280`, overridable via `styles.yml` `colors.muted`): backed by `.dwe/prompt-cache.yml` (TTL 2 min). `readStack(root, composeProject, now)` returns the cached state when fresh; on stale or missing cache it invokes `refreshStack` (shells out to `docker ps -q --filter label=com.docker.compose.project=<project>` via the test-injectable `dockerPsFunc(ctx, project, env)` with a 150 ms hard timeout). The probe env comes from `readDockerProcessEnv(root)`: `process_env` overrides from `workspace/docker.yml` + `workspace/docker.local.yml` (per-key, local wins — mirrors `LoadDockerConfig`'s deepMerge; lenient — bad YAML and non-string values silently skipped; values literal, never template-resolved, matching the strict loader) applied over the process env via `docker.MergeEnv`, so the probe targets the same daemon (`DOCKER_HOST` / `DOCKER_CONTEXT`) as lifecycle commands; nil (inherit) when no overrides. Read only on the refresh path — fresh-cache renders pay no extra file IO. **No-downgrade rule**: `writeCache` is invoked ONLY when `refreshStack` returns `ok=true` AND state is `stackRunning` (count > 0). On `stackStopped` (zero rows) the cache is NOT written — the stale cache value is returned while it is younger than `staleTrustCap` (5× TTL = 10 min); past the cap the confirmed zero-probe **renders** as `stackStopped` (still no write), so a stack stopped outside dwe converges to `○` instead of showing `●` indefinitely. An unconfirmed zero (`refreshStack` `ok=false`) never downgrades at any age, and with no cache at all the result is `stackNone` (icon omitted). Rationale: a zero-result from `docker ps` is indistinguishable between a genuinely stopped stack and a wrong label filter (templated `docker.yml.project_name` cannot be resolved by prompt); writing `stopped` could downgrade a correct `running` left by an authoritative writer. `stopped` writes are reserved exclusively for authoritative writers (lifecycle commands + `dwe status`). The compose project name in prompt comes from `readComposeProjectName`, mirroring `ResolveComposeProjectName` precedence with literal values only: `project_name` from `workspace/docker.local.yml` then `workspace/docker.yml` (local wins; template values fall back), else `prefix-name`, else `project.name`. Projects with templated `project_name` see prompt refresh as a persistent zero-result while lifecycle/status writes keep the cache accurate — past `staleTrustCap` that zero renders `○` until the next authoritative write. The `docker` binary name is hardcoded — `binaries.docker: podman` bypasses prompt-driven refresh (lifecycle writes still work); `process_env` IS honored (see above), only the binary name is fixed. Extending that precedence list: **every branch of `readComposeProjectName` lowercases its result**, hand-repeating `config.normalizeComposeProjectName`. This is the single deliberate exception to the one-resolver rule in `internal/core/project/config/docker.go` — the prompt hot path must not load the full config — and the repeat is mandatory, not stylistic: without it the `com.docker.compose.project` label filter misses every container of a project whose `project.name` / `project.prefix` / `docker.yml project_name` carries uppercase, and the prompt would render a permanently-stopped stack.
- `internal/shared/promptcache/` — leaf package: best-effort writer + invalidation API for `.dwe/prompt-cache.yml`. **Zero `internal/core/**` imports** (the leaf invariant — `core/project/stack.HealthState` does the `Health → state-string` mapping in `core/` so callers compose `promptcache.Write(root, stack.HealthState(h))`). Public surface: string constants `StateRunning` / `StatePartial` / `StateStopped`; `Write(root, state string) error` validates `state` is one of the three constants (returns error otherwise without writing), ensures `<root>/.dwe/` exists, encodes `{updated_at: time.Now().UTC().Format(time.RFC3339), state: <state>}` to YAML, atomic write via `os.CreateTemp(<root>/.dwe/, "prompt-cache-*.tmp")` + `os.Rename`; `Remove(root string) error` removes the cache file, idempotent on `os.ErrNotExist`. Callers (lifecycle / deploy / service / snapshot / status CLI packages) invoke directly — `_ = promptcache.Write(...)` or `_ = promptcache.Remove(...)`; errors are deliberately ignored at every call site (best-effort: the cache is observability, not correctness). **Writer matrix**: see `docs/guides/starship.md` § Writer map. The rule that matrix encodes: a site that ends with the WHOLE stack in a known state **writes** it — `dwe run`, `dwe restart` with no service arg, and `dwe services enable|disable --apply` (after the entire toggle plan including after-hooks) write `running`; `dwe stop` with no service arg and project-wide `dwe reset run` write `stopped` (reset writes before journal cleanup, so a later `journal.Remove` failure cannot leave a stale `running`); top-level `dwe status` writes the exact aggregate via `stack.HealthState(stack.HealthFromStatusInput(...))`. Every site whose effect is SCOPED or whose outcome is unknown **invalidates instead of guessing** via `promptcache.Remove`: each `<service>`/`--service` variant of `stop` / `restart` / `reset run`, `dwe deploy run` (it can no-op via "already up-to-date"), and `dwe snapshot restore` / `rollback` / **`create`** (the create workflow may stop or restart containers, and on failure may have partially mutated container state). `dwe status <subcommand>` neither writes nor invalidates — only the top level performs the full aggregation. `dwe prompt`'s own refresh only ever writes `running`, **never** `stopped`: a zero-result `docker ps` is indistinguishable from a wrong-label filter, so the no-downgrade rule bounds writes, not rendering — a confirmed zero-result is still *rendered* as stopped once the stale value is older than `staleTrustCap` (5× TTL = 10 min).
- `internal/shared/generatedstore/` — leaf store for service-minted "generated-once" secrets (Laravel `APP_KEY`, Magento `crypt.key`, …), mirroring `promptcache`'s leaf-IO shape. **Zero `internal/core/**` imports.** Backs `.dwe/generated.yml` (`const DefaultRelPath`, no `schema_version`); on-disk schema is `services: {<svc>: {<field>: <value>}}` (string values, block scalars for multi-line). `Load(path)` returns an empty store on a missing file but **surfaces** a corrupt file (NOT swallowed); `Save(path, s)` is atomic (temp + rename, mirrors `journal.Save`). Accessors: `Has(svc, field)`, `Get(svc, field)`, `Service(svc) map[string]string`, `SetIfAbsent(svc, field, val) bool` (write-if-absent — the harvest contract: a stored value is never overwritten), `ClearService(svc)`, `ClearAll()`, `IsEmpty()`. The store is intentionally separate from `.dwe/deploy/state.yml` so snapshot restore (which overwrites the journal) leaves harvested secrets untouched. `.dwe/` is fully gitignored, so secrets stay out of git. Consumed by `templates/config` (`RenderConfigs`/`HarvestGenerated`), the `generated-missing` predicate, `renderConfigsForRun`, and `reset --clear-generated`.
- `internal/shared/docker/` — `Compose` for building/executing `docker compose` with policy args; `NewCompose(cfg, dockerCfg, baseDir)` / `NewComposeAll(cfg, dockerCfg, baseDir)`; always read the binary via `Compose.BinName()` (nil/empty-safe). **`Compose.BaseDir` is the project root and is applied as `cmd.Dir` on every docker subprocess** (`Exec`, internal `output`, `RunningServices`) so that relative `-f compose.yaml` paths in `cfg.ComposeFiles()` resolve correctly even when dwe is invoked from a project subdirectory. Callers constructing raw `exec.Command` against `compose.BinName()` (e.g. `internal/core/project/stack/topology.go`, `stack/daemons.go`, `internal/core/usercommands/runtime/runners/service/exec.go`, `internal/cli/command/daemonset.go`, `internal/cli/compose/compose.go` raw passthrough) MUST also set `cmd.Dir = compose.BaseDir` (or the equivalent `baseDir` parameter for the stack helpers). Empty `BaseDir` is only safe inside tests that never exec. Health helpers: `WaitContainersHealthy`, `HealthStatus`, `ContainerIDsFor`. `StopContainer(ctx, dockerBin, name string, timeoutSec int) error` for direct container stop (no compose); idempotent on "No such container". `RemoveContainer(ctx, dockerBin, name string) error` is the symmetric `docker rm -f` helper, also idempotent on "No such container"; the pair backs both `dwe stop <name>` (stop only) and the per-service-reset synthetic `docker_stop_remove_container` builtin (stop+rm). `RestartContainer(ctx, dockerBin, name string, timeoutSec int) error` issues `docker restart -t <timeoutSec>`; unlike `StopContainer`, it is **NOT idempotent** — returns `ErrNoSuchContainer` when the container does not exist, so callers can differentiate "never deployed" from "stopped". `DefaultStopTimeoutSec` exported constant shared with `internal/core/execution/builtin/containers/daemon_stop.go`. **Build-base prepull** (`prepull.go` + `dockerfilerefs.go`, backing `build.prepull_bases`): `func (c *Compose) DeriveBuildBases(services []string) ([]BaseRef, error)` runs `c.output(c.BuildInternalArgs("config", "--format", "json"))` on the SAME `Compose` instance the caller will `Exec` with — never `BuildArgs`, since user `args.global` (e.g. `--ansi always`) would corrupt the machine-readable JSON. It decodes `services.<name>.{platform,build.{context,dockerfile,dockerfile_inline,args,platforms}}`, resolves `dockerfile` relative to the (already-absolute) `context` unless it is itself absolute, and feeds each Dockerfile's bytes plus the service's `build.args` and its resolved **target platform** into the hand-rolled, pure (no I/O) parser in `dockerfilerefs.go` — handles line continuations, `# escape=`, pre-`FROM` `ARG` defaults overridden by `build.args`, `FROM [--platform=...] ref [AS stage]` with stage-name/`scratch` exclusion, and `${VAR}`/`${VAR:-def}`/`$VAR` substitution (an unresolvable var, e.g. buildkit's `$BUILDPLATFORM`, is skipped via a `trace` warning, never an error). Each result is a `BaseRef{Ref, Platform}` — the `Platform` is the FROM's effective build platform: a `FROM --platform=<p>` literal/ARG-resolvable value; the service's **target platform** (`build.platforms` wins, else `services.<name>.platform`) for a bare `FROM` **or** a `--platform=$TARGETPLATFORM` builtin (Compose already knows the target platform, so a base with no explicit platform builds for it); or `""` for `--platform=$BUILDPLATFORM` (the native build host → daemon default) / an unresolvable var / no service target platform. **The platform is load-bearing, not cosmetic**: on a host whose default platform differs from the pinned/target one (e.g. `platform: linux/amd64` on an arm64 mac), prepulling/probing the default variant would leave buildkit to fetch the target variant from the LAN registry — the exact fetch this feature avoids. A multi-entry `build.platforms` yields one `BaseRef` per target platform. Dedup + sort key is `(Ref, Platform)`, so the same ref pinned to two platforms yields two entries. Returns sorted, deduped external refs; an empty `services` slice means "derive for all". `ImageExists(ref, platform string) bool` / `PullImage(ref, platform string) error` mirror the `volumes.go` raw-`exec.Command(bin, ...)` pattern but — DELIBERATELY unlike `volumeExists` — set `c.BuildEnv()`/`c.BaseDir` so `DOCKER_HOST`/context overrides from `process_env` apply; both append `--platform <p>` when `platform != ""` (an `image inspect` that predates `--platform` support just errors → treated as "missing" → a safe pull, per the advisory contract); `PullImage` streams stdout/stderr straight to the terminal (base pulls can take minutes) with a `trace.Command` echo first, matching `Compose.Exec`. All three are pure mechanics + errors — `internal/shared/docker` never prints user-facing text; the caller (`internal/cli/docker`) owns warnings and the advisory degrade-to-warning contract.
- `internal/shared/daemon/` — cross-cutting daemon domain utilities: `ResolveContainerName(projectFullName, renderedTemplate)` (prepends `<projectFullName>-`, validates against `^[a-zA-Z0-9_][a-zA-Z0-9_.-]*$` — the authoritative runtime gate on container names), `StandardLabels(projectFullName, daemonID, params)` (emits `--label key=value` argv pairs for the three standard labels), `FilterArgsByLabels(projectFullName, daemonID)` (`--filter label=…` argv for `docker ps`); sentinel errors `ErrContainerNameInvalid` / `ErrDaemonAlreadyRunning` / `ErrDaemonNotRunning`; label-key constants `LabelProject` / `LabelDaemonID` / `LabelDaemonParams`. Consumed by `internal/core/execution/builtin`, `internal/core/project/stack`, `internal/core/validate/commands`, and `internal/cli`.
- `internal/shared/bridgeproto/` — host-bridge wire protocol leaf (note: `shared/daemon/` above is TAKEN by user-command daemon utilities — hence the `bridge*` naming). Length-prefixed binary frames identical on both transports: `[4-byte BE uint32 payload length][1-byte type][payload]`, types 0x01–0x09 (HELLO / STDIN / STDOUT / STDERR / STDIN_CLOSE / SIGNAL / RESIZE-reserved / EXIT / ERROR), `MaxPayload` guard bounds memory. `ProtocolVersion` const + ERROR code constants (`auth_failed`, `version_mismatch`, `cwd_outside_project`, `tty_unsupported`, `daemon_shutting_down`). Deliberately NO channel-id in the header — one connection = one command; multiplexing would be a new `protocol_version`. `WriteFrame` emits header+payload in a **single `Write`** so concurrent frame writers on a `net.Conn` cannot interleave. Token helpers: generate (crypto/rand 32 bytes hex), read/write file 0600, constant-time compare — `TokenEqual` **rejects empty tokens on both sides** so a missing token file can never authenticate. Peercred: `PeerUID`/`PeerIsSameUser` behind build tags (`SO_PEERCRED` linux / `LOCAL_PEERCRED` darwin / fail-closed stub elsewhere, mirroring the `shared/lock` fallback shape).
- `internal/shared/bridgeclient/` — the in-container shim core; **leaf** (no `core/`/`cli/` imports), no cobra, no lipgloss (mirrors `shared/prompt` — `cmd/dwe-shim` is a thin main over it). Transport selection per design D2: stat + dial `$DWE_BRIDGE_DIR/host.sock` (300 ms timeout — a dead socket inode on Docker Desktop refuses instantly, by design; never platform-detect) → fall back to TCP `host.docker.internal:<port>` reading the `port`+`token` files (2×100 ms retry on missing — daemon-restart window). Token is sent only on TCP; unix authenticates via peercred. HELLO always carries `tty: false` (even on a real terminal — design D11). Color: `OptionsFromEnv` probes the shim's own stdout (`stdoutIsTerminal` seam) and on a real terminal appends `CLICOLOR_FORCE=1` plus `COLORTERM=truecolor` via `ForceColorEnv` (container `NO_COLOR` / explicit `CLICOLOR_FORCE` / explicit `COLORTERM` win unchanged) — the host-side dwe writes into a pipe and would otherwise downgrade to no-color and quantize the palette to the container's bare `TERM=xterm`. `SetStdinTTYEnv` force-owns `DWE_BRIDGE_STDIN_TTY` (spoofed values dropped, appended only when the shim's stdin IS a terminal) — the signal that lets host runners give user-command children a full PTY (see § runio). `StripEnv` is the **single strip-set source** (the daemon re-filters through it) — `DWE_BRIDGE_SERVICE` (`EnvBridgeService`) is deliberately NOT in it: the overlay injects it per service (value = the workspace service KEY, not the compose container name) and it must reach the host-side dwe, whose `registry.applyBridgeVisibility` consumes it via the `InContainer()` / `CallingService()` helpers here; container-reported, hence advisory (UX boundary, not security). The canonical `EnvInvokedFrom`/`InvokedFromContainer`/`EnvNonInteractive` constants also live here (leaf — importable from `core/` registry code); `core/bridge` keeps aliases for daemon-side call sites. `TranslateCwd` is path-boundary-aware prefix rewrite (`/workspace` does not match `/workspaces`), untranslatable cwds pass through as-is — the daemon (`resolveCwd` in `core/bridge/session.go`) FALLS BACK to its own project root for any cwd it cannot place inside the project (hooks routinely `cd` out of the container mount and send `/`), logging a note instead of rejecting; `ErrCodeCwdOutsideProject` is no longer emitted (kept for wire compat). Unreachable policy (design D10): default error + exit 1 (a hook must block the commit), `DWE_BRIDGE_UNREACHABLE=warn` → warning + exit 0; a connection dying before delivering ANY frame is mapped to unreachable too (Desktop's host proxy can accept the connect and only then discover nothing listens); `version_mismatch` renders the fixed "shim outdated, re-run `dwe deploy` to refresh shim binaries" remedy. Tests run the real client against in-process fake servers over loopback TCP + tmpdir unix sockets — no Docker.
- `internal/shared/lock/` — project-level file lock with stale-PID detection (Unix-only build tag): `Acquire(path)` uses `flock(LOCK_EX|LOCK_NB)`; on `EWOULDBLOCK` reads the holder PID and calls `Kill(pid, 0)` to reclaim stale locks. Lock files: `.dwe/deploy/deploy.lock` and `.dwe/snapshots/snapshot.lock`. Lock files are intentionally left on disk after `Release` with the last writer's PID; **never** delete a conflict lock file (inode-race invariant, `lock.go:120-125`). `project.go` exposes `AcquireProjectLocks(baseDir) (release func(), err error)` — the **single seam** for project-mutating commands: acquires `deploy.lock` first, `snapshot.lock` second (alphabetical), releases in reverse; on second-lock failure the first is released before returning the error; error message is `"<deploy|snapshot> operation in progress: pid N"` (wraps `lock.HeldError`). Every snapshot mutating subcommand (`create` / `restore` / `rollback` / `remove` / `pack` / `unpack`) and every deploy lifecycle command (`deploy run` / `run` / `stop` / `restart` / `reset`) calls this helper — never `lock.Acquire` directly on the deploy or snapshot lock path. Lifecycle commands acquire the project locks **after** `preflightRun` succeeds. Behavior detail: `docs/reference/config/state/index.md` § Lock file and `docs/reference/config/snapshot.md` § Lock interaction.
- `internal/shared/git/` — git update probe: `Probe`, `PullFFOnly`, `Decide`. **Mode is binary: `ModeOn` / `ModeOff`** (`UpdateMode` string type) — the former `prompt`/`auto`/`check` triad was collapsed (YAGNI) and the dead `ActionPullAuto` constant removed. Safety matrix: not-a-repo / `ModeOff` → skip; dirty / no-upstream / fetch-failed / diverged → warn; up-to-date → skip; behind + `ModeOn` → `ActionPullPrompt` on a TTY (ask, then `pull --ff-only`), `ActionWarn` ("behind … skipping in non-interactive") otherwise. The consumer (`workflow/lifecycle/run.go`) maps the resolved `on`/`off` string to `git.UpdateMode` before calling `Decide`. `Runner` interface for tests.
- `internal/shared/envfile/` — `.env` generation: `BuildContent(cfg)`, `Regenerate(configPath)`. No cobra / UI deps. Host-id resolution (`HostUID` / `HostGID`) delegates to `shared/hostid`.
- `internal/shared/hostid/` — leaf owner of the platform host-id logic shared by `envfile` and `tpl`: `Current() Info` does a single `user.Current()` read and returns `{UID, GID}` (`UID()` / `GID()` are convenience wrappers). `envfile.HostUID` / `HostGID` delegate here; `tpl.CurrentHostInfo` returns `HostInfo(hostid.Current())` (an identical-field struct conversion, so existing `HostInfo` literals in tests stay valid). Centralizes what was duplicated across `envfile/render.go` and `tpl/render_command.go`.
- `internal/shared/tpl/` — Go template engine: `Render`, `RenderCommand`, `EvalCondition`, `EvalCommandCondition` (render-first). `FuncMap()` returns a per-call shallow clone of a sprout-backed map (`std`, `strings`, `numeric`, `slices`, `maps`, `regexp`, `conversion`, `time`, `filesystem`, `semver`) plus domain helper `appURL`. Hermetic (no env, FS, network, crypto). `commandFuncMap` adds `resolve` / `resolveMap` / `resolveFile` (command scope only). **Snapshot template scope gate**: `${snapshot.*}` resolves only inside snapshot workflow blocks. The gate is enforced by `validateSnapshotScope(expr, scope)` in `render_command.go`, called from `RenderCommand` **before** `CompileVarSyntax`. `CompileVarSyntax` has no error path and must stay pure-syntactic — never add scope logic there. Three scope values: `SnapshotScopeNone` (zero value, default — compile error on any `${snapshot.*}`), `SnapshotScopeCreate` (all keys except `created_at`), `SnapshotScopeRestoreOrRemove` (all keys). Use `meta.BuildSnapshotVars(...)` (from `internal/core/workflow/snapshot/meta`) to build the map; inject via `RunContext.Render.Snapshot` + `.SnapshotScope`. **`generated` namespace** (config render): `RenderContext.Generated map[string]string` holds the current service's harvested values; `CompileVarSyntax` routes `case "generated"` → `{{ resolveGenerated .Generated "<name>" }}`. Like every other `${...}` resolver, `resolveGenerated` is **lenient** (absent key → `""`) — there is no strict-vs-lenient distinction. Config templates reference per-service config fields via `${services.<name>...}` (the curated `injectServicesIntoRaw` subset only) and free-form values via `${vars.<path>}`; a singular current-service `${service....}` binding does NOT exist. **Unknown-head whitelist**: `CompileVarSyntax`'s default branch (bare dot-path → `{{ resolve .Raw "X" }}`) fires only when `X`'s head is in the exported `KnownVarHeads` — the union of `config.allowedRootKeys` (duplicated here since `tpl` must not import `internal/core/project/config`; a cross-check test in `config` asserts `allowedRootKeys ⊆ tpl.KnownVarHeads`) and the special namespaces (`files`, `host`, `param`, `context`, `snapshot`, `generated`, `args`). An unrecognized head (`${HOME}`, a typo, a stale pre-strict-root bare dot-path like `${databases.x}`) is left as a literal `${...}` instead of collapsing to `""` — this is a correctness control, not a security boundary. **The head must also carry a tail**: `IsVarNamespaceRef(inner)` (= known head AND (`hasTail` OR head == `args`)) is the exported rule every caller asks — the compiler's own default branch, `ValidateRawScope`, `pipeline.hasKnownVarRef`, `pipeline.UnresolvedTemplateRefs` — so a head-only `${host}` / `${files}` / `${services}` stays literal instead of resolving against `Raw` (erasing to `""`, or interpolating a whole sub-map as `map[...]` text) — those are lowercase shell variables that happen to collide with namespace names: a `${vars.*}` value reaching a host `sh -c` program is ordinary substitution, not a trust boundary, so the whitelist never sanitizes or quotes. `__configPath` is deliberately excluded from `KnownVarHeads` — it is a loader-injected internal key, not part of the authoring contract. **Raw/Host-only scope gate**: `ValidateRawScope(expr)` is the sibling of `validateSnapshotScope` for the five heads that resolve from a caller-populated `RenderContext` field rather than `Raw`/`Host` (`param`, `context`, `files`, `generated`, `args` — `snapshot` stays with `validateSnapshotScope`, which has the better message). The one render path whose context is structurally Raw + Host only — `pipeline.renderContextFor`, which scenario steps share since `envtest` has no renderer of its own — calls it before `RenderCommand` and fails on error, so a reference to a namespace with no source there is an error naming the namespace instead of a silent `""` from the lenient resolvers. Do NOT wire it into `usercommands.buildRunContext` or workflow sub-steps: those render `with:` against the parent command's context, where `${param.*}` is legitimate. **`VarPattern`** is the exported `${...}` matcher (`= varPattern`, `\$\{([a-zA-Z_][a-zA-Z0-9_.]*)\}`) reused by `varsusage`'s static scanner so it keys off the renderer's OWN pattern semantics (no internal whitespace, no leading digit) rather than a divergent grep. The tail rule is not arbitrary: every namespace form in the authoring contract is dotted (`${vars.db.host}`, `${project.name}`, `${host.uid}`, `${files.id.path}`), while head-only `${host}` / `${files}` / `${services}` tokens are ordinary shell variables and are rife in pipeline `cmd:` strings — which only began rendering when resolve-time rendering landed, which is what forced the rule. Both mis-rewrites it prevents (erasure to `""`, or a `map[...]` dump of a whole sub-map) were **silent**, and that silence is the exact failure mode the known-head whitelist exists to stop. `${args}` is the ONE bare form the contract defines, hence the explicit `head == "args"` escape — and it is still rejected by `ValidateRawScope` on the pipeline path, where the namespace has no source at all. `IsKnownVarHead(head)` is the exported head-alone entry point over the unexported `knownVarHeadSet`; outside callers must ask through it (or `IsVarNamespaceRef` for a full inner path) rather than re-indexing the exported slice, so there is exactly one membership index and it cannot drift from the rewrite. **One documented exception to the whitelist**: `docker.yml`'s `project_name` goes through `config.resolveVarTemplate` (`internal/core/project/config/docker.go`) instead — see there.
- `internal/shared/pathsafe/` — filesystem safety: `CheckNoSymlinks`, `ContainedRel` (rejects `..` / `.` escaping), `EnsureRealUnder` (callers must `filepath.EvalSymlinks` before passing). Used by template renderers to prevent escape attacks.
- `internal/shared/render/` — `Writer` with ANSI output methods (Success / Error / Warning / Info / Definition / TableHeader / ASCII art); plain passthrough for logs; `Writer.Confirm` is the non-TTY Y/n fallback. `Writer.ASCII(lines []string, font string) error` emits plain figlet text — callers wrap output in lipgloss style externally (typically via `accent`). No built-in color path; `validFigureColors` and `NewColorFigure` were dropped as part of the branding refactor.
- `internal/shared/liveui/` — shared live-output infrastructure: `LiveLine` + `LiveBlock` (sticky footer + multi-row block via `bubbles/v2/spinner` + private ticker) and output helpers (`LineTee`, `ANSIOnlyStripper`, `LogSanitizer`, `JoinWriters`). Consumed by `internal/core/execution/pipeline/plain.go` and by workflow parallel groups in `internal/core/usercommands/runtime/runners/workflow/parallel.go`. **Nine non-negotiable invariants** for the live view are enumerated in the package doc comment at the top of `liveline.go`, which is also what the in-code `Invariant #N` citations refer to — read them before touching anything here. Two of the nine (#6 `\r` is data, #7 prompt handoff through `widgets.SetHuhHooks` + `SuspendForExec`/`ResumeAfterExec`) are enforced in `internal/core/execution/pipeline`, not here; the numbering is shared because the design is. **Block-mode pending state (integration-tests stage 3b)** — `SetBlockRowPending(idx, label)` sets a block row's label WITHOUT starting its stopwatch (a queued scenario must not accrue elapsed time while it waits for a worker slot), keyed on an **explicit `pending bool` field on `blockRow`** — set only by `SetBlockRowPending`, cleared by `SetBlockRowRunning`/`SetBlockRowFinal`. `renderBlockRowLocked` renders a `pending` row with a gray `IconRunning` dot instead of the blue spinner and omits the elapsed bracket; render keys on the flag ONLY, **never** inferred from `startTime.IsZero()` — the workflow parallel runner (`runners/workflow/parallel.go`) keeps queued rows never-started for real durations (`SetBlockRowRunning` fires inside each worker goroutine after an errgroup slot is granted), so inferring pending from a zero start time would silently change that consumer's queued-row rendering. Existing consumers (which never call `SetBlockRowPending`) render byte-identically by construction. Guards mirror `SetBlockRowRunning` (out-of-range/disabled → no-op); `SetBlockRowRunning` after `SetBlockRowPending` starts the stopwatch then (first-call `startTime.IsZero()` branch). Consumed by `internal/cli/test/`'s aggregated `dwe test run --parallel` display. **Rendering model — hand-driven models, never a bubbletea program.** `LiveLine` updates and renders its `bubbles/v2` models directly (`spinner.Model.Update(spinner.TickMsg{…})` on each tick of the private 10 Hz ticker, then `spinner.View()` composed into an ANSI frame written to `termOut`); it must NEVER call `tea.NewProgram`, `term.MakeRaw`, or issue terminal capability queries. The footer is painted into normal scrollback next to the pipeline's own stdout/stderr streaming — not an alt-screen program — so an event loop or raw mode here would take ownership of the terminal the pipeline output is still writing to. The only terminal syscall in the package is the width probe `term.GetSize(os.Stdout.Fd())`, itself overridable through the `liveLineTestHooks.widthFn` seam.
- `internal/shared/trace/` — leaf diagnostic sink for `-v`/`--debug` output. **Zero `internal/core/**` imports** (the leaf invariant); imported by `docker` / `git` / the execution engine / `preflight` for emit, but only `cli/` calls `Configure` + installs the slog handler. **All output is stderr-only** so stdout (incl. `--output json`) stays a clean machine-readable contract. Public surface: `Level` enum (`LevelOff` / `LevelVerbose` / `LevelDebug`, `int32`-backed via `atomic.Int32`), `Configure(w io.Writer, lvl Level)` (called once from root; `w` may be nil), `Enabled(l Level) bool`, `Command(ctx, name, args...)` (Verbose+; `$`-prefixed copy-pasteable line), `Decision(ctx, format, …)` (Verbose+; step run/skip + reason, `when:`/condition + preflight results), `Debugf(ctx, format, …)` (Debug-only; timings/exit codes/env/config internals), `LinePrinter` interface (`PrintLine(string)`, must be concurrency-safe), `SetPrinter(p) (restore func())`, `WithLinePrinter(ctx, p)`, `FormatCommand(args) string`, and `NewSlogHandler()` (`slog.go`). **Three-level emit precedence** (`emit`): (1) ctx-carried printer (`printerFrom` / `WithLinePrinter`) — set per parallel sub-step for correct attribution; beats (2) global active printer (last remaining entry of the `SetPrinter` stack) — set by `PlainReporter.StartPipeline` to wrap `r.live.PrintlnDiag`, which frames the line above the footer via `termOut` but writes the line **text to the diagnostics writer = `os.Stderr`** (set via `LiveLine.SetDiagWriter` in `NewPlainReporter`), NOT `r.live.Println` (screen/stdout) — so in-pipeline `-v`/`--debug` echoes stay off stdout and `dwe run --debug 2>debug.log` captures them; beats (3) the `Configure` fallback writer (`os.Stderr`, used outside any pipeline + in JSON mode). `LevelOff` is a zero-alloc early return after one atomic load → no overhead when no flag is set. **Concurrency contract**: the global printer stack is mutated both by sequential nested-pipeline callers and — since parallel `dwe test run --parallel N>1` scenarios each run their own in-process steps pipeline — by concurrent siblings under `mu`. `SetPrinter` stamps each entry with a monotonic id and its restore removes **exactly its own entry by id** (not an index truncation of everything above it) under `sync.Once`, so an out-of-order restore never drops a still-active sibling's printer (which would leak its `-v`/`--debug` diagnostics to the fallback `os.Stderr` and garble the aggregated live display); the active printer is the last remaining entry. Parallel sub-steps within one pipeline attribute via the ctx printer and never touch the global slot. **slog handler** (`slog.go`): installed via `slog.SetDefault` **only** at `LevelDebug` (`-v` never touches slog); `Enabled` is always true (gating happens at install time), records route through the same ctx→global→fallback precedence, and `WithAttrs`/`WithGroup` are supported. `installSlogHandler` captures Go's pristine default logger once and **restores it** when a later same-process `Execute` runs below Debug — so a prior `--debug` does not leak the always-emit handler into a subsequent non-debug run (matters for embedded/repeated `Execute`; the one-shot CLI never revisits). **Level precedence** (`levelFrom` / `debugEnvTruthy` in `cli/root.go`): `--debug` OR truthy `DWE_DEBUG` → Debug; else `--verbose` → Verbose; **flag beats env** (`DWE_DEBUG` in `{"","0","false","no","off"}`, case-insensitive + trimmed, is off). **`FormatCommand`/`quoteArg` is the single quoting source** — lifted byte-identically from the old `docker/compose.go` `formatCommand`/`quoteArg`, so compose error-wrap goldens must stay byte-identical to it. **Tier contents**: Verbose carries user-facing lifecycle command echoes (`Compose.Exec`) plus pipeline decisions; Debug additionally echoes **read-only docker probes** — `Compose.output` and `Compose.RunningServices` each wrap their `trace.Command` call in an explicit `if trace.Enabled(trace.LevelDebug)` guard **to keep `dwe status -v` quiet**, since a single status render fans out into a burst of `ps`/`inspect` probes that would bury the lifecycle echoes a user asked `-v` for, so a new read-only probe must copy that guard while a user-facing lifecycle command echoes unguarded at Verbose+ — alongside the per-subprocess `cwd=` / `env=` lines, the exit code + duration line, and `slog.Debug` records. `trace` is also the **single** diagnostic sink: a domain package must never hand-roll a `fmt.Fprintln(os.Stderr)` diagnostic, which would bypass the three-level precedence and land un-framed mid-live-frame on raw stderr instead of above the footer. User-facing details: `docs/guides/troubleshooting.md` § Verbose & debug output.
- `internal/shared/i18n/` — localization system: embedded + project-layer translation file loading, locale resolution, and typed lookup. **Schema**: `Bundle{UI map[string]string, Commands map[string]CommandStrings, Groups map[string]GroupStrings}` (CommandStrings and GroupStrings carry description + params/title respectively). **File format**: YAML files under `internal/shared/i18n/translations/` (built-in) and `<projectRoot>/workspace/i18n/` (project overlay, optional); strict `KnownFields(true)` decode. **Loader**: `Load(projectRoot)` reads embedded files + project overrides (project-wins deep merge per locale); returns a `*Store` with methods `T(locale, uiKey, fallback)` (generic lookup for `ui.*` namespace), plus typed helpers `CommandDescription` / `CommandConfirmationText` / `ParamDescription` / `GroupTitle` / `GroupDescription`. `LoadProjectBundles(projectRoot)` returns per-file parse metadata for the validator (source path, locale, parse error if any); missing directory → `(nil, nil)`, OS error → sentinel `ProjectFile{Locale: "", ParseErr}`, parse error → non-fatal (skipped, file appears in result slice). **Resolver**: `ResolveLocale(flagLang, configLang, sysLang)` precedence chain: flag → config → system `$LANG` parse → `"en"` default. **Scope v1**: user-command `description`/`confirmation_text`/`param.description`, command-group `title`/`description`, and doc-generator `ui.*` strings. DWE's own Cobra strings stay English; `${snapshot.*}` variables stay English. **Coverage test**: `coverage_test.go` ensures every `ui.*` key in built-in `en.yml` exists in every other built-in locale. **Two disjoint i18n namespaces exist and must never be conflated.** This package owns namespace one: `workspace/i18n/<lang>.yml` — YAML command/UI strings, strict-decoded into `Bundle`, project-wins deep-merged per locale, validated by the `internal/core/validate/i18n/` domain. Namespace two is `docs/i18n/<lang>/**` (today `reference/`, `guides/`, and a flat `README.md` — there is no `docs/i18n/<lang>/internals/` tree; `docs/internals/` is excluded from the mirror) — markdown long-form documentation resolved by `internal/core/docs/lang.go`'s `ResolveContent`, which does whole-file per-file fallback to the English source with **no merge at all** and has **no validator** (freshness is guarded by `lang_test.go::TestRussianTranslationsAreFresh` in CI, not by `dwe validate`). Different file formats, different loaders, no shared code path — a key never crosses between them, and a missing translation degrades differently on each side (YAML: per-key fallback to `en`; markdown: whole-document fallback). User-visible details: `docs/reference/config/i18n.md`.

## Entrypoint (`cmd/dwe/`)

- `cmd/dwe-shim/main.go` — the standalone in-container shim binary: a thin main over `internal/shared/bridgeclient` (signal.Notify for SIGINT/SIGTERM, `bridgeclient.OptionsFromEnv`, `os.Exit(bridgeclient.Run(...))`). No cobra, no lipgloss — the whole point is a ~3 MB static `CGO_ENABLED=0` linux build (`make shims` → `internal/core/bridge/shimassets/bin/`, embedded into the host dwe and materialized to `.dwe/bridge/` at prepare time).
- `cmd/dwe/main.go` — entry point (uses `fang.Execute` for styled help/errors). Four non-obvious startup behaviors: (1) **Prompt hot-path bypass** (`isPromptInvocation`): if `os.Args` is exactly `["…", "prompt"]` or `["…", "prompt", "--check"]`, `prompt.Run(os.Stdout, os.Args[2:])` is called and `os.Exit` fires immediately — before cobra, fang, pflag pre-parse, and `loadHelpColorScheme`. Any other `prompt` shape (e.g. `--help`, unknown flags, `--check=true`) falls through to cobra so help/errors render normally. (2) **Pre-Cobra pflag pre-parse** (`configPathFromArgs`): extracts `--config/-c` from `os.Args` using pflag with `UnknownFlags=true` *before* Cobra runs, so the project root can be located without triggering Cobra flag parsing. (3) **Early styles injection** (`loadHelpColorScheme`): calls `project.Locate` + `config.LoadStylesConfig` + `styles.ApplyStyles` before `fang.Execute`, then passes a `fang.WithColorSchemeFunc` so `--help` output uses the project's `accent`/`muted` tokens. Silent on all failures — Fang's default colors apply when styles cannot be loaded. The scheme func is ALWAYS installed (even without styles.yml) because it also pins the palette mode: fang's own `mustColorscheme` resolves isDark=false whenever stdout is not a terminal, which would render the LIGHT palette into a pipe — invisible dark-on-dark text over the host bridge. Both lipgloss v1 and v2 resolve DARK on pipes, so the wrapper forces `lipgloss.LightDark(true)` when `!term.IsTerminal(stdout)` and every dwe surface agrees. `styles.ApplyStyles` runs twice on a normal invocation (once here before Cobra, once from root `PersistentPreRunE`); the second call is a no-op when styles haven't changed. (4) **Custom error handler**: suppresses output for `ErrSilent` (command already printed its own error) and for `ExitCode`-bearing errors (diagnostics already printed via `fang.WithErrorHandler`); delegates all others to `fang.DefaultErrorHandler`. Exit-code errors also produce a non-zero `os.Exit(ec.ExitCode())` after `fang.Execute` returns.
