## dwe commands list

List available commands

### Synopsis

List all available declarative commands from workspace/commands/.

An optional group filter narrows the output to a specific command group (e.g. 'db', 'services.main').
Use --all to include private commands.

```
dwe commands list [group] [flags]
```

### Examples

```
  dwe commands list
  dwe commands list db
  dwe commands list --all
```

### Options

```
      --all    Include private commands
  -h, --help   help for list
```

### Options inherited from parent commands

```
  -c, --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe commands](dwe_commands.md)	 - Run, inspect, and list dwe commands

###### Auto generated by spf13/cobra on 1-Jun-2026
