## devbox validate env

Validate environment readiness

### Synopsis

Run built-in environment probes (docker binary, docker daemon, compose plugin, git/shell binaries, .devbox writable).

```
devbox validate env [flags]
```

### Options

```
  -h, --help   help for env
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default: auto-discover from cwd upward)
      --quiet           hide ok/info rows
      --stage string    filter checks by stage (deploy, run, stop, command)
      --strict          treat warnings as errors (exit code 1)
```

### SEE ALSO

* [devbox validate](devbox_validate.md)	 - Validate project configuration and files

###### Auto generated by spf13/cobra on 26-May-2026
