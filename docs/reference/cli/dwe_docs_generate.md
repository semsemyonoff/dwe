## dwe docs generate

Generate reference documentation

### Synopsis

Generate reference documentation for the dwe CLI command tree and/or the
declarative command registry (workspace/commands/).

Supported formats: markdown, yaml, man, all
Supported scopes:  all, cli, commands

```
dwe docs generate [flags]
```

### Examples

```
  dwe docs generate
  dwe docs generate --format markdown --scope cli --out docs/reference
  dwe docs generate --format all --scope all --include-private
```

### Options

```
      --format string     Output format: markdown, yaml, man, all (default "markdown")
  -h, --help              help for generate
      --include-hidden    Include hidden CLI commands
      --include-private   Include private registry commands
      --lang string       Language code (default: from userconfig / $LANG)
      --out string        Output directory for generated docs (default "docs/reference")
      --scope string      Scope: all, cli, commands (default "all")
```

### Options inherited from parent commands

```
  -c, --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe docs](dwe_docs.md)	 - Browse and manage documentation

###### Auto generated by spf13/cobra on 1-Jun-2026
