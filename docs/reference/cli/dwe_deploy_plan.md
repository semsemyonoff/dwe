## dwe deploy plan

Show resolved deploy plan

### Synopsis

Print all phases and steps from workspace/deploy.yml as they would be executed.

The implicit .env generation step is always shown first. Use --service to filter
the plan to steps relevant to a specific service. Use --format shell for script-friendly output.

```
dwe deploy plan [flags]
```

### Examples

```
  dwe deploy plan
  dwe deploy plan --service main
  dwe deploy plan --format shell
```

### Options

```
      --format string    output format: table or shell (default "table")
  -h, --help             help for plan
      --service string   show plan for a single service only
```

### Options inherited from parent commands

```
  -c, --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe deploy](dwe_deploy.md)	 - Deploy pipeline commands

###### Auto generated by spf13/cobra on 1-Jun-2026
