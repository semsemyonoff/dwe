## dwe validate linters

Run external linters (shellcheck, hadolint, generic)

### Synopsis

Run external linters configured in workspace/validate.yml and autodetected built-ins (shellcheck, hadolint). With an id, runs only that linter.

```
dwe validate linters [id] [flags]
```

### Options

```
  -h, --help   help for linters
```

### Options inherited from parent commands

```
      --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
      --quiet           hide ok/info rows
      --stage string    filter checks by stage (deploy, run, stop, command)
      --strict          treat warnings as errors (exit code 1)
```

### SEE ALSO

* [dwe validate](dwe_validate.md)	 - Validate project configuration and files

###### Auto generated by spf13/cobra on 4-Jun-2026
