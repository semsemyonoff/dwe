## devbox docs llms-txt

Emit an llms.txt project index for AI agents

### Synopsis

Emit a single llms.txt document to stdout (or --output PATH).

The document follows the llms.txt spec: a dense ~2-5KB index that gives an AI
agent a complete picture of "what this devbox project is and where to find more
detail," without having to ingest the full embedded docs tree.

Works both inside a project (project-aware: includes services, commands, URLs)
and outside one (project-agnostic: generic devbox reference).

Examples:
  devbox docs llms-txt
  devbox docs llms-txt --output llms.txt
  devbox docs llms-txt --include-internals
  devbox docs llms-txt --no-project

```
devbox docs llms-txt [flags]
```

### Options

```
  -h, --help                help for llms-txt
      --include-internals   Include internals architecture docs section
      --lang string         Language code (default: from userconfig / $LANG / en)
      --no-project          Force project-agnostic output even if devbox.yml exists
      --output string       Write output to PATH instead of stdout
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default: auto-discover from cwd upward)
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [devbox docs](devbox_docs.md)	 - Browse and manage documentation

###### Auto generated by spf13/cobra on 31-May-2026
