## dwe deploy state show

Show the current deploy state

### Synopsis

Print the contents of .dwe/deploy/state.yml in human-readable YAML format.

Shows per-step status, timestamps, action hashes, and duration metrics.
If the state file does not exist, shows a message indicating no state.

```
dwe deploy state show [flags]
```

### Examples

```
  dwe deploy state show
```

### Options

```
  -h, --help   help for show
```

### Options inherited from parent commands

```
      --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe deploy state](dwe_deploy_state.md)	 - Manage deploy state

###### Auto generated by spf13/cobra on 4-Jun-2026
