## dwe deploy run

Execute the deploy plan

### Synopsis

Execute the full deploy pipeline from workspace/deploy.yml phase by phase.

Steps are run in declaration order. The .env file is regenerated as the implicit
first step. Use --service to run only the steps relevant to a specific service.

State tracking allows idempotent deploys: steps that previously succeeded with
matching hashes are skipped on re-run. Use --force to ignore prior state and
re-run all steps (when: conditions are still evaluated — for a fully clean
install run 'dwe reset run && dwe deploy run'). Use --resume to continue
from the last failed step in a partially deployed project.

File logging is enabled by default for deploy and writes to .dwe/logs/deploy.log.
Disable it with 'log: false' at the top of workspace/deploy.yml.

```
dwe deploy run [flags]
```

### Examples

```
  dwe deploy run
  dwe deploy run --service main
  dwe deploy run --force
  dwe deploy run --resume
```

### Options

```
      --force             ignore state and re-run all steps (when: still applies; use 'dwe reset run && dwe deploy run' for a true clean install)
  -h, --help              help for run
  -y, --non-interactive   suppress interactive prompts
      --resume            continue from the last failed step
      --service string    deploy a single service only
      --silent            suppress the end-of-run desktop notification
      --skip-preflight    bypass environment probes and project checks before running
```

### Options inherited from parent commands

```
      --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe deploy](dwe_deploy.md)	 - Deploy pipeline commands

###### Auto generated by spf13/cobra on 4-Jun-2026
