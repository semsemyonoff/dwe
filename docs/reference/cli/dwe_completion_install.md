## dwe completion install

Install shell completion for dwe

### Synopsis

Install the dwe shell completion script to the standard location for your
shell. The target file is determined automatically from the shell name (or
detected from $SHELL), and the parent directory is created if missing.

Supported shells: bash, zsh, fish, powershell

To install for a specific shell:

    dwe completion install zsh
    dwe completion install bash

Override the target directory:

    dwe completion install zsh --path ~/.config/completions

Preview without writing:

    dwe completion install --dry-run

NOTES
  - Dotfiles (e.g. ~/.zshrc, PowerShell $PROFILE) are never modified.
  - For zsh, add the completion directory to fpath and call compinit.
  - For PowerShell, source the installed .ps1 from your $PROFILE.

```
dwe completion install [shell] [flags]
```

### Options

```
      --dry-run       print the target path and generated content preview; do not write
  -h, --help          help for install
      --path string   install to this directory instead of the default location
```

### Options inherited from parent commands

```
  -c, --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe completion](dwe_completion.md)	 - Generate the autocompletion script for the specified shell

###### Auto generated by spf13/cobra on 3-Jun-2026
