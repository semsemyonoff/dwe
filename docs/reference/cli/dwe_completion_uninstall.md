## dwe completion uninstall

Uninstall shell completion for dwe

### Synopsis

Remove the dwe shell completion script from the standard location for your
shell. The target file is determined automatically from the shell name (or
detected from $SHELL).

Supported shells: bash, zsh, fish, powershell

To uninstall for a specific shell:

    dwe completion uninstall zsh
    dwe completion uninstall bash

Override the target directory:

    dwe completion uninstall zsh --path ~/.config/completions

NOTES
  - Dotfiles (e.g. ~/.zshrc, PowerShell $PROFILE) are never modified.
  - If the completion file does not exist, the command exits successfully.
  - Homebrew tap users should use generate_completions_from_executable instead.

```
dwe completion uninstall [shell] [flags]
```

### Options

```
  -h, --help          help for uninstall
      --path string   uninstall from this directory instead of the default location
```

### Options inherited from parent commands

```
      --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe completion](dwe_completion.md)	 - Generate the autocompletion script for the specified shell

###### Auto generated by spf13/cobra on 4-Jun-2026
