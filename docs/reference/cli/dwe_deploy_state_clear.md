## dwe deploy state clear

Clear the deploy state

### Synopsis

Delete the deploy state file at .dwe/deploy/state.yml.

This removes all step status records, hashes, and run metrics. The next 'dwe deploy run'
will treat all steps as needing to be executed.

In interactive mode (TTY), a confirmation prompt is shown. Use -y/--non-interactive to skip the prompt.

```
dwe deploy state clear [flags]
```

### Examples

```
  dwe deploy state clear
  dwe deploy state clear -y
```

### Options

```
  -h, --help              help for clear
  -y, --non-interactive   skip confirmation prompt
```

### Options inherited from parent commands

```
  -c, --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe deploy state](dwe_deploy_state.md)	 - Manage deploy state

###### Auto generated by spf13/cobra on 1-Jun-2026
