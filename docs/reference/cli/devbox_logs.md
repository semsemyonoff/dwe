## devbox logs

Stream container logs for a service

### Synopsis

Stream Docker container logs for a named project service.

In text mode (default), output is passed through unchanged from docker logs.
With --output json, emits NDJSON: one {"ts","stream","msg"} object per line.

```
devbox logs <service> [flags]
```

### Examples

```
  devbox logs myapp
  devbox logs myapp --tail 100 --follow
  devbox logs myapp --output json --since 5m
```

### Options

```
  -f, --follow         stream new log lines as they arrive
  -h, --help           help for logs
      --since string   show logs since duration (e.g. 5m, 1h) or RFC3339 timestamp
      --tail int       number of trailing log lines to show (0 = all) (default 50)
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [devbox](devbox.md)	 - devbox — local development environment toolkit

###### Auto generated by spf13/cobra on 29-May-2026
