## devbox docs export

Export documentation to a directory

### Synopsis

Export all documentation files to a target directory.

Files are organized by source (devbox, project, internals).
Missing translations are exported with a banner indicating the original language.

Examples:
  devbox docs export /tmp/docs
  devbox docs export /tmp/docs --lang ru
  devbox docs export /tmp/docs --include-project --force

```
devbox docs export <dir> [flags]
```

### Options

```
      --force               Overwrite non-empty target directory
  -h, --help                help for export
      --include-internals   Include internals docs
      --include-project     Include project docs (from ./docs/)
      --lang string         Language code (default: from --lang flag / userconfig / $LANG / en)
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [devbox docs](devbox_docs.md)	 - Browse and manage documentation

###### Auto generated by spf13/cobra on 31-May-2026
