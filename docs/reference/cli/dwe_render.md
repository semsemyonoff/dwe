## dwe render

Render derived artifacts from the merged workspace config

### Synopsis

Generate files derived from the merged workspace config (workspace.yml + defaults.yml + local.yml).

Subcommands:
  env  — generate .env from the exports.env spec
  ide  — generate IDE config files from template packs
  ai   — generate hub-level agents documentation from template packs
  git  — generate shell git hooks from template packs

### Examples

```
  dwe render env --out .env
  dwe render ide
  dwe render ai
  dwe render git
```

### Options

```
  -h, --help   help for render
```

### Options inherited from parent commands

```
      --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe](dwe.md)	 - Manage Docker-based local development environments
* [dwe render ai](dwe_render_ai.md)	 - Generate hub-level agents docs from template packs
* [dwe render env](dwe_render_env.md)	 - Generate .env from exports.env spec (stdout or --out <file>)
* [dwe render git](dwe_render_git.md)	 - Generate git hooks from template packs
* [dwe render ide](dwe_render_ide.md)	 - Generate IDE configs from template packs

###### Auto generated by spf13/cobra on 4-Jun-2026
