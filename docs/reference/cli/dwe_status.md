## dwe status

Show stack health and per-section status (read-only)

### Synopsis

Display the running status of the entire dwe stack.

The default view prints a health indicator followed by all sections in order:
apps, tools, infra, deploy, topology, git workspace, daemons. Each section is
also addressable as 'status <section>'; --no-<section> flags suppress sections
in the default view.

```
dwe status [flags]
```

### Examples

```
  dwe status
  dwe status apps
  dwe status deploy main
  dwe status --no-git --no-topology
```

### Options

```
  -h, --help          help for status
      --no-apps       suppress the apps section
      --no-daemons    suppress the daemons section
      --no-deploy     suppress the deploy section
      --no-git        suppress the git workspace section
      --no-infra      suppress the infra section
      --no-tools      suppress the tools section
      --no-topology   suppress the topology section
      --no-tui        force plain text output even on a TTY
```

### Options inherited from parent commands

```
  -c, --config string   path to workspace.yml (default: auto-discover from cwd upward)
  -o, --output string   output format: text or json (default "text")
      --pretty          pretty-print JSON output (only with --output json)
```

### SEE ALSO

* [dwe](dwe.md)	 - dwe — local development environment toolkit
* [dwe status apps](dwe_status_apps.md)	 - Show only the apps section
* [dwe status daemons](dwe_status_daemons.md)	 - Show only the daemons section
* [dwe status deploy](dwe_status_deploy.md)	 - Show deploy status (table) or per-service deploy detail
* [dwe status git](dwe_status_git.md)	 - Show only the git workspace section
* [dwe status infra](dwe_status_infra.md)	 - Show only the infra section
* [dwe status tools](dwe_status_tools.md)	 - Show only the tools section
* [dwe status topology](dwe_status_topology.md)	 - Show only the topology section

###### Auto generated by spf13/cobra on 1-Jun-2026
