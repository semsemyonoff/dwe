schema_version: "2"

# The project block is load-bearing.
project:
  name: sectest
  prefix: dwe

vars:
  svc_defaults: &svc
    enabled: true

  app:
    <<: *svc
    name: myapp            # trailing note
    notes: |
      a literal block that must not move
      # including this line

# Trailing note kept at the end of the file.
