# GENERATED by dwe — do not edit; customize via workspace/local.yml
services:
  app-admin:
    volumes:
      - type: bind
        source: /host/proj/.dwe/bridge
        target: /dwe-bridge
        read_only: true
      - type: bind
        source: /host/proj/.dwe/bridge/shim-linux-amd64
        target: /opt/dwe/bin/dwe
        read_only: true
    environment:
      DWE_BRIDGE_DIR: /dwe-bridge
      DWE_BRIDGE_PROJECT: acme-shop
      DWE_BRIDGE_SERVICE: admin
      DWE_BRIDGE_UNREACHABLE: warn
      DWE_CONTAINER_WORKSPACE: /srv/admin
      DWE_HOST_WORKSPACE: /host/proj/services/admin
    extra_hosts:
      - host.docker.internal:host-gateway
  app-main:
    volumes:
      - type: bind
        source: /host/proj/.dwe/bridge
        target: /dwe-bridge
        read_only: true
      - type: bind
        source: /host/proj/.dwe/bridge/shim-linux-arm64
        target: /usr/local/bin/dwe
        read_only: true
    environment:
      DWE_BRIDGE_DIR: /dwe-bridge
      DWE_BRIDGE_PROJECT: acme-shop
      DWE_BRIDGE_SERVICE: main
      DWE_CONTAINER_WORKSPACE: /workspace
      DWE_HOST_WORKSPACE: /host/proj/services/main
    extra_hosts:
      - host.docker.internal:host-gateway
  queue:
    volumes:
      - type: bind
        source: /host/proj/.dwe/bridge
        target: /dwe-bridge
        read_only: true
      - type: bind
        source: /host/proj/.dwe/bridge/shim-linux-arm64
        target: /usr/local/bin/dwe
        read_only: true
    environment:
      DWE_BRIDGE_DIR: /dwe-bridge
      DWE_BRIDGE_PROJECT: acme-shop
      DWE_BRIDGE_SERVICE: queue
    extra_hosts:
      - host.docker.internal:host-gateway
