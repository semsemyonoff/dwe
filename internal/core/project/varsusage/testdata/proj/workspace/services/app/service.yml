type: app
# ${vars.db.host} in a YAML comment is never a reference
info:
  - value: "${ vars.db.host }"
  - value: "${1vars.db}"
  - text: "real ${vars.db.host}"
  - text: 'panel {{ resolve .Raw "vars.ui.label" }}'
