services:
  web:
    type: app
    container: app-web
    mandatory: true
    dir: ./services/web
    dir_internal: /workspace
    work_dir_internal: /workspace/src
    ports:
      http: 8080
      grpc: 9090
    hosts:
      main: web.localhost
