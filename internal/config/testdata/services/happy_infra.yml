services:
  db:
    type: infra
    container: postgres
    mandatory: true
    ports:
      postgres: 5432
    hosts:
      main: db.internal
  cache:
    type: infra
    container: redis
    depends_on:
      - db
    ports:
      redis: 6379
